/* eslint-disable */
// @generated by @pyreon/lathe from Bookshelf 1.2.0.
// Do not edit: change the spec and run `lathe generate`.

import { createAuthor, createBook, seedFaker } from './faker'

/**
 * Atlas scenarios for Bookshelf, one set per generated preview.
 *
 * @example
 * ```ts
 * // atlas.config.ts
 * import { scenarios } from './src/gen/atlas.scenarios'
 * import { wrapper } from './src/gen/atlas.wrapper'
 * export default { scenarios, wrapper }
 * ```
 */

// A fixed seed, so the "Data" scenario renders the same values every run.
seedFaker(1)

export const scenarios = {
  'ListAuthorsPreview': [
    { name: 'Default', args: {} },
    { name: 'Data', args: { data: Array.from({ length: 5 }, () => createAuthor()) } },
    { name: 'Loading', args: {"force":"loading"} },
    { name: 'Error', args: {"force":"error"} },
    { name: 'Empty', args: {"force":"empty"} },
  ],
  'GetBookPreview': [
    { name: 'Default', args: {} },
    { name: 'Data', args: { data: createBook() } },
    { name: 'Loading', args: {"force":"loading"} },
    { name: 'Error', args: {"force":"error"} },
    { name: 'Empty', args: {"force":"empty"} },
  ],
  'ListBooksPreview': [
    { name: 'Default', args: {} },
    { name: 'Data', args: { data: Array.from({ length: 5 }, () => createBook()) } },
    { name: 'Loading', args: {"force":"loading"} },
    { name: 'Error', args: {"force":"error"} },
    { name: 'Empty', args: {"force":"empty"} },
  ],
}
