/* eslint-disable */
// @generated by @pyreon/lathe from Bookshelf 1.2.0.
// Do not edit: change the spec and run `lathe generate`.

import type { VNodeChild } from '@pyreon/core'
import { installMocks } from './mocks'
import { QueryClient, QueryClientProvider } from '@pyreon/query'

/**
 * Wraps every generated preview: a QueryClient, with the mock routes installed
 * so the previews render with no server. Queries never retry or refetch, so
 * an error shows at once and a card does not change while you read it.
 */
const client = new QueryClient({
  defaultOptions: { queries: { retry: false, gcTime: Infinity, staleTime: Infinity } },
})

// Serve the generated fixtures, so every preview renders with no server.
installMocks()

export function wrapper(props: { children?: VNodeChild }) {
  return <QueryClientProvider client={client}>{props.children}</QueryClientProvider>
}
