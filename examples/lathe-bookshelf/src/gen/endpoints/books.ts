/* eslint-disable */
// @generated by @pyreon/lathe from Bookshelf 1.2.0.
// Do not edit: change the spec and run `lathe generate`.

import type { NewBook } from '../schemas/NewBook'
import { api } from '../client'
import { Book } from '../schemas/Book'
import { s } from '@pyreon/validate'

/**
 * Add a book.
 *
 * `POST /books`
 *
 * Parameters:
 * - `json` (body, application/json) — `NewBook`
 *
 * @example
 * ```ts
 * const result = await createBook({ json: { title: 'sample title' } })
 * ```
 */
export const createBook = /* @__PURE__ */ api.endpoint<'POST /books', typeof Book, { json: NewBook }>('POST /books', { response: Book })

/**
 * One book by id.
 *
 * `GET /books/:bookId`
 *
 * Parameters:
 * - `bookId` (path)
 *
 * @example
 * ```ts
 * const result = await getBook({
 *   params: { bookId: '00000000-0000-4000-8000-000000000000' },
 * })
 * ```
 */
export const getBook = /* @__PURE__ */ api.endpoint<'GET /books/:bookId', typeof Book, { params: { bookId: string } }>('GET /books/:bookId', { response: Book })

const listBooks$response = /* @__PURE__ */ s.array(Book)
/**
 * Every book in the catalogue.
 *
 * `GET /books`
 *
 * @example
 * ```ts
 * const result = await listBooks()
 * ```
 */
export const listBooks = /* @__PURE__ */ api.endpoint<'GET /books', typeof listBooks$response, {}>('GET /books', { response: listBooks$response })
