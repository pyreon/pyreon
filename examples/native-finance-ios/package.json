{
  "name": "@pyreon/example-native-finance-ios",
  "version": "0.50.0",
  "private": true,
  "description": "PRIVATE — iOS host for the SHARED examples/native-finance app source. The REAL-APP device gate: multi-screen routing + useAuth + useForm + a cross-screen store + a computed balance, proven on a real Simulator.",
  "license": "MIT",
  "scripts": {
    "build": "./scripts/build.sh",
    "lint": "echo '[native-finance-ios] no TS lint surface; the app source lives in examples/native-finance'",
    "test": "echo '[native-finance-ios] device-tested via xcodebuild (native-device workflow); no node test surface'",
    "typecheck": "echo '[native-finance-ios] no TS typecheck surface'"
  },
  "devDependencies": {
    "@pyreon/native-cli": "workspace:*"
  }
}
