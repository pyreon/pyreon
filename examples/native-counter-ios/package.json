{
  "name": "@pyreon/example-native-counter-ios",
  "version": "0.50.0",
  "private": true,
  "description": "PRIVATE / EXPERIMENTAL — first iOS example for the PMTC pipeline. Phase 0 roadmap PR 3 scaffold; counter implementation lands in PR 4.",
  "license": "MIT",
  "scripts": {
    "build": "./scripts/build.sh",
    "xcode": "./scripts/xcode-setup.sh",
    "lint": "echo '[native-counter-ios] no TS lint surface; skipping'",
    "test": "echo '[native-counter-ios] no test surface; counter behaviour proven via @pyreon/native-compiler fixture snapshots'",
    "typecheck": "echo '[native-counter-ios] no TS typecheck surface'"
  },
  "devDependencies": {
    "@pyreon/native-cli": "workspace:*"
  }
}
