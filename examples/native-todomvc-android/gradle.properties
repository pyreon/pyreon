# Gradle properties — JVM heap + AndroidX enablement.
# Mirrors a default Android Studio project's gradle.properties.

org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
