# Gradle properties — JVM heap + AndroidX enablement.
# Mirror of `native-todomvc-android/gradle.properties`.

org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
