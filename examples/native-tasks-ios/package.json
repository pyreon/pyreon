{
  "name": "@pyreon/example-native-tasks-ios",
  "version": "0.50.0",
  "private": true,
  "description": "PRIVATE / EXPERIMENTAL — Tasks showcase iOS host (XcodeGen + SwiftUI). Compiles the SAME TasksApp.tsx source from native-tasks/ via PMTC. Closes Gap 5 (iOS host shell for the tasks showcase).",
  "license": "MIT",
  "scripts": {
    "build": "./scripts/build.sh",
    "xcode": "xcodegen generate && open PyreonTasks.xcodeproj",
    "lint": "echo '[native-tasks-ios] no TS lint surface; skipping'",
    "test": "echo '[native-tasks-ios] no test surface; PMTC emit covered by @pyreon/native-compiler tests'",
    "typecheck": "echo '[native-tasks-ios] no TS typecheck surface'"
  },
  "devDependencies": {
    "@pyreon/native-cli": "workspace:*"
  }
}
