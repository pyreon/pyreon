{
  "name": "@pyreon/example-native-tasks",
  "version": "0.0.18",
  "private": true,
  "description": "PRIVATE / EXPERIMENTAL — Gap 5 multi-feature showcase. Tasks app exercising a cross-screen defineStore (auth flag + task list), store-backed beforeEnter auth-gate, multi-screen routing, typed route params, and canonical primitives via PMTC. SINGLE source compiles to web + iOS + Android.",
  "license": "MIT",
  "scripts": {
    "build:swift": "./scripts/build-swift.sh",
    "build:kotlin": "./scripts/build-kotlin.sh",
    "build": "bun run build:swift && bun run build:kotlin",
    "lint": "echo '[native-tasks] no TS lint surface; skipping'",
    "test": "echo '[native-tasks] no test surface; PMTC emit covered by @pyreon/native-compiler tests'",
    "typecheck": "echo '[native-tasks] @ts-nocheck on source; PMTC handles typing'"
  },
  "devDependencies": {
    "@pyreon/core": "workspace:*",
    "@pyreon/form": "workspace:*",
    "@pyreon/i18n": "workspace:*",
    "@pyreon/machine": "workspace:*",
    "@pyreon/native-cli": "workspace:*",
    "@pyreon/primitives": "workspace:*",
    "@pyreon/reactivity": "workspace:*",
    "@pyreon/router": "workspace:*",
    "@pyreon/store": "workspace:*"
  },
  "dependencies": {
    "@pyreon/hooks": "workspace:*"
  }
}
