{
  "name": "@pyreon/example-native-router-demo-android",
  "version": "0.50.0",
  "private": true,
  "description": "PRIVATE / EXPERIMENTAL — Router demo for Android / Jetpack Compose. Compiles the SAME RouterApp.tsx source as native-router-demo-ios into Kotlin/Compose. Closes Gap 5 (Android Espresso parity beyond TodoMVC + Counter).",
  "license": "MIT",
  "scripts": {
    "build": "./scripts/build.sh",
    "lint": "echo '[native-router-demo-android] no TS lint surface; skipping'",
    "test": "echo '[native-router-demo-android] no test surface; PMTC emit covered by @pyreon/native-compiler tests'",
    "typecheck": "echo '[native-router-demo-android] no TS typecheck surface'"
  },
  "devDependencies": {
    "@pyreon/native-cli": "workspace:*"
  }
}
