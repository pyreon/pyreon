# @pyreon/validate

- **@pyreon/validate**:
  - Pyreon's `s` validator runtime + DX helpers on Standard Schema. `withField`/`getMeta`/`resolveMetaField`/`parseReactive`/`watchValid`/`formatErrors` (`parseReactiveAsync` supersedes stale in-flight results — an awaited stale frame resolves to the LATEST run's verdict). `s` runtime: primitives + object/array composition + modifiers + 20+ checks + object algebra (`.pick/.omit/.partial/.required/.extend/.merge/.keyof/.catchall`). ASYNC members (`.refine`/`.transform`/registered `.serverCheck`) work inside EVERY composition (union/DU/intersection/map/set/record/tuple + the JIT — a sync `parse()` of an async tree reports the ONE canonical "use parseAsync" issue at the root). `s.discriminatedUnion` accepts literal/enum/nativeEnum discriminants (dev-guarded: non-registrable field or duplicate tag throws at construction). **`.catch` runs against a PRIVATE child ctx** — a catch window can never eat a SIBLING field's issues (fuzz-found shared-ctx bug, both backends). `toJsonSchema(schema)` (subpath `@pyreon/validate/json-schema`) emits draft 2020-12 (input-shape contract; unrepresentable kinds throw or `{ unrepresentable: 'any' }`; cyclic lazy throws — no $defs in v1).
  - **Tree-shaking**: DX helpers tree-shake clean; the chainable `s.` API does NOT (prototype methods) — use `pyreon({ optimizeValidators: true })` to rewrite module-level `const X = s.<chain>` to the lean `@pyreon/validate/mini` form at build time (function chaining in, tree-shakeable output, parity-locked; ~−41% measured). `pyreon({ compileValidators: true })` inlines a monomorphic verdict for `.is()` (1.6–3.0× faster, nanoseconds — only matters in hot `.is()` loops). Both flags cover statically-analyzable chains within the emit slice (primitives + common checks + object/array/optional); out-of-slice chains gracefully stay full-runtime.
  - Perf vs Zod/Valibot/ArkType (process-isolated bench, pooled CI95 + tie detection, author-judge disclosed; standings RE-VERIFIED 2026-07-22 vs zod 4.4.3/valibot 1.4.1/arktype 2.2.3 — **fastest-or-CI-tied on all 12 package-bench cells**): wins error path outright every shape (27.9–77× Zod, 14.9–92.7× ArkType, 1.1–6.5× Valibot on the latest run); valid path now WINS flat-object (1.46×), nested (1.21×), arrays (1.37×), scalar-int (2.4×) AND discriminated-union (1.89×) vs ArkType on the REWRITTEN multi-schema megamorphic `scripts/bench/core/validate.ts` (2026-07 harness fix — the old harness's constant closed-over inputs + all-4-libs-one-process + single duration-run FABRICATED cross-lib verdicts like "valibot 2-4× ahead on scalars"; now rotated 8-input pools, per-LIB process isolation ×3 pooled with shapes interleaved inside each lib for real megamorphic ICs, round-robin timing runs so GC/tier debt spreads across cells, CI95 + 🤝 ties), and on the package bench now WINS scalar-email (~1.3×) + number-range (~1.9×) + deep-nested (~1.5×) outright with flat-object a 🤝 CI-tie (was ~1.2× behind) — ahead of Zod everywhere. The former last megamorphic loss — BARE scalar-string valid, valibot's minimal typeof pipe 1.44–1.97× ahead — closed to a 🤝 CI-tie (8.8 vs 8.0ns; the residual median gap is `.parse()`'s `Result` envelope vs valibot's leaner one), and ArkType's former ~1.7× MONOMORPHIC bare-scalar edge (single-schema hot loop) FLIPPED: scalar-number ~2.4× AHEAD, scalar-string tie-to-marginal-win at the Result-API floor, mono flat-object flipped to a win — all via the 2026-07 **pure-JIT reused-ctx seam** (`_jitPure`: a fully-inline JIT tree — no `_runInto` fallback — provably runs no user code, never returns a Promise, never writes `pending`, so `parse`/`~standard.validate`/`is` reuse a per-schema ctx: zero per-parse ctx/issues-array allocation, the escaped issues array swapped out on failure; locked by a 10k-case pure-seam differential fuzz running interleaved parse/is/~standard input SEQUENCES through the same instance against the unchanged general seam). scalar-string INVALID is a valibot⇄pyreon CI-tie band; scalar-int wins both paths. The 2026-07 pass (flat-object flip + scalar close): **format-check JIT inlining** — a memoized registry resolver (`makeFormatResolver`, version-counter cached, no per-parse `Map.get`) + a `_pred` predicate on format ops so the JIT runs the format closure only on FAILURE (no `ctx.path` push/pop on the valid path — the dominant per-field cost of an object with an `.email()`/`.url()` field, which had kept flat-object-with-email ~1.16× behind); a lazy **`EMPTY_PATH` sentinel** in `makeCtx` (a scalar / flat-object parse allocates ZERO path arrays — the frozen sentinel is swapped for a real array by `mutablePath` only when a keyed descent actually writes → ~2× the ctx-alloc throughput, halving the per-parse allocation); a **sync-only JIT body** (no async `A/B`/`Promise.all` machinery emitted when the tree has no fallback/array subtree); atop the earlier JIT static-path elision + async-aware fallback deferral + table-driven email scanner (~1.6× the Zod-parity regex, equivalence-locked). **DU-root JIT** (2026-07 follow-up): `tryCompileJit` accepts a plain `s.discriminatedUnion` root (+ nested DU fields/elements) — a RAW-VALUE discriminant `switch` when every tag is literal-bakeable (`===` ≡ the interpreter's `Map.get` SameValueZero except NaN; a NaN-tagged union keeps a captured-Map small-int dispatch), member bodies inlined with the already-read discriminant PRESET, non-inlinable members per-case `_runInto` fallbacks (async included); plus all-inline-primitive object bodies (members AND plain objects) strip-clone via ONE object literal (final shape in a single allocation, no per-key structure transitions). Package-bench `du.3-member` (ROTATED tags — the dispatch is actually measured; HONEST framing, cell is tier-up-noisy at ~20ns scale): the VERDICT stabilizes — before, the interpreter row measured as an outright ArkType win in some runs (22.7 vs 12.9ns, CIs disjoint) and a tie in others; with the JIT it measures 🤝 CI-tied consistently — but the MEDIAN delta is within run-to-run noise (~22–24ns both states; do NOT quote a before→after ns delta for this row). The interpreter was never dispatch-bound here: members were already individually JIT-compiled via `_runInto`, so the residual vs ArkType is the ctx/Result/strip-clone allocations ArkType's alias-input semantics don't pay (the same monomorphic-residual class as bare scalars). `invalid` is an outright Pyreon win in BOTH states (~81–84ns: 2.8–3.1× Valibot, ~15× ArkType, ~25× Zod — the interpreter already won it). The change's durable value: verdict stability, nested-DU inlining, the one-alloc strip-clone (no measurable delta on `array.20-objects` — measured, not assumed), and the DU rows + differential suite themselves. Locked by the JIT↔interpreter differential fuzz (now FOUR suites — the DU suite covers NaN/enum/`__proto__`/fallback/async members + nested DUs) + `format-registry-routing` (the memoized resolver still swaps to the `/server` validator on install).
