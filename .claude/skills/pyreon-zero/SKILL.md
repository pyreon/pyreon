---
name: pyreon-zero
description: @pyreon/zero render modes (SSG/SSR/ISR/SPA/auto), routeRules, deploy adapters, prerender output, ISR caching and auth safety, and the https() dev-TLS plugin. Load before touching packages/zero/** or any build adapter.
---

Read `.agents/guides/zero/README.md` in full (the tool-neutral source of truth for this topic). Edit that file, not this shim.
