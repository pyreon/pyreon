import { defineComponentTheme } from '@pyreon/ui-theme'

export const inputGroupTheme = defineComponentTheme('InputGroup', (_t, _m) => ({
  base: {
    display: 'flex',
    alignItems: 'stretch',
  },
}))
