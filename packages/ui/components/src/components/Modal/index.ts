import { ModalBase } from '@pyreon/ui-primitives'
import { createComponent } from '../../factory'
import { modalTheme } from './theme'

const Modal = createComponent('Modal', ModalBase, modalTheme)
export default Modal
