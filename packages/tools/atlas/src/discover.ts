/**
 * Subpath entry for `@pyreon/atlas/discover` (dev/build-time, Node only). The
 * build tool derives this entry from the export KEY; the modules live in the
 * `discover/` folder.
 */
export * from './discover/index'
