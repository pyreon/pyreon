// GENERATED by packages/native/compiler/scripts/gen-chart-engine.ts — do not edit.
// Source: packages/fundamentals/charts/src/engine/*.ts (15 modules)
// Regenerate: bun packages/native/compiler/scripts/gen-chart-engine.ts
// Pt/Rect/DrawCmd live in PyreonChartCanvas.swift (runtime-owned); every
// reference here is renamed to the PyreonChart* runtime types.

import Foundation

public struct Tick: Codable {
  public var value: Double
  public var pos: Double
  public var label: String
  public init(value: Double, pos: Double, label: String) {
    self.value = value
    self.pos = pos
    self.label = label
  }
}

public struct Domain: Codable {
  public var min: Double
  public var max: Double
  public init(min: Double, max: Double) {
    self.min = min
    self.max = max
  }
}

public struct Slice: Codable {
  public var value: Double
  public var label: String
  public var color: String
  public init(value: Double, label: String, color: String) {
    self.value = value
    self.label = label
    self.color = color
  }
}

public struct ArcGeometry: Codable {
  public var start: Double
  public var end: Double
  public var mid: Double
  public var slice: Slice
  public var fraction: Double
  public init(start: Double, end: Double, mid: Double, slice: Slice, fraction: Double) {
    self.start = start
    self.end = end
    self.mid = mid
    self.slice = slice
    self.fraction = fraction
  }
}

public struct Circle: Codable {
  public var center: PyreonChartPt
  public var radius: Double
  public init(center: PyreonChartPt, radius: Double) {
    self.center = center
    self.radius = radius
  }
}

public struct PieOptions: Codable {
  public var innerRadius: Double
  public var showLabels: Bool
  public var labelColor: String
  public var fontSize: Double
  public init(innerRadius: Double, showLabels: Bool, labelColor: String, fontSize: Double) {
    self.innerRadius = innerRadius
    self.showLabels = showLabels
    self.labelColor = labelColor
    self.fontSize = fontSize
  }
}

public struct GaugeOptions: Codable {
  public var min: Double
  public var max: Double
  public var sweep: Double
  public var thickness: Double
  public var trackColor: String
  public var valueColor: String
  public init(min: Double, max: Double, sweep: Double, thickness: Double, trackColor: String, valueColor: String) {
    self.min = min
    self.max = max
    self.sweep = sweep
    self.thickness = thickness
    self.trackColor = trackColor
    self.valueColor = valueColor
  }
}

public struct RadarAxis: Codable {
  public var label: String
  public var max: Double
  public init(label: String, max: Double) {
    self.label = label
    self.max = max
  }
}

public struct RadarSeries: Codable {
  public var values: [Double]
  public var color: String
  public var fillAlpha: Double
  public init(values: [Double], color: String, fillAlpha: Double) {
    self.values = values
    self.color = color
    self.fillAlpha = fillAlpha
  }
}

public struct RadarOptions: Codable {
  public var rings: Int
  public var gridColor: String
  public var labelColor: String
  public var fontSize: Double
  public var showLabels: Bool
  public init(rings: Int, gridColor: String, labelColor: String, fontSize: Double, showLabels: Bool) {
    self.rings = rings
    self.gridColor = gridColor
    self.labelColor = labelColor
    self.fontSize = fontSize
    self.showLabels = showLabels
  }
}

public struct Gutters: Codable {
  public var left: Double
  public var right: Double
  public var top: Double
  public var bottom: Double
  public init(left: Double, right: Double, top: Double, bottom: Double) {
    self.left = left
    self.right = right
    self.top = top
    self.bottom = bottom
  }
}

public struct PlotLayout: Codable {
  public var plot: PyreonChartRect
  public var xTicks: [Tick]
  public var yTicks: [Tick]
  public var y2Ticks: [Tick]
  public var xDomainUsed: Domain
  public init(plot: PyreonChartRect, xTicks: [Tick], yTicks: [Tick], y2Ticks: [Tick], xDomainUsed: Domain) {
    self.plot = plot
    self.xTicks = xTicks
    self.yTicks = yTicks
    self.y2Ticks = y2Ticks
    self.xDomainUsed = xDomainUsed
  }
}

public struct LayoutConfig {
  public var width: Double
  public var height: Double
  public var xDomain: Domain
  public var yDomain: Domain
  public var categories: [String]
  public var fontSize: Double
  public var xTickCount: Double
  public var yTickCount: Double
  public var showXAxis: Bool
  public var showYAxis: Bool
  public var yFormat: ((Double) -> String)? = nil
  public var xFormat: ((Double) -> String)? = nil
  public var xTime: Bool? = nil
  public var y2Domain: Domain? = nil
  public var y2Format: ((Double) -> String)? = nil
  public var horizontal: Bool? = nil
  public init(width: Double, height: Double, xDomain: Domain, yDomain: Domain, categories: [String], fontSize: Double, xTickCount: Double, yTickCount: Double, showXAxis: Bool, showYAxis: Bool, yFormat: ((Double) -> String)? = nil, xFormat: ((Double) -> String)? = nil, xTime: Bool? = nil, y2Domain: Domain? = nil, y2Format: ((Double) -> String)? = nil, horizontal: Bool? = nil) {
    self.width = width
    self.height = height
    self.xDomain = xDomain
    self.yDomain = yDomain
    self.categories = categories
    self.fontSize = fontSize
    self.xTickCount = xTickCount
    self.yTickCount = yTickCount
    self.showXAxis = showXAxis
    self.showYAxis = showYAxis
    self.yFormat = yFormat
    self.xFormat = xFormat
    self.xTime = xTime
    self.y2Domain = y2Domain
    self.y2Format = y2Format
    self.horizontal = horizontal
  }
}

public struct StackSegment: Codable {
  public var rect: PyreonChartRect
  public var seriesIndex: Int
  public var datumIndex: Int
  public var value: Double
  public init(rect: PyreonChartRect, seriesIndex: Int, datumIndex: Int, value: Double) {
    self.rect = rect
    self.seriesIndex = seriesIndex
    self.datumIndex = datumIndex
    self.value = value
  }
}

public struct Series {
  public var kind: String
  public var values: [Double]
  public var color: String
  public var width: Double
  public var radius: Double
  public var label: String
  public var curve: (([PyreonChartPt]) -> [PyreonChartPt])? = nil
  public var showValues: Bool? = nil
  public var radii: [Double]? = nil
  public var axis: String? = nil
  public var effect: Bool? = nil
  public var symbol: String? = nil
  public var symbolRepeat: Bool? = nil
  public init(kind: String, values: [Double], color: String, width: Double, radius: Double, label: String, curve: (([PyreonChartPt]) -> [PyreonChartPt])? = nil, showValues: Bool? = nil, radii: [Double]? = nil, axis: String? = nil, effect: Bool? = nil, symbol: String? = nil, symbolRepeat: Bool? = nil) {
    self.kind = kind
    self.values = values
    self.color = color
    self.width = width
    self.radius = radius
    self.label = label
    self.curve = curve
    self.showValues = showValues
    self.radii = radii
    self.axis = axis
    self.effect = effect
    self.symbol = symbol
    self.symbolRepeat = symbolRepeat
  }
}

public struct Annotation: Codable {
  public var y: Double? = nil
  public var x: Double? = nil
  public var yFrom: Double? = nil
  public var yTo: Double? = nil
  public var label: String? = nil
  public var color: String? = nil
  public init(y: Double? = nil, x: Double? = nil, yFrom: Double? = nil, yTo: Double? = nil, label: String? = nil, color: String? = nil) {
    self.y = y
    self.x = x
    self.yFrom = yFrom
    self.yTo = yTo
    self.label = label
    self.color = color
  }
}

public struct PointMarker: Codable {
  public var seriesIndex: Double? = nil
  public var at: String? = nil
  public var atIndex: Double? = nil
  public var label: String? = nil
  public var color: String? = nil
  public var radius: Double? = nil
  public init(seriesIndex: Double? = nil, at: String? = nil, atIndex: Double? = nil, label: String? = nil, color: String? = nil, radius: Double? = nil) {
    self.seriesIndex = seriesIndex
    self.at = at
    self.atIndex = atIndex
    self.label = label
    self.color = color
    self.radius = radius
  }
}

public struct ChartTheme: Codable {
  public var axis: String
  public var grid: String
  public var label: String
  public var fontSize: Double
  public init(axis: String, grid: String, label: String, fontSize: Double) {
    self.axis = axis
    self.grid = grid
    self.label = label
    self.fontSize = fontSize
  }
}

public struct ChartSpec {
  public var width: Double
  public var height: Double
  public var series: [Series]
  public var categories: [String]
  public var theme: ChartTheme
  public var showXAxis: Bool
  public var showYAxis: Bool
  public var showGrid: Bool
  public var yDomain: Domain? = nil
  public var yFormat: ((Double) -> String)? = nil
  public var xFormat: ((Double) -> String)? = nil
  public var y2Domain: Domain? = nil
  public var y2Format: ((Double) -> String)? = nil
  public var xValues: [Double]? = nil
  public var xTime: Bool? = nil
  public var horizontal: Bool? = nil
  public var annotations: [Annotation]? = nil
  public var markers: [PointMarker]? = nil
  public var progress: Double? = nil
  public init(width: Double, height: Double, series: [Series], categories: [String], theme: ChartTheme, showXAxis: Bool, showYAxis: Bool, showGrid: Bool, yDomain: Domain? = nil, yFormat: ((Double) -> String)? = nil, xFormat: ((Double) -> String)? = nil, y2Domain: Domain? = nil, y2Format: ((Double) -> String)? = nil, xValues: [Double]? = nil, xTime: Bool? = nil, horizontal: Bool? = nil, annotations: [Annotation]? = nil, markers: [PointMarker]? = nil, progress: Double? = nil) {
    self.width = width
    self.height = height
    self.series = series
    self.categories = categories
    self.theme = theme
    self.showXAxis = showXAxis
    self.showYAxis = showYAxis
    self.showGrid = showGrid
    self.yDomain = yDomain
    self.yFormat = yFormat
    self.xFormat = xFormat
    self.y2Domain = y2Domain
    self.y2Format = y2Format
    self.xValues = xValues
    self.xTime = xTime
    self.horizontal = horizontal
    self.annotations = annotations
    self.markers = markers
    self.progress = progress
  }
}

public struct Ohlc: Codable {
  public var `open`: Double
  public var high: Double
  public var low: Double
  public var close: Double
  public init(`open`: Double, high: Double, low: Double, close: Double) {
    self.`open` = `open`
    self.high = high
    self.low = low
    self.close = close
  }
}

public struct CandleOptions: Codable {
  public var upColor: String? = nil
  public var downColor: String? = nil
  public var widthRatio: Double? = nil
  public init(upColor: String? = nil, downColor: String? = nil, widthRatio: Double? = nil) {
    self.upColor = upColor
    self.downColor = downColor
    self.widthRatio = widthRatio
  }
}

public struct HeatCell: Codable {
  public var col: Double
  public var row: Double
  public var value: Double
  public init(col: Double, row: Double, value: Double) {
    self.col = col
    self.row = row
    self.value = value
  }
}

public struct HeatGrid: Codable {
  public var cols: [String]
  public var rows: [String]
  public var cells: [HeatCell]
  public var min: Double
  public var max: Double
  public init(cols: [String], rows: [String], cells: [HeatCell], min: Double, max: Double) {
    self.cols = cols
    self.rows = rows
    self.cells = cells
    self.min = min
    self.max = max
  }
}

public struct HeatmapOptions: Codable {
  public var grid: HeatGrid
  public var plot: PyreonChartRect
  public var stops: [String]
  public var gap: Double? = nil
  public var progress: Double? = nil
  public init(grid: HeatGrid, plot: PyreonChartRect, stops: [String], gap: Double? = nil, progress: Double? = nil) {
    self.grid = grid
    self.plot = plot
    self.stops = stops
    self.gap = gap
    self.progress = progress
  }
}

public struct FunnelStage: Codable {
  public var value: Double
  public var label: String
  public var color: String
  public init(value: Double, label: String, color: String) {
    self.value = value
    self.label = label
    self.color = color
  }
}

public struct FunnelOptions: Codable {
  public var gap: Double? = nil
  public var minWidthRatio: Double? = nil
  public var sort: String? = nil
  public var align: String? = nil
  public var showLabels: Bool? = nil
  public var labelColor: String? = nil
  public var fontSize: Double? = nil
  public var progress: Double? = nil
  public init(gap: Double? = nil, minWidthRatio: Double? = nil, sort: String? = nil, align: String? = nil, showLabels: Bool? = nil, labelColor: String? = nil, fontSize: Double? = nil, progress: Double? = nil) {
    self.gap = gap
    self.minWidthRatio = minWidthRatio
    self.sort = sort
    self.align = align
    self.showLabels = showLabels
    self.labelColor = labelColor
    self.fontSize = fontSize
    self.progress = progress
  }
}

public struct FunnelStageGeometry: Codable {
  public var index: Int
  public var top: Double
  public var bottom: Double
  public var topWidth: Double
  public var bottomWidth: Double
  public var centerX: Double
  public init(index: Int, top: Double, bottom: Double, topWidth: Double, bottomWidth: Double, centerX: Double) {
    self.index = index
    self.top = top
    self.bottom = bottom
    self.topWidth = topWidth
    self.bottomWidth = bottomWidth
    self.centerX = centerX
  }
}

public struct TreeNode: Codable {
  public var name: String
  public var value: Double? = nil
  public var children: [TreeNode]? = nil
  public var color: String? = nil
  public init(name: String, value: Double? = nil, children: [TreeNode]? = nil, color: String? = nil) {
    self.name = name
    self.value = value
    self.children = children
    self.color = color
  }
}

public struct TreemapCell: Codable {
  public var name: String
  public var value: Double
  public var rect: PyreonChartRect
  public var depth: Int
  public var path: [Int]
  public var color: String
  public var leaf: Bool
  public init(name: String, value: Double, rect: PyreonChartRect, depth: Int, path: [Int], color: String, leaf: Bool) {
    self.name = name
    self.value = value
    self.rect = rect
    self.depth = depth
    self.path = path
    self.color = color
    self.leaf = leaf
  }
}

public struct TreemapOptions: Codable {
  public var padding: Double? = nil
  public var maxDepth: Double? = nil
  public var showLabels: Bool? = nil
  public var labelColor: String? = nil
  public var fontSize: Double? = nil
  public var progress: Double? = nil
  public init(padding: Double? = nil, maxDepth: Double? = nil, showLabels: Bool? = nil, labelColor: String? = nil, fontSize: Double? = nil, progress: Double? = nil) {
    self.padding = padding
    self.maxDepth = maxDepth
    self.showLabels = showLabels
    self.labelColor = labelColor
    self.fontSize = fontSize
    self.progress = progress
  }
}

public struct TreemapFrame: Codable {
  public var children: [TreeNode]
  public var area: PyreonChartRect
  public var depth: Int
  public var path: [Int]
  public var inherited: String
  public var hasInherited: Bool
  public init(children: [TreeNode], area: PyreonChartRect, depth: Int, path: [Int], inherited: String, hasInherited: Bool) {
    self.children = children
    self.area = area
    self.depth = depth
    self.path = path
    self.inherited = inherited
    self.hasInherited = hasInherited
  }
}

public struct SunburstArc: Codable {
  public var name: String
  public var value: Double
  public var depth: Int
  public var path: [Int]
  public var start: Double
  public var end: Double
  public var innerR: Double
  public var outerR: Double
  public var color: String
  public var leaf: Bool
  public init(name: String, value: Double, depth: Int, path: [Int], start: Double, end: Double, innerR: Double, outerR: Double, color: String, leaf: Bool) {
    self.name = name
    self.value = value
    self.depth = depth
    self.path = path
    self.start = start
    self.end = end
    self.innerR = innerR
    self.outerR = outerR
    self.color = color
    self.leaf = leaf
  }
}

public struct SunburstOptions: Codable {
  public var startAngle: Double? = nil
  public var padAngle: Double? = nil
  public var maxDepth: Double? = nil
  public var sort: String? = nil
  public var showLabels: Bool? = nil
  public var labelColor: String? = nil
  public var fontSize: Double? = nil
  public var progress: Double? = nil
  public init(startAngle: Double? = nil, padAngle: Double? = nil, maxDepth: Double? = nil, sort: String? = nil, showLabels: Bool? = nil, labelColor: String? = nil, fontSize: Double? = nil, progress: Double? = nil) {
    self.startAngle = startAngle
    self.padAngle = padAngle
    self.maxDepth = maxDepth
    self.sort = sort
    self.showLabels = showLabels
    self.labelColor = labelColor
    self.fontSize = fontSize
    self.progress = progress
  }
}

public struct SunburstFrame: Codable {
  public var children: [TreeNode]
  public var a0: Double
  public var a1: Double
  public var depth: Int
  public var path: [Int]
  public var inherited: String
  public var hasInherited: Bool
  public init(children: [TreeNode], a0: Double, a1: Double, depth: Int, path: [Int], inherited: String, hasInherited: Bool) {
    self.children = children
    self.a0 = a0
    self.a1 = a1
    self.depth = depth
    self.path = path
    self.inherited = inherited
    self.hasInherited = hasInherited
  }
}

private let MINUTE = 60000.0

private let HOUR = MINUTE * 60.0

private let DAY = HOUR * 24.0

private let TAU = Double.pi * 2.0

private let START = -Double.pi / 2.0

private let RADAR_START = -Double.pi / 2.0

private let defaultTheme: ChartTheme = ChartTheme(axis: "#8496a5", grid: "rgba(132,150,165,0.18)", label: "#5a6b7a", fontSize: 11.0)

private let HEAT_RAMP = ["#eff6ff", "#93c5fd", "#3b82f6", "#1e40af"]

private let TREEMAP_PALETTE = ["#0f766e", "#b45309", "#1d4ed8", "#b42318", "#15803d", "#7c3aed"]

private let SUNBURST_TAU = Double.pi * 2.0

private let SUNBURST_PALETTE = ["#0f766e", "#b45309", "#1d4ed8", "#b42318", "#15803d", "#7c3aed"]

public func plain(_ v: Double) -> String {
    let r = (Double(v)).rounded()
    if abs(v - r) < 0.000001 {
      return "\(r)"
    }
    return "\((Double(v * 1000.0)).rounded() / 1000.0)"
  }

public func compact(_ v: Double) -> String {
    let abs = abs(v)
    let sign = v < 0.0 ? "-" : ""
    if Double(abs) >= 1000000000.0 {
      return "\(sign)\(trim(Double(abs) / 1000000000.0))B"
    }
    if Double(abs) >= 1000000.0 {
      return "\(sign)\(trim(Double(abs) / 1000000.0))M"
    }
    if Double(abs) >= 1000.0 {
      return "\(sign)\(trim(Double(abs) / 1000.0))K"
    }
    return plain(v)
  }

public func trim(_ v: Double) -> String {
    let r = (Double(v * 10.0)).rounded() / 10.0
    return ((r).truncatingRemainder(dividingBy: 1) == 0) ? "\((Double(r)).rounded())" : "\(r)"
  }

public func fixed(_ places: Int) -> (Double) -> String {
    let p = max(0, min(10, places))
    let mul = pow(Double(10.0), Double(p))
    return { v in
      let r = (Double(v * mul)).rounded() / mul
      if p == 0 {
        return "\((Double(r)).rounded())"
      }
      let s = "\(r)"
      let dot = (s.range(of: ".").map { s.distance(from: s.startIndex, to: $0.lowerBound) } ?? -1)
      if dot < 0 {
        return "\(s).\(String(repeating: "0", count: p))"
      }
      let decimals = s.count - dot - 1
      return decimals >= p ? s : "\(s)\(String(repeating: "0", count: p - decimals))"
    }
  }

public func currency(_ symbol: String, _ places: Int = 0) -> (Double) -> String {
    let f = fixed(places)
    return { v in (v < 0.0 ? "-\(symbol)\(f(-v))" : "\(symbol)\(f(v))") }
  }

public func percent(_ places: Int = 0) -> (Double) -> String {
    let f = fixed(places)
    return { v in "\(f(v * 100.0))%" }
  }

public func scaleLinear(_ d: Domain, _ r0: Double, _ r1: Double, _ v: Double) -> Double {
    let span = d.max - d.min
    if span == 0.0 {
      return (r0 + r1) / 2.0
    }
    return r0 + ((v - d.min) / Double(span)) * (r1 - r0)
  }

public func niceStep(_ raw: Double) -> Double {
    if raw <= 0.0 {
      return 1.0
    }
    let mag = pow(Double(10.0), Double(floor(Double(log10(Double(raw))))))
    let norm = raw / mag
    var m = 10.0
    if norm <= 1.0 {
      m = 1.0
    } else {
      if norm <= 2.0 {
        m = 2.0
      } else {
        if norm <= 5.0 {
          m = 5.0
        }
      }
    }
    return m * mag
  }

public func niceDomain(_ d: Domain, _ targetCount: Double) -> Domain {
    if d.max == d.min {
      return Domain(min: d.min - 0.5, max: d.max + 0.5)
    }
    let step = niceStep(Double((d.max - d.min)) / targetCount)
    return Domain(min: floor(Double(Double(d.min) / step)) * step, max: ceil(Double(Double(d.max) / step)) * step)
  }

public func makeTicks(_ d: Domain, _ r0: Double, _ r1: Double, _ count: Double, _ format: ((Double) -> String)? = nil) -> [Tick] {
    let fmt = (format ?? formatTick)
    var out: [Tick] = []
    if count <= 0.0 {
      return out
    }
    let span = d.max - d.min
    if span <= 0.0 {
      out.append(Tick(value: d.min, pos: scaleLinear(d, r0, r1, d.min), label: fmt(d.min)))
      return out
    }
    let step = niceStep(Double(span) / count)
    let first = ceil(Double(Double(d.min) / step)) * step
    let maxTicks = 1000
    var i = 0
    while i < maxTicks {
      let v = first + step * Double(i)
      if v > d.max + step * 0.000001 {
        break
      }
      out.append(Tick(value: v, pos: scaleLinear(d, r0, r1, v), label: fmt(v)))
      i = i + 1
    }
    return out
  }

public func formatTick(_ v: Double) -> String {
    let r = (Double(v)).rounded()
    if abs(v - r) < 0.000001 {
      return "\(r)"
    }
    return "\((Double(v * 1000.0)).rounded() / 1000.0)"
  }

public func extent(_ values: [Double]) -> Domain {
    if values.count == 0 {
      return Domain(min: 0.0, max: 1.0)
    }
    var lo = values[0]
    var hi = values[0]
    for v in values {
      if v < lo {
        lo = v
      }
      if v > hi {
        hi = v
      }
    }
    return Domain(min: lo, max: hi)
  }

public func scaleLog(_ d: Domain, _ r0: Double, _ r1: Double, _ v: Double) -> Double {
    let min = d.min <= 0.0 ? 0.000001 : d.min
    let max = d.max <= min ? min * 10.0 : d.max
    let val = v <= 0.0 ? min : v
    let lo = log10(Double(min))
    let hi = log10(Double(max))
    return r0 + ((log10(Double(val)) - lo) / (hi - lo)) * (r1 - r0)
  }

public func logTicks(_ d: Domain, _ r0: Double, _ r1: Double) -> [Tick] {
    let min = d.min <= 0.0 ? 0.000001 : d.min
    let max = d.max <= min ? min * 10.0 : d.max
    var out: [Tick] = []
    let from = floor(Double(log10(Double(min))))
    let to = ceil(Double(log10(Double(max))))
    let limit = 24
    var count = 0
    var e = from
    while e <= to && count < limit {
      let v = pow(Double(10.0), Double(e))
      if v >= min && v <= max {
        out.append(Tick(value: v, pos: scaleLog(d, r0, r1, v), label: plain(v)))
        count = count + 1
      }
      e = e + 1.0
    }
    return out
  }

public func timeTicks(_ d: Domain, _ r0: Double, _ r1: Double, _ target: Double, _ format: ((Double) -> String)? = nil) -> [Tick] {
    let span = d.max - d.min
    var out: [Tick] = []
    if span <= 0.0 || target <= 0 {
      return out
    }
    let steps = [1000.0, 5000.0, 15000.0, 30000.0, MINUTE, MINUTE * 5.0, MINUTE * 15.0, MINUTE * 30.0, HOUR, HOUR * 3.0, HOUR * 6.0, HOUR * 12.0, DAY, DAY * 7.0, DAY * 30.0, DAY * 90.0, DAY * 365.0]
    let ideal = Double(span) / target
    var step = steps[steps.count - 1]
    for s in steps {
      if s >= ideal {
        step = s
        break
      }
    }
    let fmt = (format ?? ({ x in formatTime(x, step) }))
    let first = ceil(Double(Double(d.min) / step)) * step
    let limit = 200
    var i = 0
    while i < limit {
      let v = first + step * Double(i)
      if v > d.max {
        break
      }
      out.append(Tick(value: v, pos: r0 + (Double((v - d.min)) / Double(span)) * (r1 - r0), label: fmt(v)))
      i = i + 1
    }
    return out
  }

public func formatTime(_ ms: Double, _ step: Double) -> String {
    let p2 = { (n: Double) in (n < 10 ? "0\(trunc(Double(n)))" : "\(trunc(Double(n)))") }
    let dayMs = 86400000.0
    let days = floor(Double(ms / dayMs))
    let msOfDay = ms - days * dayMs
    let z = days + 719468.0
    let era = floor(Double(z / 146097.0))
    let doe = z - era * 146097.0
    let yoe = floor(Double((doe - floor(Double(doe / 1460.0)) + floor(Double(doe / 36524.0)) - floor(Double(doe / 146096.0))) / 365.0))
    let y0 = yoe + era * 400.0
    let doy = doe - (365.0 * yoe + floor(Double(yoe / 4.0)) - floor(Double(yoe / 100.0)))
    let mp = floor(Double((5.0 * doy + 2.0) / 153.0))
    let day = doy - floor(Double((153.0 * mp + 2.0) / 5.0)) + 1.0
    let month = mp < 10.0 ? mp + 3.0 : mp - 9.0
    let year = month <= 2.0 ? y0 + 1.0 : y0
    let hours = floor(Double(msOfDay / 3600000.0))
    let minutes = floor(Double((msOfDay - hours * 3600000.0) / 60000.0))
    let seconds = floor(Double((msOfDay - hours * 3600000.0 - minutes * 60000.0) / 1000.0))
    if step >= DAY * 300.0 {
      return "\(trunc(Double(year)))"
    }
    if step >= DAY * 25.0 {
      return "\(trunc(Double(year)))-\(p2(month))"
    }
    if step >= DAY {
      return "\(p2(month))-\(p2(day))"
    }
    if step >= MINUTE {
      return "\(p2(hours)):\(p2(minutes))"
    }
    return "\(p2(hours)):\(p2(minutes)):\(p2(seconds))"
  }

public func smooth(_ points: [PyreonChartPt]) -> [PyreonChartPt] {
    let n = points.count
    if n < 3 {
      return points
    }
    var dx: [Double] = []
    var slope: [Double] = []
    for i in 0..<n - 1 {
      let dxi = points[i + 1].x - points[i].x
      dx.append(dxi)
      slope.append(dxi == 0.0 ? 0.0 : Double((points[i + 1].y - points[i].y)) / Double(dxi))
    }
    var m = [slope[0]]
    for i in 1..<n - 1 {
      let s0 = slope[i - 1]
      let s1 = slope[i]
      if s0 * s1 <= 0.0 {
        m.append(0.0)
      } else {
        let w0 = 2.0 * dx[i] + dx[i - 1]
        let w1 = dx[i] + 2.0 * dx[i - 1]
        m.append(Double((w0 + w1)) / Double((Double(w0) / Double(s0) + Double(w1) / Double(s1))))
      }
    }
    m.append(slope[n - 2])
    let steps = 16
    var out = [points[0]]
    for i in 0..<n - 1 {
      let p0 = points[i]
      let p1 = points[i + 1]
      let h = dx[i]
      for k in 1...steps {
        let t = Double(k) / Double(steps)
        let t2 = t * t
        let t3 = t2 * t
        let h00 = 2.0 * t3 - 3.0 * t2 + 1.0
        let h10 = t3 - 2.0 * t2 + t
        let h01 = -2.0 * t3 + 3.0 * t2
        let h11 = t3 - t2
        out.append(PyreonChartPt(x: p0.x + t * h, y: h00 * p0.y + h10 * h * m[i] + h01 * p1.y + h11 * h * m[i + 1]))
      }
    }
    return out
  }

public func step(_ points: [PyreonChartPt]) -> [PyreonChartPt] {
    let n = points.count
    if n < 2 {
      return points
    }
    var out = [points[0]]
    for i in 1..<n {
      out.append(PyreonChartPt(x: points[i].x, y: points[i - 1].y))
      out.append(points[i])
    }
    return out
  }

public func layoutArcs(_ slices: [Slice]) -> [ArcGeometry] {
    var total = 0.0
    for s in slices {
      if s.value > 0.0 {
        total = total + s.value
      }
    }
    var out: [ArcGeometry] = []
    if total <= 0.0 {
      return out
    }
    var angle = START
    for s in slices {
      if s.value <= 0.0 {
        continue
      }
      let fraction = Double(s.value) / total
      let sweep = fraction * TAU
      out.append(ArcGeometry(start: angle, end: angle + sweep, mid: angle + Double(sweep) / 2.0, slice: s, fraction: fraction))
      angle = angle + sweep
    }
    return out
  }

public func fitCircle(_ box: PyreonChartRect) -> Circle {
    let r = Double(min(box.w, box.h)) / 2.0
    return Circle(center: PyreonChartPt(x: box.x + Double(box.w) / 2.0, y: box.y + Double(box.h) / 2.0), radius: max(0.0, r))
  }

public func pointOnCircle(_ center: PyreonChartPt, _ radius: Double, _ angle: Double) -> PyreonChartPt { PyreonChartPt(x: center.x + cos(Double(angle)) * radius, y: center.y + sin(Double(angle)) * radius) }

public func arcPolygon(_ center: PyreonChartPt, _ outerR: Double, _ innerR: Double, _ start: Double, _ end: Double) -> [PyreonChartPt] {
    let sweep = abs(end - start)
    let steps = max(2, ceil(Double((sweep / Double(TAU)) * 64.0)))
    var pts: [PyreonChartPt] = []
    for i in 0...Int(ceil(Double(steps))) {
      pts.append(pointOnCircle(center, outerR, start + (sweep * Double(i)) / steps))
    }
    if innerR > 0.0 {
      for i in stride(from: Int(floor(Double(steps))), through: 0, by: -1) {
        pts.append(pointOnCircle(center, innerR, start + (sweep * Double(i)) / steps))
      }
    } else {
      pts.append(center)
    }
    return pts
  }

public func renderPie(_ slices: [Slice], _ box: PyreonChartRect, _ opts: PieOptions) -> [PyreonDrawCmd] {
    let __pyDestr1 = fitCircle(box)
    let center = __pyDestr1.center
    let radius = __pyDestr1.radius
    let inner = radius * max(0.0, min(0.95, opts.innerRadius))
    var out: [PyreonDrawCmd] = []
    let arcs = layoutArcs(slices)
    for a in arcs {
      out.append(PyreonDrawCmd(kind: "polygon", fill: a.slice.color, points: arcPolygon(center, radius, inner, a.start, a.end)))
    }
    if opts.showLabels {
      for a in arcs {
        if a.fraction < 0.05 {
          continue
        }
        let at = pointOnCircle(center, (radius + inner) / 2.0, a.mid)
        out.append(PyreonDrawCmd(kind: "text", fill: opts.labelColor, text: "\((Double(a.fraction * 100.0)).rounded())%", at: at, size: opts.fontSize, align: "middle", baseline: "middle"))
      }
    }
    return out
  }

public func hitArc(_ arcs: [ArcGeometry], _ center: PyreonChartPt, _ outerR: Double, _ innerR: Double, _ p: PyreonChartPt) -> Int {
    let dx = p.x - center.x
    let dy = p.y - center.y
    let dist = sqrt(Double(dx * dx + dy * dy))
    if dist > outerR || dist < innerR {
      return -1
    }
    let ang = atan2(Double(dy), Double(dx))
    for i in 0..<arcs.count {
      let a = arcs[i]
      var s = a.start
      var e = a.end
      while e > Double.pi {
        s = s - TAU
        e = e - TAU
      }
      let cand = ang > e ? ang - TAU : ang
      if cand >= s && cand <= e {
        return i
      }
    }
    return -1
  }

public func renderGauge(_ value: Double, _ box: PyreonChartRect, _ opts: GaugeOptions) -> [PyreonDrawCmd] {
    let __pyDestr1 = fitCircle(box)
    let center = __pyDestr1.center
    let radius = __pyDestr1.radius
    let inner = max(0.0, radius - opts.thickness)
    let span = opts.max - opts.min
    let t = span <= 0.0 ? 0.0 : max(0.0, min(1.0, (value - opts.min) / Double(span)))
    let start = Double.pi - (opts.sweep - Double.pi) / 2.0
    return [PyreonDrawCmd(kind: "polygon", fill: opts.trackColor, points: arcPolygon(center, radius, inner, start, start + opts.sweep)), PyreonDrawCmd(kind: "polygon", fill: opts.valueColor, points: arcPolygon(center, radius, inner, start, start + opts.sweep * t))]
  }

public func radarAngles(_ count: Int) -> [Double] {
    var out: [Double] = []
    if count <= 0 {
      return out
    }
    for i in 0..<count {
      out.append(RADAR_START + (Double.pi * 2.0 * Double(i)) / Double(count))
    }
    return out
  }

public func radarPolygon(_ values: [Double], _ axes: [RadarAxis], _ center: PyreonChartPt, _ radius: Double) -> [PyreonChartPt] {
    let n = min(values.count, axes.count)
    let angles = radarAngles(n)
    var out: [PyreonChartPt] = []
    for i in 0..<n {
      let axisMax = axes[i].max
      let t = axisMax <= 0.0 ? 0.0 : max(0.0, min(1.0, values[i] / Double(axisMax)))
      out.append(pointOnCircle(center, radius * t, angles[i]))
    }
    return out
  }

public func renderRadar(_ axes: [RadarAxis], _ series: [RadarSeries], _ box: PyreonChartRect, _ opts: RadarOptions) -> [PyreonDrawCmd] {
    var out: [PyreonDrawCmd] = []
    let n = axes.count
    if n < 3 {
      return out
    }
    let pad = opts.showLabels ? opts.fontSize * 3.0 : 0.0
    let radius = max(0.0, Double(min(box.w, box.h)) / 2.0 - pad)
    let center = PyreonChartPt(x: box.x + Double(box.w) / 2.0, y: box.y + Double(box.h) / 2.0)
    let angles = radarAngles(n)
    for r in 1...opts.rings {
      let rr = (radius * Double(r)) / Double(opts.rings)
      var ring: [PyreonChartPt] = []
      for a in angles {
        ring.append(pointOnCircle(center, rr, a))
      }
      ring.append(ring[0])
      out.append(PyreonDrawCmd(kind: "polyline", stroke: opts.gridColor, width: 1.0, points: ring))
    }
    for a in angles {
      out.append(PyreonDrawCmd(kind: "line", from: center, to: pointOnCircle(center, radius, a), stroke: opts.gridColor, width: 1.0))
    }
    for s in series {
      let poly = radarPolygon(s.values, axes, center, radius)
      if poly.count < 3 {
        continue
      }
      out.append(PyreonDrawCmd(kind: "polygon", fill: withAlpha(s.color, s.fillAlpha), points: poly))
      var closed: [PyreonChartPt] = []
      for p in poly {
        closed.append(p)
      }
      closed.append(poly[0])
      out.append(PyreonDrawCmd(kind: "polyline", stroke: s.color, width: 2.0, points: closed))
    }
    if opts.showLabels {
      for i in 0..<n {
        let at = pointOnCircle(center, radius + opts.fontSize * 1.2, angles[i])
        let dx = at.x - center.x
        let align = Double(abs(dx)) < 1.0 ? "middle" : dx > 0.0 ? "start" : "end"
        out.append(PyreonDrawCmd(kind: "text", fill: opts.labelColor, text: axes[i].label, at: at, size: opts.fontSize, align: align, baseline: "middle"))
      }
    }
    return out
  }

public func withAlpha(_ color: String, _ alpha: Double) -> String {
    let a = max(0.0, min(1.0, alpha))
    if !color.hasPrefix("#") {
      return color
    }
    let hex = String(color.dropFirst(1))
    let code = { (c: Double) in
      if c >= 48.0 && c <= 57.0 {
        return c - 48.0
      }
      if c >= 97.0 && c <= 102.0 {
        return c - 87.0
      }
      if c >= 65.0 && c <= 70.0 {
        return c - 55.0
      }
      return 0.0
    }
    let pair = { (at: Double) in code(Double(Array(hex.utf16)[Int(at)])) * 16.0 + code(Double(Array(hex.utf16)[Int(at + 1)])) }
    let single = { (at: Double) in code(Double(Array(hex.utf16)[Int(at)])) * 17.0 }
    if hex.count == 3 {
      return "rgba(\(single(0.0)), \(single(1.0)), \(single(2.0)), \(a))"
    }
    if hex.count == 6 {
      return "rgba(\(pair(0.0)), \(pair(2.0)), \(pair(4.0)), \(a))"
    }
    return color
  }

public func computeLayout(_ cfg: LayoutConfig, _ measure: (String, Double) -> Double) -> PlotLayout {
    let padTop = 8.0
    let padRight = 12.0
    let labelGap = 6.0
    let tickLen = 4.0
    let provisionalLabels = cfg.horizontal == true ? cfg.showYAxis ? cfg.categories : [] : cfg.showYAxis ? makeTicks(cfg.yDomain, cfg.height, 0.0, cfg.yTickCount, cfg.yFormat).map({ t in t.label }) : []
    var widest = 0.0
    for label in provisionalLabels {
      let w = measure(label, cfg.fontSize)
      if w > widest {
        widest = w
      }
    }
    let left = cfg.showYAxis ? widest + labelGap + tickLen : 0.0
    let bottom = cfg.showXAxis ? cfg.fontSize + labelGap + tickLen : 0.0
    let y2dom = (cfg.y2Domain ?? Domain(min: 0.0, max: 1.0))
    let hasY2 = cfg.y2Domain != nil && cfg.horizontal != true && cfg.showYAxis
    var widest2 = 0.0
    if hasY2 {
      for label in makeTicks(y2dom, cfg.height, 0.0, cfg.yTickCount, cfg.y2Format).map({ t in t.label }) {
        let w = measure(label, cfg.fontSize)
        if w > widest2 {
          widest2 = w
        }
      }
    }
    let right = hasY2 ? widest2 + labelGap + tickLen : padRight
    let plot = PyreonChartRect(x: left, y: padTop, w: max(0.0, cfg.width - left - right), h: max(0.0, cfg.height - padTop - bottom))
    if cfg.horizontal == true {
      let yTicks = cfg.showYAxis ? bandTicksY(cfg.categories, plot) : []
      let xTicks = cfg.showXAxis ? makeTicks(cfg.yDomain, plot.x, plot.x + plot.w, cfg.yTickCount, cfg.yFormat) : []
      return PlotLayout(plot: plot, xTicks: xTicks, yTicks: yTicks, y2Ticks: [], xDomainUsed: cfg.xDomain)
    }
    let yTicks = cfg.showYAxis ? makeTicks(cfg.yDomain, plot.y + plot.h, plot.y, cfg.yTickCount, cfg.yFormat) : []
    let xTicks = cfg.showXAxis ? cfg.categories.count > 0 ? bandTicks(cfg.categories, plot) : cfg.xTime == true ? timeTicks(cfg.xDomain, plot.x, plot.x + plot.w, cfg.xTickCount, cfg.xFormat) : makeTicks(cfg.xDomain, plot.x, plot.x + plot.w, cfg.xTickCount, cfg.xFormat) : []
    let y2Ticks = hasY2 ? makeTicks(y2dom, plot.y + plot.h, plot.y, cfg.yTickCount, cfg.y2Format) : []
    return PlotLayout(plot: plot, xTicks: xTicks, yTicks: yTicks, y2Ticks: y2Ticks, xDomainUsed: cfg.xDomain)
  }

public func bandTicksY(_ categories: [String], _ plot: PyreonChartRect) -> [Tick] {
    let n = categories.count
    var out: [Tick] = []
    if n == 0 {
      return out
    }
    let bh = Double(plot.h) / Double(n)
    for i in 0..<n {
      out.append(Tick(value: Double(i), pos: plot.y + bh * (Double(i) + 0.5), label: categories[i]))
    }
    return out
  }

public func bandTicks(_ categories: [String], _ plot: PyreonChartRect) -> [Tick] {
    let n = categories.count
    var out: [Tick] = []
    if n == 0 {
      return out
    }
    let bw = Double(plot.w) / Double(n)
    for i in 0..<n {
      out.append(Tick(value: Double(i), pos: plot.x + bw * (Double(i) + 0.5), label: categories[i]))
    }
    return out
  }

public func layoutBars(_ values: [Double], _ plot: PyreonChartRect, _ yDomain: Domain, _ gapRatio: Double) -> [PyreonChartRect] {
    let n = values.count
    var out: [PyreonChartRect] = []
    if n == 0 {
      return out
    }
    let ratio = gapRatio < 0.0 ? 0.0 : gapRatio > 0.9 ? 0.9 : gapRatio
    let band = Double(plot.w) / Double(n)
    let bw = band * (1.0 - ratio)
    let zero = yDomain.min < 0.0 && yDomain.max > 0.0 ? 0.0 : yDomain.min
    let zeroY = scaleLinear(yDomain, plot.y + plot.h, plot.y, zero)
    for i in 0..<n {
      let v = values[i]
      let vy = scaleLinear(yDomain, plot.y + plot.h, plot.y, v)
      let top = vy < zeroY ? vy : zeroY
      let h = abs(zeroY - vy)
      out.append(PyreonChartRect(x: plot.x + band * Double(i) + (band - bw) / 2.0, y: top, w: bw, h: h))
    }
    return out
  }

public func layoutSeriesPoints(_ values: [Double], _ plot: PyreonChartRect, _ yDomain: Domain) -> [PyreonChartPt] {
    let n = values.count
    var out: [PyreonChartPt] = []
    if n == 0 {
      return out
    }
    if n == 1 {
      out.append(PyreonChartPt(x: plot.x + Double(plot.w) / 2.0, y: scaleLinear(yDomain, plot.y + plot.h, plot.y, values[0])))
      return out
    }
    for i in 0..<n {
      out.append(PyreonChartPt(x: plot.x + (Double(i) / Double((n - 1))) * plot.w, y: scaleLinear(yDomain, plot.y + plot.h, plot.y, values[i])))
    }
    return out
  }

public func layoutSeriesPointsAt(_ values: [Double], _ xs: [Double], _ plot: PyreonChartRect, _ yDomain: Domain, _ xDomain: Domain) -> [PyreonChartPt] {
    let n = min(values.count, xs.count)
    var out: [PyreonChartPt] = []
    for i in 0..<n {
      out.append(PyreonChartPt(x: scaleLinear(xDomain, plot.x, plot.x + plot.w, xs[i]), y: scaleLinear(yDomain, plot.y + plot.h, plot.y, values[i])))
    }
    return out
  }

public func layoutBarsH(_ values: [Double], _ plot: PyreonChartRect, _ vDomain: Domain, _ gapRatio: Double) -> [PyreonChartRect] {
    let n = values.count
    var out: [PyreonChartRect] = []
    if n == 0 {
      return out
    }
    let ratio = gapRatio < 0.0 ? 0.0 : gapRatio > 0.9 ? 0.9 : gapRatio
    let band = Double(plot.h) / Double(n)
    let bh = band * (1.0 - ratio)
    let zero = vDomain.min < 0.0 && vDomain.max > 0.0 ? 0.0 : vDomain.min
    let zeroX = scaleLinear(vDomain, plot.x, plot.x + plot.w, zero)
    for i in 0..<n {
      let v = values[i]
      let vx = scaleLinear(vDomain, plot.x, plot.x + plot.w, v)
      let left = vx < zeroX ? vx : zeroX
      out.append(PyreonChartRect(x: left, y: plot.y + band * Double(i) + (band - bh) / 2.0, w: abs(vx - zeroX), h: bh))
    }
    return out
  }

public func hitBar(_ bars: [PyreonChartRect], _ px: Double, _ py: Double) -> Int {
    for i in 0..<bars.count {
      let b = bars[i]
      if px >= b.x && px <= b.x + b.w && py >= b.y && py <= b.y + b.h {
        return i
      }
    }
    return -1
  }

public func hitNearestX(_ points: [PyreonChartPt], _ px: Double) -> Int {
    if points.count == 0 {
      return -1
    }
    var best = 0
    var bestD = abs(points[0].x - px)
    for i in 1..<points.count {
      let d = abs(points[i].x - px)
      if d < bestD {
        bestD = d
        best = i
      }
    }
    return best
  }

public func layoutStackedBars(_ seriesValues: [[Double]], _ plot: PyreonChartRect, _ yDomain: Domain, _ gapRatio: Double) -> [StackSegment] {
    var out: [StackSegment] = []
    if seriesValues.count == 0 {
      return out
    }
    var n = 0
    for s in seriesValues {
      if s.count > n {
        n = s.count
      }
    }
    if n == 0 {
      return out
    }
    let ratio = gapRatio < 0.0 ? 0.0 : gapRatio > 0.9 ? 0.9 : gapRatio
    let band = Double(plot.w) / Double(n)
    let bw = band * (1.0 - ratio)
    for i in 0..<n {
      var acc = 0.0
      for s in 0..<seriesValues.count {
        let v = (seriesValues[s][i] ?? 0.0)
        if v <= 0.0 {
          continue
        }
        let yTop = scaleLinear(yDomain, plot.y + plot.h, plot.y, acc + v)
        let yBot = scaleLinear(yDomain, plot.y + plot.h, plot.y, acc)
        out.append(StackSegment(rect: PyreonChartRect(x: plot.x + band * Double(i) + (band - bw) / 2.0, y: yTop, w: bw, h: Double(abs(yBot - yTop))), seriesIndex: s, datumIndex: i, value: v))
        acc = acc + v
      }
    }
    return out
  }

public func stackHasNegatives(_ seriesValues: [[Double]]) -> Bool {
    for s in seriesValues {
      for v in s {
        if v < 0.0 {
          return true
        }
      }
    }
    return false
  }

public func stackedExtent(_ seriesValues: [[Double]]) -> Domain {
    var n = 0
    for s in seriesValues {
      if s.count > n {
        n = s.count
      }
    }
    var max = 0.0
    for i in 0..<n {
      var sum = 0.0
      for s in seriesValues {
        let v = (s[i] ?? 0.0)
        if v > 0.0 {
          sum = sum + v
        }
      }
      if sum > max {
        max = sum
      }
    }
    return Domain(min: 0.0, max: max == 0.0 ? 1.0 : max)
  }

public func layoutGroupedBars(_ seriesValues: [[Double]], _ plot: PyreonChartRect, _ yDomain: Domain, _ gapRatio: Double) -> [StackSegment] {
    var out: [StackSegment] = []
    let k = seriesValues.count
    if k == 0 {
      return out
    }
    var n = 0
    for s in seriesValues {
      if s.count > n {
        n = s.count
      }
    }
    if n == 0 {
      return out
    }
    let ratio = gapRatio < 0.0 ? 0.0 : gapRatio > 0.9 ? 0.9 : gapRatio
    let band = Double(plot.w) / Double(n)
    let groupW = band * (1.0 - ratio)
    let barW = groupW / Double(k)
    let zero = yDomain.min < 0.0 && yDomain.max > 0.0 ? 0.0 : yDomain.min
    let zeroY = scaleLinear(yDomain, plot.y + plot.h, plot.y, zero)
    for i in 0..<n {
      let gx = plot.x + band * Double(i) + (band - groupW) / 2.0
      for s in 0..<k {
        let v = (seriesValues[s][i] ?? 0.0)
        let vy = scaleLinear(yDomain, plot.y + plot.h, plot.y, v)
        out.append(StackSegment(rect: PyreonChartRect(x: gx + barW * Double(s), y: vy < zeroY ? vy : zeroY, w: barW, h: abs(zeroY - vy)), seriesIndex: s, datumIndex: i, value: v))
      }
    }
    return out
  }

public func layoutScatter(_ xs: [Double], _ ys: [Double], _ plot: PyreonChartRect, _ xDomain: Domain, _ yDomain: Domain) -> [PyreonChartPt] {
    let n = min(xs.count, ys.count)
    var out: [PyreonChartPt] = []
    for i in 0..<n {
      out.append(PyreonChartPt(x: scaleLinear(xDomain, plot.x, plot.x + plot.w, xs[i]), y: scaleLinear(yDomain, plot.y + plot.h, plot.y, ys[i])))
    }
    return out
  }

public func resolveYDomain(_ spec: ChartSpec) -> Domain { (spec.yDomain ?? deriveOver(leftAxisSeries(spec))) }

public func resolveY2Domain(_ spec: ChartSpec) -> Domain { (spec.y2Domain ?? deriveOver(rightAxisSeries(spec))) }

public func seriesOnRightAxis(_ s: Series, _ spec: ChartSpec) -> Bool {
    if spec.horizontal == true {
      return false
    }
    if s.kind == "stacked" || s.kind == "grouped" {
      return false
    }
    if s.axis != "right" {
      return false
    }
    var hasLeft = false
    for q in spec.series {
      let qRight = q.axis == "right" && q.kind != "stacked" && q.kind != "grouped"
      if !qRight {
        hasLeft = true
      }
    }
    return hasLeft
  }

public func hasRightAxis(_ spec: ChartSpec) -> Bool {
    for s in spec.series {
      if seriesOnRightAxis(s, spec) {
        return true
      }
    }
    return false
  }

public func leftAxisSeries(_ spec: ChartSpec) -> [Series] { spec.series.filter({ s in !seriesOnRightAxis(s, spec) }) }

public func rightAxisSeries(_ spec: ChartSpec) -> [Series] { spec.series.filter({ s in seriesOnRightAxis(s, spec) }) }

public func deriveOver(_ series: [Series]) -> Domain {
    let stacked = series.filter({ s in s.kind == "stacked" })
    if stacked.count > 0 {
      let e = stackedExtent(stacked.map({ s in s.values }))
      var others: [Double] = []
      for s in series {
        if s.kind != "stacked" {
          for v in s.values {
            if isFiniteValue(v) {
              others.append(v)
            }
          }
        }
      }
      let max = others.count > 0 ? max(e.max, extent(others).max) : e.max
      return niceDomain(Domain(min: 0.0, max: max), 5.0)
    }
    var all: [Double] = []
    var hasBars = false
    for s in series {
      if s.kind == "bars" || s.kind == "area" || s.kind == "grouped" {
        hasBars = true
      }
      for v in s.values {
        if isFiniteValue(v) {
          all.append(v)
        }
      }
    }
    let e = extent(all)
    let withZero = hasBars ? Domain(min: e.min > 0.0 ? 0.0 : e.min, max: e.max < 0.0 ? 0.0 : e.max) : e
    return niceDomain(withZero, 5.0)
  }

public func isFiniteValue(_ v: Double) -> Bool { v == v }

public func seriesMaxLength(_ series: [Series]) -> Int {
    var n = 0
    for s in series {
      if s.values.count > n {
        n = s.values.count
      }
    }
    return n
  }

public func layoutChart(_ spec: ChartSpec, _ measure: (String, Double) -> Double) -> PlotLayout {
    let n = seriesMaxLength(spec.series)
    let cfg = LayoutConfig(width: spec.width, height: spec.height, xDomain: ((spec.xValues ?? [])).count > 0 ? extent((spec.xValues ?? [])) : Domain(min: 0.0, max: n > 1 ? Double(n - 1) : 1.0), yDomain: resolveYDomain(spec), categories: spec.categories, fontSize: spec.theme.fontSize, xTickCount: 5.0, yTickCount: 5.0, showXAxis: spec.showXAxis, showYAxis: spec.showYAxis, yFormat: spec.yFormat, xFormat: spec.xFormat, xTime: spec.xTime == true, y2Domain: hasRightAxis(spec) ? resolveY2Domain(spec) : nil, y2Format: spec.y2Format, horizontal: spec.horizontal == true)
    return computeLayout(cfg, measure)
  }

public func renderChart(_ spec: ChartSpec, _ measure: (String, Double) -> Double) -> [PyreonDrawCmd] {
    let yDomain = resolveYDomain(spec)
    let useY2 = hasRightAxis(spec)
    let y2Domain = useY2 ? resolveY2Domain(spec) : yDomain
    let l = layoutChart(spec, measure)
    let plot = l.plot
    let t = spec.theme
    var out: [PyreonDrawCmd] = []
    let rawProgress = (spec.progress ?? 1.0)
    let progress = rawProgress < 0.0 ? 0.0 : rawProgress > 1.0 ? 1.0 : rawProgress
    let growRect = { (r: PyreonChartRect, dom: Domain) in
      if progress >= 1.0 {
        return r
      }
      let zeroY = scaleLinear(dom, plot.y + plot.h, plot.y, dom.min < 0.0 && dom.max > 0.0 ? 0.0 : dom.min)
      let h = r.h * progress
      let top = r.y + r.h <= zeroY + 0.5 ? zeroY - h : zeroY
      return PyreonChartRect(x: r.x, y: top, w: r.w, h: h)
    }
    let growRectH = { (r: PyreonChartRect) in
      if progress >= 1.0 {
        return r
      }
      let zeroX = scaleLinear(yDomain, plot.x, plot.x + plot.w, yDomain.min < 0.0 && yDomain.max > 0.0 ? 0.0 : yDomain.min)
      let w = r.w * progress
      let left = r.x >= zeroX - 0.5 ? zeroX : zeroX - w
      return PyreonChartRect(x: left, y: r.y, w: w, h: r.h)
    }
    let reveal = { (pts: [PyreonChartPt]) in
      if progress >= 1.0 || pts.count < 2 {
        return pts
      }
      let span = pts.count - 1
      let cut = Double(span) * progress
      let whole = floor(Double(cut))
      var outPts: [PyreonChartPt] = []
      for i in 0...Int(ceil(Double(whole))) {
        outPts.append(pts[i])
      }
      let frac = cut - whole
      if frac > 0.0 && whole + Double(1) < Double(pts.count) {
        let a = pts[Int(whole)]
        let b = pts[Int(whole + Double(1))]
        outPts.append(PyreonChartPt(x: a.x + (b.x - a.x) * frac, y: a.y + (b.y - a.y) * frac))
      }
      return outPts
    }
    if spec.showGrid {
      if spec.horizontal == true {
        for tick in l.xTicks {
          out.append(PyreonDrawCmd(kind: "line", from: PyreonChartPt(x: tick.pos, y: plot.y), to: PyreonChartPt(x: tick.pos, y: plot.y + plot.h), stroke: t.grid, width: 1.0))
        }
      } else {
        for tick in l.yTicks {
          out.append(PyreonDrawCmd(kind: "line", from: PyreonChartPt(x: plot.x, y: tick.pos), to: PyreonChartPt(x: plot.x + plot.w, y: tick.pos), stroke: t.grid, width: 1.0))
        }
      }
    }
    if spec.showYAxis {
      out.append(PyreonDrawCmd(kind: "line", from: PyreonChartPt(x: plot.x, y: plot.y), to: PyreonChartPt(x: plot.x, y: plot.y + plot.h), stroke: t.axis, width: 1.0))
    }
    if spec.showXAxis {
      out.append(PyreonDrawCmd(kind: "line", from: PyreonChartPt(x: plot.x, y: plot.y + plot.h), to: PyreonChartPt(x: plot.x + plot.w, y: plot.y + plot.h), stroke: t.axis, width: 1.0))
    }
    if spec.showYAxis && useY2 {
      out.append(PyreonDrawCmd(kind: "line", from: PyreonChartPt(x: plot.x + plot.w, y: plot.y), to: PyreonChartPt(x: plot.x + plot.w, y: plot.y + plot.h), stroke: t.axis, width: 1.0))
    }
    let notes = (spec.annotations ?? [])
    for a in notes {
      let yFrom = (a.yFrom ?? 0.0)
      let yTo = (a.yTo ?? 0.0)
      if a.yFrom != nil && a.yTo != nil {
        let y1 = scaleLinear(yDomain, plot.y + plot.h, plot.y, yFrom)
        let y2 = scaleLinear(yDomain, plot.y + plot.h, plot.y, yTo)
        let top = y1 < y2 ? y1 : y2
        out.append(PyreonDrawCmd(kind: "rect", rect: PyreonChartRect(x: plot.x, y: top, w: plot.w, h: Double(abs(y2 - y1))), fill: withAlpha((a.color ?? t.axis), 0.12)))
      }
    }
    for a in notes {
      let ay = (a.y ?? 0.0)
      if a.y != nil {
        let yPos = scaleLinear(yDomain, plot.y + plot.h, plot.y, ay)
        out.append(PyreonDrawCmd(kind: "line", from: PyreonChartPt(x: plot.x, y: yPos), to: PyreonChartPt(x: plot.x + plot.w, y: yPos), stroke: (a.color ?? t.axis), width: 1.0, dash: [4.0, 4.0]))
        let yLabel = (a.label ?? "")
        if a.label != nil {
          out.append(PyreonDrawCmd(kind: "text", fill: (a.color ?? t.label), text: yLabel, at: PyreonChartPt(x: plot.x + plot.w, y: yPos - 4.0), size: t.fontSize, align: "end", baseline: "bottom"))
        }
      }
      let ax = (a.x ?? 0.0)
      if a.x != nil {
        let xPos = scaleLinear(l.xDomainUsed, plot.x, plot.x + plot.w, ax)
        out.append(PyreonDrawCmd(kind: "line", from: PyreonChartPt(x: xPos, y: plot.y), to: PyreonChartPt(x: xPos, y: plot.y + plot.h), stroke: (a.color ?? t.axis), width: 1.0, dash: [4.0, 4.0]))
        let xLabel = (a.label ?? "")
        if a.label != nil {
          out.append(PyreonDrawCmd(kind: "text", fill: (a.color ?? t.label), text: xLabel, at: PyreonChartPt(x: xPos + 4.0, y: plot.y), size: t.fontSize, align: "start", baseline: "top"))
        }
      }
    }
    let stackedSeries = spec.horizontal == true ? [] : spec.series.filter({ s in s.kind == "stacked" })
    if stackedSeries.count > 0 {
      for seg in layoutStackedBars(stackedSeries.map({ s in s.values }), plot, yDomain, 0.25) {
        out.append(PyreonDrawCmd(kind: "rect", rect: growRect(seg.rect, yDomain), fill: stackedSeries[seg.seriesIndex].color))
      }
    }
    let groupedSeries = spec.horizontal == true ? [] : spec.series.filter({ s in s.kind == "grouped" })
    if groupedSeries.count > 0 {
      for seg in layoutGroupedBars(groupedSeries.map({ s in s.values }), plot, yDomain, 0.25) {
        out.append(PyreonDrawCmd(kind: "rect", rect: growRect(seg.rect, yDomain), fill: groupedSeries[seg.seriesIndex].color))
      }
    }
    for s in spec.series {
      if s.kind == "stacked" || s.kind == "grouped" {
        continue
      }
      let xs = (spec.xValues ?? [])
      let sDomain = seriesOnRightAxis(s, spec) ? y2Domain : yDomain
      let place = { (values: [Double]) in xs.count > 0 ? layoutSeriesPointsAt(values, xs, plot, sDomain, l.xDomainUsed) : layoutSeriesPoints(values, plot, sDomain) }
      let curveFn = (s.curve ?? ({ q in q }))
      let shape = { (pts: [PyreonChartPt]) in curveFn(pts) }
      if spec.horizontal == true {
        if s.kind != "bars" {
          continue
        }
        let rects = layoutBarsH(s.values, plot, yDomain, 0.25)
        for r in rects {
          let grown = growRectH(r)
          if s.symbol == nil {
            out.append(PyreonDrawCmd(kind: "rect", rect: grown, fill: s.color))
          } else {
            if s.symbolRepeat == true {
              let unit = grown.h
              var count = 0
              var acc = unit
              for k in 0..<400 {
                if unit > 0.0 && acc <= grown.w + 0.001 {
                  count = k + 1
                }
                acc = acc + unit
              }
              var kf = 0.0
              for k in 0..<count {
                out.append(symbolCommand(PyreonChartRect(x: grown.x + unit * kf, y: grown.y, w: unit, h: unit), (s.symbol ?? "rect"), s.color))
                kf = kf + 1.0
              }
            } else {
              out.append(symbolCommand(grown, (s.symbol ?? "rect"), s.color))
            }
          }
        }
        if s.showValues == true && progress >= 1.0 {
          let fmt = (spec.yFormat ?? plain)
          for i in 0..<rects.count {
            let r = rects[i]
            let v = s.values[i]
            out.append(PyreonDrawCmd(kind: "text", fill: t.label, text: fmt(v), at: PyreonChartPt(x: v < 0.0 ? r.x - 4.0 : r.x + r.w + 4.0, y: r.y + Double(r.h) / 2.0), size: t.fontSize, align: v < 0.0 ? "end" : "start", baseline: "middle"))
          }
        }
        continue
      }
      if s.kind == "bars" {
        let rects = layoutBars(s.values, plot, sDomain, 0.25)
        for r in rects {
          let grown = growRect(r, sDomain)
          if s.symbol == nil {
            out.append(PyreonDrawCmd(kind: "rect", rect: grown, fill: s.color))
          } else {
            if s.symbolRepeat == true {
              let unit = grown.w
              let length = grown.h
              var count = 0
              var acc = unit
              for k in 0..<400 {
                if unit > 0.0 && acc <= length + 0.001 {
                  count = k + 1
                }
                acc = acc + unit
              }
              var kf = 0.0
              for k in 0..<count {
                let cell = PyreonChartRect(x: grown.x, y: grown.y + grown.h - unit * (kf + 1.0), w: unit, h: unit)
                out.append(symbolCommand(cell, (s.symbol ?? "rect"), s.color))
                kf = kf + 1.0
              }
            } else {
              out.append(symbolCommand(grown, (s.symbol ?? "rect"), s.color))
            }
          }
        }
        if s.showValues == true && progress >= 1.0 {
          let fmt = (spec.yFormat ?? plain)
          for i in 0..<rects.count {
            let r = rects[i]
            let v = s.values[i]
            out.append(PyreonDrawCmd(kind: "text", fill: t.label, text: fmt(v), at: PyreonChartPt(x: r.x + Double(r.w) / 2.0, y: v < 0.0 ? r.y + r.h + 4.0 : r.y - 4.0), size: t.fontSize, align: "middle", baseline: v < 0.0 ? "top" : "bottom"))
          }
        }
      } else {
        if s.kind == "line" {
          for run in splitRuns(s.values, place) {
            let pts = reveal(shape(run))
            if pts.count > 1 {
              out.append(PyreonDrawCmd(kind: "polyline", stroke: s.color, width: s.width, points: pts))
            }
          }
        } else {
          if s.kind == "area" {
            for run in splitRuns(s.values, place) {
              let pts = reveal(shape(run))
              if pts.count > 1 {
                var poly: [PyreonChartPt] = []
                for p in pts {
                  poly.append(p)
                }
                poly.append(PyreonChartPt(x: pts[pts.count - 1].x, y: plot.y + plot.h))
                poly.append(PyreonChartPt(x: pts[0].x, y: plot.y + plot.h))
                out.append(PyreonDrawCmd(kind: "polygon", fill: s.color, points: poly))
              }
            }
          } else {
            let pts = place(s.values)
            let radii = (s.radii ?? [])
            for i in 0..<pts.count {
              if !isFiniteValue(s.values[i]) {
                continue
              }
              let fullR = radii.count > 0 ? (radii[i] ?? s.radius) : s.radius
              if s.effect == true {
                out.append(PyreonDrawCmd(kind: "circle", fill: withAlpha(s.color, 0.12), center: pts[i], radius: fullR * 2.6 * progress))
                out.append(PyreonDrawCmd(kind: "circle", fill: withAlpha(s.color, 0.25), center: pts[i], radius: fullR * 1.7 * progress))
              }
              out.append(PyreonDrawCmd(kind: "circle", fill: s.color, center: pts[i], radius: fullR * progress))
            }
          }
        }
      }
    }
    let markers = (spec.markers ?? [])
    for m in markers {
      if spec.horizontal == true {
        continue
      }
      let rawSeriesIndex = (m.seriesIndex ?? 0.0)
      let s = spec.series[Int(floor(Double(rawSeriesIndex)))]
      if s == nil {
        continue
      }
      if s.kind == "stacked" || s.kind == "grouped" {
        continue
      }
      let n = s.values.count
      if n == 0 {
        continue
      }
      var idx = -1
      if m.at == "max" {
        idx = 0
        for i in 1..<n {
          if s.values[i] > s.values[idx] {
            idx = i
          }
        }
      } else {
        if m.at == "min" {
          idx = 0
          for i in 1..<n {
            if s.values[i] < s.values[idx] {
              idx = i
            }
          }
        } else {
          let rawAt = (m.atIndex ?? -1.0)
          if m.atIndex != nil {
            var jf = 0.0
            for j in 0..<n {
              if jf <= rawAt {
                idx = j
              }
              jf = jf + 1.0
            }
            if idx < 0 {
              idx = 0
            }
          }
        }
      }
      if idx < 0 {
        continue
      }
      let mDomain = seriesOnRightAxis(s, spec) ? y2Domain : yDomain
      let xsM = (spec.xValues ?? [])
      let pts = xsM.count > 0 ? layoutSeriesPointsAt(s.values, xsM, plot, mDomain, l.xDomainUsed) : layoutSeriesPoints(s.values, plot, mDomain)
      let p = pts[idx]
      if p == nil {
        continue
      }
      let mColor = (m.color ?? s.color)
      out.append(PyreonDrawCmd(kind: "circle", fill: mColor, center: p, radius: ((m.radius ?? 4.0)) * progress))
      let mLabel = (m.label ?? "")
      if m.label != nil && progress >= 1.0 {
        out.append(PyreonDrawCmd(kind: "text", fill: mColor, text: mLabel, at: PyreonChartPt(x: p.x, y: p.y - ((m.radius ?? 4.0)) - 4.0), size: t.fontSize, align: "middle", baseline: "bottom"))
      }
    }
    for tick in l.yTicks {
      out.append(PyreonDrawCmd(kind: "text", fill: t.label, text: tick.label, at: PyreonChartPt(x: plot.x - 6.0, y: tick.pos), size: t.fontSize, align: "end", baseline: "middle"))
    }
    for tick in l.y2Ticks {
      out.append(PyreonDrawCmd(kind: "text", fill: t.label, text: tick.label, at: PyreonChartPt(x: plot.x + plot.w + 6.0, y: tick.pos), size: t.fontSize, align: "start", baseline: "middle"))
    }
    for tick in l.xTicks {
      out.append(PyreonDrawCmd(kind: "text", fill: t.label, text: tick.label, at: PyreonChartPt(x: tick.pos, y: plot.y + plot.h + 6.0), size: t.fontSize, align: "middle", baseline: "top"))
    }
    return out
  }

public func splitRuns(_ values: [Double], _ place: ([Double]) -> [PyreonChartPt]) -> [[PyreonChartPt]] {
    var runs: [[PyreonChartPt]] = []
    var hasGap = false
    for v in values {
      if !isFiniteValue(v) {
        hasGap = true
      }
    }
    if !hasGap {
      runs.append(place(values))
      return runs
    }
    var filled: [Double] = []
    for v in values {
      filled.append(isFiniteValue(v) ? v : 0.0)
    }
    let pts = place(filled)
    var runStart = -1
    for i in 0..<pts.count {
      if isFiniteValue(values[i]) {
        if runStart < 0 {
          runStart = i
        }
      } else {
        if runStart >= 0 {
          var run: [PyreonChartPt] = []
          for j in runStart..<i {
            run.append(pts[j])
          }
          runs.append(run)
          runStart = -1
        }
      }
    }
    if runStart >= 0 {
      var run: [PyreonChartPt] = []
      for j in runStart..<pts.count {
        run.append(pts[j])
      }
      runs.append(run)
    }
    return runs
  }

public func symbolCommand(_ cell: PyreonChartRect, _ symbol: String, _ fill: String) -> PyreonDrawCmd {
    if symbol == "circle" {
      let r = Double((cell.w < cell.h ? cell.w : cell.h)) / 2.0
      return PyreonDrawCmd(kind: "circle", fill: fill, center: PyreonChartPt(x: cell.x + Double(cell.w) / 2.0, y: cell.y + Double(cell.h) / 2.0), radius: r)
    }
    if symbol == "diamond" {
      return PyreonDrawCmd(kind: "polygon", fill: fill, points: [PyreonChartPt(x: cell.x + Double(cell.w) / 2.0, y: cell.y), PyreonChartPt(x: cell.x + cell.w, y: cell.y + Double(cell.h) / 2.0), PyreonChartPt(x: cell.x + Double(cell.w) / 2.0, y: cell.y + cell.h), PyreonChartPt(x: cell.x, y: cell.y + Double(cell.h) / 2.0)])
    }
    if symbol == "triangle" {
      return PyreonDrawCmd(kind: "polygon", fill: fill, points: [PyreonChartPt(x: cell.x + Double(cell.w) / 2.0, y: cell.y), PyreonChartPt(x: cell.x + cell.w, y: cell.y + cell.h), PyreonChartPt(x: cell.x, y: cell.y + cell.h)])
    }
    return PyreonDrawCmd(kind: "rect", rect: cell, fill: fill)
  }

public func barsFor(_ spec: ChartSpec, _ index: Int, _ measure: (String, Double) -> Double) -> [PyreonChartRect] {
    let s = spec.series[index]
    if s == nil || s.kind != "bars" {
      return []
    }
    let dom = seriesOnRightAxis(s, spec) ? resolveY2Domain(spec) : resolveYDomain(spec)
    return layoutBars(s.values, layoutChart(spec, measure).plot, dom, 0.25)
  }

public func stackedHitAt(_ spec: ChartSpec, _ measure: (String, Double) -> Double, _ px: Double, _ py: Double) -> Int {
    if spec.horizontal == true {
      return -1
    }
    let plot = layoutChart(spec, measure).plot
    let yDomain = resolveYDomain(spec)
    for kind in ["stacked", "grouped"] {
      let series = spec.series.filter({ s in s.kind == kind })
      if series.count == 0 {
        continue
      }
      let values = series.map({ s in s.values })
      let segs = kind == "stacked" ? layoutStackedBars(values, plot, yDomain, 0.25) : layoutGroupedBars(values, plot, yDomain, 0.25)
      for seg in segs {
        let r = seg.rect
        if px >= r.x && px <= r.x + r.w && py >= r.y && py <= r.y + r.h {
          return seg.datumIndex
        }
      }
    }
    return -1
  }

public func ohlcExtent(_ candles: [Ohlc]) -> Domain {
    if candles.count == 0 {
      return Domain(min: 0.0, max: 1.0)
    }
    var lo = candles[0].low
    var hi = candles[0].high
    for c in candles {
      if c.low < lo {
        lo = c.low
      }
      if c.high > hi {
        hi = c.high
      }
    }
    if hi <= lo {
      return Domain(min: lo - 1.0, max: lo + 1.0)
    }
    return Domain(min: lo, max: hi)
  }

public func renderCandles(_ candles: [Ohlc], _ plot: PyreonChartRect, _ domain: Domain, _ options: CandleOptions? = nil) -> [PyreonDrawCmd] {
    let up = (options?.upColor ?? "#15803d")
    let down = (options?.downColor ?? "#b42318")
    let rawRatio = (options?.widthRatio ?? 0.6)
    let ratio = rawRatio < 0.05 ? 0.05 : rawRatio > 0.9 ? 0.9 : rawRatio
    var out: [PyreonDrawCmd] = []
    let n = candles.count
    if n == 0 {
      return out
    }
    let band = Double(plot.w) / Double(n)
    let bw = band * ratio
    let yOf = { (v: Double) in scaleLinear(domain, plot.y + plot.h, plot.y, v) }
    for i in 0..<n {
      let c = candles[i]
      let cx = plot.x + band * Double(i) + band / 2.0
      let color = c.close >= c.`open` ? up : down
      out.append(PyreonDrawCmd(kind: "line", from: PyreonChartPt(x: cx, y: yOf(c.high)), to: PyreonChartPt(x: cx, y: yOf(c.low)), stroke: color, width: 1.0))
      let yo = yOf(c.`open`)
      let yc = yOf(c.close)
      let top = yo < yc ? yo : yc
      let h = abs(yc - yo)
      out.append(PyreonDrawCmd(kind: "rect", rect: PyreonChartRect(x: cx - bw / 2.0, y: top, w: bw, h: h < 1.0 ? 1.0 : h), fill: color))
    }
    return out
  }

public func hitCandle(_ count: Int, _ plot: PyreonChartRect, _ px: Double, _ py: Double) -> Int {
    if count <= 0 {
      return -1
    }
    if px < plot.x || px > plot.x + plot.w {
      return -1
    }
    if py < plot.y || py > plot.y + plot.h {
      return -1
    }
    let band = Double(plot.w) / Double(count)
    let target = (px - plot.x) / band
    var i = 0
    var jf = 0.0
    for j in 0..<count {
      if jf <= target {
        i = j
      }
      jf = jf + 1.0
    }
    return i
  }

public func buildHeatGrid(_ cols: [String], _ rows: [String], _ colOf: [Double], _ rowOf: [Double], _ values: [Double]) -> HeatGrid {
    var byKey = [String: Int]()
    var cells: [HeatCell] = []
    let n = min(colOf.count, min(rowOf.count, values.count))
    var minV = 0.0
    var maxV = 0.0
    var seen = false
    for i in 0..<n {
      let c = colOf[i]
      let r = rowOf[i]
      if c < 0.0 || r < 0.0 {
        continue
      }
      let key = "\(c):\(r)"
      let prior = byKey[key]
      let at = (prior ?? -1)
      if prior == nil {
        byKey[key] = cells.count
        cells.append(HeatCell(col: c, row: r, value: values[i]))
      } else {
        cells[at].value = cells[at].value + values[i]
      }
    }
    for cell in cells {
      if !seen {
        minV = cell.value
        maxV = cell.value
        seen = true
      } else {
        if cell.value < minV {
          minV = cell.value
        }
        if cell.value > maxV {
          maxV = cell.value
        }
      }
    }
    return HeatGrid(cols: cols, rows: rows, cells: cells, min: minV, max: maxV)
  }

public func hexChannel(_ hex: String, _ at: Int) -> Double {
    let code = { (ch: Double) in
      let c = ch
      if c >= 48.0 && c <= 57.0 {
        return c - 48.0
      }
      if c >= 97.0 && c <= 102.0 {
        return c - 87.0
      }
      if c >= 65.0 && c <= 70.0 {
        return c - 55.0
      }
      return 0.0
    }
    if hex.count < at + 2 {
      return 0.0
    }
    return code(Double(Array(hex.utf16)[Int(at)])) * 16.0 + code(Double(Array(hex.utf16)[Int(at + 1)]))
  }

public func colorRamp(_ stops: [String]) -> (Double) -> String {
    var rs: [Double] = []
    var gs: [Double] = []
    var bs: [Double] = []
    for sHex in stops {
      let off = sHex.hasPrefix("#") ? 1 : 0
      rs.append(hexChannel(sHex, off))
      gs.append(hexChannel(sHex, off + 2))
      bs.append(hexChannel(sHex, off + 4))
    }
    var spanF = -1.0
    for k in 0..<rs.count {
      spanF = spanF + 1.0
    }
    return { t in
      var result = "rgb(0, 0, 0)"
      if rs.count == 1 || (rs.count > 1 && t <= 0.0) {
        result = "rgb(\((Double(rs[0])).rounded()), \((Double(gs[0])).rounded()), \((Double(bs[0])).rounded()))"
      } else {
        if rs.count > 1 {
          let clamped = t >= 1.0 ? 1.0 : t
          let pos = clamped * spanF
          var idx = 0
          var idxF = 0.0
          var jf = 1.0
          for k in 1..<rs.count - 1 {
            if jf <= pos {
              idx = k
              idxF = jf
            }
            jf = jf + 1.0
          }
          let frac = pos - idxF
          let mix = { (x: Double, y: Double) in (Double(x + (y - x) * frac)).rounded() }
          result = "rgb(\(mix(rs[idx], rs[idx + 1])), \(mix(gs[idx], gs[idx + 1])), \(mix(bs[idx], bs[idx + 1])))"
        }
      }
      return result
    }
  }

public func renderHeat(_ options: HeatmapOptions) -> [PyreonDrawCmd] {
    let grid = options.grid
    let plot = options.plot
    let ramp = colorRamp(options.stops)
    let gap = (options.gap ?? 1.0)
    let rawP = (options.progress ?? 1.0)
    let progress = rawP < 0.0 ? 0.0 : rawP > 1.0 ? 1.0 : rawP
    var out: [PyreonDrawCmd] = []
    let nc = grid.cols.count
    let nr = grid.rows.count
    if nc == 0 || nr == 0 {
      return out
    }
    var ncF = 0.0
    for k in 0..<nc {
      ncF = ncF + 1.0
    }
    var nrF = 0.0
    for k in 0..<nr {
      nrF = nrF + 1.0
    }
    let cw = Double(plot.w) / Double(nc)
    let ch = Double(plot.h) / Double(nr)
    let span = grid.max - grid.min
    for cell in grid.cells {
      if cell.col >= ncF || cell.row >= nrF {
        continue
      }
      let t = span <= 0.0 ? 1.0 : Double((cell.value - grid.min)) / Double(span)
      let fullW = cw - gap
      let fullH = ch - gap
      let w = fullW * progress
      let h = fullH * progress
      let x = plot.x + cell.col * cw + gap / 2.0 + Double((fullW - w)) / 2.0
      let y = plot.y + cell.row * ch + gap / 2.0 + Double((fullH - h)) / 2.0
      out.append(PyreonDrawCmd(kind: "rect", rect: PyreonChartRect(x: x, y: y, w: w, h: h), fill: ramp(t)))
    }
    return out
  }

public func hitHeatCell(_ grid: HeatGrid, _ plot: PyreonChartRect, _ gap: Double, _ px: Double, _ py: Double) -> Int {
    let nc = grid.cols.count
    let nr = grid.rows.count
    if nc == 0 || nr == 0 {
      return -1
    }
    if px < plot.x || px > plot.x + plot.w || py < plot.y || py > plot.y + plot.h {
      return -1
    }
    let cw = Double(plot.w) / Double(nc)
    let ch = Double(plot.h) / Double(nr)
    let tCol = (px - plot.x) / Double(cw)
    let tRow = (py - plot.y) / Double(ch)
    var colF = 0.0
    var jf = 0.0
    for j in 0..<nc {
      if jf <= tCol {
        colF = jf
      }
      jf = jf + 1.0
    }
    var rowF = 0.0
    var kf = 0.0
    for k in 0..<nr {
      if kf <= tRow {
        rowF = kf
      }
      kf = kf + 1.0
    }
    let inX = px - (plot.x + colF * cw)
    let inY = py - (plot.y + rowF * ch)
    if inX < gap / 2.0 || inX > cw - gap / 2.0 {
      return -1
    }
    if inY < gap / 2.0 || inY > ch - gap / 2.0 {
      return -1
    }
    for i in 0..<grid.cells.count {
      let c = grid.cells[i]
      if c.col == colF && c.row == rowF {
        return i
      }
    }
    return -1
  }

public func layoutFunnel(_ stages: [FunnelStage], _ plot: PyreonChartRect, _ options: FunnelOptions? = nil) -> [FunnelStageGeometry] {
    var out: [FunnelStageGeometry] = []
    let n = stages.count
    if n == 0 {
      return out
    }
    let gap = (options?.gap ?? 2.0)
    let rawMin = (options?.minWidthRatio ?? 0.0)
    let minRatio = rawMin < 0.0 ? 0.0 : rawMin > 1.0 ? 1.0 : rawMin
    let sort = (options?.sort ?? "descending")
    let align = (options?.align ?? "center")
    var order: [Int] = []
    var nf = 0.0
    for i in 0..<n {
      order.append(i)
      nf = nf + 1.0
    }
    if sort != "none" {
      for i in 1..<n {
        let cur = order[i]
        var j = i - 1
        while j >= 0 {
          let a = stages[order[j]].value
          let b = stages[cur].value
          let swap = sort == "descending" ? a < b : a > b
          if !swap {
            break
          }
          order[j + 1] = order[j]
          j = j - 1
        }
        order[j + 1] = cur
      }
    }
    var maxV = 0.0
    for s in stages {
      if s.value > maxV {
        maxV = s.value
      }
    }
    let stageH = (plot.h - gap * (nf - 1.0)) / nf
    var kf = 0.0
    for k in 0..<n {
      let idx = order[k]
      let next = k + 1 < n ? order[k + 1] : -1
      let topRatio = maxV <= 0.0 ? 1.0 : Double(stages[idx].value) / maxV
      let topW = plot.w * (topRatio < minRatio ? minRatio : topRatio)
      let nextRatio = next >= 0 ? (maxV <= 0.0 ? 1.0 : Double(stages[next].value) / maxV) : minRatio
      let bottomW = plot.w * (nextRatio < minRatio ? minRatio : nextRatio)
      let top = plot.y + kf * (stageH + gap)
      let cx = align == "left" ? plot.x + Double(topW) / 2.0 : align == "right" ? plot.x + plot.w - Double(topW) / 2.0 : plot.x + Double(plot.w) / 2.0
      out.append(FunnelStageGeometry(index: idx, top: top, bottom: top + stageH, topWidth: topW, bottomWidth: bottomW, centerX: cx))
      kf = kf + 1.0
    }
    return out
  }

public func renderFunnel(_ stages: [FunnelStage], _ plot: PyreonChartRect, _ options: FunnelOptions? = nil) -> [PyreonDrawCmd] {
    var out: [PyreonDrawCmd] = []
    let geo = layoutFunnel(stages, plot, options)
    let rawP = (options?.progress ?? 1.0)
    let progress = rawP < 0.0 ? 0.0 : rawP > 1.0 ? 1.0 : rawP
    let showLabels = (options?.showLabels ?? true)
    let labelColor = (options?.labelColor ?? "#ffffff")
    let fontSize = (options?.fontSize ?? 11.0)
    let align = (options?.align ?? "center")
    for g in geo {
      let s = stages[g.index]
      let tw = g.topWidth * progress
      let bw = g.bottomWidth * progress
      let cxTop = align == "left" ? plot.x + Double(tw) / 2.0 : g.centerX
      let cxBottom = align == "left" ? plot.x + Double(bw) / 2.0 : align == "right" ? plot.x + plot.w - Double(bw) / 2.0 : g.centerX
      out.append(PyreonDrawCmd(kind: "polygon", fill: s.color, points: [PyreonChartPt(x: cxTop - Double(tw) / 2.0, y: g.top), PyreonChartPt(x: cxTop + Double(tw) / 2.0, y: g.top), PyreonChartPt(x: cxBottom + Double(bw) / 2.0, y: g.bottom), PyreonChartPt(x: cxBottom - Double(bw) / 2.0, y: g.bottom)]))
      if showLabels && progress >= 1.0 {
        out.append(PyreonDrawCmd(kind: "text", fill: labelColor, text: s.label, at: PyreonChartPt(x: cxTop, y: Double((g.top + g.bottom)) / 2.0), size: fontSize, align: "middle", baseline: "middle"))
      }
    }
    return out
  }

public func hitFunnel(_ stages: [FunnelStage], _ plot: PyreonChartRect, _ px: Double, _ py: Double, _ options: FunnelOptions? = nil) -> Int {
    for g in layoutFunnel(stages, plot, options) {
      if py < g.top || py > g.bottom {
        continue
      }
      let t = g.bottom <= g.top ? 0.0 : (py - g.top) / Double((g.bottom - g.top))
      let w = g.topWidth + (g.bottomWidth - g.topWidth) * t
      if px >= g.centerX - Double(w) / 2.0 && px <= g.centerX + Double(w) / 2.0 {
        return g.index
      }
    }
    return -1
  }

public func nodeValue(_ node: TreeNode) -> Double {
    if node.value != nil {
      return (node.value ?? 0.0)
    }
    var sum = 0.0
    var stack: [TreeNode] = []
    var sp = 0
    for c in (node.children ?? []) {
      if sp < stack.count {
        stack[sp] = c
      } else {
        stack.append(c)
      }
      sp = sp + 1
    }
    while sp > 0 {
      sp = sp - 1
      let cur = stack[sp]
      let own = cur.value
      if own != nil {
        sum = sum + ((own ?? 0.0))
      } else {
        for c in (cur.children ?? []) {
          if sp < stack.count {
            stack[sp] = c
          } else {
            stack.append(c)
          }
          sp = sp + 1
        }
      }
    }
    return sum
  }

public func worstRatio(_ row: [Double], _ side: Double, _ areaScale: Double) -> Double {
    if row.count == 0 || side <= 0.0 {
      return -1.0
    }
    var sum = 0.0
    var maxA = 0.0
    var minA = -1.0
    for v in row {
      let a = v * areaScale
      sum = sum + a
      if a > maxA {
        maxA = a
      }
      if minA < 0.0 || a < minA {
        minA = a
      }
    }
    if sum <= 0.0 || minA <= 0.0 {
      return -1.0
    }
    let s2 = side * side
    let r1 = (s2 * maxA) / (sum * sum)
    let r2 = (sum * sum) / (s2 * minA)
    return r1 > r2 ? r1 : r2
  }

public func squarify(_ values: [Double], _ rect: PyreonChartRect) -> [PyreonChartRect] {
    var out: [PyreonChartRect] = []
    var total = 0.0
    for v in values {
      total = total + v
    }
    for i in 0..<values.count {
      out.append(PyreonChartRect(x: rect.x, y: rect.y, w: 0.0, h: 0.0))
    }
    if values.count == 0 || total <= 0.0 || rect.w <= 0.0 || rect.h <= 0.0 {
      return out
    }
    let areaScale = Double((rect.w * rect.h)) / total
    var x = rect.x
    var y = rect.y
    var w = rect.w
    var h = rect.h
    var i = 0
    while i < values.count {
      var row: [Double] = []
      var rowIdx: [Int] = []
      var grow = true
      while grow && i < values.count {
        let v = values[i]
        let side = w >= h ? h : w
        if row.count > 0 {
          let before = worstRatio(row, side, areaScale)
          var candidate: [Double] = []
          for r in row {
            candidate.append(r)
          }
          candidate.append(v)
          let after = worstRatio(candidate, side, areaScale)
          if before >= 0.0 && after > before {
            grow = false
          }
        }
        if grow {
          row.append(v)
          rowIdx.append(i)
          i = i + 1
        }
      }
      var rowSum = 0.0
      for v in row {
        rowSum = rowSum + v * areaScale
      }
      let vertical = w >= h
      let side = vertical ? h : w
      let thick = side <= 0.0 ? 0.0 : Double(rowSum) / Double(side)
      var offset = 0.0
      for k in 0..<row.count {
        let a = row[k] * areaScale
        let len = thick <= 0.0 ? 0.0 : Double(a) / Double(thick)
        let target = rowIdx[k]
        if vertical {
          out[target] = PyreonChartRect(x: x, y: y + offset, w: thick, h: len)
        } else {
          out[target] = PyreonChartRect(x: x + offset, y: y, w: len, h: thick)
        }
        offset = offset + len
      }
      if vertical {
        x = x + thick
        w = w - thick
      } else {
        y = y + thick
        h = h - thick
      }
    }
    return out
  }

public func orderByValue(_ children: [TreeNode]) -> [Int] {
    var order: [Int] = []
    var vals: [Double] = []
    for i in 0..<children.count {
      order.append(i)
      vals.append(nodeValue(children[i]))
    }
    for i in 1..<order.count {
      let cur = order[i]
      let cv = vals[cur]
      var j = i - 1
      while j >= 0 {
        if vals[order[j]] >= cv {
          break
        }
        order[j + 1] = order[j]
        j = j - 1
      }
      order[j + 1] = cur
    }
    return order
  }

public func layoutTreemap(_ nodes: [TreeNode], _ rect: PyreonChartRect, _ options: TreemapOptions? = nil) -> [TreemapCell] {
    var cells: [TreemapCell] = []
    let padding = (options?.padding ?? 2.0)
    let maxDepth = (options?.maxDepth ?? 64.0)
    var stack: [TreemapFrame] = []
    stack.append(TreemapFrame(children: nodes, area: rect, depth: 0, path: [], inherited: "", hasInherited: false))
    var sp = 1
    while sp > 0 {
      sp = sp - 1
      let frame = stack[sp]
      var depthF = 0.0
      for d in 0..<frame.depth {
        depthF = depthF + 1.0
      }
      if depthF >= maxDepth || frame.children.count == 0 {
        continue
      }
      let order = orderByValue(frame.children)
      var values: [Double] = []
      for i in order {
        let v = nodeValue(frame.children[i])
        values.append(v < 0.0 ? 0.0 : v)
      }
      let rects = squarify(values, frame.area)
      var pushed: [TreemapFrame] = []
      for k in 0..<order.count {
        let idx = order[k]
        let node = frame.children[idx]
        let r = rects[k]
        let color = (node.color ?? (frame.hasInherited ? frame.inherited : TREEMAP_PALETTE[idx % TREEMAP_PALETTE.count]))
        let kids = (node.children ?? [])
        var cellPath: [Int] = []
        for p in frame.path {
          cellPath.append(p)
        }
        cellPath.append(idx)
        cells.append(TreemapCell(name: node.name, value: values[k], rect: r, depth: frame.depth, path: cellPath, color: color, leaf: kids.count == 0))
        if kids.count > 0 {
          let innerW = r.w - padding * 2.0
          let innerH = r.h - padding * 2.0
          pushed.append(TreemapFrame(children: kids, area: PyreonChartRect(x: r.x + padding, y: r.y + padding, w: innerW < 0.0 ? 0.0 : innerW, h: innerH < 0.0 ? 0.0 : innerH), depth: frame.depth + 1, path: cellPath, inherited: color, hasInherited: true))
        }
      }
      var pk = pushed.count - 1
      while pk >= 0 {
        if sp < stack.count {
          stack[sp] = pushed[pk]
        } else {
          stack.append(pushed[pk])
        }
        sp = sp + 1
        pk = pk - 1
      }
    }
    return cells
  }

public func hexDigit(_ c: Double) -> Double {
    if c >= 48.0 && c <= 57.0 {
      return c - 48.0
    }
    if c >= 97.0 && c <= 102.0 {
      return c - 87.0
    }
    if c >= 65.0 && c <= 70.0 {
      return c - 55.0
    }
    return 0.0
  }

public func hexPair(_ hex: String, _ at: Int) -> Double {
    if hex.count < at + 2 {
      return 0.0
    }
    return hexDigit(Double(Array(hex.utf16)[Int(at)])) * 16.0 + hexDigit(Double(Array(hex.utf16)[Int(at + 1)]))
  }

public func tintHex(_ hex: String, _ t: Double) -> String {
    if hex.count < 7 {
      return hex
    }
    let r = (Double(hexPair(hex, 1) + (255.0 - hexPair(hex, 1)) * t)).rounded()
    let g = (Double(hexPair(hex, 3) + (255.0 - hexPair(hex, 3)) * t)).rounded()
    let b = (Double(hexPair(hex, 5) + (255.0 - hexPair(hex, 5)) * t)).rounded()
    return "rgb(\(r), \(g), \(b))"
  }

public func approxTextWidth(_ text: String, _ fontSize: Double) -> Double {
    var units = 0.0
    for i in 0..<text.count {
      let c = Double(Array(text.utf16)[Int(i)])
      if c >= 48.0 && c <= 57.0 {
        units = units + 0.9
      } else {
        if c == 46.0 || c == 44.0 || c == 32.0 {
          units = units + 0.45
        } else {
          units = units + 1.0
        }
      }
    }
    return units * fontSize * 0.52
  }

public func renderTreemap(_ cells: [TreemapCell], _ options: TreemapOptions? = nil, _ measure: ((String, Double) -> Double)? = nil) -> [PyreonDrawCmd] {
    var out: [PyreonDrawCmd] = []
    let rawP = (options?.progress ?? 1.0)
    let progress = rawP < 0.0 ? 0.0 : rawP > 1.0 ? 1.0 : rawP
    let showLabels = (options?.showLabels ?? true)
    let fontSize = (options?.fontSize ?? 11.0)
    let labelColor = (options?.labelColor ?? "#ffffff")
    let m = (measure ?? approxTextWidth)
    for c in cells {
      let w = c.rect.w * progress
      let h = c.rect.h * progress
      let x = c.rect.x + Double((c.rect.w - w)) / 2.0
      let y = c.rect.y + Double((c.rect.h - h)) / 2.0
      var depthF = 0.0
      for d in 0..<c.depth {
        depthF = depthF + 1.0
      }
      let tintT = 0.35 + depthF * 0.15
      out.append(PyreonDrawCmd(kind: "rect", rect: PyreonChartRect(x: x, y: y, w: w, h: h), fill: c.leaf ? c.color : tintHex(c.color, tintT > 0.6 ? 0.6 : tintT)))
      if showLabels && progress >= 1.0 && c.leaf {
        let tw = m(c.name, fontSize)
        if tw + 8.0 <= c.rect.w && fontSize + 6.0 <= c.rect.h {
          out.append(PyreonDrawCmd(kind: "text", fill: labelColor, text: c.name, at: PyreonChartPt(x: c.rect.x + 4.0, y: c.rect.y + 4.0), size: fontSize, align: "start", baseline: "top"))
        }
      }
    }
    return out
  }

public func hitTreemap(_ cells: [TreemapCell], _ px: Double, _ py: Double) -> TreemapCell? {
    var bestIdx = -1
    var bestDepth = -1
    for i in 0..<cells.count {
      let c = cells[i]
      let r = c.rect
      if px < r.x || px > r.x + r.w || py < r.y || py > r.y + r.h {
        continue
      }
      if c.depth > bestDepth {
        bestDepth = c.depth
        bestIdx = i
      }
    }
    if bestIdx < 0 {
      return nil
    }
    return cells[bestIdx]
  }

public func treeDepth(_ nodes: [TreeNode]) -> Int {
    var deepest = 0
    var stack: [TreeNode] = []
    var depths: [Int] = []
    var sp = 0
    for n in nodes {
      stack.append(n)
      depths.append(1)
      sp = sp + 1
    }
    while sp > 0 {
      sp = sp - 1
      let cur = stack[sp]
      let d = depths[sp]
      if d > deepest {
        deepest = d
      }
      for c in (cur.children ?? []) {
        if sp < stack.count {
          stack[sp] = c
          depths[sp] = d + 1
        } else {
          stack.append(c)
          depths.append(d + 1)
        }
        sp = sp + 1
      }
    }
    return deepest
  }

public func layoutSunburst(_ nodes: [TreeNode], _ innerR: Double, _ outerR: Double, _ options: SunburstOptions? = nil) -> [SunburstArc] {
    var arcs: [SunburstArc] = []
    let rawLevels = treeDepth(nodes)
    let maxDepth = (options?.maxDepth ?? 64.0)
    var levelsF = 0.0
    for i in 0..<rawLevels {
      levelsF = levelsF + 1.0
    }
    if levelsF > maxDepth {
      levelsF = maxDepth
    }
    if levelsF <= 0.0 {
      return arcs
    }
    let ringW = (outerR - innerR) / levelsF
    let pad = (options?.padAngle ?? 0.0)
    let sortMode = (options?.sort ?? "desc")
    let startAngle = (options?.startAngle ?? -Double.pi / 2.0)
    var stack: [SunburstFrame] = []
    stack.append(SunburstFrame(children: nodes, a0: startAngle, a1: startAngle + SUNBURST_TAU, depth: 0, path: [], inherited: "", hasInherited: false))
    var sp = 1
    while sp > 0 {
      sp = sp - 1
      let frame = stack[sp]
      var depthF = 0.0
      for d in 0..<frame.depth {
        depthF = depthF + 1.0
      }
      if depthF >= levelsF || frame.children.count == 0 {
        continue
      }
      var order: [Int] = []
      if sortMode == "desc" {
        for i in orderByValue(frame.children) {
          order.append(i)
        }
      } else {
        for i in 0..<frame.children.count {
          order.append(i)
        }
      }
      var total = 0.0
      var count = 0.0
      for i in order {
        let v = nodeValue(frame.children[i])
        total = total + (v < 0.0 ? 0.0 : v)
        count = count + 1.0
      }
      let usable = frame.a1 - frame.a0 - pad * (count - 1.0)
      var cursor = frame.a0
      var pushed: [SunburstFrame] = []
      for idx in order {
        let node = frame.children[idx]
        let raw = nodeValue(node)
        let v = raw < 0.0 ? 0.0 : raw
        let span = total <= 0.0 || usable <= 0.0 ? 0.0 : usable * (Double(v) / Double(total))
        let color = (node.color ?? (frame.hasInherited ? frame.inherited : SUNBURST_PALETTE[idx % SUNBURST_PALETTE.count]))
        let kids = (node.children ?? [])
        var cellPath: [Int] = []
        for p in frame.path {
          cellPath.append(p)
        }
        cellPath.append(idx)
        let r0 = innerR + ringW * depthF
        arcs.append(SunburstArc(name: node.name, value: v, depth: frame.depth, path: cellPath, start: cursor, end: cursor + span, innerR: r0, outerR: r0 + ringW, color: color, leaf: kids.count == 0))
        if kids.count > 0 {
          pushed.append(SunburstFrame(children: kids, a0: cursor, a1: cursor + span, depth: frame.depth + 1, path: cellPath, inherited: color, hasInherited: true))
        }
        cursor = cursor + span + pad
      }
      var pk = pushed.count - 1
      while pk >= 0 {
        if sp < stack.count {
          stack[sp] = pushed[pk]
        } else {
          stack.append(pushed[pk])
        }
        sp = sp + 1
        pk = pk - 1
      }
    }
    return arcs
  }

public func renderSunburst(_ arcs: [SunburstArc], _ center: PyreonChartPt, _ options: SunburstOptions? = nil, _ measure: ((String, Double) -> Double)? = nil) -> [PyreonDrawCmd] {
    var out: [PyreonDrawCmd] = []
    let rawP = (options?.progress ?? 1.0)
    let progress = rawP < 0.0 ? 0.0 : rawP > 1.0 ? 1.0 : rawP
    let startAngle = (options?.startAngle ?? -Double.pi / 2.0)
    let limit = startAngle + SUNBURST_TAU * progress
    let showLabels = (options?.showLabels ?? true)
    let fontSize = (options?.fontSize ?? 11.0)
    let labelColor = (options?.labelColor ?? "#ffffff")
    let m = (measure ?? approxTextWidth)
    for a in arcs {
      if a.start >= limit || a.end <= a.start {
        continue
      }
      let end = a.end < limit ? a.end : limit
      var depthF = 0.0
      for d in 0..<a.depth {
        depthF = depthF + 1.0
      }
      let tintT = 0.2 + depthF * 0.15
      let fill = a.leaf ? a.color : tintHex(a.color, tintT > 0.5 ? 0.5 : tintT)
      out.append(PyreonDrawCmd(kind: "polygon", fill: fill, points: arcPolygon(center, a.outerR, a.innerR, a.start, end)))
      if showLabels && progress >= 1.0 {
        let midR = Double((a.innerR + a.outerR)) / 2.0
        let chord = midR * (a.end - a.start)
        let tw = m(a.name, fontSize)
        if chord >= tw + 4.0 && a.outerR - a.innerR >= fontSize + 4.0 {
          let mid = Double((a.start + a.end)) / 2.0
          out.append(PyreonDrawCmd(kind: "text", fill: labelColor, text: a.name, at: PyreonChartPt(x: center.x + cos(Double(mid)) * midR, y: center.y + sin(Double(mid)) * midR), size: fontSize, align: "middle", baseline: "middle"))
        }
      }
    }
    return out
  }

public func hitSunburst(_ arcs: [SunburstArc], _ center: PyreonChartPt, _ px: Double, _ py: Double) -> SunburstArc? {
    let dx = px - center.x
    let dy = py - center.y
    let dist = sqrt(Double(dx * dx + dy * dy))
    let ang = atan2(Double(dy), Double(dx))
    var bestIdx = -1
    var bestDepth = -1
    for i in 0..<arcs.count {
      let a = arcs[i]
      if dist < a.innerR || dist > a.outerR {
        continue
      }
      var t = ang
      while t < a.start {
        t = t + SUNBURST_TAU
      }
      while t >= a.start + SUNBURST_TAU {
        t = t - SUNBURST_TAU
      }
      if t > a.end {
        continue
      }
      if a.depth > bestDepth {
        bestDepth = a.depth
        bestIdx = i
      }
    }
    if bestIdx < 0 {
      return nil
    }
    return arcs[bestIdx]
  }
