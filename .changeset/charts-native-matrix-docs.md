---
'@pyreon/charts': patch
---

Docs: the multiplatform capability matrix gains a Charts (plot engine) row and the web-only table no longer lists `@pyreon/charts` as a whole — the `echarts` facade is web-only, the `/plot` engine and its hosts render natively. The manifest's multiplatform rationale says the same.
