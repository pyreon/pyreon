---
'@pyreon/charts': minor
---

Scatter and effectScatter series render on the polar coordinate (`PolarSeries.kind: 'scatter'`, `radius` from `symbolSize`): points at the line placement, circles only, hittable like line points — on web, iOS and Android.
