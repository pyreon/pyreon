---
'@pyreon/charts': minor
---

Line and scatter labels sit where ECharts puts them: a line's above its symbol, a scatter point's inside it, both against the symbol's box and with the same automatic colours as bar labels. A line with no symbols shows no labels, as in ECharts.
