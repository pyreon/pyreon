---
'@pyreon/create-zero': patch
---

Lift node-side coverage to ≥95% statements. Add `pathExists` helper tests covering file / directory / missing-path branches. Bump `coverageThresholds.statements` 94 → 95, `lines` 94 → 95, `functions` 94 → 95. Updates BELOW_FLOOR_EXEMPTIONS entry.
