---
'@pyreon/charts': minor
---

Series symbols: `points(y, { symbol })` draws every datum as a rect / circle / diamond / triangle, and `line(y, { symbol })` draws a symbol at every datum over the line.
