---
'@pyreon/mcp': patch
---

Regenerated API reference for `@pyreon/http` (typed endpoint input, `responseType`, `queryStyle`, `keyScope`, literal-colon paths), `@pyreon/query` (`useQuery` select generic) and `@pyreon/lathe` (native compile verification, `validate` option).
