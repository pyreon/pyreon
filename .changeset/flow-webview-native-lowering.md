---
'@pyreon/flow': minor
'@pyreon/native-compiler': minor
---

Lower `FlowWebView` to the native WebView bridge on iOS and Android, including the built-in host, live graph and command updates, reverse-channel callbacks, and static host styling. Add generated-host freshness and public-prop totality ratchets so the web and native contracts cannot silently drift.
