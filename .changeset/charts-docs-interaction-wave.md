---
'@pyreon/charts': patch
---

Docs: manifest entries for `<OptionChart>`, `<GanttChart>`, `createChartLink` and `sonifyValues` (MCP `get_api`, llms), an Interaction section (dataZoom, navigator, zoom presets, keyboard, update animation, linked charts), Gantt, sonification and option-host sections on the plot docs page, and the README.
