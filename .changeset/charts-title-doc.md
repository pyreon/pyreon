---
'@pyreon/charts': patch
---

The `<Chart>` API reference now names a common surprise: `title` labels the chart for screen readers and the accessible table, and `showTitle` draws it above the chart.
