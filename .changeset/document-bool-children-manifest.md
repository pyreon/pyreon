---
'@pyreon/document': patch
---

A `true` child in a document tree now renders nothing (JSX semantics) instead of the text "true". Manifest/docs updated for JSX / `h()` trees; the long example no longer uses a non-existent `<List items>` prop.
