---
'@pyreon/native-compiler': patch
---

Lower static option-format calendar heatmap, parallel-coordinate, and river charts through their native engines on SwiftUI and Compose, preserving literal ranges, values, axes, categories, domains, line styles, and grouped streams.
