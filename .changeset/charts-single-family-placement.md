---
'@pyreon/charts': minor
---

The shared canvas host gains a `frame` prop: a family chart (pie, gauge, sunburst, chord, funnel, treemap, tree, sankey) draws inside that rect of the whole chart instead of filling the box, so a title and legend can draw over it.
