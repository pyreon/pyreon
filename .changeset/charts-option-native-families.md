---
'@pyreon/native-compiler': minor
---

Lower static ECharts-shaped treemap, sunburst, tree, sankey, and graph `OptionChart` options through their native SwiftUI and Compose chart hosts. Native validation now uses an isolated Swift module cache and invalidates compiler verdicts when validator command flags change.
