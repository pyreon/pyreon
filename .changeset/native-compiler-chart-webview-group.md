---
'@pyreon/native-compiler': patch
---

`<ChartWebView group>` lowers on both targets: the group name travels in the hosted envelope (`pyreonChartWebViewData(…, group:)`) so the page joins its connected group natively.
