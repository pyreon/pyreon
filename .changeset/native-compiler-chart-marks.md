---
'@pyreon/native-compiler': minor
---

`<OptionChart>` lowers `markLine` and `markPoint` on Swift and Kotlin: statistics (`average` / `max` / `min` / `median`), `coord`s (category names included), point-to-point pairs, colours and sizes resolve at compile time over the literal series; a mark the facade cannot place is named rather than dropped.
