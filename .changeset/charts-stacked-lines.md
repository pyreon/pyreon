---
'@pyreon/charts': minor
---

`@pyreon/charts/plot` option facade: `series.stack` on a line series stacks it on the running total of its group (a gap carries the total), and a stacked area series resolves to the engine's `stackedArea` kind — the option no longer warns that stacked lines are unsupported.
