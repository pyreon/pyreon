---
"@pyreon/primitives": patch
---

Add the missing MIT `LICENSE` file. `@pyreon/primitives` is published with `LICENSE` in its `files` array but the file itself was absent from the package, so the published tarball shipped without a license. Adds the standard MIT license (identical to its sibling `@pyreon/core` packages) — no code change.
