---
'@pyreon/flow': minor
---

Add a generic parsed reverse-message channel and typed host-error callback to `FlowWebView`, and preserve arbitrary JSON-safe node and edge fields for bundled editor hosts.
