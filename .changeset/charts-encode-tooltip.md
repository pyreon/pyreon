---
'@pyreon/charts': minor
---

`Series.extras` (`[{ label, numbers | texts }]`): the tooltip lists the named extra values under the series value and the accessible table prints one column per extra.
