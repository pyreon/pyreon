---
'@pyreon/charts': minor
---

The engine's `Annotation` gains a segment form (`x1` / `y1` / `x2` / `y2`) for point-to-point lines, and `PointMarker.at` accepts `'average'` (the datum nearest the mean).
