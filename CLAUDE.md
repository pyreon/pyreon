# Pyreon — Signal-Based UI Framework

## Overview

Full-stack UI framework with fine-grained reactivity (signals). SSR, SSG, islands, SPA.
All packages under `@pyreon/*` scope.

**Open work**: This file (`CLAUDE.md`) is the single source of truth — feature status, shipped patterns, and open gaps are tracked inline next to the code they describe. The prior `.claude/plans/open-work-2026-q3.md` index was retired after it accumulated multiple stale "ready to execute" entries for already-shipped work (rocketstyle collapse tail, T2.2 doc-examples typecheck, iOS counter CI smoke — each documented as open while their CI / source artifacts proved otherwise). Capturing status next to the code makes drift visible at the point of edit. For prioritization across multiple open items, ask the user — the engineering bar is "do it properly, not quickly," not "burn down a backlog."

## Benchmark Results (Chromium via Playwright)

**Pyreon (idiomatic JSX — what users actually write) is the fastest FRAMEWORK on this synthetic row-list benchmark**, after the 2026-06 *objectivity pass* (see Methodology). On the proven pooled run below it wins **7 of 9 framework verdicts outright**, ties Solid on `select`, and Solid edges it on `remove` (7.20 vs 7.30ms) — every margin is honest (Pyreon competes on equal footing with the others; there is no manufactured "compiled" tier — see "One Pyreon entry" below). The robust, defensible signals: Pyreon sits in a **tight cluster with Vue and Solid** on small ops + create-1k (it leads that cluster by a few hundred µs); it holds a **genuine 2.4–3.0× edge over React/Svelte/Preact at bulk-create (10k) scale**; a large lead on React's keyed `swap` (7.1ms — ~10×) and Vue/Svelte's `append` (3.2–4.1×); and the `select`/`partial` ops where signal frameworks update O(changed) while VDOM re-runs render to diff O(total). Its only measurable cost vs optimal **Vanilla** is bulk-create (~7% at 1k, ~6% at 10k — per-row signal allocation + cleanup closure + keyed-For map); small mutations are tied-or-faster vs Vanilla within noise. On **retained memory** Pyreon is mid-pack (3.14MB — only Vue heavier), the one dimension it does NOT lead.

**One Pyreon entry — no manufactured tiers.** Earlier revisions of this benchmark shipped TWO Pyreon columns ("Pyreon" raw `h()`/JSX + "Pyreon (compiled)" hand-written `_tpl()`) and let only the hand-tuned one compete in rankings. That was a measurement artifact two ways: (a) the Pyreon compiler ALREADY lowers idiomatic JSX to the same `_tpl()` cloneNode + fine-grained-binding output a hand-written `_tpl()` impl produces, so they were never really two tiers; (b) a real **compiler inefficiency** masked the equivalence — the compiler emitted a wasteful per-row `_bind()` renderEffect for static item-property reads like `{String(row.id)}` in a `<For>` callback (it misregistered the callback's `row` param as reactive props), so idiomatic JSX paid 1k–10k unnecessary effect allocations + disposers per list. **PR #1501 fixed that** (`<For>`/render-callback item params are runtime values, not reactive props → bare property reads bake to static `textContent`); idiomatic JSX now emits byte-identically to hand-tuned `_tpl` and measures equal-or-faster. So the benchmark now shows ONE honest `Pyreon` entry = the idiomatic JSX users ship, competing in every ranking.

**The earlier "4–8× faster than everyone on small-list ops" claims were ALSO a measurement artifact, fixed in the same objectivity work.** Two harness bugs inflated them: (a) async frameworks (React/Vue/Preact) paid an `rAF` commit wait *inside the timed region*, folding a full animation-frame of browser scheduling latency (~4–6ms) into every small-list number; (b) the `select` and `clear` benches had **no reset hook**, so only the first of 21 runs did real work and the median of `[real, 0, 0, …]` was 0µs. The fix replaced the `rAF` waits with the tightest commit each framework allows (`flushSync` for React/Svelte, one microtask for Vue/Preact, nothing for the synchronous signal frameworks) and added per-run reset hooks, so every run measures real work and the comparison is close to pure framework CPU. It also fixed a non-idiomatic Solid text binding (hand-written `createRenderEffect(() => td.textContent = …)` → Solid's real compiler output `insert()`).

Numbers below are a **proven `--repeat 5` pooled run** (100 samples per op, 5 full passes with **independently reshuffled framework order each pass** so position-dependent confounds average out) of `examples/benchmark`'s `bench:fair` harness (**Apple M3 Max, 14 cores, 96 GB, Chromium 148** — the harness prints this machine stamp with every run). Per-framework page isolation (fresh `page.goto`), `--js-flags=--expose-gc` + per-iteration forced GC, adaptive warmup (5–15 iters, stops when rolling p90 stabilises within 10%), median + 95% bootstrap CI + CV across the pooled samples. Real Chromium via Playwright on the production `vite build`, DOM-verified renders, genuine `react@19` / `solid-js@1.9` / `vue@3.5` / `svelte@5` / `preact@10` deps. **Tightest-commit timing** (no `rAF` scheduler latency) + **per-run resets** on every op. **9 krausest-canon ops** (create/replace/partial/select/swap/remove/clear/create-10k/append-1k-to-10k). Wall-clock ms is machine-dependent — the column-to-column *ratios* are the signal. Reproduce: `cd examples/benchmark && bun bench:fair --repeat 5`.

| Benchmark          | Vanilla JS | **Pyreon** | Vue 3    | SolidJS  | React 19 | Svelte 5 | Preact   | Framework verdict + honest read |
| ------------------ | ---------- | ---------- | -------- | -------- | -------- | -------- | -------- | ------------------------------- |
| Create 1,000 rows  | 8.40ms     | **9.00ms** | 10.00ms  | 10.40ms  | 11.60ms  | 12.60ms  | 13.90ms  | 🥇 Pyreon outright (frameworks); ~7% over Vanilla |
| Replace 1,000 rows | 8.50ms     | **9.00ms** | 9.90ms   | 10.20ms  | 11.30ms  | 12.90ms  | 13.80ms  | 🥇 Pyreon outright; same shape as create |
| Partial update     | 800µs      | **700µs**  | 1.60ms   | 4.50ms   | 1.10ms   | 2.30ms   | 1.30ms   | 🥇 Pyreon outright — now *beats* Vanilla (700 vs 800µs); `_bindText` direct-subscriber ~6× leaner than Solid's effect-based `insert` (real architectural diff) |
| Select row         | 0µs        | **0µs**    | 700µs    | 0µs      | 300µs    | 400µs    | 400µs    | 🤝 tied Solid=Pyreon at 0µs (timer floor); VDOM pays 0.3–0.7ms to diff all 1000 children |
| Swap rows          | 800µs      | **700µs**  | 1.40ms   | 800µs    | 7.10ms   | 2.40ms   | 1.10ms   | 🥇 Pyreon outright (700µs vs Solid/Vanilla 800µs); React 7.10ms = keyed-list O(n) reconcile (~10×) |
| Remove row         | 6.90ms     | **7.30ms** | 8.40ms   | 7.20ms   | 7.50ms   | 8.80ms   | 7.80ms   | 🥇 SolidJS outright (7.20 vs Pyreon 7.30ms) — Pyreon's sole loss; most architecture-neutral op (6.9–8.8ms band) |
| Clear rows         | 100µs      | **200µs**  | 400µs    | 500µs    | 1.00ms   | 400µs    | 800µs    | 🥇 Pyreon outright; VDOM unmount 0.4–1.0ms |
| Create 10,000 rows | 89.80ms    | **95.00ms**| 110.50ms | 115.50ms | 226.20ms | 237.70ms | 288.20ms | 🥇 Pyreon outright at 1.058× Vanilla; Vue 1.16×, Solid 1.22×, React 2.38×, Svelte 2.50×, Preact 3.03× slower |
| Append 1k→10k rows | 20.90ms    | **22.40ms**| 92.30ms  | 23.80ms  | 24.70ms  | 72.10ms  | 28.30ms  | 🥇 Pyreon outright; Vue 4.12× / Svelte 3.22× pay a full keyed-reconcile on the grow |

(Vanilla JS is the no-framework baseline, held OUT of the framework verdict. `Pyreon` = the idiomatic JSX impl (`pyreon.tsx`) — what users write. Median of 100 pooled samples (`--repeat 5`, 2026-06-17 run, Apple M3 Max / Chromium 148.0.7778.96); full CI95 + CV in the live harness output. Pyreon is **leader or tied-leader in 8 of 9 framework verdicts — 7 outright** (create/replace/partial/swap/clear/create-10k/append), ties Solid on `select`, and **Solid takes `remove` outright** (7.20 vs 7.30ms — Pyreon's only outright loss, the most architecture-neutral op).)

**Retained JS heap after suite (post-GC, MB) — the one dimension Pyreon does NOT lead:** Preact 2.18 < Solid 2.26 < Svelte 2.45 < React 2.57 < Vanilla 2.59 < **Pyreon 3.14** < Vue 3.97. Per-row signal allocation + cleanup closures cost Pyreon more retained memory than the VDOM frameworks; the benchmark measures and reports this honestly instead of only showing the wall-clock wins. (The PR #1501 compiler fix shaved this from ~3.33 → ~3.15MB by dropping the wasteful per-row `_bind` effects; the 2026-06-11 optimizations left retained heap unchanged within noise.)

**2026-06-11 create-path optimizations (now IN the table above — the run is post-optimization).** Two shipped changes attack the one place Vanilla still led (bulk-create): (1) `createSelector` inline-first-subscriber storage (the signal `_d1` trick on the selector's per-key buckets — a 10k-row create previously allocated 10k single-entry Sets; PR #1537) and (2) the compiler's **baked text placeholder** (sole-dynamic-text children ship `' '` in the template HTML + bind via `.firstChild` instead of per-row `createTextNode+appendChild`; both backends byte-identical; PR #1540). Measured by **interleaved A/B/A/B replication** (both changes flipped together within one tree, cool machine, Vanilla stable across all four runs, bundle shapes verified per state): **create-10k gap vs Vanilla 8.5/9.0ms → 5.5/6.5ms (−3ms, −31%, replicated across both flips)**, append trending −1ms; create-1k effect ≤300µs (below cross-run resolution — an earlier hot-machine pairing overclaimed "gap → 0" there). A third candidate (hoisted bind function) measured as a **null result** under proper pairing and is recorded as do-not-re-propose. Full methodology + profiles: `.claude/audits/create-path-perf-2026-06-11.md`; the SSR-side campaign (escapeHtml charCode scan, key-marker fast paths, attr-name memo — blog-shaped pages −10%, 1k-row tables −13% per render, A/B/A/B-replicated) is at `.claude/audits/ssr-throughput-2026-06-11.md`.

**What this means in practice:** After the 2026-06 objectivity pass (per-pass-reshuffled order, ONE honest Pyreon entry, retained-heap dimension, +2 krausest ops, the PR #1501 compiler fix), the honest picture is *Pyreon (idiomatic JSX) is the fastest framework on this synthetic row-list benchmark, with mid-pack retained memory* — strong, but NOT "infinitely faster." **Pyreon does not win literally everything:** **Solid takes `remove row` outright** (7.20 vs 7.30ms — the most VDOM-neutral op, a hair's-width margin), `select` is a **tie** with Solid (both at the 0µs timer floor), and on **retained heap Pyreon is 6th of 7** (3.14MB; only Vue heavier). **The real, robust advantages:** (1) bulk-create at 10k — Pyreon 95.0ms vs Vue 110 / Solid 116 / React 226 / Svelte 238 / Preact 288 (the VDOM 2.4–3.0× is genuine reconciliation cost); (2) `append-1k→10k` where Vue (4.12×) and Svelte (3.22×) pay a full keyed-reconcile while Pyreon edges Solid (22.4 vs 23.8ms); (3) React's keyed `swap` (7.10ms — O(n) reconcile, ~10× Pyreon's 700µs); (4) `partial update` where Pyreon's `_bindText` direct-subscriber (~8µs/update) beats even Solid's idiomatic `insert()` (~42µs/update) — the one place Pyreon clearly out-fine-grains Solid; (5) `select`/`partial` O(changed)-vs-VDOM-O(total). Against **Vanilla**, Pyreon's only measurable cost is bulk-create (~7% at 1k, ~6% at 10k — per-row signal allocation + cleanup closure + keyed For-loop map); the small mutations are tied-or-faster within noise, and after PR #1501 idiomatic JSX no longer carries per-row `_bind` overhead for static item reads. **This run measures close to pure framework CPU** (tightest commits) — *not* React's default async path, so real-world async latency would sit higher.

Earning a legitimate broad "fastest" claim still requires either (a) the compiler-pass collapse for rocketstyle (see "Compile-time rocketstyle collapse" section below — no other framework has Pyreon's multi-dimensional theme system to compile away) or (b) a **real-app** head-to-head, which genuinely does not exist yet: the `examples/cpa-pw-app-{react,solid,vue,preact,svelte}` ports run on Pyreon's compat shims (Pyreon runtime, not the real frameworks), so they cannot serve as a fair Pyreon-vs-React real-app comparison. Building real-framework app ports is tracked, multi-day work — **deliberately not faked here**. The honest scope for that work, written out so future contributors can pick it up:

1. **Canonical app**: TodoMVC (industry-standard reference) OR a thin blog-with-routes (covers SSR, navigation, hydration — closer to what real Pyreon users ship). TodoMVC is faster to bootstrap; blog gives more representative coverage. Recommend starting with TodoMVC.
2. **Real-framework ports (5)**: Pyreon, React 19 + react-dom, SolidJS, Vue 3, Svelte 5. Each in its OWN `examples/real-bench/<framework>/` directory with the framework's idiomatic build setup (Create React App for React, Vite-Solid for Solid, etc.). NO Pyreon-compat shim — each uses the actual framework runtime via real npm-published deps. Preact is optional (a Preact port mostly duplicates the React shape; lower priority).
3. **Benchmark scenarios** (real-app shape, not synthetic): (a) cold-start to interactive (TTI), (b) toggle 100 todos completed, (c) filter all/active/completed cycle, (d) add 10 todos in rapid succession, (e) clear completed × 50 todos, (f) drag-reorder 20 todos. Each scenario is one timed region; same methodology as `bench:fair` (per-framework page isolation, GC between iterations, adaptive warmup, 20 timed runs, median + CI95).
4. **Methodology discipline**: same `commit?`-hook per-framework wait pattern as `bench:fair` (sync frameworks no `commit`, async frameworks supply their scheduler wait). Each app must use its framework's idiomatic state management (Pyreon → signals; React → useState/useReducer; Solid → createSignal; Vue → ref/reactive; Svelte → $state). NO cross-framework patterns (don't make React use signals, etc.).
5. **Bundle-size delta** alongside wall-clock: real apps trade off framework overhead AND bundle weight. Report gzipped main bundle per framework next to the perf medians — closes the second half of the "fast in real apps" claim.
6. **Time estimate**: 3-5 days. 1 day per framework port + 1 day on the harness + 1 day on validation/results. The harness can be lifted directly from `examples/benchmark/bench-fair.ts` — same Playwright loop, same per-page isolation, same JSON output shape. The work is in writing the apps themselves, not the runner.

**Why this matters**: the current benchmark shows Pyreon (idiomatic JSX) leading or tying every framework on the synthetic row-list wall-clock ops (and clearly faster than React/Vue/Svelte/Preact on bulk-create + append + partial), but those are isolated DOM operations against contrived 1k-row data — and Pyreon is mid-pack on retained memory, which the suite now reports. A real-app head-to-head closes the credibility gap — and may surface NEW bottlenecks (router cost, form input latency, large form re-render) that the synthetic benchmark doesn't exercise. Until it lands, "fastest" claims stop at the synthetic benchmark's evidence and don't extrapolate to real apps. **The single biggest remaining objectivity gap is the author-judge problem** (the framework author writes + judges the benchmark) — only an upstream submission to the independent krausest/js-framework-benchmark fully resolves it; the 2026-06 op-set alignment is the prerequisite.

**Methodology — designed for objectivity:**
- ✓ **Per-framework page isolation** — each framework runs in its OWN fresh `page.goto('?framework=<name>')` so no cross-suite memory pressure / heap bias / JIT cache poisoning from earlier suites. (Previous all-frameworks-one-page method gave first-run frameworks a measurable advantage.)
- ✓ **Forced GC between iterations** — Chromium launches with `--js-flags=--expose-gc`; `runner.ts` calls `globalThis.gc()` between iterations. Removes the dominant source of inter-run variance (heap growth from prior iteration running unsynchronised collection during the next timed region).
- ✓ **Adaptive warmup** — keeps warming until the JS engine reaches steady state (rolling p90 of last 3 samples within 10% of the prior 3). Bounded `WARMUP_MIN=5`, `WARMUP_MAX=15`. Strictly more objective than a fixed warmup — React's MessageChannel scheduler reproducibly needs 10-15 iterations; signal frameworks usually stabilise in 5-7.
- ✓ **20 timed runs, median + 95% bootstrap CI + CV** — bootstrap (1000 resamples) is non-parametric so it doesn't assume normal distribution (timing data is right-skewed by GC outliers). CV (stddev/mean) tells you how stable the measurement was: <10% is tight, >20% means jittery and the median should be read with caution.
- ✓ **CI95-overlap tied-within-noise detection** — `bench-fair.ts` output flags each test with a `🤝` marker listing every framework whose CI95 overlaps with the leader's. Removes the "is a 5% difference real?" judgement call from the reader.
- ✓ Real Chromium via Playwright on production `vite build` output (not happy-dom, not dev mode).
- ✓ DOM verification per iteration (`expectRows(N)` / `expectRowsWithSelected`) — frameworks that "win" by not committing **throw**, not pass.
- ✓ Seeded mulberry32 RNG with per-framework reset — every framework sees identical row labels.
- ✓ Real published deps (`react@19.2.4`, `solid-js@1.9.11`, `vue@3.5.x`, `svelte@5.x`, `preact@10`) — no in-house ports.
- ✓ **Tightest-commit-per-framework, no rAF (objectivity pass)** — each impl uses the smallest commit that still passes DOM verification, so the timed region isolates framework work: **React + Svelte 5** `flushSync()` (synchronous, zero scheduler wait); **Vue** `await nextTick()` + **Preact** `await Promise.resolve()` (one microtask — their real flush boundary, no rAF); **Vanilla / Pyreon (raw + compiled) / Solid** nothing (synchronous DOM/signal writes). The earlier `rAF + setTimeout(0)` for React/Preact folded a full animation-frame of browser scheduling latency into their timed region (~4–6ms) and inflated every small-list number — the single biggest fairness bug, now removed. Trade-off: `flushSync` is not React's default async path, so these are CPU-objective numbers, not real-world-async-latency.
- ✓ **Per-run reset hooks on EVERY op (objectivity pass)** — `select`/`clear` previously had none, so only the first of 21 runs did real work (select re-selected the already-selected row; clear cleared the already-empty list) and the median was a meaningless 0µs. Each op now resets untimed before each timed run (clear repopulates 1000 rows; select deselects), so all 21 runs measure real work. The old `rAF` wait had masked this by adding a constant ~5ms to every run regardless.
- ✓ **Idiomatic per-framework impls, verified** — each uses its framework's real compiler output / best-practice API: React/Preact `memo` + keys, Solid `template()` + `insert()` + `createSelector` + `For` (the non-idiomatic `createRenderEffect(() => td.textContent=…)` text binding was fixed to `insert()`), Svelte `$state` runes + `flushSync`, Vue keyed `v-for` + `ref`, Vanilla cached node refs. No competitor is sandbagged; audited file-by-file.
- ✓ **Optional CPU throttling** — `bun bench:fair --throttle 4` enables 4× slowdown via CDP `Emulation.setCPUThrottlingRate` (mimics mid-tier devices). Off by default; the uncongested numbers are the reference.
- ✓ **Randomized framework execution order (2026-06 objectivity pass)** — `bench-fair.ts` Fisher-Yates–shuffles the run order each process (printed as `[bench-fair] run order: …`) so thermal/JIT-warmth ordering bias can't favour whichever framework happens to run first or last. The OUTPUT tables keep a fixed canonical column order regardless. Prior to this, every run executed frameworks in declaration order, which gave the first-run framework a small but systematic edge on the very first op of its suite even with per-page isolation.
- ✓ **Machine stamp printed with results (2026-06)** — every run prints CPU model + logical cores + RAM + OS + the exact Chromium version (`browser.version()`) before the tables, so absolute-ms numbers are reproducible/attributable instead of floating context-free. (The representative table below was an Apple M3 Max / Chromium 148 run.)
- ✓ **Per-pass reshuffled execution order (2026-06)** — a single fixed order pins every framework to the same slot every `--repeat` pass, so position-dependent confounds (CPU thermal drift across a sweep, JIT/heap state inherited from the prior framework's page) bias the SAME framework the SAME way every pass and pooling never averages it out. The harness now reshuffles the order INDEPENDENTLY each pass (`aggregateRepeated` keys by framework NAME, not positional index), turning slot into noise the 100-sample pool cancels. The single-pass order is still randomized. This was a real harness bug: an earlier `--repeat` shuffled once per process.
- ✓ **Retained-heap metric (2026-06 — closes the "still missing memory" gap)** — Chromium launches with `--enable-precise-memory-info`; after each framework's full suite completes (DOM already cleaned), the harness forces GC 3× and reads `performance.memory.usedJSHeapSize`, printed as a "Retained JS heap after suite (post-GC, MB)" table. Honestly captioned as framework-runtime + post-cleanup retention (NOT the krausest after-create metric). **This is the dimension where Pyreon does NOT lead** — the proven run: Preact 2.19 < Solid 2.26 < Svelte 2.44 < React 2.57 < Vanilla 2.59 < **Pyreon 3.15** < Vue 3.97 MB. Pyreon sits 6th of 7: per-row signal allocation + cleanup closures cost more retained heap than the VDOM frameworks, and the benchmark now says so out loud.
- ✓ **One Pyreon entry — no manufactured tiers (2026-06 — supersedes the prior two-entries caveat)** — the benchmark previously shipped a raw `Pyreon` JSX entry AND a hand-written `Pyreon (compiled)` `_tpl()` entry, ranking only the latter. That was doubly artifactual: the compiler already lowers idiomatic JSX to the same `_tpl()` cloneNode output, AND a compiler inefficiency (wasteful per-row `_bind()` for static `<For>`-item reads, fixed in **PR #1501**) was the only thing keeping idiomatic JSX measurably behind hand-tuned `_tpl`. With that fixed, the two emit byte-identically and measure equal-or-faster, so the hand-tuned impl was deleted. The single `Pyreon` entry IS the idiomatic JSX users ship, competing head-to-head with Solid's compiled `template()` / Svelte's `.svelte` output / React 19 — apples-to-apples, no informational footnote sleight-of-hand.
- ✓ **Two new krausest-canon ops (2026-06): `remove row` + `append 1,000 to 10,000 rows`** — brings the op set to 9 and closes the gap vs the upstream js-framework-benchmark canon (single-row delete + incremental large-list append, the two ops most likely to expose keyed-reconcile cost). Both are idiomatic per framework (targeted DOM removal / immutable-or-mutable state append matching each lib's convention) and DOM-verified. `remove row` is the most VDOM-neutral op so far — every framework lands 6.9–8.8ms (a single keyed splice forces a full-list reconcile in every architecture), and **Solid edges Pyreon here** (7.20 vs 7.30ms); `append` is high-CV (GC + layout under a 10k→11k grow) and Pyreon edges Solid (22.4 vs 23.8ms) while Vue/Svelte pay a full reconcile.
- ⚠ `createSelector` for select-row in Pyreon/Solid vs `React.memo` for React — O(1) vs O(N). Each is idiomatic for its framework but is a genuine architectural advantage signal frameworks have over VDOM-diff.
- ⚠ Numbers are machine-dependent (developer-class hardware). CI / lower-spec machines will produce different absolute ms; the *ratios* + the CI95 intervals are the portable signals.
- ✓ **(was ⚠, now fixed) Pure-CPU-ish timing** — the prior methodology measured time-to-visible-commit incl. an `rAF` wait that inflated async frameworks' small-list numbers; the objectivity pass switched to `flushSync`/microtask so the timed region is close to pure framework CPU. Residual caveat: this is CPU-objective, NOT real-world-async-latency — React's default (non-`flushSync`) path would show higher wall-clock for users.
- ⚠ **Pyreon vs Solid is close on most ops, with real Pyreon edges.** Both are compiled fine-grained signal libs (cloneNode template + per-row signal + `createSelector` + keyed `For`), tied on `select` within noise, Pyreon edges `swap` (700 vs 800µs); Solid edges `remove` (7.20 vs 7.30ms). Pyreon's reproducible advantages: (1) `partial update` — Pyreon's `_bindText` direct-subscriber text binding (~8µs/update) vs Solid's idiomatic `insert()`/`createRenderEffect` (~42µs/update), a genuine ~5× per-update primitive difference confirmed after switching Solid to its real compiler output; (2) `create 1k` (9.00 vs 10.40ms) + `create 10k` (95.0 vs 115.5ms). These are real, not impl artifacts — and now that PR #1501 removed the per-row `_bind` for static item reads, idiomatic-JSX Pyreon emits the same `_tpl()` output a hand-tuned impl would, so these edges hold for what users actually ship. The className binding is symmetric (both effect-based after the objectivity pass).
- ✓ **(was ❌, now partially addressed) Memory metrics** — the 2026-06 retained-heap table (above) surfaces post-suite allocation pressure per framework. Still missing the finer-grained version: *per-iteration* peak heap + GC-pause durations via CDP `HeapProfiler` (the retained-heap number is one post-GC snapshot per framework, not a per-op allocation trace).
- ⚠ **The deepest objectivity limit is structural, not fixable in-repo: the benchmark author IS the framework author.** Test SELECTION (these 9 row-list ops) is the js-framework-benchmark canon and is precisely where fine-grained signal frameworks shine; there is no op here where a VDOM architecture might win (deep prop-diffing through large component trees, concurrent rendering under input pressure). A slow Pyreon impl always gets optimized; a slow competitor impl needs someone to notice. The only real fix is **submitting a Pyreon entry to the independent krausest/js-framework-benchmark** where community maintainers review every implementation — the 2026-06 op-set alignment (`remove row` + `append`) is the prerequisite for that. Until then, "fastest" claims stop at this synthetic suite's evidence and the honest headline is "fastest framework on the row-list ops (wins 7/9 framework verdicts, ties Solid on select, Solid edges remove), mid-pack on retained memory, with a real bulk-create + partial-update edge over VDOM."

Key optimizations: `_tpl()` (cloneNode), `_bind()` (static-dep tracking), `TextNode.data`, zero-alloc mount pipeline (lazy hooks, lazy EffectScope, devtools gated on `__DEV__`, per-definition WeakMap caches in rocketstyle, dimension-prop memo at the rocketstyle wrapper, `$element` bundle interning + styler classCache extension), **`_bindText` single-subscriber fast path** (inline-slot `_d1` for the dominant per-row signal case — no Set allocation, no iterator overhead; promotes to `_d: Set` on second subscriber), **`_set` inline-batch direct dispatch** (unbatched writes open the batch window without a `batch(closure)` allocation and dispatch single-subscriber channels — `_d1`, `_s` size 1 — DIRECTLY instead of round-tripping through the pending queues; cascades/diamonds/multi-pass re-fire still drain through the shared two-tier flush. Single-subscriber notify ~131ns → ~16ns; set→effect rerun ~162ns → ~46ns; effect-propagation bench 5× — see `batch.ts:openInlineBatch`).

### Core micro-benchmarks (`scripts/bench/` — `bench:router` / `bench:reactivity` / `bench:head` / `bench:ssr` / `bench:server` / `bench:compiler` / `bench:sync`)

Bun-run cross-library micro-benchmarks, separate from the browser DOM suite above. **Honest standings (2026-06, post-objectivity fixes):**

- **Router (`bench:router`** vs find-my-way / Hono / radix3 / react-router / TanStack / vue-router / path-to-regexp**): Pyreon is the fastest at realistic table sizes** — 1.00× leader at 50 + 200 routes, ahead of radix3 at every size; Hono's all-routes-in-one-regex leads the 10-route toy table only (and measures ~4.5× SLOWER than Pyreon at 50+ routes). Achieved by the 2026-06 `resolveRoute` hot-path overhaul (see the router section).
- **Reactivity (`bench:reactivity`** vs `@preact/signals-core` + solid-js (browser build)**): honest mid-field on raw-primitive microbenches.** Pyreon beats Solid on signal create (48 vs 113ns), computed diamond (17.0 vs 43.8µs/100 updates) and deep chain; Solid leads effect-heavy paths (propagation 8.6 vs 35.6µs, fan-out); **Preact signals-core leads every row** (its versioned linked-list dependency tracking avoids the per-rerun dep-Set teardown/re-subscribe Pyreon pays). **Why this does NOT contradict the DOM benchmark's `partial update` win:** compiled Pyreon apps don't run raw `effect()` for text/attr updates — the compiler emits `_bindText`/`_bindDirect` direct-subscriber bindings (~8µs/update DOM-included vs Solid's effect-based `insert` ~42µs), so the microbench's effect-path gap is largely bypassed in real rendered apps. **Tracked candidate, not yet built:** versioned/linked-list dependency tracking for `effect()` (the Preact architecture) — a reactivity-core re-architecture, to be attempted only with a measured real-app-shape campaign (the earlier hybrid-Set subscriber experiment measured NULL real-app gains — characterize before prototyping).
- **Head (`bench:head`** vs unhead (Vue/Nuxt)**): Pyreon 4.9–7.2× faster** on SSR head serialization at 5/20/50 tags.
- **Real-app (`examples/real-bench`** — TodoMVC-shape vs REAL `react-dom@19`**): Pyreon ~5.7–16× faster** (2026-06-17 run, 20 samples — add-100: 500µs vs 8.20ms ≈ 16×; toggle-1000: 300µs vs 1.70ms ≈ 5.7×; clear-1000: 200µs vs 1.30ms ≈ 6.5×). These are µs-scale, high-CV measurements — the *magnitude* (per-row signal patch vs React's whole-list re-render) is the signal, not the exact ratio; the ratio wobbles run-to-run.
- **SSR / server / compiler**: Pyreon-only throughput sanity (350K empty renders/s; 162K req/s simple handler). The compiler's reactive pass costs ~2.3× over bare OXC at BUILD time — the price of the reactivity analysis no competitor performs; still ~10–30× faster than Solid's legacy Babel pipeline.
- **Sync (`bench:sync`** — Pyreon-only, CRDT→signal hot paths**): validates the signature claim (remote-op→signal ~5µs/write) + characterizes the two O(N)-per-change costs (µs-scale, high-CV/machine-load-dependent — the O(N) SHAPE + order of magnitude are the signal, not exact values). Awareness recompute is clean O(N), low single-digit µs at realistic peer counts (dozens), ~10–30µs only at 200–500 peers; syncedList `toArray` rebuild O(N), ~1–10µs for typical lists, ~tens of µs at 5000. **Two speculative optimizations were MEASURED + REJECTED (do-not-re-propose without a NEW real-app measurement):** incremental awareness (delta-track peers vs full snapshot per change) — at realistic live-cursor counts (dozens) the recompute is sub-3µs, only matters at 200+ simultaneous cursors which the package already documents as a v1 limit with the throttle-cursor-publishes mitigation, AND the `Signal<PeerState[]>` API still forces an O(N) array materialization so the win is smaller than it looks; syncedList delta-apply (splice vs `toArray()`) — the keyed `<For>` already keeps the DOM surgical and huge lists belong in `@pyreon/virtual`. The bench's lasting value is regression-guarding the O(N) characterization + the signature-path cost.

**Objectivity contract for `scripts/bench/` (two bugs this section guards against, both fixed 2026-06):** (1) every bench MUST force `NODE_ENV=production` BEFORE framework imports evaluate — Pyreon's dev mode keeps the reactive-devtools registry always-on (per-primitive `new Error()` capture + fire recording), so dev-mode benching measures the instrumentation, not the framework (signal create read 1.4M ops/s in dev vs ~21M in production); (2) competitor IMPORTS must resolve to the build that actually does the work — under Bun/Node resolution, bare `solid-js` resolves via the `node` condition to `dist/server.js`, the SSR build where signals/memos/effects are INERT stubs (every "Solid" number was a no-op loop, ~42ns for "100 updates"); the bench imports `solid-js/dist/solid.js` (browser production build) explicitly. Related Solid trap: `createEffect`'s FIRST run is deferred until the owning `createRoot` body completes — benching inside the root callback times writes against a not-yet-subscribed effect; create inside the root, bench AFTER it returns.

## Package Overview

| Package                  | Description                                                                                                                                             |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `@pyreon/reactivity`     | signal, computed, effect, onCleanup, batch, createSelector, createStore, untrack, wrapSignal, isServer/isClient                                          |
| `@pyreon/core`           | VNode, h(), Fragment, lifecycle, context, JSX runtime, Suspense, ErrorBoundary, lazy(), Dynamic, cx(), splitProps, mergeProps, createUniqueId           |
| `@pyreon/runtime-dom`    | DOM renderer, mount, hydrateRoot, Transition, TransitionGroup, KeepAlive, SVG/MathML namespace, custom elements                                         |
| `@pyreon/compiler`       | JSX transform: Rust native (napi-rs, 3.7-8.9x faster) + JS fallback. `shouldWrap`, static hoisting, `_bind`, pure calls, spread templates. Reactivity-Lens sidecar (`analyzeReactivity`) |
| `@pyreon/runtime-server` | renderToString, renderToStream, Suspense 30s timeout, XSS-safe templates, For key markers                                                               |
| `@pyreon/router`         | hash+history+SSR, context-based, prefetching, guards, loaders, useIsActive, View Transitions, middleware, typed search params                           |
| `@pyreon/head`           | useHead, HeadProvider, renderWithHead, ScriptTag                                                                                                        |
| `@pyreon/server`         | createHandler (SSR), renderPage (shared string-mode pipeline), prerender (SSG), island(), middleware                                                                                              |
| `@pyreon/vite-plugin`    | JSX transform + SSR dev middleware + signal-preserving HMR                                                                                              |
| `@pyreon/react-compat`   | Full hooks (incl. useReducer/useRef/useId/useSyncExternalStore/useTransition/useDeferredValue/useImperativeHandle/useActionState/useOptimistic/use), forwardRef, memo, lazy, Suspense, createContext, createPortal shims. Companion compat layers: `preact-compat`, `vue-compat`, `solid-compat`, `svelte-compat` — each at near-full public-API parity (see their docs pages for the per-framework limitation matrix) |
| `@pyreon/storybook`      | Storybook renderer — mount, render, and interact with Pyreon components                                                                                 |
| `@pyreon/typescript`     | TypeScript config presets: base, app (noEmit), lib (declarations)                                                                                       |
| `@pyreon/lint`           | Pyreon-specific linter — 91 rules, 18 categories (incl. opt-in best-practices: frontend/query/rx/i18n/storage + router/form), config files, watch mode, AST cache, LSP server |
| `@pyreon/test-utils`     | Testing utilities — initTestConfig, withThemeContext, getComputedTheme, renderProps, resolveRocketstyle, mountReactive, mountAndExpectOnce, accessInternal, callInternal, mockAdapter |
| `@pyreon/manifest`       | Private: type + `defineManifest` helper for per-package manifests that feed the doc + MCP generators (T2.1)                                             |
| `@pyreon/perf-harness`   | Private: dev-time counter registry + snapshot/diff/record API. Framework packages emit via `globalThis.__pyreon_count__?.(name)` — zero import coupling |
| `@pyreon/vitest-config`  | Private: `defineNodeConfig` / `defineBrowserConfig` — single canonical shape for every package's vitest config. Enforces merge order + per-category coverage defaults by construction. Locked in by `pyreon/vitest-config-uses-shared` lint rule. See `packages/internals/vitest-config/README.md`. |

### UI System (Component Library)

| Package                       | Description                                                          |
| ----------------------------- | -------------------------------------------------------------------- |
| `@pyreon/ui-core`             | Config engine, init(), utilities, HTML tags                          |
| `@pyreon/styler`              | CSS-in-JS: styled(), css, keyframes, theming                         |
| `@pyreon/unistyle`            | Responsive breakpoints, CSS property mappings, unit utilities        |
| `@pyreon/elements`            | 5 foundational primitives (Element, Text, List, Overlay, Portal)     |
| `@pyreon/attrs`               | Chainable HOC factory (.attrs(), .config(), .statics())              |
| `@pyreon/rocketstyle`         | Multi-state styling (states, sizes, variants, themes, dark mode)     |
| `@pyreon/coolgrid`            | 12-column responsive grid (Container, Row, Col)                      |
| `@pyreon/kinetic`             | CSS-transition animations (Transition, Stagger, Collapse)            |
| `@pyreon/kinetic-presets`     | 120+ animation presets                                               |
| `@pyreon/connector-document`  | Bridge between ui-system components and @pyreon/document             |
| `@pyreon/document-primitives` | Rocketstyle-based document components — render in browser AND export |

### UI Component Library (packages/ui/)

| Package                 | Description                                                                                                                      |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `@pyreon/ui-theme`      | Default theme (colors, spacing, typography, borders, shadows, transitions) + rocketstyle ThemeDefault/StylesDefault augmentation |
| `@pyreon/ui-components` | 67 rocketstyle components across 10 categories (Button, Card, Input, etc.)                                                       |
| `@pyreon/ui-primitives` | Headless behavior primitives (ComboboxBase, CalendarBase, etc.)                                                                  |

#### @pyreon/ui-components — Architecture

- Three base components: `el` (Element — structured layout), `txt` (Text — inline typography), `list` (List — flowing children)
- Factory at `packages/ui/components/src/factory.ts` re-exports `el`, `txt`, `list`, `rs` from `bases/`
- **Layout in `.attrs()`**: `tag`, `direction`, `alignX`, `alignY`, `gap`, `block` — these target Element's inner layout
- **CSS in `.theme()`**: colors, spacing, borders, shadows — these target the styled outer wrapper
- **Pseudo-states in `.theme()`**: `hover: {}`, `focus: {}`, `active: {}`, `disabled: {}` objects — bases generate `:hover`/`:focus-visible`/`:active`/`:disabled` CSS
- **`:hover` is unconditional** — applied to ALL components with hover theme, not just interactive ones. Only `cursor: pointer` is gated on `onClick`/`href`
- **CSS property naming**: unistyle convention (`borderWidthTop`) not CSS-spec (`borderTopWidth`)
- **useBooleans: false** is the rocketstyle default (both type-level and runtime) — dimension props accept string values (`state="primary"`, `size="level3"`), not booleans. Opt in with `rocketstyle({ useBooleans: true })` for vitus-labs-style boolean shorthand. Before April 2026 the type default was `true` while the runtime default was `false` — boolean props typechecked but were silently dropped at runtime. Fixed by aligning the type default (rocketstyle `init.ts`)
- Theme augmentation: `@pyreon/ui-theme` augments `ThemeDefault extends Theme` and `StylesDefault extends ITheme` — apps must NOT re-augment

### UI System — Key Technical Details

#### @pyreon/styler (CSS-in-JS)

- `styled('div')\`color: red\``→ returns`ComponentFn`
- `css\`...\``→ lazy`CSSResult`, resolved on use
- `keyframes\`...\`` → returns animation name string
- Theme: `ThemeContext` is a **reactive** context (`createReactiveContext<Theme>`); `useTheme()` returns a `Theme` snapshot at call time, `useThemeAccessor()` returns the raw `() => Theme` accessor for tracking inside effects. Whole-theme swaps (user-preference themes) propagate through the resolver effect in `styled()` DynamicStyled and re-resolve CSS + swap class names without remounting the VNode. `PyreonUI` wraps `enrichTheme(props.theme)` in `computed` so the enriched theme updates when `props.theme` changes
- `createGlobalStyle\`...\`` → inject global CSS
- Singleton `StyleSheet` with FNV-1a hashing, dedup cache, SSR support
- `createSheet()` for isolated sheet instances
- **`innerRef` → `ref` alias**: a `styled()` component accepts `innerRef` as a `ref` alias (uniform with `@pyreon/elements` Element, which has always supported `innerRef`). A styled component renders a single DOM node so plain `ref` already targets it; before this, `<Styled innerRef={fn}>` SILENTLY dropped the ref (not a forwarded DOM prop) — the callback never fired (e.g. a `@pyreon/virtual` scroll container's element was never captured → empty list). `aliasInnerRef` (in `styled.tsx`) normalizes at both `StaticStyled` + `DynamicStyled` entry: when `innerRef` is set and `ref` is not, it shallow-copies descriptors (preserving getter-shaped reactive props + symbol brands) with `ref` ← `innerRef` and `innerRef` dropped (so it never reaches the DOM as an invalid `innerref=` attribute). Explicit `ref` always wins; zero-alloc fast path returns `rawProps` unchanged when there's nothing to alias. Real-Chromium-locked (`styler.browser.test.tsx` — static + dynamic delivery, ref-wins, no-attr; bisect-verified: neutralizing the alias fails the two delivery specs).
- **SSR fast path in `DynamicStyled` (`styled.tsx`)**: a module-level `IS_SERVER = typeof document === 'undefined'` (same detection as `sheet.ts`). On the server, `DynamicStyled`'s reactive branch (fired when `$rocketstyle`/`$rocketstate` are accessors — i.e. EVERY rocketstyle component) used to allocate a `computed` subscription + a `ref` closure + a `renderEffect` per component. All three are client-only dead weight server-side: refs never fire during `renderToString`, and no signal changes within a single SSR pass — they subscribe + allocate but emit zero HTML. The fast path resolves the class once via `doResolve` and emits, skipping all three. The emitted className is **byte-identical** to the reactive path's initial value (proven), so hydration — where the client (`IS_SERVER` false → full reactive path) re-establishes the machinery — sees no mismatch. Measured (`renderToString` of 2,000 reactive styled components, 8-pair drift-controlled A/B): **~9.7ms → ~1.95ms (~5×, CI [+7.54, +7.91ms], 8/8 faster)** — the reactive-machinery allocation is ~80% of per-component SSR cost when the resolve is cached (repeated components); always positive. Bisect-locked by the node-env `src/__tests__/styled-ssr-fast-path.test.tsx` (reverting the fast path makes the server run the client branch, which attaches a `ref` function — the test asserts it's absent). Ported from vitus-labs/styler's IS_SERVER `DynamicStyled` variant split. The companion micro-opts in that vitus-labs commit (proxyCache Map→null-proto, staticComponentCache tuple-promote, CSSResult `= undefined` drop, hash 4× unroll) MEASURED as noise per vitus-labs's own audit and were NOT ported — only the structural SSR work-skip carries.

#### @pyreon/unistyle (Responsive Props)

- Single value, mobile-first array `[xs, sm, md, lg]`, or breakpoint object `{ xs: ..., md: ... }`
- 170+ CSS property mappings for responsive shorthand
- Unit utilities for consistent spacing/sizing
- **`themeToCssVars(theme, options?)` — CSS custom properties autogenerated from a plain theme JSON** (Phase 1 of the CSS-variables theming track). Returns `{ vars, css, registry }`: `vars` is the same-shape tree with every eligible leaf replaced by a plain `'var(--px-<path>)'` string (plain strings → flow through the ENTIRE value pipeline untouched; `stripUnit`'s css-number regex doesn't match `var(`/`calc(`, so `value()`/`values()`/edge/borderRadius/`styles()`/`makeItResponsive` pass them verbatim — promoted from incidental behavior to a TESTED contract in `__tests__/cssVariables-passthrough.test.ts`); `css` is a ready-to-inject `:root { … }` block; `registry` maps `varName → emitted value` for consumers that can't evaluate `var()` (document export, devtools). **Units are baked at EMISSION via the existing `value()` + rootSize** — `spacing.small: 8` emits `--px-spacing-small: 0.5rem`, so themes stay authored in px and downstream never converts a var. Default per-top-level-key policy: `spacing`/`fontSize`/`headingSize`/`elementSize`/`borderRadius` → rem, `borderWidth` → px, everything else verbatim (unitless scales like `lineHeight`/`zIndex`/custom `ratio` keys stay valid `calc()` multipliers); override via `units: { myScale: 'rem' }`. Excluded (kept raw): `breakpoints`/`rootSize`/`__PYREON__` (`@media` can't read `var()`), arrays, functions, booleans, null/undefined, empty strings, non-finite numbers. Proportional sizing is native CSS: `` `calc(${vars.spacing.small} * ${vars.ratio.medium})` ``. Pure (no injection) + WeakMap-cached by theme identity (same theme + options → SAME result object — load-bearing for downstream identity-keyed caches). Kebab-collision (`xSmall` vs `x-small`) throws loudly. Real-Chromium smoke locks: computed styles resolve emitted values, calc-over-vars computes, **`[data-theme="dark"]` attribute flip changes computed styles with ZERO class change** (the mechanism the parked Phase 2 builds on). **Phase 2 SHIPPED (same branch chain)**: `init({ cssVariables: true | { prefix, attribute } })` on ui-core's config singleton (`resolveCssVariables()` is the one defaulted accessor); PyreonUI tokenizes the enriched theme via `themeToCssVars`, injects the `:root` block once via styler `injectRules` (SSR-aware — rides `getStyleTag()` + stream flush, proven by a node-env renderToString test), provides the var-leaf tree, and renders a layout-neutral `display: contents` wrapper carrying the mode attribute (SERVER-RENDERED — SSR/SSG ship the right mode; nested `inversed` providers scope via the cascade, real-Chromium-proven); rocketstyle's `mode(a, b)` becomes a hashed deduped var-pair factory (`--px-m-<fnv1a>`, picked at the single `getThemeFromChain` injection site; numbers warn in dev — emitted verbatim, units can't apply), `_resolveRsEntry` neither READS the mode signal nor keys on it under the flag (cross-mode cache identity → styler's identity-keyed classCache skips resolution on flip), and `getThemeByMode` is skipped (no brands exist). **Measured lock (real Chromium, bisect-verified-with-restore)**: `rocketstyle/src/__tests__/css-variables-mode.browser.test.tsx` — a mode flip changes computed styles with ZERO className writes, `styler.resolve: 0`, `rocketstyle.getTheme: 0`; the flag-off control asserts the classic path DOES re-resolve. Flag off = byte-identical behavior (all pre-existing suites green untouched). **Phase 3 SHIPPED (same chain)**: styler dev-mode resolved-CSS validator in `sheet.insert` (warns once per finding on NaN / undefined / malformed `var()` concat, AND `content-visibility: auto` resolved without `contain-intrinsic-size` — a CLS footgun the static `pyreon/content-visibility-needs-intrinsic-size` lint rule can't see when the CSS is computed at runtime — naming the declaration; the safety net for legacy arithmetic + string-concat on var tokens); coolgrid grid math var-aware (`isCssVarValue` branch emits native `calc()` for Row margins / Col gap-margin / Col width — pre-fix a var gap SILENTLY skipped all spacing and emitted the malformed `var(--x)px`; NOTE multiplication not division: `calc(x / -2)` invalidates the whole shorthand); `resolveCssVarReferences(value, registry)` in unistyle (inline var refs for non-CSS consumers — document export/devtools; calc inlined NOT evaluated); REAL `@pyreon/ui-components` Button + full default ui-theme proven var-safe in real Chromium (zero validator findings, flip keeps className — `ui-components.browser.test.tsx`); **measured (real-app, `examples/cssvars-bench` + `scripts/bench-cssvars.ts`): a 300-real-component theme toggle is ~1.9× faster (16.5ms→8.8ms), ~2.05× at 600 components, holding 1.89× under 4× CPU throttle — and vars does ZERO per-component re-runs on flip (`rocketstyle.dimensionMemo.hit` 12000→0) vs classic's full O(N) re-run, the deterministic signal. NOTE the headline is STEADY-STATE toggle (both modes warmed): classic does NOT re-resolve themes on a warmed flip (the `_rsMemo` caches both modes), so the win is not 'classic re-resolves' — it's that vars does NO component-level JS at all (one `documentElement` attribute write + native CSS-var recalc) while classic re-runs every component's accessor. The earlier 100-box micro-bench's bigger ratios were a COLD-first-flip measurement.**. **Phase 4 partial**: `_rsCollapse` single-class fast path SHIPPED (identical light/dark classes — what var-mode collapse produces — skip the mode binding entirely; zero-subscription browser-locked, bisect-verified). **The whole chain is ONE stacked branch — Phase 1 is PR #1518, Phases 2–4 are PR #1528 (based on #1518; auto-retargets to main + needs a rebase to trigger CI when #1518 merges).** **Phase 4b SHIPPED**: the dark/light FOUC fix is a **root-vs-nested PyreonUI split** — the ROOT provider writes the mode attribute to `document.documentElement` via a client effect (so it sits at `:root`, where the var rules cascade from AND where the pre-paint script writes), returning children UNWRAPPED; only NESTED / `inversed` providers render the `display:contents` wrapper that scopes an override to their subtree. This is the correct shape because a head script (the only placement guaranteed before first paint) runs before the in-body wrapper parses, and a wrapper would be a closer ancestor than `<html>` that would defeat it. `cssVariablesPrePaintScript({ attribute?, storageKey?, fallback? })` (exported from `@pyreon/ui-core`) builds the blocking `<head>` script that sets the attribute on `documentElement` from localStorage (`zero-theme`) / `prefers-color-scheme` before paint. **zero needs NO change** — its existing `themeScript` export already writes `documentElement.dataset.theme` and composes with the root provider; non-zero apps inject `cssVariablesPrePaintScript()`. The ONE uncovered case (documented, rare): a hardcoded `mode="dark"` SSR app with no stored preference — the mode lives only in the app JSX, unknown to a pre-paint script; stamp `<html data-theme="dark">` server-side for that. **Q2 SHIPPED (document export under cssVariables)**: `resolveModeVar(value, mode)` (exported from `@pyreon/rocketstyle`, backed by a `varName → {light,dark}` registry the mode-pair factory populates) resolves `mode(a,b)` pair vars; connector-document's `resolveStyles` + `extractDocumentTree` gained a decoupled `resolveVar?` hook (keeps the bridge dep-light — only `@pyreon/document`); `@pyreon/document-primitives`' `extractDocNode({ theme?, mode? })` AUTO-builds the resolver (composing `resolveModeVar` + unistyle's `resolveCssVarReferences` over a `themeToCssVars(theme).registry`) so PDF/DOCX/email export inlines `var(--px-…)` to raw values — doc primitives mostly emit raw literals (untouched), the resolver is the safety net for doc components that reference theme tokens / mode pairs under cssVariables. Theme authoring stays UNCHANGED in all phases (mode pairs remain component-level `mode(a, b)`; no theme-level mode shapes — explicitly rejected). **Perf-bug the benchmark caught + fixed**: `PyreonUI` provided the core context as an EAGER object (`{ theme: enrichedTheme(), mode: modeComputed(), … }`), so reading `.theme` transitively read `modeComputed()` and subscribed every theme-reading component to mode — defeating mode-free resolution (every component re-ran on flip even under cssVariables). Fixed by providing LAZY getters (`get theme(){…} get mode(){…}`): a `.theme`-only reader subscribes only to `enrichedTheme` (stable on flip → no re-run); classic resolution reading `.mode` still re-runs (correct). This is what turned the real-app steady-state toggle from 0.74× (slower!) to ~1.9× faster — without it the var-mode win is invisible in steady state. Reference: `packages/ui-system/ui-core/src/PyreonUI.tsx` coreContext provide; benchmark `examples/cssvars-bench/` + `scripts/bench-cssvars.ts`. **Retained heap is NEUTRAL** (measured, `bench-cssvars.ts --heap`, 300 real components post-GC): classic 10.57 MB vs cssVariables 10.51 MB — vars marginally LOWER (classic caches both light+dark `_rsMemo` entries; vars caches one mode-free entry + the var-pair registry — a wash). **Bundle cost ~2.2 KB gz** (the net-new modules, esbuild prod+gz: unistyle `cssVariables.ts` 1.6 KB + ui-core `cssVariablesPrePaint.ts` 0.6 KB; rocketstyle/PyreonUI deltas are small interleaved additions), within the locked per-package budgets; the styler dev validator tree-shakes out in prod. **Real-app dogfood + e2e gate**: `examples/cssvars-bench` is a human-openable interactive demo (visible dark/light toggle over real `@pyreon/ui-components` + `mode(a,b)` boxes); `e2e/cssvars-showcase.spec.ts` (`bun run test:e2e:cssvars`, own config on port 5211, wired into `scripts/e2e-affected.ts` so a ui-core/rocketstyle/unistyle/ui change runs it) proves the mode end-to-end in real Chromium: under `?vars=1` a toggle is ONE `documentElement[data-theme]` write, the cascade re-resolves the `mode(a,b)` sentinel color, and the Buttons' classNames DON'T churn; under `?vars=0` the same toggle re-resolves (classic) and writes NO documentElement attribute. **Why NOT dogfooded by flipping `ui-showcase` wholesale**: ui-showcase feeds `verify-modes` + the rocketstyle-collapse probe cells (`rs-collapse-*-probe` routes), and cssVariables changes collapse output (single class vs dual) → those gates would break; a URL-param approach hits SSR hydration mismatch (server renders classic, client re-resolves var). The dedicated client-mounted example dogfoods without destabilizing those gates. **ui-system-wide validation (are we sure it works everywhere?)**: a comprehensive audit + per-package var-mode coverage confirms it. The only bug class is JS arithmetic on a theme value (a `var()` string can't go through `gap / 2` → `NaN`) — and an audit of EVERY ui-system package found it ONLY in `coolgrid` (fixed: `isCssVarValue` → native `calc()`, browser-tested). Every package that CONSUMES theme values does so exclusively through the styler `css` + unistyle `value()`/`makeItResponsive` chokepoint (var()/calc() pass through untouched — the tested contract) and has a dedicated var-mode test: rocketstyle (measured flip), ui-core (mode + SSR), unistyle (pass-through contract + ReDoS), coolgrid (real gap), `@pyreon/elements` (var through the full Element component → styler → resolved 8px, zero validator findings — `elements/src/__tests__/css-variables.browser.test.tsx`), ui-components (real Button + full default theme). The remaining packages are ORTHOGONAL by construction — `@pyreon/kinetic` + `@pyreon/kinetic-presets` (animation; only runtime dep is `@pyreon/sized-map`, NO styler/unistyle/ui-core dep, styles via className-merge + literal-CSS presets — the flag has no code path in), `@pyreon/attrs` (prop plumbing), `@pyreon/ui-primitives` (headless behavior/ARIA) — all audit-clean for theme-value math. So a kinetic-under-cssVariables test would add a fake dep to flip a flag kinetic never reads; deliberately NOT written (hollow). The styler dev validator (`sheet.insert` NaN/malformed-var scan) is the runtime safety net that would surface any future undetected math-on-var site in dev. Reference: `packages/ui-system/unistyle/src/cssVariables.ts`, `packages/ui-system/ui-core/src/{config,PyreonUI}.tsx`, `packages/ui-system/rocketstyle/src/utils/theme.ts`, `packages/ui-system/styler/src/sheet.ts` (validator), `packages/ui-system/coolgrid/src/{Row,Col}/styled.ts`, `packages/core/runtime-dom/src/template.ts` (`_rsCollapse` fast path).

#### @pyreon/attrs (HOC Factory)

- `attrs(component)` → chainable builder
- `.attrs({ prop: value })` → inject default props
- `.config({ dimensions: {...} })` → rocketstyle config
- `.statics({ method: fn })` → attach static methods
- `.compose(enhancer)` → apply HOC wrapper

#### @pyreon/rocketstyle (Multi-State Styling)

- `rocketstyle(component)` → multi-dimensional styling engine
- Dimensions: `state`, `size`, `variant`, `theme`, + custom
- Dark/light mode via `useDarkMode` dimension
- Each dimension maps prop values to CSS via `styled()` templates
- **Per-definition caching architecture**: all caches are created once per `rocketComponent()` call (component definition) and shared across all instances via WeakMap:
  - `_dimensionsCache` — `getDimensionsMap` result keyed on dimension-themes object identity
  - `_reservedKeysCache` — `Object.keys(reservedPropNames)` keyed on keywords object identity
  - `_omitSetCache` — pre-built `Set<string>` for `omit()` keyed on reserved-keys array identity (avoids per-mount Set allocation)
  - `ALL_PSEUDO_KEYS` / `STATIC_OMIT_KEYS` — merged key arrays computed once
  - `LocalThemeManager` — WeakMap tiers for baseTheme, dimensionThemes, and per-mode resolved themes
  - `_rsMemo` — dimension-prop memo (2026-Q2 follow-up to E2's B-FINDING). `WeakMap<theme, Map<keyString, { rocketstyle, rocketstate }>>` keyed by `mode|dimensionPropTuple|pseudoState`. The two accessors `$rocketstyleAccessor` / `$rocketstateAccessor` share a single resolver that returns the SAME object identities on cache hit, which lets the styler's downstream `classCache` (also identity-keyed) skip the resolve pipeline entirely. Hit emits `rocketstyle.dimensionMemo.hit`; miss emits `rocketstyle.getTheme` (semantics changed: now counts FRESH resolutions, not accessor invocations). LRU bound at 128 entries per theme — sized for high-cardinality surfaces (data tables with state derived from row data, design systems with many tokens crossed with size/variant axes, dashboards rendering many small interactive components). Previously 32; raised in the rs-cap PR after the rs-precompute spike (closed PR #761) bisect-verified that a 60-unique-tuple Button mount loop had 45% cache-miss rate at cap=32 (888/2000 lookups) and 46% wall-clock regression vs the cap-fits-workload case. At cap=128 the cold-resolve count drops to 0 for that workload. Memory cost ~12KB per definition per theme (128 × ~100 bytes), negligible vs the wall-clock win. Regression lock: `packages/ui-system/rocketstyle/src/__tests__/memo-cap.test.ts` (64-tuple + 100-tuple warm-pass cold-resolves must be 0; 200-tuple control still evicts). Oldest-first eviction. Wall-clock measurement (E2 bench, 200 Buttons × 5 runs): baseline dropped from 8.80ms to 4.80ms (~45% improvement); per-Button styler.resolve dropped from 22 to 6 (~73% reduction — the residual 6 come from styled wrappers OUTSIDE the rocketstyle layer where the memo doesn't reach). Real apps must mount one shared `<PyreonUI>` provider for the memo to span instances — each provider mount creates a fresh `enrichedTheme` via `computed()`, which produces a different WeakMap key
- **getTheme in-place merge**: dimension slices merged directly onto `finalTheme` instead of creating a new `{}` target per `merge()` call. Pseudo-state defaults use a frozen shared `EMPTY_PSEUDO` object instead of 6 `{}` allocations per call
- **Dev guard**: uses `__DEV__` (backed by `import.meta.env.DEV`) — tree-shaken to zero bytes in production

#### @pyreon/kinetic (Animations)

- `kinetic(component)` → animation-enabled wrapper
- `.preset(fadeIn)` → apply preset from `@pyreon/kinetic-presets`
- `.enter({ opacity: 0 })` / `.enterTo({ opacity: 1 })` — enter animation
- `.leave({ opacity: 1 })` / `.leaveTo({ opacity: 0 })` — leave animation
- `.collapse()` — height-based collapse/expand
- `.stagger({ delay: 50 })` — staggered children
- `.group()` — TransitionGroup wrapper
- 4 modes: transition, collapse, stagger, group
- **SSR contract — initially-hidden Transitions render structural content.** `<Transition show={() => false}>` always emits children in SSR, with hidden-state classes inlined (`leaveTo` if defined, else `enterFrom`). Critical for SSG sites using the documented scroll-reveal pattern (`useIntersection` + sticky-signal) — IntersectionObserver can't fire on the server, so `show` is false at SSR; prior to this contract, the wrapped content was structurally absent from prerendered HTML. Matches Framer Motion / react-transition-group / react-spring ecosystem norm: content is structural, animation is visual. Implementation: `Transition` branches at setup on `props.show()` — initially-visible uses the original `<Show>`-gated mount (preserves runtime-unmount semantic); initially-hidden always renders children + the `watch(stage)` effect drives the enter animation on `show` flip true on the SAME element. **Trade-off**: for initially-hidden Transitions, `unmount: true` no longer triggers a true DOM removal after a later leave animation completes — the element stays in DOM with the leave-to class applied. Initially-visible Transitions keep the unmount semantic. Companion fix in `applyEnter`: made symmetric to `applyLeave`, clearing residual `leave`/`leaveFrom`/`leaveTo` classes at start so the SSR-baked hidden class doesn't fight `enterTo`. Reference: `packages/ui-system/kinetic/src/Transition.tsx` `wasInitiallyShown` branch + `Transition.ssr.test.tsx` (7 specs via real `renderToString`) + `kinetic.browser.test.tsx` (2 new specs for the client-side mount + enter-on-flip). Bisect-verified at the SSR layer: reverting the branch fails 6 of the 7 specs with `expected '' to contain '<section'`; the 7th (initially-visible no-regression) keeps passing.

#### @pyreon/kinetic-presets (120+ Presets)

- Framework-agnostic CSS transition objects
- `fade`, `slideUp`, `slideDown`, `slideLeft`, `slideRight`, `scaleIn`, + 100 more
- `compose(preset1, preset2)` — merge presets
- `withDuration(preset, ms)` — override duration
- Factory functions for custom parameterized presets

#### @pyreon/elements (Base Primitives)

- `Element` — base block with responsive style props
- `Text` — inline text with typography props
- `List` — list container (ul/ol/dl)
- `Overlay` — positioned overlay with backdrop
- `Portal` — renders children outside DOM hierarchy
- **Overlay focus restore (a11y)**: `useOverlay`'s `showContent` captures the active element (typically the trigger); `hideContent` returns focus to it on close **only** when focus is still inside the closing overlay (or was lost to `<body>`/null) — if the user deliberately moved focus to another control, it's left there. Modal overlays get this from native `<dialog>.showModal()`; non-modal (dropdown/popover/tooltip) needed it explicitly (pre-fix `_prevFocusEl` was declared but never used → keyboard focus dropped to the top of the document on dismiss). Real-Chromium regression-locked at `packages/ui-system/elements/src/__tests__/Overlay-focus-restore.browser.test.tsx` (bisect-verified — neuter the `prev.focus()` and the restore spec fails while the no-steal spec stays green).
- **Element simple-path fast path (2026-Q2)**: when the Element has no `beforeContent` / `afterContent` and the tag doesn't need the button/fieldset/legend two-layer flex fix, Element inlines the `Wrapper` helper directly into a single styled invocation. Skips one component invocation, one `splitProps` call, and one `mountChild` per Element. Real-Chromium wall-clock benchmark (`packages/ui-system/elements/src/__tests__/perf-stress.browser.test.tsx`): 31-45% faster across all shapes — 500-child single-tree mount drops from 2.90 ms to 1.60 ms (−45%), 5000-mount stress from 31.80 ms to 19.70 ms (−38%). Compound elements (with before/after content) and needsFix tags still go through the original Wrapper for backward compat. The downstream contract change: simple-element rendered VNode now exposes the HTML tag as `props.as` and layout fields under `props.$element.{direction, alignX, alignY, block, equalCols, extraStyles}` instead of flat `props.{tag, direction, …}`. Tests reading these read both shapes via a `getLayoutProps()` helper; production styled-components consumers see no behavior change since `as` is the styled-components canonical tag selector.
- **`$element` bundle interning (2026-Q2 follow-up to PR #344)**: every `<WrapperStyled $element={{...}}>` call site (Element fast path + Wrapper helper's 4 paths + the **Content helper's compound before/after slots**) routes through `internElementBundle()` (`packages/ui-system/elements/src/helpers/internElementBundle.ts`). Same primitive-value tuple → same object identity, which lets the styler's `elClassCache` hit and skip the resolve pipeline entirely. (Content was the one consumer originally missed — it allocated a fresh bundle per mount → guaranteed `elClassCache` miss → a full `styler.resolve` per slot per mount on the compound `beforeContent`/`afterContent` path; the wrap drops 20 compound-Element mounts from 183 `styler.resolve` to <20, bisect-locked by `packages/ui-system/elements/src/__tests__/content-intern.test.tsx`.) Bail when any value is a function (signal accessor) or a non-string object (CSSResult / css callback). LRU bound at 256 entries; oldest-first eviction. Companion change in `@pyreon/styler/styled.tsx`: the existing `($rocketstyle, $rocketstate)`-keyed `classCache` is paired with a new single-key `elClassCache: WeakMap<$element, Map<$childFix, className>>` for non-rocketstyle styled components. E2 bench (200 Buttons × 5 runs, real Chromium): wall-clock 4.80 ms → **3.60 ms** (−25% vs PR #344, **−59% vs pre-memo baseline**); per-1000-mount `styler.resolve` collapses to **0** (was 6000) because every cached path returns before the styler's CSS template runs. The Element-only probe is even cleaner: TWO same-shape Elements drop from 6 → **0** styler.resolve.

#### @pyreon/ui-core — PyreonUI (Unified Provider)

- `PyreonUI` — single provider replacing 3 separate providers (theme, mode, config)
- Props: `theme` (theme object), `mode` (`"light"` | `"dark"` | `"system"`), `inversed` (boolean, flips mode)
- `mode="system"` — auto-detects OS dark mode via `prefers-color-scheme` media query
- `useMode()` — hook returning current resolved mode as a signal (`"light"` or `"dark"`)
- `enrichTheme(theme)` — utility from `@pyreon/unistyle` that merges user theme with default breakpoints/spacing
- `init()` is preserved for custom environments — `PyreonUI` calls it internally but apps can still call `init()` directly
- **`init({ cssVariables: true })` — opt-in CSS-variables theming mode (ui-system-wide)**: PyreonUI autogenerates `--px-*` custom properties from the theme JSON, provides a var-leaf theme tree, and renders a `display: contents` wrapper carrying `data-theme` (configurable via `{ prefix, attribute }`); dark/light flip = ONE attribute write (zero re-resolution / className churn — measured + bisect-locked in rocketstyle's `css-variables-mode.browser.test.tsx`); component-level `mode(a, b)` pairs become hashed var pairs; boot-time contract (set before first render). Flag off = byte-identical classic behavior

### Fundamentals (Ecosystem Libraries)

| Package               | Description                                                                                                                                          |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `@pyreon/store`       | Global state management — composition stores returning `StoreApi<T>`                                                                                 |
| `@pyreon/state-tree`  | Structured reactive state tree — models, snapshots, patches, middleware                                                                              |
| `@pyreon/form`        | Signal-based form management — composable `field()` definitions, `useField('name')` from context, `<Form>/<Submit>` components, arrays, validation   |
| `@pyreon/validation`  | Schema adapters for forms (Zod, Valibot, ArkType) + Standard Schema duck-typing                                                                      |
| `@pyreon/validate`    | DX overlay on Standard Schema — `withField` metadata, `parseReactive` signal-driven validation, `formatErrors` i18n bridge. Works with any spec-compliant validator. |
| `@pyreon/query`       | Pyreon adapter for TanStack Query                                                                                                                    |
| `@pyreon/table`       | Pyreon adapter for TanStack Table                                                                                                                    |
| `@pyreon/virtual`     | Pyreon adapter for TanStack Virtual                                                                                                                  |
| `@pyreon/i18n`        | Reactive i18n with async namespace loading, plurals, interpolation                                                                                   |
| `@pyreon/feature`     | Schema-driven CRUD primitives — auto-generated queries, forms, tables, stores                                                                        |
| `@pyreon/charts`      | Reactive ECharts bridge with lazy loading, auto-detection, typed options                                                                             |
| `@pyreon/storage`     | Reactive client-side storage — localStorage, sessionStorage, cookies, IndexedDB                                                                      |
| `@pyreon/hooks`       | 36 signal-based hooks (useHover, useFocus, useBreakpoint, useClipboard, useDialog, useTimeAgo, useOnline, useEventListener, useInfiniteScroll, etc.) |
| `@pyreon/hotkeys`     | Keyboard shortcut management — scope-aware, modifier keys, conflict detection                                                                        |
| `@pyreon/permissions` | Reactive permissions — RBAC, ABAC, feature flags, subscription tiers                                                                                 |
| `@pyreon/machine`     | Reactive state machines — constrained signals with type-safe transitions                                                                             |
| `@pyreon/flow`        | Reactive flow diagrams — signal-native nodes, edges, pan/zoom, auto-layout via elkjs                                                                 |
| `@pyreon/code`        | Reactive code editor — CodeMirror 6 with signals, minimap, diff editor                                                                               |
| `@pyreon/rich-text`   | Reactive WYSIWYG rich-text editor — signal-backed layer over TipTap (MIT, ProseMirror); lazy-loaded, a11y-labeled, collab via @pyreon/sync           |
| `@pyreon/document`    | Universal document rendering — 18 primitives, 20 output formats                                                                                      |
| `@pyreon/rx`          | Signal-aware reactive transforms — filter, map, sortBy, groupBy, pipe, debounce, throttle, 37 functions                                              |
| `@pyreon/toast`       | Toast notifications — toast(), toast.success/error/warning/info/loading, Toaster component, a11y                                                     |
| `@pyreon/url-state`   | URL-synced state — useUrlState(key, default) or schema mode, auto type coercion, SSR-safe                                                            |
| `@pyreon/dnd`         | Signal-driven drag and drop — wraps `@atlaskit/pragmatic-drag-and-drop` (useDraggable, useDroppable, useSortable, useFileDrop)                       |
| `@pyreon/sync`        | Local-first CRDT-backed sync — a synced signal IS a signal (remote op → one fine-grained DOM update). Engine-neutral seam + Yjs engine (`/yjs`), IndexedDB persistence, cross-tab + WebSocket transport, collaborative text/lists, ephemeral presence + live cursors (`syncedAwareness`), relay server with authz (`/server`) |
| `@pyreon/a11y`        | Accessibility primitives — `announce()` screen-reader live regions (zero setup, no provider, SSR-safe), `<VisuallyHidden>`, `createA11yId` for ARIA relationships. The shared foundation other packages build on for out-of-the-box a11y |

### Zero (Full-Stack Meta-Framework)

| Package                | Description                                                                                                                                                          |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `@pyreon/zero`         | Full-stack meta-framework: file-system routing, SSR/SSG/ISR/SPA, API routes, server actions, theme, fonts, image optimization, SEO, adapters                         |
| `@pyreon/zero-content` | Content layer for markdown-driven Pyreon sites: compile-time `.md` → Pyreon JSX, typed collections (zod), MDX components scanned from `src/mdx/`, tight zero coupling |

#### @pyreon/zero — Key Features

- Client-safe main entry: `@pyreon/zero` exports only client-safe code (components, theme, middleware). Server-only code at `@pyreon/zero/server`
- Server import stubs: importing server-only APIs from the main entry gives clear error messages instead of silent failures
- Deployment adapters: Vercel (`vercelAdapter()`), Cloudflare Pages (`cloudflareAdapter()`), Netlify Functions (`netlifyAdapter()`) — in addition to Node, Bun, static
- CSP middleware: `cspMiddleware({ directives })` with `useNonce()` for inline scripts
- Env validation: `validateEnv({ PORT: 3000, DEBUG: false, API_KEY: String })` with `schema()` for custom parsers, `publicEnv()` for client-safe subset
- Request logging: `loggerMiddleware()` with configurable format and levels
- AI integration: `aiPlugin()` — generates llms.txt, JSON-LD inference metadata, AI plugin manifest
- `useRequestLocals()` — bridge middleware locals into component tree
- **Per-route render modes / hybrid rendering (Phase 2 of the render-modes plan)**: `export const renderMode = 'ssg' | 'spa' | 'isr' | 'ssr'` on any route file (or layout — cascades to descendants) overrides the app-level `mode`, which becomes the default for undeclared routes. ONE resolver (`packages/zero/zero/src/route-modes.ts:resolveRenderModeForPath` — leaf-first matched-chain walk + layout cascade + app default) drives BOTH the build and the runtime so they can never disagree. Inside `mode: 'ssr' | 'isr'`: 'ssg' routes are prerendered at build (ssgPlugin joins the plugin array BEFORE ssrPlugin — ordering is load-bearing so prerendered files exist when adapter staging copies the client dir) and served static-first by the emitted node/bun servers via the `_pyreon-ssg-paths.json` manifest (boot-time read) + excluded from the Cloudflare worker via `_routes.json`; 'spa' routes get the CSR shell at request time (built template, placeholders blanked, GET-only; no template → SSR fallback); 'isr' routes route through a shared SWR cache handler; 'ssr' under an 'isr' app = per-route cache bypass. Inside `mode: 'ssg'`: 'spa' routes emit the shell; 'ssr'/'isr' declarations are a BUILD ERROR (`assertModesSupported` — a static deploy has no server; the error names each route + the fix). The runtime dispatch (`entry-server.ts:wirePerRouteModes`) returns the plain app-level handler when no route declares a divergent mode — the zero-change path. The hybrid SSG pass skips 404 emission + the kind-'ssg' adapter invocation (the server/ssrPlugin own those) and no-ops without an SSR sub-build when no route declares a static mode (cheap file-scan pre-check). Static-first proof in e2e: `hybrid-static.ts`'s loader bakes a timestamp; two production requests return the SAME value (`e2e/ssr-node.spec.ts`). Reference: `route-modes.ts`, `ssg-plugin.ts` (hybrid gate + partition), `adapters/{node,bun}.ts` (manifest boot-read), `adapters/cloudflare.ts` (worker excludes), probe routes `examples/ssr-showcase/src/routes/hybrid-{static,spa}.ts`.
- Locale-aware favicons: `faviconPlugin({ locales: { de: { source: "./icon-de.svg" } } })` — per-locale favicon generation. Generates the full set (`.ico`/`.svg`/16/32/apple-touch/192/512/`site.webmanifest`) from one source + auto-injects all `<head>` tags. `sharp`-backed (opt install: `bun add -D sharp`). **Missing-`sharp` is a hard `vite build` error when a `source` is configured** (dev stays a soft warning) — never silently ships a faviconless production site. Documented in `docs/src/content/docs/zero.md` → Favicons.
- OG image generation: `ogImagePlugin({ templates, locales })` — build-time OG image rendering
- Reactive favicon switching: dual light/dark PNG/ICO **and SVG** with theme-synced `media` attribute swap. **Bug class fixed — a browser-preferred static asset bypassing the reactive opt-in attribute.** Pre-fix, an SVG source emitted a single static `<link rel=icon type=image/svg+xml href=/favicon.svg>` with NO `data-favicon-theme`; browsers prefer an SVG favicon over PNG, and the theme-swap script / `initTheme()` only toggle `[data-favicon-theme]` links — so the favicon never followed the theme in any SVG-capable browser (dev or prod), silently defeating the whole feature, while the `darkSource` JSDoc documented an unimplemented `prefers-color-scheme` mechanism. Fix: when `darkSource` is set, `transformIndexHtml` + `faviconLinks` (SSR) emit two theme-aware SVG links (`/favicon-light.svg` `data-favicon-theme=light`, `/favicon-dark.svg` `data-favicon-theme=dark media="not all"`) carrying the SAME contract the PNG dual-variant uses; build emits both variant files alongside the wrapped `favicon.svg` (kept as the no-JS / direct-ref OS-`prefers-color-scheme` fallback only); dev `configureServer` serves them (locale-aware, dev-badge on the active light variant). No-dark / PNG sources unchanged. The general lesson: any asset a browser auto-prefers (SVG favicon over PNG, source-order overrides, etc.) MUST carry the reactive opt-in attribute itself, or the toggle silently can't reach it. Reference: `packages/zero/zero/src/favicon.ts` (`transformIndexHtml` / `faviconLinks` / `generateFaviconSet` / `configureServer`); bisect-verified `favicon-plugin-hooks.test.ts` + `favicon.test.ts`.
- Meta completion: og:image:width/height, og:video, og:audio, noIndex, ogTemplate, favicon prop on Meta component
- `<Icon>` + `createIcon` — renders a FULL loaded SVG (4th member of the Image/Link/Script family). Does NOT synthesize an `<svg>` around `<path>` children — you load a complete svg and Icon makes it container-sizable + theme-aware. Two source props: `as` (imported `./x.svg?component` → rendered DIRECTLY, no host wrapper, svg attrs forward — recommended) or `svg` (imported `./x.svg?raw` markup string → inlined via ONE unavoidable `<span>` host). Container-fill defaults (`fill="currentColor"`, `display:block;width:100%;height:100%`) spread-overridable; no fixed size (consumer wrapper sizes it). Two layers (`createIcon(source)` per-glyph factory + `Icon` one-off); intentionally NO `useIcon` hook. Exports: `Icon`, `createIcon`, `IconProps` (extends `SvgAttributes`), `SvgComponent`. Reference: `packages/zero/zero/src/icon.tsx`; tests `src/tests/icon.test.ts` (real-`h()` happy-dom: component + raw-string + createIcon).
- `iconsPlugin` + `createNamedIcon` — folder → strictly-typed icon set. `iconsPlugin({ dir | sets, out?, mode? })` (from `@pyreon/zero/server`) scans `*.svg` (kebab name from filename), writes a gitignored generated `icons.gen.tsx` that calls `createNamedIcon(REGISTRY)` so `keyof typeof REGISTRY` IS the typed `name` surface (autocomplete + go-to-definition, zero per-app wiring; regenerates on add/unlink in dev, idempotent write). **Named multi-set form** `iconsPlugin({ sets: { ui: { dir }, brand: { dir, mode } } })` (XOR with `dir`, throws if both/neither) emits one file with a strictly-typed component PER set under NAMESPACED names so they never clash — `ui` → `<UiIcon>` + `type UiIconName`, `brand` → `<BrandIcon>` + `type BrandIconName`; per-set binding prefixes (`ui_check` / `brand_check`) keep two sets sharing a glyph filename collision-free; `mode` is per-set. Writes a REAL file (not a virtual module) because the published `@pyreon/zero` can't `import` a plugin virtual module — same Rolldown import-ordering constraint as islands' `hydrateIslandsAuto(registry)`. Two render modes (colorful-vs-system split): `mode: 'inline'` (default — system icons; `?raw` markup inlined via `Icon`, `currentColor`-themeable, recolorable via CSS `color`) and `mode: 'image'` (colorful / brand icons; svg emitted as a static asset, rendered `<img>`, NO mutation, original colors preserved). `createNamedIcon<R>(registry, { mode? })` is the runtime half (typed by `keyof R`). **Inline mode emits TWO shapes (the tree-shaking fix): (1) per-icon PascalCase components — `export const CheckCircle = /*#__PURE__*/ createIcon(checkCircle)` (multi-set: set-namespaced `UiArrowLeft` / `BrandLogoMark`) — the PREFERRED surface; `import { CheckCircle } from './icons.gen'` drops every unused icon AND the runtime registry via standard ESM dead-code elimination. The `/*#__PURE__*/` is load-bearing — it lets the bundler drop the unused cross-package `createIcon` / `createNamedIcon` calls it can't prove pure on its own. (2) `<Icon name="…" />` — the `registry[name]` runtime lookup, kept as the escape hatch for DYNAMIC / data-driven names (`<Icon name={cmsKey} />`), which necessarily retains the whole set. `componentNameFromIconName(name)` (`check-circle` → `CheckCircle`; leading-digit → `Icon1password`) is the new exported helper. Image-mode sets stay registry-only (`createIcon` renders `?raw` markup, not an `<img>`) and image-mode generator output is byte-identical to before — minimal blast radius. The bigger byte win than per-icon SVGO minification: a real app declares far more icons than it renders, so dropping the unused ones (100% elimination) dominates shaving the used ones (~10-30%). Why tree-shaking and not async/lazy-load per icon: icons are tiny, numerous, above-the-fold and instant-by-nature — lazy-loading them emits dead chunks + adds an async paint (FOUC/CLS) + a request waterfall, the wrong tradeoff for this asset class; async is the right tool only for an unbounded data-driven set, which is exactly what the `<Icon name={…}>` registry escape hatch serves.** Exports: `iconsPlugin`, `iconNameFromFile`, `scanIconDir`, `generateIconSetSource`, `componentNameFromIconName`, `componentNameFromSetKey`, `IconsPluginConfig` (server entry); `createNamedIcon`, `createIcon`, `Icon`, `IconMode`, `NamedIconProps` (client entry). Reference: `packages/zero/zero/src/icons-plugin.ts` + `icon.tsx:createIcon`/`createNamedIcon`; tests `src/tests/icons-plugin.test.ts` (pure scanner/generator + per-icon export shape) + `icons-treeshake.test.ts` (real Vite/Rolldown build proves an unused icon's bytes are dropped — bisect-verified via an opaque-side-effect `@pyreon/zero` stub so the `/*#__PURE__*/` annotation, not the bundler's purity guesswork, is what's exercised) + `icon.test.ts` (createIcon/createNamedIcon real-`h()`, both modes). **NOT YET: per-icon tree-shakeable exports for `mode: 'image'` (needs an image-icon factory — `createIcon` renders `?raw` markup, not `<img>`) + SVGO `?optimize` minification for SVG (excluded from the imagePlugin optimize pipeline; SVGO is not a dep) + monorepo package-sourced sets + copy-to-public — explicit follow-ups.**
- **Image optimization — bi-modal `<Image>` + `createImageRegistry` + `<NoOptimize>` (PRs #1353/#1357/#1363).** `<Image src={…}>` is **bi-modal**: `src` accepts a `?optimize` `ProcessedImage` descriptor (Shape A — width/height/srcset/formats/placeholder inferred, supply only display props) OR a runtime string URL (Shape B — `width`+`height` REQUIRED at the type level so a remote/signal-driven URL can't silently cause CLS). The `optimize?: boolean` prop opts out (`false` → bare `<img>`, bypass pipeline) or forces optimization back ON inside an outer `<NoOptimize>` boundary (`true`). `<OptimizedImage source={descriptor} alt>` is the one-prop form (whole descriptor as a single prop — guards against the #1 real-world CLS cause, pulling just `hero.src` onto a raw `<img>`; the opt-in lint rule `pyreon/no-discarded-optimize-fields` flags that shape). `<NoOptimize>` / `useNoOptimize(): boolean` is a context-based subtree opt-out boundary — every `<Image>` inside renders a bare `<img>` unless it sets `optimize` to opt back in. `createImageRegistry(map)` builds a typed name→descriptor registry (autocomplete over imported assets). All built on `createImage(Base)` so consumers layer rocketstyle / custom wrappers without losing the pipeline. Reference: `packages/zero/zero/src/{image.tsx,image-registry.ts,no-optimize.tsx}`; docs `docs/src/content/docs/images-and-fonts.md`.
- **Resource-hint primitives — `usePreconnect` / `useDnsPrefetch` / `usePreload` (PR #1366).** Three typed `useHead` wrappers emitting `<link rel="preconnect|dns-prefetch|preload">`. `usePreconnect(origin, { credentials? })` opens DNS+TCP+TLS early (default `crossorigin="anonymous"`; `credentials: true` → `use-credentials`) — reserve for the 1-3 most-critical external origins. `useDnsPrefetch(origin)` is the cheaper DNS-only hint for likely-but-uncertain origins (pair both for old+modern browser coverage). `usePreload(href, { as, type?, crossorigin?, media?, imagesrcset?, imagesizes?, fetchpriority? })` is the typed critical-resource fetch hint — `as` is REQUIRED (the preload scanner ignores `<link rel=preload>` without it); deduped by `href` via `@pyreon/head`. Three separate helpers (not one mega-hook) because each has distinct typed semantics — preconnect always needs crossorigin, preload always needs `as`. Reference: `packages/zero/zero/src/use-resource-hints.ts`; docs `docs/src/content/docs/images-and-fonts.md` → Resource Hints.
- **Font preload + `?font` import — `usePreloadFont` / `fontImportPlugin` / `fontPlugin` (PRs #1359/#1360).** `usePreloadFont(href, opts?)` emits `<link rel=preload as=font crossorigin>` for a critical font, auto-inferring the MIME `type` from the extension via `inferFontMimeType(href)` (woff2/woff/ttf/otf). The `?font` Vite import (`fontImportPlugin`) turns `import Inter from './Inter.woff2?font'` into an auto-emitted `@font-face` + a hashed-URL `FontDescriptor` — one import wires BOTH the face declaration and the content-hashed asset URL. `fontPlugin` is the build-time companion; both auto-wire via `zero({ font })`. Reference: `packages/zero/zero/src/{use-preload-font.ts,font-plugin.ts,font-import-plugin.ts}`; docs `docs/src/content/docs/images-and-fonts.md`.
- **`zero({ image, font })` auto-wiring (PR #1356).** `imagePlugin` / `fontPlugin` are appended to the Vite plugin chain automatically when `zero({ image: {...}, font: {...} })` is configured — no separate `plugins: [imagePlugin(), fontPlugin()]` wiring needed.
- **`fontPlugin({ subsets })` — self-hosted Google Font subset scoping (opt-in).** By default self-hosting downloads EVERY subset Google returns (Ubuntu = 6: latin/latin-ext/cyrillic/cyrillic-ext/greek/greek-ext, each a separate `@font-face` + woff2), so a Latin-only site ships ~all of them. `subsets: ['latin','latin-ext']` narrows the emitted set (~−40% font weight on the example `Inter:wght@400;500;600;700;800` config). **Zero runtime change** — the browser already skips unrequested subsets via `unicode-range`; the win is build output / deploy size / static-host quota. **Mechanism (load-bearing): css2 IGNORES `&subset=`** (verified — `&subset=latin` still returns all 6), so the fix FILTERS the returned CSS by its per-subset comment labels (`filterCssBySubsets`, exported from `@pyreon/zero/font`) BEFORE `extractFontUrls` — one spot governs downloads + emitted assets + inlined CSS. **Opt-in** (omit = keep all; a `['latin']` default would silently break Cyrillic/Greek/Vietnamese pages); **self-host only** (no effect with `selfHost:false` / dev); **fail-safe** (an allowlist matching nothing, or a CSS with no labels, keeps all subsets — never a fontless build). The subset allowlist is folded into the `node_modules/.cache/zero-fonts` cache key so two configs differing only in `subsets` can't collide on a stale entry. **Companion fix (same PR): correct preload subset.** `collectGoogleFontTags` previously preloaded `selfHostedFontFiles.slice(0, familyCount)` = the FIRST file, which is css2's `cyrillic-ext` block (Google returns subsets cyrillic-ext→…→latin) — so a Latin site preloaded a Cyrillic woff2 AND failed to preload the latin one it actually renders. Now `pickPreloadHrefs(selfHostedCSS, primary, limit)` picks the PRIMARY subset's file (`subsets?.[0] ?? 'latin'`), capped at the same one-per-family budget, falling back to the first files only if the primary is absent. Multi-family one-per-family preload precision is still a separate pre-existing concern (untouched). **ReDoS-safe parsing:** `filterCssBySubsets` + `pickPreloadHrefs` share `splitSubsetBlocks`, a linear label-INDEX scan (`/\/\*\s*([\w-]+)\s*\*\//g` + slice between offsets) — NOT a `lazy-body + look-ahead` regex, which CodeQL flags as polynomial-ReDoS on untrusted fetched CSS. `fontCacheKey(cssUrl, subsets)` is the exported cache-key helper. Reference: `packages/zero/zero/src/font.ts:filterCssBySubsets` + `selfHostFonts`.
- **`fontPlugin({ fallbackAdjust })` — auto-computed size-adjusted fallback fonts (default ON) to ELIMINATE font-swap CLS (the `next/font` technique).** When a Google Font swaps in over a system font with different metrics (x-height / ascent / descent / line-gap / avg glyph width), layout SHIFTS — the dominant font-related CLS source. `fallbackAdjust` (default `true`) emits a paired `@font-face` for `"<Family> Fallback"` whose `size-adjust` + `ascent/descent/line-gap-override` make the system fallback's box MATCH the web font, so the swap moves nothing. **Two corrections over the naive proposal, both load-bearing:** (1) zero/font ALREADY had a `fallbacks: Record<string, FallbackMetrics>` config + `fallbackFontFaces()` emitter — the real gap was AUTO-COMPUTING the metrics, not the emission; (2) **the emitted `"X Fallback"` @font-face is inert unless the fallback family is in the cascade** — and zero apps control their own `font-family` (`--font-sans: 'Ubuntu', sans-serif`), which never includes `"Ubuntu Fallback"`, so the pre-existing manual `fallbacks` feature did NOTHING for CLS. The fix emits a `--pyreon-font-<slug>` CSS var carrying the full stack (`--pyreon-font-ubuntu: Ubuntu, "Ubuntu Fallback", Arial`); the app uses `font-family: var(--pyreon-font-ubuntu)` (one line) and the fallback genuinely enters the cascade. **`--pyreon-font-<slug>` is the SINGLE integration point for BOTH font-consumption surfaces** (a follow-up after the user flagged "we use the font in the ui-system theme too"): (a) plain CSS — `body { font-family: var(--pyreon-font-ubuntu) }`, or the **opt-in `fallbackAdjust: { applyTo: 'body' | <selector> }`** which auto-writes that binding for the FIRST configured family so the plain-CSS path is zero-touch (`applyTo: true` ⇒ `'body'`; off by default — auto-writing a global `font-family` is opinionated + multi-family "which is primary" is ambiguous); (b) the `@pyreon/ui-system` theme — set `theme.fontFamily.base = 'var(--pyreon-font-inter)'`, which reaches every rocketstyle/styler component because styler passes `var()` through verbatim (the unistyle cssVariables passthrough contract). `applyTo` only covers INHERITING content (body + elements not setting their own font); themed components take their font from the THEME, not the cascade — so a fully-themed app does BOTH. The theme can't be auto-injected (it's RUNTIME `<PyreonUI theme>` app config, decoupled from the build-time font plugin). `fallbackAdjust` type = `boolean | { applyTo?: string | boolean }`; `resolveApplyToSelector` + `renderApplyFontFamily` (font-fallback.ts) are the pure helpers. **Metric source = ground truth:** unpacks the ACTUAL downloaded `woff2` via `@capsizecss/unpack` `fromBlob` (never a stale precomputed guess; works for ANY font); CDN mode (`selfHost:false`, no download) falls back to `@capsizecss/metrics`' precomputed table (~1,900 fonts); a font in neither is skipped with a build warning, NEVER a build failure. The override math is `@capsizecss/core`'s `createFontStack([webMetrics, systemFallbackMetrics])` (the exact `next/font` computation). System fallback by capsize `category` (sans→Arial, serif→Times New Roman, mono→Courier New). A manual `fallbacks` entry WINS (auto skips that family). **`@capsizecss/*` are build-time-only deps, DYNAMICALLY imported in `fontPlugin`'s `buildStart`** — never in a client bundle (the `@pyreon/zero` `.` main-entry budget is unaffected: font.ts is the `/font` subpath, capsize is dynamic + externalized). Logic lives in `packages/zero/zero/src/font-fallback.ts` (`computeAutoFallbacks` / `buildFallbackCss` / `slugifyFamily` / `renderFontFamilyVars` / `renderAutoFallbackFaces`); wired in `font.ts` (`buildStart` computes from `selfHostedFontFiles` buffers; `transformIndexHtml` emits the `@font-face`s + `:root` vars). Tests: `packages/zero/zero/src/tests/font-fallback.test.ts` (14 network-free unit specs on the deterministic capsize math + the precomputed/skip/dedup/warn orchestration; + 1 `PYREON_FONT_E2E=1`-gated network spec that unpacks a real Ubuntu woff2 — skipped in CI to avoid flake). Real-plugin end-to-end verified locally (download → unpack → emit the computed `@font-face` + var into dist HTML). Reference: `packages/zero/zero/src/{font-fallback.ts,font.ts}`; docs `docs/src/content/docs/images-and-fonts.md` → "Eliminating font-swap CLS (`fallbackAdjust`)".
- **SSG per-route `<link rel=modulepreload>` — islands-safe (PR #1358, closes #1355).** Each prerendered route's HTML gets `<link rel=modulepreload>` for its OWN route-chunk graph, derived from the Vite manifest's `imports` (STATIC dependencies only) — NEVER `dynamicImports`, so island / lazy chunks stay deferred and the islands zero-JS contract holds. The route module is resolved via `record.component._hmrId` (the lazy wrapper carries `_hmrId`, not the record), keyed `relative(root, _hmrId)` against the manifest; the route's own `index.html` entry is subtracted from the import delta. Reference: `packages/zero/zero/src/ssg-plugin.ts`.
- **SSG output format — `ssg.format: 'file' | 'directory' | 'both'` (default `'directory'`; mirrors Astro `build.format`).** Controls which on-disk form each prerendered route writes: `'directory'` → `dist/<route>/index.html` (historical behavior), `'file'` → `dist/<route>.html` (Next.js `output:'export'` style), `'both'` → both, byte-identical. **Motivation: the slash-less-URL 301.** A host that doesn't auto-rewrite slash-less URLs to the trailing-slash form (GitHub Pages, raw Cloudflare R2 / S3 without index-document config, plain nginx without `try_files`) answers a direct hit to `/resume` (the canonical share/link form) with a `301 → /resume/ → 200` round-trip — a real mobile-perf cost (Lighthouse "Avoid multiple page redirects"). The file form lets those hosts serve `/resume` directly with no redirect. **Recommend `'both'`** when redirects matter (keeps trailing-slash links + the `seoPlugin` sitemap's trailing-slash URLs working via the directory form AND serves slash-less share URLs with no redirect via the file form, for one extra HTML file per route); `'file'` is leanest but a page is then reachable ONLY slash-less (trailing-slash 404s on dir-mapping hosts); with `'both'` set a canonical (`<Meta canonical>`) so search engines dedupe the two URL forms. Root (`/`) always writes `dist/index.html` (no `dist/.html`) and an already-`.html` path is verbatim — both regardless of format. Implementation is adapter-agnostic in the SSG plugin (NOT `staticAdapter` — `staticAdapter.build()` only stages files via `materialize`): `selectSsgTargets(distDir, path, format)` (pure, `_internal`-exported) resolves the file target(s); `writeRouteOutputs` writes each byte-identically (per-target traversal guard + mkdir), used by all 3 per-route write sites (main render, meta-refresh redirect stub, `onPathError` fallback). `scripts/serve-ssg.ts` (the SSG e2e test server) prefers a `<route>.html` sibling for the slash-less form ONLY (an explicit trailing slash keeps serving the directory index — models GitHub Pages / S3 / nginx). Bisect-verified at 3 layers: unit `selectSsgTargets` (all formats × root/nested/.html), unit `serve-ssg` (.html-sibling preference + trailing-slash → dir), build-artifact `verify-modes ssr-showcase × ssg (format:'both')` (`dist/about.html` byte-identical to `dist/about/index.html`; no `dist/.html`). Reference: `packages/zero/zero/src/ssg-plugin.ts:selectSsgTargets`/`writeRouteOutputs` + `types.ts:SsgConfig.format`; docs `docs/src/content/docs/ssg.md` → "Output format (directory vs file URLs)".
- **Adapter immutable-cache scoping to `<base><assetsDir>` (PR #1361).** Deploy adapters scope their 1-year `immutable` cache rule to the resolved `build.assetsDir` at the served `base` (new `assetUrlPrefix(base, assetsDir)` helper + `AdapterBuildOptions.assetsDir` threaded from `configResolved`), not a hardcoded `/assets/` — so a custom `assetsDir` (`build: { assetsDir: 'static' }`) or a subpath deploy (`zero({ base: '/blog/' })`) keeps long-cache on its hashed chunks. CDN adapters (vercel `config.json` / netlify `netlify.toml` / cloudflare `_headers`) scope `<base><assetsDir>/*`; node/bun stay `assetsDir`-only (NO base — their self-hosted handler serves by raw `url.pathname` with no base-stripping, so subpath isn't supported there regardless). Default `/` + `'assets'` → `/assets/` (no behavior change for the common case). The node/bun cache check was ALSO fixed to gate on `url.pathname.startsWith('/<assetsDir>/')` not file extension (PR #1352 — extension-based caching immutable-poisoned non-hashed root files like `/app.js`). Reference: `packages/zero/zero/src/adapters/*`.

## PMTC Multi-Target Architecture

The Pyreon Multi-Target Compiler (PMTC) emits SwiftUI + Compose from one `.tsx` source. **Scope the "100%" precisely** (it has been over-read): what is "done" is the **emit vocabulary** — all 15 canonical primitives map to both targets (#888 K-FINAL). It does NOT mean "all packages work on native," "device-proven," or "production-ready." Three independent reality-checks (2026-06 audit) confirm the honest picture: (a) **PMTC compiles a narrow, declarative TS subset and fails SILENTLY outside it** — local object/array literals, destructuring, `Map`/`Set`, `for`/`while`/`switch`/`try`, template literals (→`""`), optional chaining (→`""`), generics, `interface`/`enum`/`class` decls, and fractional math on iOS (`signal<number>(9.99)`→`var x: Int = 9.99`) are dropped/mis-emitted, most with NO warning; the per-PR gate is `swiftc -parse` (syntax-only, NOT typecheck) + `kotlinc`-against-stubs, which structurally cannot catch this (proven by the repo's own `rx-full.tsx` fixture failing real `swiftc -typecheck` while passing the `-parse` gate). (b) **Device proof is advisory** (`native-device.yml` gates zero PRs; currently green but below its 14-night promotion bar; strong for Tasks/Counter/Router, launch-only for TodoMVC, absent for native-analytics). (c) **The rich web packages (`@pyreon/flow`/`charts`/`code`/`dnd`/`document`/`query`/`table`/`virtual` + the whole CSS-in-JS ui-system stack) are web-only by architecture** — PMTC compiles your source, not npm libraries, so canvas/DOM/vendor-bound packages have no native path. Native reach = reactive logic + the canonical primitives + the ServiceBackend-ported hooks + the logic packages with Swift/Kotlin runtime ports (store/machine/state-tree/i18n/form/permissions/storage/router). Full honest capability matrix + TS-surface ceiling: `docs/src/content/docs/multiplatform.md` → "What runs on native". The shared-code story above the compiler lives in a **four-layer model**:

```text
Layer 4: <NativeIOS> / <NativeAndroid> / <Web> escape hatches (per-platform, opt-in)
Layer 3b: @pyreon/elements (web-only rich primitives, rocketstyle/styler-coupled)
Layer 3a: @pyreon/primitives (canonical multi-platform primitives — 15 of them)
Layer 2: useStorage / useRouter / useFetch / usePermissions (ServiceBackend pattern)
Layer 1: Custom hooks (useDebounce, useToggle, ...) — pure-logic, 100% shared
Layer 0: signal() / computed() / effect() — runs identically; PMTC maps to @State / mutableStateOf
```

**Canonical multi-platform primitives** (Layer 3a, `@pyreon/primitives`): **15 semantic primitives** — `Stack`, `Inline`, `Layer`, `Scroll`, `Spacer`, `Text`, `Heading`, `Image`, `Icon`, `Button`, `Press`, `Link`, `Field`, `Toggle`, `Modal` — designed for cross-platform from scratch. `<Stack>` (NOT `<View>` or `<VStack>`), `<Inline>`, `<Press>`, `<Field>`, etc. Compiles to: DOM (web) / SwiftUI primitives (iOS) / Compose primitives (Android). One canonical name per concept (`onPress` everywhere, not `onClick` vs `action:`). Tokens-first styling (`padding={4}` / `gap="md"`). No responsive props or animations in v1 — apps that need responsive web use `@pyreon/elements` directly. Source of truth: `packages/native/compiler/src/canonical-primitives.ts` `SWIFT_NAMES` + `KOTLIN_NAMES` (both maps carry exactly 15 entries).

**Cross-platform a11y vocabulary** (`AccessibilityProps` in `packages/core/primitives/src/types/shared.ts`, mixed into every primitive via `HtmlPassthroughProps extends AccessibilityProps`): platform-NEUTRAL a11y props that lower per-target — `accessibilityLabel` → web `aria-label` / iOS `.accessibilityLabel` / Android `semantics{contentDescription}`; `accessibilityHidden` → web `aria-hidden="true"` / iOS `.accessibilityHidden` / Android `semantics{invisibleToUser}`. Prefer these over raw `aria-*` (web-only) so the same component is accessible on every target. **Web lowering ships** (`web/passthrough.ts:collectPassthroughAttrs` maps the neutral props to aria-*; a raw `aria-label`/`aria-hidden` wins as the explicit web override; `accessibilityHidden` emits the STRING `"true"`, not presence-only `""`). **The iOS/Android PMTC emit is a tracked follow-up** — until it lands, native targets render without the a11y attribute (graceful degradation, no crash). Regression-locked by the "Cross-platform a11y vocabulary" describe block in `packages/core/primitives/src/tests/primitives.test.tsx` (Stack + Button mount → aria-label/aria-hidden; raw-aria-wins; hidden-false-omitted; bisect-verified).

**Critical architectural split**: `@pyreon/elements` stays web-only-rich (rocketstyle/styler-coupled); `@pyreon/primitives` is the NEW multi-platform-minimal layer. Different architectural tiers. No naming collision because imports are explicit.

**Per-platform import resolution**: `import { Stack } from '@pyreon/primitives'` runs the real implementation on web (renders DOM); on iOS/Android the PMTC compiler INTERCEPTS the JSX and emits native code BEFORE the runtime is reached — the import is type-anchor-only on native targets.

**Existing Layer-2 pattern**: `@pyreon/storage`'s `StorageBackend` interface + `createStorage(backend)` factory is the blueprint every Layer-2 service follows. `@pyreon/native-runtime-{swift,kotlin}` ship `@PyreonAppStorage` (Swift property wrapper) + `rememberPyreonStorage` (Compose Composable) — same API parity, different runtime impl. Compiler emits `useStorage<T>` calls as these runtime helpers via Phase 2.5 (#891) — collapsed 14-line inline Codable bridge to one-line property-wrapper call.

**Native data + services hooks** (runtime port + compiler emit, both targets): `useForm` v2 (validated forms — validators as native closures, runtime Field bindings, submit gating; device-proven by the tasks login's error-path smoke), `useFetch` (emit complete; device-proven end-to-end — see lifecycle below), `usePermissions`, `useOnline`, `useClipboard`, plus `useColorScheme` (#1103 — emit-only; SwiftUI's `@Environment(\.colorScheme)` and Compose's `isSystemInDarkTheme()` provide the platform primitive). `useValidation` planned. Runtime files live at `packages/native/runtime-{swift,kotlin}/`. **Phase 5 (#1689) — seven more hooks emit** (runtime containers shipped this arc; emit added): `useAuth<User>` → `PyreonAuth<User>`, `useDatabase` → `PyreonDatabase`, `useGeolocation` → `PyreonGeolocation`, `useMap` → `PyreonMapState`, `useWebSocket('wss://…')` → `PyreonWebSocket` (string-literal URL), `usePush` → `PyreonPushNotifications`, `usePayments` → `PyreonPayments`. Same reactive-container template as `useOnline`/`usePermissions` (Swift `@State` + bare `@Observable` reads; Kotlin `remember {}` + `.value` on `MutableState` fields, bare on `Bool` getters/methods). Compile-rung proof: an archetype finance+realtime+maps component using all seven emits typecheck-clean Swift (`swiftc`) AND Kotlin (`kotlinc`) from one `.tsx` (`native-data-hooks.test.ts`). **Two honest limits:** (1) **lifecycle auto-start is NOT emitted** — `geolocation.start()`/`websocket.connect()`/`push.start()` on mount don't auto-fire (web hooks do); call `.start()`/`.connect()` from an effect/host today. Full auto-start is **blocked on the real Kotlin transport backends** (the Kotlin containers take an app-injected source the compiler can't synthesize), so it lands with that Android-CI backend work. (2) **`useSecureStorage` is deferred** (warns + drops) — the Kotlin secret store needs an app-injected `EncryptedSharedPreferences` backend. Runtime behavior remains compile-verified, not device-proven (the nightly device gate + example apps are the runtime-proof layer).

**Subset-widening (compile-rung) — two follow-ups closing the most pervasive silent-drop classes:** (1) **plain component-body value-consts (#1691)** — `const label = 'Total: '` / `const doubled = base() * 2` / `const sum = 5 + 3` used to be SILENTLY DROPPED (`parse.ts` captured only call/arrow inits → `if (init.type !== 'CallExpression') return null`); they now emit as a body-local `let` (Swift, injected at the top of the `body` ViewBuilder so it can read `@State`) / `val` (Kotlin composable body), captures-once like a JS `const`. (2) **hook-result destructure** — `const { data, isPending } = useFetch(url)` (THE idiomatic data-fetch shape) used to warn-drop; it now **lowers** to a synthetic single-binding container `const __pyHookN = useFetch(url)` (parsed via the SAME single-binding path, by recursion) + one field alias per key consumed in `parseExpr`'s Identifier case (mirroring `storeAliases`), so each local rewrites to `__pyHookN.<field>` — **byte-identical native output to the supported single-binding form** on both targets, user keeps the call form (`data()` accessor vs `isPending` plain). Faithful + non-regressing by construction: a container-less hook (`useSecureStorage` warns+drops → recurses to null) or a rest/nested pattern falls through to the same warn-drop as before; `useParams` keeps its own per-key lowering; `useLoaderData` destructure stays warn-drop (opaque `T`). Both are pure parse-layer, zero emit-shape change, verified at the emit-shape + real `swiftc`/`kotlinc` rung (`native-value-const.test.ts`, `native-hook-destructure.test.ts`). These widen the supported-TS subset toward "one `.tsx` → any app" — local consts + hook destructure appear in essentially every real component. The next-frontier compile-rung gaps (param destructure, multi-statement handler control-flow, anonymous-object→struct, JSX spread, template literals) are tracked; the structural ceiling (arbitrary async, device-SDK map/chart rendering, web-coupled libs) stays `<Web>`/`<NativeIOS>`/`<NativeAndroid>` escape-hatch territory by design, NOT roadmapped as "coming soon."

**Native lifecycle — real `<Suspense>` / `<ErrorBoundary>` semantics (Phase 2, device-proven).** Both compile to an INLINE conditional read in the component body — Swift `Group { if <pending/errored> { fallback } else { children } }`, Kotlin `if (<pending/errored>) { ... } else { ... }` — where the condition ORs every `useFetch` container's `.isPending` (Suspense) / `.error != nil` (ErrorBoundary) in that component. Two non-obvious requirements both surfaced ON A REAL SIMULATOR (compile-only `swiftc -typecheck` / `kotlinc` cannot catch either):
1. **Inline read, not arg-passing.** The `@Observable` read MUST be in the component's own `body` so SwiftUI Observation / Compose recomposition tracks it. The earlier `PyreonSuspenseWrapper`/`PyreonErrorBoundaryWrapper` CHILD-STRUCT approach (passing `isLoading`/`hasError` as a value/closure arg to a wrapper) does NOT establish tracking — the child reads its own stored copy, not the `@Observable`. Those wrapper structs were deleted; the emit is inline.
2. **Swift `.task` needs a STABLE-identity host.** A fetch-bearing component appends a mount-time `.task { begin → resolve|reject }`. SwiftUI ties a `.task`'s lifetime to its host VIEW IDENTITY; attached to a transparent `Group { if isPending … }`, SwiftUI redistributes the modifier onto the if/else BRANCH, so each loading/error flip changes the branch identity and CANCELS+RESTARTS the task → the fetch perpetually thrashes and never settles (the boundary renders NOTHING — not even its fallback). Fix: `emitSwiftComponent` wraps any fetch-bearing component's body in a concrete `ZStack` so `.task` attaches to a stable host that fires once on appear. **Kotlin needs no equivalent** — its fetch harness is a `LaunchedEffect(Unit)` SIBLING keyed by the stable `Unit`, which runs once and is not cancelled by recomposition. Compiler regression specs in `fetch-computed-shapes.test.ts` (`Swift `.task` stable-host wrap (device-found)`) lock the ZStack contract; the `examples/native-tasks-ios` `lifecycle-page` XCUITest (good-fetch `lc-quote` + failed-fetch `lc-error` both render) is the device proof. Reference: `packages/native/compiler/src/{emit-swift,emit-kotlin}.ts:emitSwift/KotlinSuspense + emitSwift/KotlinErrorBoundary` + the `_hasFetchDecl` ZStack branch in `emitSwiftComponent`.

**Cross-platform layout gotcha — `<Inline>` is a NON-WRAPPING Row on Android (overflows); SwiftUI HStack shrinks (device-found, ROOT-CAUSED on a local emulator).** The native lifecycle device-proof initially failed ONLY on Android: after a 6th button (`tasks-lifecycle`) was added to the TasksPage action-button `<Inline>`, the Android instrumented test's `tasks-logout → /login` step timed out (`login-page` never rendered). It LOOKED like "the lifecycle screen breaks navigation," but bisecting on a real Android emulator (the breakthrough — local Android SDK + AVD, ~20s/run) isolated it to the **button, not the lifecycle**: removing just the `tasks-lifecycle` button (keeping the /lifecycle route + screen + fetches) made it PASS. **Mechanism:** `<Inline>` lowers to a Compose `Row`, which does NOT wrap — 6 buttons overflow the phone width and push the last one (`tasks-logout`) OFF-SCREEN, so Compose `performClick` injects a touch at off-screen coordinates, logout never fires, login never appears. **iOS hid it** because SwiftUI `HStack` shrinks children to fit, keeping every button on-screen + tappable. **Fix:** the example's action buttons stack vertically (`<Stack>`) — full-width + reachable on both targets (real-emulator-proven: the full vocab→lifecycle→`lc-error`→logout→login flow now passes on Android). **The lifecycle itself was NEVER the bug — it is device-proven on BOTH iOS and Android.** **General rule for multi-platform layouts:** a horizontal group of N children that fits on iOS (HStack auto-shrinks) can overflow + clip the last ones on Android (Row no-wrap) — keep horizontal groups few + narrow, or stack vertically; treat "tap target works on iOS but the Android tap times out" as a Row-overflow suspect FIRST. Reference: `examples/native-tasks/src/TasksApp.tsx` TasksPage action buttons (`<Stack>` + the inline comment); the local-emulator bisect (#1539-app PASS / 6-button FAIL / button-removed PASS).

**Native routing runtime** (both targets, `packages/native/router-{swift,kotlin}/`): `PyreonRouter` + `RouterProvider` + `RouterView` + `Link` + hooks (`useNavigate`, `useParams`, `useLoaderData`). Global guards `beforeEach` / `afterEach` (#1108), throw-redirect via `router.redirect()` (#1109, re-entry-safe via `_inGuard`). Per-route `beforeEnter`, nested-routes (`RouteRecord.children`), and per-route `loader` auto-emit have all landed. Loader auto-emit (v1): a `loader: () => <expr>` wraps the route's component in a runtime `PyreonRouteLoader` host (`packages/native/router-{swift,kotlin}/.../RouteLoader.{swift,kt}`) whose `.task` (SwiftUI) / `LaunchedEffect` (Compose) fires the loader ONCE on appear and stores the result via `router.setLoaderData(<active path>, …)`, where the already-shipped `useLoaderData<T>()` reads it — keyed by the runtime active path (the `navigationDestination` `path` closure var on Swift / `currentPath` on Kotlin) so it matches BOTH literal and `:param` routes; the home route keys by its literal path (= `currentPath` at launch); guarded by `loaderData[path] == nil` so re-renders never re-run. v1 captures only ZERO-PARAM, EXPRESSION-body loaders (signal/store reads + sync expressions, e.g. `() => globalCache()`); a param-using (`(ctx) => …`), block-body, or truly-async loader WARNS and emits unloaded (the component still renders). Compiler pieces: `RouteIR.loader` + `parseRouteArray` extraction + `wrapLoader` (Swift `navigationDestination` + `RouterProvider` home) / `loaderWrap` (Kotlin `when`-dispatch) + the `PyreonRouteLoader` kotlin-stub; `.task` host is the route's concrete component view (stable identity — no fetch-arc ZStack concern), Kotlin `LaunchedEffect(path)` runs once. `ctx.params` threading + async-loader decode (the `useFetch<T>` territory) are the next arc (see [.claude/audits/native-readiness-2026-06-02.md](.claude/audits/native-readiness-2026-06-02.md) Phase A roadmap).

**Implementation roadmap** (originally scoped as 10 PRs across 5 phases): Phase A (foundation — primitives package + canonical web DOM runtimes for all 15) ✅ Done; Phase B (PMTC emit for iOS + Android — extend `canonical-primitives.ts` mapping table) ✅ both targets all 15 primitives; Phase C (`@pyreon/native-router-{swift,kotlin}` runtimes) ✅ core navigation + global guards shipped, per-route guards / nested routes / typed `useParams<T>` open; Phase D (web target for PMTC + cross-platform example dir) ✅ Done; Phase E (TodoMVC migration to canonical vocab — `<VStack>` → `<Stack>`) ✅ Done. End-user docs: `docs/src/content/docs/multiplatform.md`. Post-Phase-E follow-ups (router parity, real-device CI promotion, supported-TS-surface enumeration, real-app showcase, asset pipeline) are tracked in the readiness-audit roadmap above — current overall score 66/100, demo-quality, not production-ready.

**Migration is additive** — existing `<VStack>` / `<HStack>` / `<TextField>` in PMTC source continue to work via current per-target emit. New canonical vocab ships alongside. Deprecation warnings on SwiftUI-flavored tags land AFTER Phase E migration is proven on TodoMVC. Removal in a major-version bump LATER.

## Fundamentals — Key Technical Details

### @pyreon/store

- `defineStore(id, setup)` — composition stores, singleton by ID, returns `StoreApi<T>`
- `StoreApi<T>`: `.store` (user state/actions), `.id`, `.state` (snapshot), `patch()`, `subscribe()`, `onAction()`, `reset()`, `dispose()`
- Auto-classifies setup returns: signals → state tracking, functions → wrapped actions
- `addStorePlugin(plugin)`, `setStoreRegistryProvider()` for SSR, `resetStore(id)` / `resetAllStores()`
- **Patch hot-path leanness (`index.ts`).** The object-form `patch({...})` skips two avoidable costs when no subscriber is attached (the common case): (1) it no longer allocates a per-key `{key,newValue,oldValue}` event object during the batched write — those events feed the patch-`subscribe` notification, which is itself gated on `subscribers.size > 0`, so with no subscriber the event objects were pure waste; (2) the per-patched-key membership test uses a precomputed `Set` (`signalKeySet.has(key)`) instead of an O(signalKeys) `Array.includes` scan (scales with store width). ~12% faster patch (controlled same-run A/B, all 135 tests pass; byte-identical behavior). The remaining ~4–5× gap to Zustand's `setState` is intrinsic — Pyreon's per-field signals each pay real write machinery (the SAME fine-grained model that WINS `dispatch`/`read`/reactivity), vs Zustand's single shallow object merge. Surfaced by the `bench:stores` benchmark.
- **Benchmark vs Zustand + Jotai (`bun run --filter=@pyreon/store bench:stores`).** Objective 3-way head-to-head (`bench/store-bench.ts`): `NODE_ENV=production`, idiomatic per library (Pyreon `defineStore(id, () => ({signals, actions}))`, Zustand `createStore((set) => …)`, Jotai `atom()` + `createStore()`), a correctness gate asserting all three agree before timing, and per-op PROCESS ISOLATION (fresh `bun` child per op). Representative run (Apple M3, median ns/op; ratios stable across runs). **The honest picture is mixed — Pyreon does NOT win everything:** Pyreon wins the per-field hot path (`dispatch` ~2–3.7× / `write→1 subscriber` ~2× vs Zustand) and ties Zustand on `read` (both single-digit ns, the gap is noise), but **LOSES to Zustand on two ops**: `setup` (~20× slower — Pyreon's `defineStore` builds a global registry entry for SSR/devtools/`resetAllStores`; Zustand is a bare closure — disclosed; once-per-store, not a hot path) and bulk `patch` (~4× slower — Pyreon's mutation-tracking patch with per-field signal writes vs Zustand's shallow object merge; prefer the faster per-field `store.x.set()` dispatch path when you don't need bulk/patch-listener semantics). **Pyreon dominates Jotai's vanilla store on every op** (write/notify 18–54×, read 13–30×) — but disclosed: Jotai is React-render-dedup-optimized, so these are its honest *vanilla* `get`/`set` numbers, not its React render path. ns is machine-dependent — the ratio is the portable signal.

### @pyreon/state-tree

- **Benchmark vs MobX-State-Tree (`bun run --filter=@pyreon/state-tree bench:mst`).** Objective head-to-head (`bench/state-tree-bench.ts`): `NODE_ENV=production`, idiomatic per library, a correctness gate asserting both produce identical results before timing, and **per-op PROCESS ISOLATION** (each op in a fresh `bun` child — load-bearing: running all ops in one process let mobx's global scheduler state accumulate and inflated later ops ~10×, a measurement artifact, not MST's real cost). Representative run (Apple M3, median ns/op): Pyreon faster on every op — `action toggle` ~49× (57 vs 2822ns), `applyPatch` ~39× (75 vs 2913ns), `reactive write→observer` ~28× (110 vs 3064ns), `read` ~7× (6 vs 38ns), `getSnapshot` ~1.5× (28 vs 41ns). **Honest disclosure:** `create` (~10×) and `applySnapshot` (~39×) are NOT pure apples-to-apples — MST validates the snapshot against the model type on those ops, plain state-tree does not (schema mode would). The robust signal is the action/patch/reactive hot path, where Pyreon's signal write (~57–110ns) beats MST's mobx-action machinery (~2.8–3.1µs); MST is the known-heavy incumbent. ns is machine-dependent — the ratio is the portable signal.
- `model({ state })` (plain) or `model({ schema, initial?, onValidationError? })` (schema-driven) — chainable builder; `.views(f)` / `.actions(f)` accumulate onto a live `self` proxy (each layer sees prior ones); then `.create(initial?)` for an instance or `.asHook(id)` for a singleton. The single-config `model({ state, views, actions })` form was REMOVED — chain instead.
- **Schema mode is validation-driven AND strictly typed from the schema.** Pass the schema DIRECTLY — `@pyreon/validate`'s `s.object(...)`, a raw `z.object(...)`, valibot, arktype, or any [Standard Schema](https://standardschema.dev) validator — and the field types flow through end-to-end (`self.name()` is `string`, not `unknown`); no `@pyreon/validation` adapter wrapper needed. The inference reads the output type out of the schema's `~standard.validate` return (`types.ts:InferSchemaState`), so it works even when the optional `~standard.types` slot is absent (which it is on `@pyreon/validate`); the `@pyreon/validation` `zodSchema()` adapter's `_infer` path still matches first. `@pyreon/validation`'s `extractParseFn` drives runtime validation for raw schemas (via `isStandardSchema` → `wrapStandardSchema`). Schema mode installs five BARE-name mutation helpers on the instance + `self` — `set` / `patch` / `deepPatch` / `update` / `reset` — each validating the merged result through the schema before writing signals (a schema field colliding with a helper name throws at `.create()`); direct signal writes (`self.field.set(v)`) bypass validation (documented escape hatch).
- `ModelDefinition.create(initial?)` / `.asHook(id)` — instances or singleton hooks
- `.lifecycle(self => ({ afterCreate?, beforeDestroy? }))` — chainable instance lifecycle. `afterCreate` runs once at the END of `.create()` (after every view/action layer — `self` fully built; bottom-up for nested field-models); `beforeDestroy` runs on `destroy(instance)`. Only those two keys are honored (`.lifecycle()` throws on any other — typo guard).
- `destroy(instance)` / `isAlive(instance)` — tear down (run `beforeDestroy`, recurse into field-nested children, clear patch listeners + middleware, mark dead; idempotent) / liveness. **`destroy` tears down SUBSCRIPTIONS + runs cleanup — it does NOT free memory** (Pyreon signals have no per-signal dispose; GC reclaims once you drop refs). After `destroy`, actions + the schema mutation helpers dev-warn and no-op; direct signal writes (`self.field.set`) stay UNguarded (escape hatch). NO "drop from devtools registry" — devtools keys by user-name (no reverse map; the WeakRef registry self-cleans on GC).
- `clone(instance)` / `getType(instance)` — structural deep copy (snapshot → `def.create()`, independent signals/listeners/lifecycle; schema mode re-validates) / the producing `ModelDefinition` back-ref. Both rely on a `meta.definition` back-ref set at `.create()` time.
- `.volatile(self => ({...}))` — signal-backed TRANSIENT state (in-flight flags, drag/hover UI state, live refs like websockets/timers). Reactive (`self.x()` / `self.x.set()`, strictly typed via a 5th `TVolatile` generic on `ModelDefinition`/`ModelInstance`) but PLAIN signals (not `trackedSignal`) → emit no patches, absent from `meta.stateKeys` → excluded from `getSnapshot`/`applySnapshot`/`onPatch`/`onSnapshot`. Reserved-name-checked against state/helpers/views/actions/other-volatile. Runs after schema helpers, before views/actions (so views/actions read it via `self`).
- `onSnapshot(instance, cb)` — MICROTASK-COALESCED snapshot subscription (all writes in one sync burst → ONE emit on the next microtask; MST-like async). Does NOT fire on subscribe; volatile changes don't fire it. Implemented via the patch-write hook + a `meta.scheduleSnapshotNotify` coalescer (a `snapPending` flag dedups the microtask), NOT an `effect()` — so it never fires-on-create and never depends on `getSnapshot`'s untracked `.peek()` reads (the broken-`effect()` design pass-2 rejected). `meta.snapshotListeners` is cleared by `destroy`; the `trackedSignal` write-gate fires when EITHER patch or snapshot listeners exist.
- `onAction(instance, cb)` — observe-only action subscription (name/args/path before the call); sugar over `addMiddleware` (observe then `next`). Cannot block/alter — use `addMiddleware` to intercept.
- `getSnapshot(instance)` / `applySnapshot(instance, snapshot)` — typed recursive serialization (reads via `.peek()`; nested field-models recurse). **In schema mode `applySnapshot` re-validates**: it routes the (partial) snapshot through the schema `patch` helper so a malformed snapshot is REJECTED (the schema is the source of truth) — closing the integrity hole where a raw write bypassed validation. Plain mode keeps the direct-write + recurse path. Both leave snapshot-absent keys unchanged.
- `onPatch(instance, listener)` / `applyPatch(instance, patch|patches)` — JSON patch record/replay. Patches are `replace`-only (one signal per field; an array/object field emits a whole-value `replace`, not granular add/remove). Nested patches are prefixed (`/profile/name`).
- `addMiddleware(instance, fn)` — action interception chain (sync + async; `await next(call)`). The action runner also gates on liveness (no-op after `destroy`).
- `getParent(node)` / `getRoot(node)` / `getPath(node)` / `isRoot(node)` / `hasParent(node)` — tree traversal. A node gets a tree `parent` when written into another model's state — as a field value, an ARRAY element, or a plain-object value — because parent tracking runs on the initial value AND every tracked-signal write (the always-on `afterSet` hook in `trackedSignal`, NOT listener-gated). So array-held children (the headline `todos: Todo[]` shape) ARE tracked, not just field-nested — the general solution (scan written arrays/objects one container level deep) resolves the "field-nested-only = false parity" concern. `getPath` carries field keys not array indices in v1; a detached (array-removed) node keeps its last parent until GC (v1 limitation). `meta.parent`/`meta.parentKey` hold the link; `tree.ts:scanForChildren` is the scanner.
- `identifier()` / `reference(Type)` / `resolveIdentifier(root, Type, id)` — normalized references (closes #1751). `identifier()` is a plain-mode field MARKER (or `model({ schema, identifier: 'id' })` in schema mode) declaring which field is the node's id; `model()` records it on the definition as `_identifierKey` (plain-mode replaces the marker with its default at definition time). `reference(Type)` is a plain-mode field that STORES the target's id but RESOLVES to the live node on read — built as a resolving accessor (`self.author()` → node, `.set(node|id)`, `.id()`, `.setId()`, `.peek()`), tracked in `meta.referenceKeys` (its backing id-signal), NOT a `trackedSignal`; `getSnapshot`/`applySnapshot` serialize/restore the raw id via that map; `StateSignals`/`Snapshot` types special-case `ReferenceField` (instance member is the accessor, snapshot value is `string|number`). `resolveIdentifier` walks `root`'s subtree (`references.ts:walkInstances`, depth-first cycle-safe, reads owned state, SKIPS reference keys so it never follows a reference) for a node whose `definition === Type` and id-field equals `id` — O(n) per resolve (a root id-index is the documented future optimization). Resolution is via `getRoot(node)`, so the referencing node + target must share a root. Throws if the target type has no identifier. `reference()` is plain-mode (the marker lives in `state`); the TARGET can be schema-mode (via `identifier` config).
- Nested models compose for tree-shaped state in PLAIN mode (a `state` field whose value is another `ModelDefinition`); schema mode is flat (a schema can't carry a `ModelDefinition`). `/devtools` subpath export = WeakRef registry of live instances.

### @pyreon/form

- `useForm({ initialValues, onSubmit, validators?, schema?, validateOn?, debounceMs? })` — reactive form state (legacy API)
- `field(name, defaultValue, validator?)` — composable field definition carrying name as literal type. `useForm({ fields: [email, password], onSubmit })` infers `FormState<{ email: string; password: string }>` from the array
- `useField('name')` — context-based overload, reads form from nearest `<Form>` / `<FormProvider>`. Accepts generic: `useField<string>('email')`. Also: `useField(form, name)` (explicit form)
- `<Form of={form}>` — renders `<form>` + `FormProvider`, binds `onSubmit`. Children use `useField('name')` without prop drilling
- `<Submit>` — auto-disables during submission
- `useFieldArray(initial?)` — dynamic array fields with stable keys. Full mutation surface: `append` / `prepend` / `insert` / `remove` / `update` / `move` / `swap` / `replace`. Always render with `<For each={items()} by={i => i.key}>` — `.key` is a monotonic number assigned at insert time, not the array index
- `useWatch(form, name?)` — typed overloads: single field → `Signal<T>`, multiple fields → tuple of signals, no args → `Computed<TValues>`
- `useFormState(form, selector?)` — computed form-level summary (`isValid`, `isDirty`, `isSubmitting`, `isValidating`, `submitCount`, `isSubmitted`, `isSubmitSuccessful`, `errors`). Selector narrows the tracked subset so a button gated on `canSubmit` doesn't re-render when `submitCount` changes
- **react-hook-form-parity accessors on `FormState`** (all strictly typed against `keyof TValues`): `trigger(name?)` validates a field / subset / (no arg) the whole form on demand, immediately (bypasses `debounceMs`), returning the validated set's validity; `getValues(name?)` reads one value or all; `dirtyFields()` / `touchedFields()` return the changed / visited fields as `Partial<Record<keyof TValues, boolean>>` records (reactive); `getFieldState(name)` returns a field's live `FieldState` (same object as `fields[name]`); `isSubmitted` (`Accessor<boolean>`, `submitCount > 0`) and `isSubmitSuccessful` (`Signal<boolean>`, true only after a submit whose `onSubmit` ran without validation failure/throw; cleared by `reset()`). **Known gap (deliberate, not yet built): nested/deep typed field paths** (`user.address.city`) — `TValues` keys are FLAT; the composable `field()` + flat model is the current design. RHF/TanStack-style deep paths are a tracked future effort.
- **Auto-wired field accessibility (zero-config ARIA)**: `register(field)` returns — beside `value`/`onInput`/`onBlur` — a stable auto-generated `id`, reactive `aria-invalid` (`'true'` while errored, attribute REMOVED when valid — not `aria-invalid=""`), and reactive `aria-describedby` (→ the error element while errored, removed when valid so nothing dangles). New `errorProps(field)` (`{ id, role: 'alert' }`) and `labelProps(field)` (`{ for }`) — on BOTH `useForm` and `useField` (field-scoped, no arg) — return ids that AGREE with the input, so `<label {...labelProps('email')}>` + `<input {...register('email')}>` + `<span {...errorProps('email')}>{err}</span>` gives full label↔control + input↔error association + an announced error with NO hand-threaded ids, all derived from the existing `error` signal and reactive IN PLACE (0 re-renders). This makes the most common form-a11y mistake the zero-effort path and resolves the label↔control association an AST lint rule structurally can't (the deferred `control-needs-label` "high-risk cliff") at the runtime layer where the ids ARE knowable. Rides the proven function-valued-prop → `applyProp` `renderEffect`-wrap path (same as `disabled`/`readOnly`); needs no new package. `aria-required` is NOT auto-wired (the form has no per-field `required` concept — deferred). Companion `@pyreon/core` change: the JSX `aria-invalid` + `aria-describedby` attribute types now also accept the reactive accessor form (`() => …`), matching `aria-required`/`aria-readonly` (additive). Tests: `form/src/tests/a11y-autowire.test.ts` (node — accessor values + id agreement) + `a11y-autowire-dom.test.tsx` (happy-dom mount — the real `<input {...register()}>` spread gets `aria-invalid`/`aria-describedby` reactively + the error element links). First slice of the ecosystem-wide a11y plan (`.claude/plans/a11y-dx-multiplatform.md`).
- **Performance (vs the most popular libs, benchmarked in `packages/fundamentals/form/bench/`)**: on the keystroke hot path Pyreon's per-field signal `.set` beats TanStack Form's store clone+notify by **~79×** (`update-field` 46ns vs 3.60µs), `reset` by ~7×, AND full-values `read` by ~1.3× (`form-bench.ts`, headless — the fairest direct peer comparison); TanStack edges Pyreon only on once-per-form `setup` (~1.3×) — the deliberate price of per-field fine-grained signals, which buy the ~79× write win (a dirty-count-inline attempt to close `setup` was measured + reverted: it regressed the keystroke path ~2× via V8 deopt of a larger `setValue`). **`values()` / `getValues()` are epoch-cached** — rebuilt only when a value-mutation method (`setValue`/`reset`/`setInitialValues`) bumps an internal `_valuesEpoch`; a clean read is an int-compare + cached-object return (`read-all-values` 109ns → ~6ns, beating TanStack's plain-object ~7ns), and the write path pays only one integer increment (no signal/subscriber → keystroke path untouched). Direct `fields.x.value.set()` bypasses the epoch (consistent with bypassing dirty/validation — use `setFieldValue`). **The keystroke path has NO per-field `effect()`** — auto-validation (change-mode + post-failed-submit live revalidation) is driven INLINE from `setValue` reading `submitCount.peek()`, not a per-field `effect()` tracking `valueSig`. The effect approach cost a per-field EffectScope at setup (the dominant per-field allocation) AND an effect flush on EVERY keystroke (a no-op in the default blur mode); removing it cut the hot path **242ns → 46ns (5.3×)**, setup ~17%, reset ~35% — all behavior-preserving (bisect-verified: dropping the `submitCount.peek()` trigger fails 3 post-submit-revalidation specs; dropping the change-mode setup-validation fails the change-mode spec). change-mode forms still validate initial values once at setup (a one-time `validateField` call in the `useForm` field loop, replacing the effect's setup-run). On re-renders (`form-rerender-bench.ts`, 10 keystrokes into 1 field of a 20-field form): **Pyreon 0 component re-renders** (signals patch the bound node, values still reactive) = react-hook-form's 0 (uncontrolled refs — but RHF gives up reactive value binding) < Formik's 10 (controlled — re-renders the form every keystroke). Competitors are devDeps (`@tanstack/form-core`, `react-hook-form`, `formik`, `react`/`react-dom`); run `bun bench/form-bench.ts` + `bun bench/form-rerender-bench.ts`. **This is the TIER-A store-primitive comparison** (closest architectural peer = TanStack `form-core`); for the user-perceived real-app comparison see the Tier-B bench below — never extrapolate a Tier-A multiplier to "fastest form library" (the headline footgun the DOM bench's objectivity pass killed).
- **Tier-B real-app cross-framework benchmark (`examples/form-bench/`, real Chromium via Playwright)** — the user-perceived complement to the Tier-A headless bench. Pyreon vs **the six most popular form libs across the ecosystem**: React Hook Form / TanStack Form / Formik (React), vee-validate (Vue), Felte (Svelte), `@modular-forms/solid` (Solid — the true fine-grained-signal peer). Each in its IDIOMATIC model (Pyreon `register`+signals; RHF uncontrolled `register`+`zodResolver`; TanStack controlled `form.Field`+standard-schema; Formik controlled `useFormik`; Vue vee-validate `defineField` in `h()` render fns; Svelte Felte `use:form` in a real `.svelte`; Solid modular-forms `Field` via low-level `solid-js/web` so no Solid-JSX transform fights Pyreon's), the SAME zod schema, per-framework tightest commit boundary (`flushSync` React/Svelte, `nextTick` Vue, synchronous Pyreon/Solid), per-run resets, DOM verification per iteration, median + bootstrap CI95 + CV + tied-within-noise `🤝`, per-framework page isolation + forced GC + retained heap, randomized order — the same objectivity discipline as the DOM `bench:fair`. The whole non-Pyreon set is no-JSX EXCEPT Svelte's compiled `.svelte` (the one extra Vite plugin); that's how 7 frameworks coexist with Pyreon's compiler in one config. **Representative (M3 Max / Chromium 149, 40 pooled samples)**: **`@modular-forms/solid` (the fellow signal lib) ties Pyreon** on mount / keystroke-blur / reset (CI overlap) — the closest peer, exactly as predicted; the **validate-every-keystroke** scenario is the architectural signal — whole-form-revalidate-per-keystroke (TanStack 26× / Formik 10× / RHF 8× / Solid 5× / Svelte-Felte 6×) vs Pyreon's per-field patch, with **Vue (vee-validate) the surprise at 2×**; the other ops are 1.3–10× but Pyreon sits at Chromium's ~100µs `performance.now()` floor on those (sub-ms, imperceptible — NOT a runaway claim). **Retained heap**: Pyreon lightest (3.16 MB) < Solid 3.32 < Svelte 3.73 < Vue 3.79 < RHF/Formik 4.08 < TanStack 4.56. **Bundle (`bun bundle-size.ts`, gzipped; Pyreon + React libs + Vue + Solid — Svelte's `.svelte`-compile size tracked)**: the two signal libs lead — **Solid's TOTAL is smallest** (FULL validated form 72.5 KB; its 4.5 KB runtime undercuts even Pyreon's lean 11.9 KB), **Pyreon's marginal form DELTA is smallest** (65.8 KB); both ~1.7–1.9× lighter than RHF 132 / Formik 134 / TanStack 140 (Vue mid at 95.3). The form DELTA is within ~1.2× across every column (zod-dominated) — the spread is the runtime, not the form lib. The committed fairness + honesty + author-judge contract is `examples/form-bench/METHODOLOGY.md` (two tiers never conflated; Felte's eager validate-on-input noted; never extrapolate Tier-A primitive multipliers to "fastest form library"). Run: `bun bench-form.ts [--repeat N] [--only "<name>"]` + `bun bundle-size.ts`.
- `FormProvider` / `useFormContext<TValues>()` — context pattern for nested components, no prop drilling
- Per-field fine-grained signals: `value`, `error`, `touched`, `dirty` are independent `Signal<T>` — reading one doesn't subscribe to others
- `validateOn: 'blur' | 'change' | 'submit'` — **default is `'blur'`**, not `'change'`, so users aren't scolded mid-keystroke. `showError` (from `useField`) always gates on `touched` so even `validateOn: 'change'` forms don't flash errors until first blur
- Async validators are version-tracked — stale results discarded if user types faster than the validator resolves. Combine with `debounceMs` to cut in-flight request count. `isValidating` signal true while any field has a pending async validation
- Server errors: `form.setFieldError(name, msg)` / `form.setErrors({ email: 'Taken' })` — does NOT touch `touched` state, so errors display immediately regardless of blur status
- Manifest-driven docs (T2.1 + T2.5.1): `packages/fundamentals/form/src/manifest.ts` is the single source for the `llms.txt` bullet + `llms-full.txt` section + MCP `api-reference.ts` region. 7 MCP entries generated from the manifest's enriched `api[]` (was 2 hand-written). Inline-snapshot test (`manifest-snapshot.test.ts`) locks the rendered output locally in addition to the CI `Docs Sync` gate.

### @pyreon/i18n

- `createI18n({ locale, messages, loader?, fallbackLocale?, pluralRules?, onMissingKey?, numberFormats?, dateFormats?, relativeTimeFormats?, formats? })`
- `t(key, values?)` — interpolation with `{{name}}`, **inline format specifiers** (`{{amount, currency}}`, `{{when, date}}`, `{{n, relativetime, day}}`), pluralization with `_one`/`_other`/`_zero` suffixes, **`context`** (gender/variant — i18next resolution order `key_male_one` → `key_male` → `key_one` → `key`), **`defaultValue`** (returned + interpolated when the key is missing), and **`$t(key)` nesting** (depth-capped at 4 to break cycles).
- **Intl formatters on the instance** (the 2026-Q3 reference-parity addition — apps previously hand-rolled `Intl` in ~10 example files): `n(value, options?)` (`Intl.NumberFormat` — currency/percent/units, or a named `numberFormats` entry), `d(value, options?)` (`Intl.DateTimeFormat` — accepts `Date`/epoch-ms/string, or a named `dateFormats` entry; bare inline `date`/`time`/`datetime` map to default styles), `rt(value, unit, options?)` (`Intl.RelativeTimeFormat`). All three READ `locale()` so they re-run reactively on locale change, and memoize the underlying `Intl.*Format` per `(locale, optionsKey)`. Named-format config (vue-i18n style, per-locale) + custom inline `formats` (named formatter fns) are resolved with fallback-locale support. The inline-spec resolver + formatter registry live in `formatters.ts` (`/core`-safe — `Intl` + reactivity only); `interpolate(template, values?, { format? })` gained an optional format-resolver param (bare `interpolate()` ignores specs).
- Namespace lazy loading with deduplication, `addMessages()` for runtime additions
- `<I18nProvider value={i18n}>` / `useI18n()` context (the prop is **`value`** — renamed from `instance` in the reference-parity PR to match `PermissionsProvider` + what the manifest already documented). `<Trans>` rich JSX interpolation now **auto-reads `t` from the nearest `<I18nProvider>`** via `useI18n()` — `t` is optional (pass it only to override the context instance or render provider-less). `components` values are `(children) => VNode` functions; the prop is `i18nKey` (NOT `key`, which JSX reserves).
- **Two entry points**: `@pyreon/i18n` (full — includes JSX `Trans`/`I18nProvider`/`useI18n`) and `@pyreon/i18n/core` (framework-agnostic — only `createI18n`, `interpolate`, `resolvePluralCategory`, types). The formatters (`n`/`d`/`rt`) are on the instance returned by `createI18n`, so they are available from BOTH entries. Use `/core` for backend translation pipelines, edge workers, or any non-JSX consumer. The `/core` entry transitively only depends on `@pyreon/reactivity` — zero JSX, zero `@pyreon/core`. The main entry has a `/** @jsxImportSource @pyreon/core */` pragma on `trans.tsx` so even bun runtimes without JSX-aware tsconfigs compile it correctly.
- **`Intl.PluralRules` is memoized per locale (`pluralization.ts:_pluralRulesCache`)** — `resolvePluralCategory` previously allocated a fresh `new Intl.PluralRules(locale)` on EVERY `t()` plural call (~11µs each; the `Intl` constructor is the dominant cost, `.select()` is cheap), so a plural lookup cost ~50× a plain interpolation. The n/d/rt formatters were already `(locale, options)`-memoized; the plural path was the one Intl consumer that wasn't. Caching the `PluralRules` per locale drops plural resolution **~11µs → ~700ns (~16×)** — measured + surfaced by the `bench:i18next` benchmark below. Custom-`pluralRules` callers are unaffected (they early-return before the Intl path). Locale-keyed (the codebase only uses default cardinal rules); the Map is bounded by the app's locale set, like the formatter caches.
- **Benchmark vs i18next (`bun run --filter=@pyreon/i18n bench:i18next`).** Objective head-to-head (`bench/i18n-bench.ts`): `NODE_ENV=production`, idiomatic per library (i18next is the REFERENCE Pyreon mirrors — `{{name}}` interpolation, `key_one`/`key_other` plurals, and a built-in Intl `number`/`datetime` formatter reachable from `t()` are identical by design; i18next ≥21.3 ships the formatter in core), a correctness gate asserting `t`/interpolation/plural/number are byte-identical (date formats the same instant — the two libraries' default `Intl.DateTimeFormat` options differ, disclosed), and per-op PROCESS ISOLATION (fresh `bun` child per op). Representative run (Apple M3, median ns/op; **absolutes are machine-load-sensitive — the ratio is the portable signal**): **Pyreon faster on every op** — `t (plain)` ~18×, `interpolation` ~5.5×, `plural` ~4× (post-fix; was ~4× SLOWER before the `PluralRules` memoization above — the benchmark is what surfaced it), `number` ~3.4× (byte-identical, both `Intl.NumberFormat(en)`), `date` ~2.8× (defaults differ — disclosed). i18next's `t` walks an async-inited plugin/format pipeline; Pyreon's `t` is a direct reactive lookup. **Escaping disclosure (objectivity audit):** i18next HTML-escapes interpolated values BY DEFAULT (XSS-safety); Pyreon's `t` does not. The bench sets i18next `escapeValue: false` so both output raw — an apples-to-apples interpolation-SPEED comparison, and the CONSERVATIVE choice toward Pyreon (it gives i18next its faster non-escaping path, so Pyreon's interpolation win is understated, never inflated). A realistic i18next app with escaping on is somewhat slower than measured.

### @pyreon/query

- Full TanStack Query adapter: `useQuery`, `useMutation`, `useMutationState`, `useInfiniteQuery`, `useQueries`
- Prefetch: `usePrefetchQuery` / `usePrefetchInfiniteQuery` — warm the cache in component setup (cache-guarded, no re-fetch when already present) so a child's `useQuery` / `useSuspenseQuery` resolves with no loading flash
- Suspense: `useSuspenseQuery`, `useSuspenseInfiniteQuery`, `useSuspenseQueries` (aggregate — the result IS the `QuerySuspense` gate + a `data` array), `QuerySuspense` boundary
- SSR: `HydrationBoundary` — component that hydrates a server-dehydrated cache (`dehydrate(client)` blob) into the nearest `QueryClient` synchronously, before children render (`nativeCompat`-marked, complements the `dehydrate`/`hydrate` function re-exports)
- Error recovery: `QueryErrorResetBoundary` (component) + `useQueryErrorResetBoundary()` (hook returning `{ reset }`) — pair with a sibling `ErrorBoundary` so fallback retry clears errored queries
- **Offline persistence** (`@pyreon/query/persist` subpath): `PersistQueryClientProvider` (drop-in for `QueryClientProvider` that restores the cache from a persister on mount + persists on change), `useIsRestoring()` / `IsRestoringProvider` (reactive restore flag — exported from main too), and identity re-exports of TanStack's framework-agnostic persist engine (`persistQueryClient`/`persistQueryClientRestore`/`persistQueryClientSave`/`persistQueryClientSubscribe`/`removeOldestQuery`) + storage persisters (`createSyncStoragePersister` for localStorage/sessionStorage, `createAsyncStoragePersister` for IndexedDB/RN). **The fetch-gate**: all 6 query-reading hooks defer their first observer subscription (hence fetch) until `useIsRestoring()` flips false (via `subscribeWhenRestored` in `is-restoring.ts`) — so a restored cache is never clobbered by a redundant network fetch. No `<PersistQueryClientProvider>` → `isRestoring` is always false → hooks subscribe synchronously (byte-equivalent to before; SSR unaffected). The persist deps (`@tanstack/query-persist-client-core` + the two persisters) are subpath-only — tree-shaken from the main bundle.
- **In-app devtools** (`@pyreon/query/devtools` subpath, dev-only): `QueryDevtools` — a ~50-line `onMount` shim over `@tanstack/query-devtools`'s framework-agnostic `TanstackQueryDevtools` engine (the SAME panel React/Solid/Vue ship). Resolves the client from context or a `client` prop; mounts the engine into a host `<div>`, unmounts on cleanup; `nativeCompat`-marked. Import from `@pyreon/query/devtools` (NOT main) so the heavy engine tree-shakes out of production; gate render on `import.meta.env.DEV`. Config props read once at mount (reactive re-config is out of scope for v1). NOTE: a single `@tanstack/query-core@5.101.0` is pinned tree-wide via root `package.json` `overrides` so persist (pins 5.101.0) + devtools (resolves query-core) + the adapter share ONE query-core type (avoids the dual-version `#private`-brand mismatch).
- `useSubscription(options)` — reactive WebSocket with auto-reconnect, integrates with QueryClient for cache invalidation
- `useSSE(options)` — Server-Sent Events hook with QueryClient integration, same pattern as useSubscription but read-only. Updates `lastEventId()` on every incoming SSE `id` field. To resume across remount, pair `useStorage` with the new `initialLastEventId?: string | (() => string)` option and put the ID in the URL so the server reads it as a query param — `EventSource` has no API to set `Last-Event-ID` on the first connection. The seed is read once at mount; subsequent changes to the accessor are ignored (use the reactive `url` for runtime overrides).
- Global counters: `useIsFetching(filters?)` / `useIsMutating(filters?)` → `Signal<number>` for top-of-page spinners
- `useQueryClient()` — imperative access to the nearest `QueryClient` (throws if no provider mounted)
- TanStack core re-exports (identity-equal to `@tanstack/query-core`): `QueryClient`, `QueryCache`, `MutationCache`, all four observers (`QueryObserver`, `InfiniteQueryObserver`, `MutationObserver`, `QueriesObserver`), `dehydrate`, `hydrate`, `skipToken` (v5 sentinel — `queryFn: skipToken` type-safely disables a query), `keepPreviousData`, `hashKey`, `matchQuery`, `matchMutation`, `replaceEqualDeep`, `focusManager`, `onlineManager`, `notifyManager`, `isServer`, `isCancelledError`, `CancelledError`, `defaultShouldDehydrateQuery`, `defaultShouldDehydrateMutation` (+ all types: `QueryKey`, `QueryFilters`, `MutationFilters`, `Mutation`, `MutationState`, `QueryState`, `DehydratedState`, `HydrateOptions`, `InfiniteData`, `DefaultError`, `FetchQueryOptions`, `FetchInfiniteQueryOptions`, etc.) — consumers import everything from `@pyreon/query`. NOTE: `streamedQuery` / `experimental_streamedQuery` are NOT re-exported (not in the pinned query-core@5.101 surface); persistence (`@tanstack/query-persist-client-core`) and devtools (`@tanstack/react-query-devtools`) are separate TanStack ecosystem packages, out of scope for the adapter
- Fine-grained signals per field (data, error, isFetching independent) — each field-level read only subscribes to that field
- `useMutation({ invalidates: [['posts']] })` — auto-invalidates query keys on successful mutation. Preserves user `onSuccess` callback.
- `defineQueries({ user: () => opts, posts: () => opts })` — named parallel queries returning typed object instead of array
- **Options as a function**: `useQuery` / `useInfiniteQuery` / `useQueries` / `useSuspenseQuery` take options as a FUNCTION (not an object) so `queryKey` and other fields can read Pyreon signals — changing a tracked signal re-runs the observer options and refetches automatically. `useMutation` options are a plain object (mutations are imperative, no tracking needed).
- Manifest-driven docs (T2.1 + T2.5.1): `packages/fundamentals/query/src/manifest.ts` is the single source for the `llms.txt` bullet + `llms-full.txt` section + MCP `api-reference.ts` region. 25 MCP entries generated from the manifest's enriched `api[]` (was 2 hand-written). Inline-snapshot test (`manifest-snapshot.test.ts`) locks the rendered output locally in addition to the CI `Docs Sync` gate.

### @pyreon/table

- `useTable(options)` — reactive TanStack Table with signal-driven options, auto state sync
- `flexRender(component, props)` — renders column def templates (strings, functions, VNodes)

### @pyreon/virtual

- `useVirtualizer(options)` — element-scoped with reactive `virtualItems`, `totalSize`, `isScrolling`
- `useWindowVirtualizer(options)` — window-scoped variant with SSR-safe checks

### @pyreon/validation

- `zodSchema()` / `zodField()` — duck-typed Zod adapter (works with v3 and v4)
- `valibotSchema(schema, safeParseFn)` / `valibotField()` — Valibot standalone-function style
- `arktypeSchema()` / `arktypeField()` — ArkType sync adapter
- `wrapStandardSchema()` / `isStandardSchema()` / `extractParseFn()` — generic Standard Schema duck-typing (any spec-compliant validator including Zod 3.24+, Valibot 1.0+, ArkType 2.0+, Effect Schema, typia)

### @pyreon/validate

Pyreon's validator + DX layer on [Standard Schema](https://standardschema.dev). Two halves: (1) **DX helpers** (`withField` / `parseReactive` / `formatErrors`) that work on top of ANY spec-compliant validator (Zod 3.24+ / Valibot 1.0+ / ArkType 2.0+); (2) since v1, **Pyreon's own `s` validator runtime** — chainable + function-comp hybrid (`s.string().email()` / `pipe(string(), email())`), Standard Schema-native. **Tree-shaking — two tiers (re-measured 2026-06, Rollup/Vite against the PUBLISHED lib, which is what real consumers bundle):** (a) **DX helpers tree-shake cleanly** — a `withField`-only import is ~0.3KB gz, the entire `s` runtime dropped (verified zero `StringSchema`/`validateIp`/… symbols leak). (b) **The chainable `s.` API does NOT tree-shake its checks** — `string()` returns a class whose prototype carries all 17 string-format methods, so any schema pulls every email/url/uuid/cc/phone/ip regex (a 3-field `s.object({string.min, string.email, number.int.min})` ≈ **11KB gz** via the `s` namespace, ~8.4KB via named chainable constructors). Chaining fundamentally can't tree-shake (methods must live on the prototype) — so the win comes from a **compile-time rewrite, NOT a second user-facing API**. (c) **RECOMMENDED — `pyreon({ optimizeValidators: true })` (opt-in, build-only):** you keep writing the beautiful chainable `s.string().email().min(2)`; `@pyreon/vite-plugin` rewrites each statically-analyzable module-level `const X = s.<chain>` (in `.ts` modules) into the lean form at build time, importing only the constructors + actions it uses, so the bundle prunes the rest. **Function chaining in, tree-shakeable output — no `/mini` for users to learn.** Proven byte-identical END TO END (`s.object({ name: s.string().min(2), email: s.string().email(), age: s.number().int().min(0).max(150) })` → `object({ name: string().check(minLength(2)), … })`, **11,098 B → 6,539 B gz, −41%** measured; parity locked verdict-for-verdict by `validate/tests/compile-rewrite-equivalence.test.ts`). Conservative: a dynamic schema (built in a function / conditionally / non-literal arg) or a `.tsx` schema hits the `analyzeValidate` bail and gracefully stays full-runtime (correct, not pruned). (d) Underneath, the rewrite lowers to **`@pyreon/validate/mini`** — the lean engine (lean `string()`/`number()` base constructors with a generic `.check(...)` + standalone actions `minLength`/`email`/`url`/`minValue`/`integer`/…). `/mini` is the compiler's emit TARGET, not the headline user API; it's also importable directly as an escape hatch for dynamic schemas or non-Vite bundlers (`string().check(email(), minLength(2))` / `pipe(string(), email(), minLength(2))`), Standard-Schema-native + parity-locked (`tests/mini-parity.test.ts`). The compiler machinery: `@pyreon/compiler:analyzeValidate` already parses `const X = s.<chain>` into a typed IR (`ValidateNode`, used by `compileValidators`); the new `emitSchemaSource(node, aliasPrefix)` lowers that SAME IR to lean schema source + the import set (parallel to `emitValidator`'s verdict lowering); `@pyreon/vite-plugin`'s `optimize-validators.ts` overwrites each schema's initializer span + injects the aliased `@pyreon/validate/mini` import. **Earlier docs claimed the runtime was ~3.9KB and referenced a phantom `@pyreon/validate/checks/string` subpath — both wrong** (the 3.9KB measured `s` imported from `src/v1` directly with esbuild, not the published `@pyreon/validate` entry; the subpath never existed in `exports`). **esbuild-direct caveat**: raw esbuild tree-shakes the pre-bundled lib chunk less aggressively than Rollup (~5KB even for a DX-only import); Vite/Webpack/Rollup/Rolldown — what nearly all consumers use — get the figures above. The shipped chainable classes are UNTOUCHED by the mini work (zero behavior change; `s.` users unaffected). Reference: `packages/fundamentals/validate/src/{mini.ts,actions/{string,number,_core}.ts}`. Follow-ups: extend mini's action set to the long-tail formats (phone/ip/creditCard/cuid/ulid/nanoid/emoji/jwt/cidr/duration) + lean the chainable methods to delegate to the actions (DRY).

- `withField(schema, meta)` — attach `FieldMeta` (label / hint / placeholder / i18n keys / autoFocus / autoComplete / defaultValue) to any Standard Schema. The returned schema is the SAME REFERENCE — Pyreon mutates a Symbol-keyed non-enumerable slot. Mutation (not cloning) is required because ArkType's `Type` instances are callable functions whose `~standard.validate` does `this(input)` — a shallow clone is not callable and breaks that contract. Symbol-keyed non-enumerable mutation is invisible to `JSON.stringify` / `for…in` / `Object.keys` / structured clone / library-internal comparators. Re-wrapping merges (later keys win).
- `getMeta(schema)` — read attached metadata. Accepts both objects AND functions (callable schemas).
- `resolveMetaField(schema, field, t?)` — read a single field through optional i18n. `t('auth.email.label')` wins over `meta.label` when it resolves to a non-key string.
- `parseReactive(schema, source)` — returns `Computed<ParseResult>` that re-validates on signal changes. Sync.
- `parseReactiveAsync` — async variant for schemas with async refinements. Caller handles staleness via `watch`.
- `watchValid(schema, source, cb)` — fires only on validity flips, not every error change. Returns unsubscribe.
- `formatError` / `formatErrors` / `formatErrorsByPath` — resolve issues to strings via the `t` from `useI18n()`. Pyreon issues carry optional `{ key, params, fallback }`; bare StdSchema issues fall through to `message` (no overhead, no special-casing). `formatErrorsByPath` builds a per-field map keyed by issue path — compatible with `@pyreon/form`'s `Errors` shape.
- **v1 `s` validator runtime** — string/number/boolean/literal/enum primitives + object/array composition + optional/nullable/default/transform/refine/brand/describe modifiers + 20+ built-in checks. Opt-in by import; third-party validators (Zod / Valibot / ArkType / typia) remain fully supported through the same DX helpers. Existing `zodSchema()` / `valibotSchema()` / `arktypeSchema()` adapters in `@pyreon/validation` remain unchanged.
- **Object algebra** — `.pick` / `.omit` / `.partial` / **`.required`** (inverse of partial — unwraps `.optional()`/`.nullish()` fields back to required) / `.extend` / `.merge` / `.keyof` + unknown-key policy (`.strip` default / `.strict` / `.passthrough` / **`.catchall(schema)`** — validates every unknown key against `schema` and keeps it; takes precedence over strip/strict/passthrough; preserved through pick/omit/partial/extend/merge). `.required`/`.catchall` closed the two open follow-ups flagged in `v1.ts`. **JIT-correctness note**: `.catchall` disqualifies an object from the JIT plain-object inline path (`jit.ts:isPlainObject` checks `!s._catchall`) — otherwise the shape-only inline loop would silently strip unknown keys without validating them; a catchall object (root or nested) falls back to the interpreter, which is correct. Bisect-locked in `object-algebra.test.ts` (the nested-catchall-under-JIT spec).

### @pyreon/validate — Performance vs Zod / Valibot / ArkType

Benchmarked by `bun run bench:validate` (`scripts/bench/core/validate.ts` — custom warmup+timed-ops harness, `NODE_ENV=production`, a correctness gate asserts all four libs agree valid/invalid before timing, idiomatic schema per lib). Representative run (Bun 1.3, Apple Silicon, ops/sec; the column *ratios* are the signal):

- **Error/invalid-input path — `@pyreon/validate` WINS every shape**: small-object 8.0M ops/s vs Valibot 3.2M (2.5×) / Zod 0.21M (39×) / ArkType 0.20M (40×); nested 5.9M (3.6× Valibot, 40× Zod, 50× ArkType); array-of-20 0.87M (2.6×/8×/18×); discriminated-union 15.3M (3.5× Valibot, 18× ArkType, 30× Zod). The `s` runtime early-exits on the first failing op; Zod + ArkType allocate rich structured error objects (their cost), Valibot is closer but still behind.
- **Valid-input path — `s` is 2nd, behind only ArkType's JIT, and FASTER than Zod + Valibot**: small-object 14.1M ops/s (ArkType 22.8M is 1.62× faster; Valibot 6.7M / Zod 6.0M are 2.1–2.3× SLOWER than `s`); nested 10.4M (ArkType 1.40×; `s` 2.3–3.5× faster than Valibot/Zod); array-of-20 0.89M (ArkType 1.43×; `s` 2.3–3.3× faster than Valibot/Zod); discriminated-union 25.3M (ArkType 1.83×; `s` faster than Zod 17.8M + Valibot 4.7M).
- **The valid-parse gap to ArkType is the documented JIT frontier** — ArkType compiles each type to one flat monomorphic `new Function` predicate (see "what is ArkType JIT"); Pyreon's `core/jit.ts` does the same recursively for object/array/primitive trees but still does per-field `path.push/pop` on the valid path (ArkType defers path construction to the error path), and the **discriminated-union root currently isn't JIT'd** (falls to the interpreter — the widest valid-parse gap at 1.83×, the clearest tracked perf follow-up). Beating ArkType on valid-parse is the open frontier `v1.ts` notes; the bench quantifies exactly where.
- **Known remaining feature gaps** (vs Zod, honest completeness audit): `.catch(fallback)` (error-fallback value), `.readonly()` (freeze output), `.pipe`-with-descriptor-actions (Valibot-style, for tree-shaking), and the longer-tail string formats (datetime / ip / cidr / base64 / emoji / ulid). Core primitive/composition/object-algebra surface is at Zod parity after `.required`/`.catchall`.
- **Compiler-emit (SHIPPED, end-to-end)** — `@pyreon/compiler:analyzeValidate(code)` parses each `const X = s.<schema>` into a typed IR (`ValidateNode`, conservative `unsupported` bail) and `emitValidator(node)` lowers an emittable IR to an inlined monomorphic validator source string (typia-class: straight-line `typeof`/regex/comparison, no op-array traversal). A cross-runtime emit-equivalence gate (`validate/src/tests/emit-equivalence.test.ts`) builds each corpus schema BOTH as runtime `s` and via `analyzeValidate→emitValidator` and asserts verdict parity. The runtime half is `Schema.is(input)` + the internal `_attachCompiledVerdict(fn)` hook. The build wiring is **opt-in `pyreon({ compileValidators: true })`** in `@pyreon/vite-plugin`: production builds append `X._attachCompiledVerdict((v) => { try { return (<emitValidator>)(v).length === 0 } catch { return false } })` to every MODULE-LEVEL, fully-emittable `const X = s.<schema>` (gated on `ValidateSchemaInfo.topLevel` — a function/block-scoped attach at module end would ReferenceError), so `X.is(v)` runs the inlined verdict instead of `X.parse(v).ok`. Build-only (dev keeps the runtime path — already correct + HMR-reactive); runs for both `.ts` (esbuild-compiled after) and `.tsx` (verdict appended AFTER the JSX compile — BOTH paths transform-test-covered in `vite-plugin/src/tests/compiled-verdicts.test.ts`); byte-equivalent to the runtime (speed only); composed/aliased/unsupported schemas skip silently and fall back to runtime `.is()`. **MEASURED benefit (`bun run --filter=@pyreon/validate bench:compiled-verdict`, 500k×7, 3× replicated): compiled `.is()` is 1.6–3.0× faster than `parse().ok`** — biggest on cheap schemas (number 2.9×, array 3.0× — Result-alloc + parse-machinery overhead dominates), smallest on expensive ones (email regex 1.6×, object 1.8× — the regex/field work is shared). **Honest scope: it's NANOSECONDS (~20ns/call) — only matters in a hot `.is()` loop (filtering/gating large arrays); `.parse()` (the common form/request path) is UNCHANGED, and only module-level fully-emittable schemas qualify.** The codegen lives in `packages/tools/vite-plugin/src/compiled-verdicts.ts` (internal `buildCompiledVerdicts`), isolated in its own file + excluded from CodeQL (`.github/codeql/codeql-config.yml` paths-ignore — a build-time generator whose input is the app's OWN schema source is a false positive for `js/bad-code-sanitization`, same accepted pattern as the validate runtime JIT). Reference: `packages/core/compiler/src/validate-emit.ts`, `packages/tools/vite-plugin/src/compiled-verdicts.ts`, `packages/fundamentals/validate/bench/compiled-verdict.ts`.

### @pyreon/feature

- `defineFeature({ name, schema, api })` — schema-driven CRUD primitives
- Auto-generates: `useList`, `useById`, `useSearch`, `useCreate`, `useUpdate`, `useDelete`, `useForm`, `useTable`, `useStore`
- Composes `@pyreon/query`, `@pyreon/form`, `@pyreon/validation`, `@pyreon/store`, `@pyreon/table`

### @pyreon/charts

- `useChart<TOption>(optionsFn, config?)` — reactive ECharts bridge with lazy loading
- `<Chart />` component with event binding, auto-detects chart types and dynamically imports
- `@pyreon/charts/manual` entry for tree-shaking control
- **tslib alias is required in BOTH tests AND consumer Vite apps**: ECharts imports `tslib` for TypeScript helpers (`__extends`, `__assign`, etc.). tslib's `./modules/index.js` ESM entry destructures named helpers from a `__toESM(require_tslib())` default — the helpers live as top-level vars on the CJS factory, so the destructure reads `undefined` and the page throws `TypeError: Cannot destructure property "__extends"` the moment ECharts loads. The fix is `resolve.alias: { tslib: '<path-to>/tslib.es6.js' }` — that file is a flat ESM module with proper named exports. **Consumer Vite apps**: `import { chartsViteAlias } from '@pyreon/charts/vite'`, then spread it into `resolve.alias` (`alias: { ...chartsViteAlias() }`). It resolves the right path via echarts itself, falls back to walking up `node_modules` for hoisted layouts, and returns `{}` when tslib isn't reachable so apps that don't use Charts don't break. **Browser tests**: `tslibBrowserAlias(import.meta.url)` from `vitest.browser.ts` does the same job for vitest configs. Helper unit tests for the test-side variant live in `packages/internals/test-utils/src/tests/vitest-browser-helpers.test.ts`. Two helpers exist because Vite config files run under Node's `node` condition (so they need the package's `lib/vite.js` build artifact), while vitest browser configs are loaded inside the test runner's bundler context (so they can reach the repo-root `vitest.browser.ts` directly). Reference: `examples/fundamentals-playground/vite.config.ts`, `examples/app-showcase/vite.config.ts`, `packages/fundamentals/charts/vitest.browser.config.ts`. Tracking upstream: [microsoft/tslib#189](https://github.com/microsoft/tslib/issues/189).

### @pyreon/storage

- `useStorage(key, default, options?)` — reactive signal backed by localStorage, cross-tab synced
- `useSessionStorage`, `useCookie`, `useIndexedDB`, `useMemoryStorage` — backend variants
- `createStorage(backend)` — factory for custom backends (encrypted, remote, etc.)
- All return `StorageSignal<T>` — extends `Signal<T>` with `.remove()`
- **`StorageOptions.writeDebounceMs?` (opt-in, local/session)** — coalesce the synchronous `JSON.stringify` + `setItem` so a high-frequency setter (persist-as-you-type drafts) doesn't pay main-thread I/O per `.set`. The signal still updates SYNCHRONOUSLY (UI reactive); only the persist write is debounced (latest value wins). A pending write is flushed on `pagehide`/`beforeunload` via ONE shared idempotent listener (a module-level `pendingFlushes` set — NOT a per-signal listener, avoiding the event-listener pile-up leak class), and `.remove()` cancels any pending write so the timer can't resurrect a removed entry. Default (omit / `0`) = synchronous write, byte-identical to prior behavior. The IndexedDB backend already debounced async writes (`debounceMs`); this is the synchronous-backend analog. Reference: `packages/fundamentals/storage/src/local.ts` (`scheduleWrite` / `flush` / `ensureUnloadFlush`).
- **Wrapper-signal contract**: each storage factory wraps a base `signal()` with a callable that delegates `.direct` / `.subscribe` / `.peek` / `.set` AND forwards the internal `_v` field via getter. The `_v` forwarding is load-bearing for the compiler-emitted `_bindText` fast path — `_bindText(theme, textNode)` reads `theme._v` directly (skipping the function call), so without the getter the binding wrote `''` on initial render AND on every subsequent subscriber notification. Pre-fix shape (pre-2026-05-13): SSR rendered correctly but post-hydration text nodes went empty across every storage backend. See `packages/fundamentals/storage/src/{local,cookie,custom,indexed-db}.ts` for the canonical `Object.defineProperty(storageSig, '_v', { get: () => sig._v, configurable: true })` shape and `.claude/rules/anti-patterns.md` "Signal-like wrapper callables missing the internal `_v` field" for the general contract.

### @pyreon/hotkeys

- `useHotkey(shortcut, handler, options?)` — component-scoped, auto-unregisters on unmount
- `useHotkeyScope(scope)` — activate a scope for a component's lifetime
- Supports `mod` (⌘ on Mac, Ctrl elsewhere), scope-based activation

### @pyreon/permissions

- `createPermissions(initial?)` — reactive permissions instance, callable as `can(key, context?)`
- `can.not(key)` / `can.all(...keys)` / `can.any(...keys)` — inverse and multi-checks
- `can.assert(key, context?, message?)` — throw-on-deny (a custom `message`, or `[Pyreon] permission denied: '<key>'` by default) for loaders / route guards / server actions; evaluates predicates + wildcards exactly like `can()`
- `can.set(map)` / `can.patch(map)` / `can.clear()` — replace / merge / wipe permissions reactively
- `PermissionsProvider` prop is **`value`** (`<PermissionsProvider value={can}>`) — iter-2 renamed it from `instance` to match the documented/conventional name (the code had drifted from the docs/MCP which always said `value`)
- **Wildcard matching (most-specific-first):** `'posts.*'` matches exactly ONE segment (`posts.read`, NOT `posts.read.title`); `'posts.**'` matches any depth strictly below `posts` (covers `posts.read` AND `posts.a.b.c`, but NOT the bare `posts` node); `'*'` matches everything. Resolution order: exact → `parent.*` → nearest-ancestor `**` (longest prefix first) → global `*` → denied. Because resolution is most-specific-first, an exact or `**` deny overrides a broader subtree grant (`'posts.**': true` + `'posts.admin.**': false` grants posts but denies the admin subtree) — the CASL `cannot`-over-`can` shape, in the flat-key idiom. (`*` global stays recursive-everything; `prefix.*` stays one-segment — both unchanged, `**` is the new recursive-subtree primitive.)
- `PermissionsProvider` / `usePermissions()` — context pattern for SSR/testing
- **Deliberately out of scope vs CASL** (the predicate model replaces the DSL): MongoDB-style condition matching → predicates `(context) => boolean` (arbitrary code, more flexible); rule packing/serialization + subject-type detection → not applicable to the flat-string-key model. Async permission checks → resolve into a signal the predicate reads (async offloaded to Pyreon primitives).

### @pyreon/machine

- `createMachine({ initial, states })` — constrained signal with type-safe transitions
- `machine()` — read state, `machine.send(event, payload?)` — trigger a transition; **returns the settled `TState`** (after any `always` cascade), or the unchanged state for an unhandled event / rejected guard
- `machine.matches(...states)`, `machine.can(event, payload?)`, `machine.nextEvents()`, `machine.isFinal()`
- Guards: `{ target: 'state', guard: (payload?) => boolean }` for conditional transitions. **Guards are throw-safe** — a guard that throws (e.g. reading a property of a missing payload) DENIES the transition rather than crashing `send`/`can` (consistent with `@pyreon/permissions` predicates)
- **`can(event, payload?)`** — predicts `send(event, payload)` EXACTLY: it evaluates the guard (throw-safe → denied) with the given payload, or `undefined` if none is passed. (Iter-2 breaking change: previously a guarded event with no payload reported `true`; now it evaluates the guard, so `can('LOGIN')` with no payload against a payload-reading guard reports `false`.)
- **Eventless `always` transitions** (`states.X.always: target | { target, guard } | [...]`) — fire SYNCHRONOUSLY on entering a state (and for the initial state at creation / on `reset()`), cascading until none apply; the first unguarded entry or first passing guard wins. Guards receive NO payload (read external signals). Transient/condition states (`check → pass | fail`) are never observed by `machine()` or reactive readers. A self-looping `always` throws after 1000 steps (config-error guard). Eventless transitions are the synchronous-transition-logic part of the statechart model — distinct from "async, use effects" which Pyreon offloads to signals/effects.
- **Final states** — `states.X.final: true`; `machine.isFinal()` (reactive) reads true in any final state; `machine.onDone(cb)` fires (with the triggering event) when a final state is entered, including via an `always` cascade. Pyreon does NOT freeze final states (a final state's `on` transitions still fire if declared — omit `on` for true terminals).
- **Lifecycle listeners** — `onEnter(state, cb)` / `onExit(state, cb)` / `onTransition(cb)` / `onDone(cb)`, all returning an unsubscribe; `dispose()` clears all. On each transition they fire in state-chart order: `onExit(from)` (machine still reads `from`) → `onTransition(from, to, event)` → `onEnter(to)` (machine reads `to`) → `onDone` if `to` is final. `onExit` pairs with `onEnter` for per-state setup/teardown — e.g. start a timer on enter, clear it on exit (the idiomatic delayed-transition pattern; Pyreon has no built-in `after`, by design — async/timers use Pyreon primitives).
- **Deliberately out of scope vs XState** (offloaded to Pyreon primitives, per the package philosophy): extended-state/context → signals; invoked actors/services → effects + signals; hierarchical/parallel states → compose multiple machines; delayed `after` → `onEnter` + `useTimeout`/`setTimeout` + `onExit` cleanup. The package is "a constrained signal with statechart transition semantics," not an XState clone.
- **Benchmark vs XState (`bun run --filter=@pyreon/machine bench:xstate`).** Objective head-to-head (`bench/machine-bench.ts`): `NODE_ENV=production`, idiomatic per library (Pyreon `createMachine({initial, states})` — the reactive instance IS the machine; XState `createMachine(config)` once + `createActor(machine).start()` per instance, reads via `getSnapshot()`), a correctness gate asserting both agree on state + guard before timing, and per-op PROCESS ISOLATION (fresh `bun` child per op). Representative run (Apple M3, median ns/op; ratios stable across runs): **Pyreon faster on every common-statechart op** — `send (transition)` ~50–55× (28 vs ~1400ns), `create (instance)` ~23× (~100 vs ~2300ns), `can (guard eval)` ~13–16× (4 vs ~60ns), `matches` ~5× (17 vs ~85ns). **Honest framing (NOT a feature-parity claim):** XState's per-op cost is genuinely higher because it does MORE — an actor/interpreter layer, immutable snapshots, richer event objects, and the hierarchical/parallel-states + invoked-actors + delayed-`after` machinery Pyreon deliberately offloads to signals/effects (see the bullet above). The bench measures the COMMON statechart ops both libraries share (create instance, send, guard-check, matches); on those, Pyreon's signal-backed transition is a large constant-factor win, but XState buys statechart features Pyreon does not have. ns is machine-dependent — the ratio is the portable signal.

### @pyreon/flow

- `createFlow<TData>({ nodes, edges, ...config })` — generic over node `data` shape. `createFlow<MyData>(...)` returns `FlowInstance<MyData>` so `node.data.kind` narrows correctly without an `[key: string]: unknown` index signature on consumer types. Defaults to `Record<string, unknown>` if no generic supplied.
- `useFlow<TData>(config)` — component-scoped wrapper around `createFlow` that auto-disposes the instance on unmount. Use inside a component body; use `createFlow` directly only for app-store / singleton flows that outlive the component tree.
- Node/edge CRUD, selection, viewport (zoom/pan/fitView), auto-layout via elkjs (lazy-loaded)
- Components: `<Flow nodeTypes={{ custom: MyNode }}>`, `<Background>`, `<MiniMap>`, `<Controls>`, `<Handle>`, `<Panel>`. JSX components are NOT generic at the call site (`<Flow<MyData> />` is invalid JSX); `FlowProps.instance` is typed as `FlowInstance<any>` so typed consumers can pass `FlowInstance<MyData>` without casting.
- Edge paths: `getBezierPath()`, `getSmoothStepPath()`, `getStraightPath()`, `getStepPath()`
- **Configurable edge markers (arrowheads)**: `MarkerType.ArrowClosed` (filled `<polygon>`, the default) / `MarkerType.Arrow` (open `<polyline>`), set per-edge via `markerStart` / `markerEnd` (a bare `MarkerType`, a full `EdgeMarker` `{ type, color?, width?, height?, strokeWidth? }`, or `null` for explicit no-marker) and graph-wide via `FlowConfig.defaultMarkerEnd` (`null` → arrowless by default). `markerEnd: null` is the opt-out that overrides the default; OMITTING it inherits the default. Helpers in `edges.ts` (`resolveMarker` / `markerId` / `resolveEdgeMarkers` / `collectEdgeMarkers`) are pure + exported; the `<defs>` set is deduped by a config-keyed `markerId` and rebuilt reactively as edges change. One `<marker orient="auto-start-reverse">` def serves both ends. Reference: `packages/fundamentals/flow/src/edges.ts` + `components/flow-component.tsx:MarkerDef`; tests `tests/markers.test.ts` (13 unit) + `tests/flow.browser.test.tsx` (2 real-Chromium: deduped shapes/colors + reactive defs).
- **Render virtualization**: `FlowConfig.onlyRenderVisibleElements` (default OFF) culls nodes whose screen rect (± a 200px margin) doesn't intersect the viewport, and edges with no visible endpoint; the rendered set re-filters reactively on pan/zoom (the `<For each>` accessors read `viewport()` + `containerSize()`). A 0×0 pre-measurement container renders all (guard). An edge spanning two off-screen nodes is culled even if its line crosses the viewport (matches React Flow). Reference: `flow-component.tsx:visibleNodeList`/`visibleEdgeList`; tests `tests/flow.browser.test.tsx` (real-Chromium cull + re-filter-on-pan + flag-off control, bisect-verified).
- **Object-snapping is opt-out for drag perf**: `FlowConfig.snapToObjects` (default `true` — no behavior change) gates the helper-line align-to-other-nodes scan in the drag pointermove handler, which is an O(N) pass over every node on EVERY frame (the *dominant* per-frame cost on large graphs — `getSnapLines` was ~75% of drag-frame work, measured). `snapToObjects: false` skips it: drag becomes the single immutable node-array update only. Measured 60-frame drag: N=1000 1.34ms→0.31ms, N=3000 3.36ms→0.78ms (~3-4×). Distinct from `snapToGrid` (grid quantization in `updateNodePosition`). Reference: `flow-component.tsx` drag `handlePointerMove` gate; tests `tests/flow.browser.test.tsx` (real-Chromium snap-on snaps / snap-off raw-position, bisect-verified).
- **Round-2 component-bug sweep (9 real bugs, several dormant because the components had NEVER rendered in a real Pyreon-compiled app).** `<Flow>` now injects its instance to MiniMap/Controls via a `FlowContext` (`components/flow-context.ts`) — the documented `<Flow instance={flow}><MiniMap/></Flow>` shape previously rendered NOTHING (children resolve `props.instance ?? useContext(FlowContext)`, explicit prop wins). That fix exposed two latent **compiler-template-ordering** bugs (static-element reactive refs computed via `.nextSibling` walks AFTER a sibling `_mountSlot` mutates the DOM): MiniMap threw `setAttribute is not a function` (`{nodes.map}` between two `<rect>`s → fixed with a static `<g>` wrapper) and Controls threw `replaceChild of null` (conditional buttons before the reactive zoom-% `<div>` → fixed with a `display:contents` wrapper). Also: edge markers rendered `[object Object]` in real apps (bare `{MarkerGlyph(m,pts)}` call under `<marker>` → use the element form `<MarkerGlyph/>`); NodeToolbar show-on-select was a static `return null` (now reactive `selected` accessor — accepts a value OR accessor); NodeResizer handle offsets were hardcoded `-4px` (now scale with `handleSize`); node drag swallowed clicks on in-node controls (now bails on `.pyreon-flow-node-toolbar`/`.nodrag`/`button,input,…`); drag-to-connect created no edge (pointer-capture made `pointerup`'s `e.target` the container → now `document.elementFromPoint`); `getAbsolutePosition` stack-overflowed on a cyclic `parentId` (visited-set guard). **The flow package's vitest-browser tests use a JSX transform that does NOT match the real `@pyreon/vite-plugin` compiler and MASKED the markers/MiniMap/Controls template bugs** (green-in-vitest, broken-in-real-app) — the real-compiler e2e is the only reliable gate; see `.claude/rules/anti-patterns.md` (`_mountSlot`-ordering + `{fn()}`-stringify entries). The remaining underlying compiler ordering bug (hoist all static `__eN` refs above `_mountSlot`) is documented there as a deferred deep fix. **Every flow feature is now exercised in `examples/app-showcase/src/routes/flow-features/` (the Flow Feature Matrix route)** + gated by `e2e/app-showcase-flow-features.spec.ts` (7 real-Chromium specs). drag-to-connect is unit-covered (`tests/node-components.browser.test.tsx`) — the e2e canvas can't reliably synthesize the 8px-handle hit-test.
- Custom node renderers receive `NodeComponentProps<TData>`: `{ id, data: () => TData, selected: () => boolean, dragging: () => boolean }`. **All non-id props are reactive accessors** — read inside reactive scopes (JSX expression thunks, `effect()`, `computed()`) so the node patches in place when any underlying state changes. Each node mounts EXACTLY ONCE across the lifetime of the graph regardless of how many drags, selection clicks, or `updateNode` data mutations happen. Internally `<Flow>` uses `<For>` keyed by `node.id` plus per-node/per-edge reactive accessors that read live state via `instance.nodeMap()` / `instance.edgeMap()` — `Computed<Map<id, entry>>` lookups, O(1) per access, each map rebuilt once per `nodes()` / `edges()` change. **A drag writes the whole `nodes()` array every pointermove frame, which notifies EVERY node + edge style/class/path thunk (they all subscribe to the one `nodes()` signal) — so per-frame work is O(N) total (one map rebuild + N O(1) gets), NOT the O(N²) it was before these maps existed.** Pre-`nodeMap` the accessors did an O(N) `instance.nodes().find()` (and edges 2× O(N) for source/target) inside each thunk → O(N²) + O(E×(2N+E)) per drag frame, contradicting this O(1)-per-accessor contract; the `nodeMap`/`edgeMap` computeds (added on `FlowInstance`) restore it. Bisect-locked by `packages/fundamentals/flow/src/tests/drag-frame-complexity.test.ts` (a 60-node/40-edge drag frame: 460 `Array.prototype.find` calls pre-fix → ~0 post-fix; count must not scale with graph size).
- Custom edge renderers receive `EdgeComponentProps`: `{ edge, sourceX: () => number, sourceY: () => number, targetX: () => number, targetY: () => number, selected: () => boolean }`. Same accessor-based contract — each custom edge mounts once and recomputes path coordinates reactively when source/target nodes move. The default edge renderer (when no custom edge type is registered) uses inline reactive thunks for `d`, `stroke`, `stroke-width`, and `class`.
- `flow.toJSON()` / `flow.fromJSON()` for serialization round-trips, `flow.layout('layered', { direction, nodeSpacing, layerSpacing })` for elkjs auto-layout
- **`LayoutOptions` algorithm applicability** (verified empirically by running each algorithm twice with different option values): `direction` applies to `layered` and `tree`; `layerSpacing` and `edgeRouting` apply to `layered` only; `nodeSpacing` is the only field respected by every algorithm. The other algorithms (`force`, `stress`, `radial`, `box`, `rectpacking`) accept the option in `LayoutOptions` (so it typechecks) but silently ignore it at layout time. Use `layered` or `tree` if you need a directional layout.
- **Dev-mode safety net**: `flow.layout()` (and the underlying `computeLayout`) emits a `console.warn` in dev mode when an option is set on an algorithm that ignores it (e.g. `direction: 'RIGHT'` on a force layout). The dev gate uses bare `process.env.NODE_ENV !== 'production'` — the bundler-agnostic library convention used by React, Vue, Preact, Solid. Every modern bundler (Vite, Webpack/Next.js, Rolldown, esbuild, Rollup, Parcel, Bun) auto-replaces `process.env.NODE_ENV` at consumer build time and tree-shakes the dev warning to zero bytes in production. **Do NOT use `typeof process !== 'undefined' && process.env.NODE_ENV !== 'production'`** (the typeof guard is dead in Vite browser builds) **and do NOT use `import.meta.env.DEV`** (Vite/Rolldown-only — dead in Webpack/Next.js, esbuild, Rollup, Parcel, Bun). `flow/src/layout.ts:warnIgnoredOptions` is the reference implementation. The full monorepo migration (~30 files across `reactivity`, `core`, `runtime-dom`, `router`, `ui-system/*`, `fundamentals/{store,storage}`, `tools/{react-compat,solid-compat,svelte-compat}`) is complete. The lint rule `pyreon/no-process-dev-gate` enforces this codebase-wide; any new code introducing `import.meta.env.DEV` or the typeof-process compound fails CI.

  **Pattern × bundler outcome.** The bundler-agnostic standard is the only pattern that fires correctly across every bundler Pyreon ships to:

  | Source pattern                                                            | Vite prod   | Webpack/Next.js | esbuild     | Rollup      | Parcel      | Bun         | Verdict               |
  | ------------------------------------------------------------------------- | ----------- | --------------- | ----------- | ----------- | ----------- | ----------- | --------------------- |
  | `process.env.NODE_ENV !== 'production'` (bare)                            | tree-shaken | tree-shaken     | tree-shaken | tree-shaken | tree-shaken | tree-shaken | ✅ universal          |
  | `typeof process !== 'undefined' && process.env.NODE_ENV !== 'production'` | dead code   | dead code       | dead code   | dead code   | dead code   | dead code   | ❌ broken everywhere  |
  | `import.meta.env.DEV` (and `(import.meta as ViteMeta).env?.DEV === true`) | tree-shaken | undefined       | undefined   | undefined   | undefined   | undefined   | ❌ Vite/Rolldown only |

  Bundle-level regression test: `flow/src/tests/integration.test.ts` feeds `layout.ts` to esbuild with the defines every modern bundler uses (`'process.env.NODE_ENV': '"production"'` for prod) and asserts dev-warning strings are absent. This proves the bundler-agnostic claim empirically per migration.

  **Enforced by `pyreon/no-process-dev-gate`** (auto-fixable). The companion rule `pyreon/dev-guard-warnings` recognises `if (process.env.NODE_ENV === 'production') return` as a valid early-return guard so `console.warn` calls in the function body don't fire spuriously. Server-only packages (`zero`, `runtime-server`, `server`, `vite-plugin`) are exempt — they always run in Node where `process` is real.

- No D3 — pan/zoom via pointer events + CSS transforms
- **Peer dep**: `@pyreon/runtime-dom` is required because the JSX templates emit `_tpl()` calls — declare it in consumer apps' deps
- **MCP api-reference is manifest-driven (T2.5.1)**: the flow region in `packages/tools/mcp/src/api-reference.ts` regenerates from the same manifest's `api[]`. Eight entries today (`createFlow`, `useFlow`, `Flow`, `Background`, `Controls`, `MiniMap`, `Handle`, `Panel`) — a strict superset of the previous hand-written surface, covering all child components. First real consumer of the marker-based `<gen-docs:api-reference:start/end @pyreon/<name>>` region protocol — `query` / `form` / `hooks` flip to the same protocol in follow-up PRs as their manifests are enriched to MCP density.

### @pyreon/code

- `createEditor({ value, language, theme, minimap, lineNumbers, foldGutter, onChange, ... })` — reactive editor instance
- `editor.value` is a writable `Signal<string>` — `editor.value()` reads, `editor.value.set(next)` writes back into CodeMirror
- `editor.cursor` / `editor.selection` / `editor.lineCount` are computed signals
- `<CodeEditor instance={editor} />`, `<DiffEditor>`, `<TabbedEditor>` — mount components, auto-cleanup on unmount
- `minimapExtension()` — canvas-based code overview
- `loadLanguage(lang)` — lazy-load 19 language grammars (`json`, `typescript`, `python`, etc.)
- **Two-way binding via `bindEditorToSignal({ editor, signal, serialize, parse, onParseError? })`** — replaces the recurring loop-prevention flag-pair boilerplate from PRs #191 + #192. Accepts a `Signal<T>` or any `SignalLike<T>` and round-trips through user-supplied `serialize`/`parse` functions. Internal flags break the format-on-input race; parse failures call `onParseError` and leave the external state at its last valid value. Returns `{ dispose }` for cleanup. Both directions are loop-safe and the editor itself also has internal CM↔signal loop guards.
- Built on CodeMirror 6 (~250KB vs Monaco's ~2.5MB)
- **Peer dep**: `@pyreon/runtime-dom` is required because `<CodeEditor>` JSX emits `_tpl()` calls

### @pyreon/document

- `render(node, format, options?)` — render document node tree to any format
- `createDocument(props?)` — builder: `.heading()`, `.text()`, `.table()`, `.toPdf()`, `.toEmail()`, etc.
- JSX primitives: `Document`, `Page`, `Heading`, `Text`, `Table`, `Image`, `List`, `Code`, `Divider`, etc.
- 20 output formats: HTML, PDF, DOCX, XLSX, PPTX, email, Markdown, text, CSV, SVG, Slack, Teams, Discord, Telegram, Notion, Confluence, WhatsApp, Google Chat, JSON, JSONL
- Heavy renderers lazy-loaded — chunks only load when `render(doc, '<format>')` is invoked. Approximate chunk sizes (vendored): PDF ~3MB (pdfmake + fonts), DOCX ~700KB, XLSX ~1.1MB, PPTX ~400KB. The 14MB published `lib/` is the trade-off for a single-install batteries-included experience; apps tree-shake at the consumer level so unused renderers don't ship to end users.

### @pyreon/document-primitives

- 18 rocketstyle components — `DocDocument`, `DocPage`, `DocSection`, `DocRow`, `DocColumn`, `DocHeading`, `DocText`, `DocLink`, `DocImage`, `DocTable`, `DocList`, `DocListItem`, `DocCode`, `DocDivider`, `DocSpacer`, `DocButton`, `DocQuote`, `DocPageBreak`
- **Same component tree renders in browser AND exports to 20 formats** — primitives carry `_documentType` static markers; `extractDocumentTree(vnode)` from `@pyreon/connector-document` walks the tree and produces a `DocNode` for `@pyreon/document`'s `render()` to consume
- **`extractDocNode(templateFn)`** — one-step alias that replaces `createDocumentExport(templateFn).getDocNode()`. Most consumers should use this. The two-step form is still exported for callers that need to pass the helper object around
- **Reactive metadata via accessor props**: `DocDocument` accepts `title?: string | (() => string)`, `author?: string | (() => string)`, `subject?: string | (() => string)`. Function values are stored in `_documentProps` and resolved by `extractDocumentTree` at extraction time, so each export click reads the LIVE value from any underlying signal — no `const initial = get()` workaround needed
- **Framework fix in `extractDocumentTree`**: real rocketstyle primitives' metadata is invisible on the JSX vnode because `_documentProps` only appears AFTER the rocketstyle attrs HOC runs — the JSX vnode's props only contain the user-supplied props (`{ title, author }`). PR #197 fixed this by invoking the full component to read post-attrs `_documentProps` (a workaround that depended on attrs purity). PR #321 (T3.1) replaced that workaround with a hoisted-attrs fast path: `@pyreon/rocketstyle` now exposes the accumulated `.attrs()` callback chain on the component function as `__rs_attrs`, and `extractDocumentTree` runs the chain directly via `attrsResult = chain.reduce(Object.assign, {})` — no styled wrapper invocation, no JSX tree creation, no dimension resolution. The non-rocketstyle Path B (full component invocation) stays as a fallback for hand-rolled test fixtures. The latent bug class is the same as before — `_documentProps` lives behind the attrs HOC — but the fast path now sidesteps the per-export cost
- `createDocumentExport(templateFn)` — kept for backward compat; internally delegates to `extractDocNode`
- Layout props live in `.attrs()` (not `.theme()`): `direction`, `gap`, `alignX`, `alignY`. Element accepts `direction: 'inline' | 'rows' | 'reverseInline' | 'reverseRows'` — `'row'` is invalid
- For fine-grained reactivity in templates that drive a live preview, pass a signal accessor (not its resolved value) and read it inside the template body via per-text-node thunks: `<DocText>{() => store.field()}</DocText>` — components run once, so reading the signal at the top of the template captures only the initial value
- Rocketstyle `.attrs<P>()` generic is the **public** prop type — runtime-filled fields like `tag` and `_documentProps` belong in the callback body, never in the generic, or they leak as required JSX props
- **Watch for prop names that collide with read-only HTML element properties.** `HTMLTableElement.rows` and `.columns` are read-only `HTMLCollection` getters — if a prop with that name reaches the DOM forwarding step, the runtime crashes with `Cannot set property rows of [object Object] which has only a getter`. Use rocketstyle's `.attrs(callback, { filter: ['rows', 'columns', ...] })` second argument to strip export-only props before they reach the DOM. `DocTable` uses this pattern. Other risky names: `style` (writable but reserved), `cells`, `tHead`, `tBodies` on table elements; `form` on form-associated inputs.

### @pyreon/storybook

- `renderToCanvas(context, canvasElement)` — core renderer for Storybook
- `Meta<TComponent>` / `StoryObj<TMeta>` — typed story definitions
- Preset: `framework: "@pyreon/storybook"` in `.storybook/main.ts`

### @pyreon/rx

- Signal-aware reactive transforms — every function overloaded: `Signal<T[]> → Computed`, `T[] → plain`
- 37 functions across 6 categories:
  - **Collections (21):** `filter`, `map`, `sortBy`, `groupBy`, `keyBy`, `uniqBy`, `take`, `skip`, `last`, `chunk`, `flatten`, `find`, `mapValues`, `first`, `compact`, `reverse`, `partition`, `takeWhile`, `dropWhile`, `unique`, `sample`
  - **Aggregation (8):** `count`, `sum`, `min`, `max`, `average`, `reduce`, `every`, `some`
  - **Operators (5):** `distinct`, `scan`, `combine`, `zip`, `merge`
  - **Timing (2):** `debounce`, `throttle`
  - **Search (1):** `search`
- `pipe(source, op1, op2, ...)` — compose transforms left-to-right
- Signal inputs produce `Computed` outputs that auto-track and re-derive when the source signal changes

### @pyreon/toast

- `toast(message)` — imperative toast creation, returns toast ID
- `toast.success(msg)`, `toast.error(msg)`, `toast.warning(msg)`, `toast.info(msg)`, `toast.loading(msg)` — preset variants
- `toast.update(id, options)` — update an existing toast (e.g., loading → success)
- `toast.dismiss(id?)` — dismiss one or all toasts
- `toast.promise(promise, { loading, success, error })` — auto-transitions through states
- `<Toaster />` — render component with Portal, CSS transitions, auto-dismiss, pause on hover
- Accessibility: `role="alert"`, `aria-live="polite"` on toast elements
- **Render layer was structurally broken until the reactive-rows + portal-delegation fix (the package's first browser test caught it — `toaster.tsx` had been coverage-excluded with zero DOM coverage; the node store suite passes because it reads the signal directly, never the rendered DOM).** Two independent bugs, both invisible to mount-only checks:
  1. **Stale rows (the `<For>` W22 contract).** `ToastItem` read `message`/`type`/`state` STATICALLY off the snapshot the `<For by={id}>` callback receives. Same-key item-object replacement does NOT re-render a keyed row, so `toast.update`, `toast.promise` transitions, AND the `entering→visible` promotion never reflected — toasts rendered stuck `--entering` (opacity:0 = invisible) and `update`/`promise` text never changed. **Fix:** `<For>` keys on the toast IDs (structure only); each row reads its live fields via a `_toastMap` computed (`id→Toast`, the `@pyreon/flow` `nodeMap` pattern) inside reactive thunks — so one update patches only that row's text node / className, 0 component re-renders. `action`/`dismissible` (immutable per toast) are read once at mount; `message`/`type`/`state` reactively.
  2. **Portal'd buttons dead.** `click` (and `submit`/`focusin`/`focusout`/…) are DELEGATED events — Pyreon installs ONE delegation listener on the mount container and stores per-element handlers as expandos. The Toaster portals into the body region, OUTSIDE the mount container, so a click on the dismiss `×` / action button bubbled `button→body` and never through the mount root → the handler never fired (dismiss, action, AND pause-on-FOCUS were all silently dead). **Fix:** the Toaster renders into a per-instance host element appended to `document.body` and calls `setupDelegation(host)` (exported from `@pyreon/runtime-dom`) — scoping delegation to the toast subtree so the buttons fire WITHOUT double-firing the app's own delegated handlers (a listener on `document.body`, an ancestor of the mount root, would catch app clicks too). Mirrors `@pyreon/elements`' per-instance Portal wrapper. **The same class affects any `@pyreon/core` `<Portal>` content with delegated handlers** (elements' overlays, dnd) — see the anti-patterns "Portal'd delegated-event handlers" entry; the broader core/elements fix is tracked separately.
  - Reference: `packages/fundamentals/toast/src/{toast.ts:_toastMap, toaster.tsx}`; regression locks: real-Chromium `src/tests/toaster.browser.test.tsx` (8 specs, bisect-verified 6-fail-on-revert) + node `src/tests/toast-map.test.ts`.
- **Benchmark (`bench/toast-bench.ts`, `bun run --filter=@pyreon/toast bench`) — fastest store dispatch vs react-hot-toast.** Headless store-op throughput (NODE_ENV=production, happy-dom for the React lib's import, median ns/op over 9 runs): **Pyreon is ~3.0–4.5× faster than react-hot-toast** — create+dismiss ~117ns vs ~357ns (3.0×), update-by-id ~79ns vs ~340ns (4.3×), create-10+clear ~0.93µs vs ~3.6µs (3.8×) (Apple M3, Node 24; stable across runs — the ratios are the portable signal). **Honest scope (deliberately NOT overclaimed):** (1) **react-hot-toast** is the fair headless peer — both have a hard, headless removal path so the store stays bounded across the timed loop. (2) **sonner is EXCLUDED from the headless bench** — its `dismiss` is animation-coupled (a dismissed toast is only removed once a MOUNTED `<Toaster>` finishes the exit animation), so headless the array grows unboundedly → O(N²); reporting that would be a benchmark artifact, not sonner's real per-op cost. (3) **No cross-lib COMMIT (wall-clock-to-render-an-update) number is published** — a happy-dom wall-clock harness measured react-hot-toast at a non-credible ~285× (its per-toast `getBoundingClientRect` height measurement is pathologically slow under happy-dom, NOT a real-browser cost) and sonner ~tied with Pyreon, so that bench was dropped rather than ship a misleading magnitude; a fair cross-lib commit comparison needs a real-browser (Playwright) harness — deferred. The **architectural** update advantage (Pyreon patches only the changed toast's text node — 0 component re-renders, independent of stack size — vs the React libs re-rendering the `<Toaster>` + reconciling every mounted toast item per update) is real-Chromium-proven by `toaster.browser.test.tsx` (update reflects via in-place patch, row mounts once), not by a contaminated wall-clock.

### @pyreon/url-state

- `useUrlState(key, defaultValue)` — returns `UrlStateSignal` synced to URL search params
- Schema mode: `useUrlState({ page: 1, sort: "name" })` — multiple params from a single call
- Auto type coercion (numbers, booleans, arrays), uses `replaceState` (no history spam)
- Configurable debounce for high-frequency updates, SSR-safe (reads from request URL on server)

### @pyreon/sync

Local-first / collaborative sync built on reactivity — a synced value IS a `Signal` (via `wrapSignal`), so a remote op becomes one `signal.set` → one fine-grained DOM update, never a re-render. Three entry points: `@pyreon/sync` (core bridge, universal, depends only on `@pyreon/reactivity`), `@pyreon/sync/yjs` (real Yjs engine — keeps `yjs` out of the core entry), `@pyreon/sync/server` (Node/Bun relay — imports `ws` + `node:http`, never enters a client bundle).

- `syncedSignal({ doc, key, initial })` — bind a `Signal<T>` to a scalar CRDT map entry. **Create-if-missing**: `initial` is written only if the key is absent (a persisted/peer value is authoritative). v1 scope is scalars (string/number/boolean).
- `syncedStore(initial, { doc })` — a flat store of synced fields over one map; `store.field()` reads, `store.field.set(v)` writes. A single-key change writes exactly one base signal (others early-return on `Set.has`).
- **The update loop (single source of truth, load-bearing):** `.set(v)` writes ONLY the CRDT; the map `observe` is the ONE writer of the base signal and fires for BOTH local and remote commits; the local echo is an `Object.is` no-op. **The network loop is prevented in the TRANSPORT** (it re-broadcasts `LOCAL_ORIGIN` updates and applies inbound ones as `REMOTE_ORIGIN`, never re-broadcasting a remote update) — NEVER by gating the observer on origin (that would drop the local UI update). `LOCAL_ORIGIN` / `REMOTE_ORIGIN` are `unique symbol` exports.
- **Engine seam:** `CrdtAdapter` / `CrdtDoc` / `CrdtMap` (engine-neutral) keep `syncedSignal`/`syncedStore` engine-free; `FakeCrdtAdapter` + `connectFakeDocs(a, b)` are the in-memory test double (no state-vector merge — can't model offline-reconnect; use the Yjs adapter for that). The seam ports the client bridge, NOT the wire format — persistence/transport/relay are Yjs-coupled.
- **`@pyreon/sync/yjs`:** `createYjsDoc(yDoc?)` (`.yDoc` exposes the Y.Doc); `syncedText(doc, key)` (`Y.Text` character-merge — `.insert`/`.delete` positional, `.set` is a prefix/suffix diff; concurrent edits keep BOTH, unlike scalar LWW); `syncedList(doc, key)` (`Y.Array` positional merge — render with keyed `<For>`); `syncedAwareness(doc, initial?)` (EPHEMERAL presence + live cursors over the Yjs awareness protocol — a SEPARATE channel from doc updates, NEVER persisted; returns reactive `local`/`others`/`states` signals + `setLocal`/`setLocalField`; `PeerState<T> = { clientId, state, isLocal }`; **create it BEFORE connecting** a transport — the transport peeks for the doc's awareness at connect via `peekDocAwareness`, so apps that never use presence pay zero awareness overhead; uses `y-protocols`); `persistViaIndexedDB(doc, dbName)` (browser-only, `await .whenSynced` BEFORE seeding); `connectViaBroadcastChannel(doc, name)` (same-origin cross-tab); `connectViaWebSocket(doc, url, opts?)` (cross-device, auto-reconnect, uses global `WebSocket` / `WebSocketImpl`, token in URL query — browser WS can't set headers, 4401 close = authz reject, terminal). **Awareness rides BOTH transports on a `MSG_AWARENESS` (=2) frame and is applied under the SHARED `REMOTE_ORIGIN`** so a received presence is never re-sent by the sibling transport (the cross-transport WS↔BroadcastChannel loop guard — same tag the doc-update echo guard uses). BroadcastChannel replays local presence on an inbound `sv` (the cross-tab analog of the relay's join-state, since BC delivers only to current subscribers). **Awareness lifecycle is DOC-owned, not view-owned**: the `Awareness` is one-per-`Y.Doc` (`WeakMap`, shared by every transport + every `syncedAwareness` view). A view's `dispose()` detaches ONLY that view's `'change'` observer — it must NOT `aw.destroy()` the shared instance (doing so strands the transports + any sibling view, and via `onCleanup` a single component unmount would silently kill the whole doc's presence). The DOC owns teardown: `YjsCrdtDoc.destroy()` → `destroyDocAwareness(doc)` (announce departure + `aw.destroy()` + WeakMap-delete); departure on disconnect is the transport's job (`removeAwarenessStates(aw, [aw.clientID], 'local')`) and the relay's socket-close purge is the real ghost-cursor guarantee. (Bug fixed 2026-06: `syncedAwareness().dispose()` originally did the full destructive teardown — listener-only now; bisect-locked by the multi-view + `doc.destroy()` specs in `awareness.test.ts`.) Presence recompute is O(N) in peer count (full snapshot rebuild + every `others()` consumer re-runs per change) — throttle cursor publishes; fine for dozens of collaborators, a v1 limit for large swarms.
- **`@pyreon/sync/server`:** `createSyncServer({ port?, server?, authorize? })` — one authoritative `Y.Doc` per room, broadcasts to other clients, GC's empty rooms (ephemeral — no persistence). The `authorize({ room, token, req })` hook is the per-room/per-doc gate (return false/throw → close 4401 before any data). **Default allows everything (dev only) — production MUST supply it.** Pass `server` to attach to an existing `http.Server`. **Awareness-stateful** (a deliberate departure from the otherwise-stateless doc relay): each room holds a `y-protocols` `Awareness` + a `socketClients: Map<WsSocket, Set<clientId>>`; a single `awareness.on('update')` handler is the ONE broadcast path (covers relayed client updates AND `removeAwarenessStates` removals — fans to all-but-origin-socket, or to ALL when origin is the `'disconnect'` string); a NEW client is sent the room's current presence on connect (instant join-presence — the relay filters its OWN clientId so no phantom relay-self leaks); a socket close PURGES that socket's clientIds + broadcasts the removal (the GUARANTEE a crashed client's cursor disappears even with no clean departure announce). `MSG_AWARENESS` frames are NEVER applied to the room doc.
- **Honest limits:** CRDTs prevent lost UPDATES, not semantic conflicts (scalar LWW drops the loser; text/list keep both ops but may interleave nonsensically — never market "never lose data"); a synced app ships ~60KB+ gz (`yjs`+`y-indexeddb`+WS client); authz is table-stakes; native/PMTC sync out of near-term scope (seam keeps the Loro-via-FFI door open).
- Manifest-driven docs: `packages/fundamentals/sync/src/manifest.ts` feeds `llms.txt` / `llms-full.txt` / MCP `api-reference`. Full reference: `docs/src/content/docs/sync.md`.

### @pyreon/lint

- `lint(options?)` — programmatic API: lint files, returns `LintResult` with counts
- `lintFile(filePath, sourceText, rules, config)` — lint a single file
- `listRules()` — returns metadata for all 91 rules
- `applyFixes(sourceText, diagnostics)` — apply auto-fixes
- `loadConfig(cwd)` — load `.pyreonlintrc.json` / `package.json` `"pyreonlint"` field
- `createIgnoreFilter(cwd)` — load `.pyreonlintignore` + `.gitignore` patterns
- `AstCache` — FNV-1a hash-keyed AST cache for repeat runs
- `watchAndLint(options)` — file watcher with 100ms debounce, re-lints changed files
- CLI: `pyreon-lint [--preset recommended|strict|app|lib|best-practices] [--fix] [--format text|json|compact] [--quiet] [--list] [--watch] [--config path] [--ignore path] [--rule id=severity] [path...]`
- **CLI colors follow the Pyreon brand handoff (#651), CLI spec §6.5** — `reporter.ts` uses the self-contained `lint/src/ansi.ts` (mirrors `@pyreon/cli`'s `doctor/render/ansi.ts`; no shared dep — both packages deliberately avoid a runtime ANSI lib). Brand tokens → nearest **xterm-256** 8-bit SGR (`38;5;N`); the handoff mandates "256-color must survive (no truecolor-only)", so no `38;2;r;g;b`. Mapping: error→ember-core (202), warning→ember-warm (220), info→cyan (45); glyphs `✗`/`!`/`ℹ`; file path `bold`, loc/ruleId `dim`. Ember stays scarce (only error/warning severities). Also closed a pre-existing gap: the reporter previously emitted raw ANSI unconditionally — `ansi.ts` adds the standard `NO_COLOR` / `FORCE_COLOR` / `isTTY` gate (parity with the doctor renderer). `--format json|compact` unchanged (machine formats, no color).
- 91 rules across 18 categories: reactivity (14), jsx (11), lifecycle (6), performance (6), ssr (4), architecture (10), store (3), form (4), styling (4), hooks (3), accessibility (3), router (5), ssg (3), frontend (11), query (1), rx (1), i18n (1), storage (1)
- **Opt-in best-practice rules** (`meta.optIn`): the `frontend` (a11y/CLS — `require-img-alt`, `img-requires-dimensions`, `no-positive-tabindex`, `prefer-zero-image`, `no-discarded-optimize-fields`, `no-autofocus`, `no-redundant-role`, `anchor-is-valid`, `heading-order`, `color-contrast`), `query` (`query-options-as-function`), `rx` (`rx-prefer-pipe`), `i18n` (`i18n-prefer-trans-for-rich-jsx`), `storage` (`no-storage-write-as-call`) categories + form's `no-signal-in-form-initial-values` + router's `prefer-typed-search-params` default **off** in `recommended`/`strict`/`app`/`lib`; the `best-practices` preset enables them wholesale, or enable per-rule in `.pyreonlintrc.json`. Library-scoped opt-in rules **auto-gate on `package.json` deps** via `utils/project-deps:isProjectDependency` (a project without `@pyreon/query` / `@pyreon/storage` never sees those rules — zero config, zero noise). Each carries a prescriptive AI-actionable fix message; `no-positive-tabindex`, `no-autofocus`, `no-redundant-role`, `no-storage-write-as-call` AND `query-options-as-function` are **auto-fixable** (`--fix`). `query-options-as-function` is ALSO a proactive MCP `validate` detector (`@pyreon/compiler` `detectPyreonPatterns`, PR #633) — an AI agent sees the fix BEFORE commit, not just after running lint.
  - **2026-Q2 frontend a11y additions**: `pyreon/no-autofocus` (autoFocus/autofocus attr disorients SR/keyboard users — fixable, removes the attr; skips `autoFocus={false}`), `pyreon/no-redundant-role` (a `role` that duplicates the element's implicit ARIA role — conservative tag→role map, `a`→`link` only with static `href`, dynamic/component skipped — fixable, removes the attr), `pyreon/anchor-is-valid` (`<a>` with no `href` or `href` of `""`/`#`/`javascript:` — not fixable, intent is ambiguous; `href={dynamic}` skipped). `control-needs-label` deliberately deferred: label/aria-label association needs cross-element id/scope resolution an AST walker can't do without false positives — the explicit "high-risk cliff" the codebase avoids.
  - **2026-Q2 `storage` category**: `pyreon/no-storage-write-as-call` (dep-gated `@pyreon/storage`) — `useStorage`/`useSessionStorage`/`useCookie`/`useIndexedDB`/`useMemoryStorage` return a `StorageSignal`; `s(next)` reads-and-discards like any signal call. Same proven conservative shape as the `signal-write-as-call` detector (tracks the `const s = useStorage(...)` binding, fires only on a bare-identifier call with ≥1 arg, skips `.set`/`.update`/`.remove` and zero-arg reads). Fixable: `s(x)` → `s.set(x)`.
- New in 2026-05: `pyreon/no-heavy-import-only-in-handler` (performance, warn) — flags a statically-imported heavy module (`@pyreon/charts|code|flow|document`, + configurable `heavyModules`) referenced ONLY inside JSX `on*` event handlers or post-setup/teardown lifecycle callbacks (`onMount`/`onUnmount`/`onCleanup`). The static import forces the heavy chunk into the initial bundle even though nothing touches it until the user interacts; the fix is a dynamic `await import()` inside the handler. The PRECISE counterpart to the blunt info-level `pyreon/no-eager-import` (which fires on EVERY heavy static import incl. render-time ones) — this one fires only when EVERY reference is provably deferred, so the fix is unambiguous and there is no "but I need it at render" false positive. Conservative by construction (any eager reference suppresses the report). **`effect` / `renderEffect` are deliberately NOT treated as deferred** — their callbacks run synchronously during component setup (the `no-imperative-effect-on-create` shape), so a heavy module used in an effect body is a render-time dependency, not a deferrable one. That exclusion was surfaced by the real-corpus e2e (`examples/app-showcase/.../LivePreview.tsx` calls `@pyreon/document`'s `render` inside an `effect` — a legit reactive render, not a defer opportunity; treating `effect` as deferred made the rule false-positive there). Distilled from the Tier-2 resumability spike's L3 heavy-import detection — the one part of that analysis with zero false-positive risk; the runtime thesis didn't survive measurement, this defensive lint use of the same detection did. Own-recursion `Program` walk (oxc passes no parent/exit), `JSXIdentifier` element-name handling so `<Chart/>` counts as a render-time use. 14 specs, bisect-verified (disabling the eager-guard collapses it to `no-eager-import` — 4 conservative DOES-NOT-FIRE specs fail); validated against all 577 example `.ts(x)` files → zero false positives.
- New in 2026-Q2: `pyreon/no-process-dev-gate` (architecture, error, auto-fixable) — flags bundler-coupled dev gates: `typeof process !== 'undefined' && process.env.NODE_ENV !== 'production'` (dead in Vite browser bundles) AND `import.meta.env.DEV` / `(import.meta as ViteMeta).env?.DEV === true` (Vite/Rolldown-only — undefined and silent in Webpack/Next.js, esbuild, Rollup, Parcel, Bun). Auto-fixes both to bundler-agnostic `process.env.NODE_ENV !== 'production'`, the universal library convention used by React, Vue, Preact, Solid. The companion `pyreon/dev-guard-warnings` recognises `if (process.env.NODE_ENV === 'production') return` as a valid early-return guard.
- New in T1.1 Phase 4: `pyreon/require-browser-smoke-test` (architecture, error, in `recommended`/`strict`/`lib`, off in `app`) — every browser-categorized package must ship at least one `*.browser.test.{ts,tsx}` file under `src/`. Locks in the T1.1 smoke harness so new browser packages can't quietly ship without coverage. `additionalPackages` option opts new packages in, `exemptPaths` opts out.
- New in 2026-05: `pyreon/storage-signal-v-forwarding` (reactivity, error) — flags a wrapper callable that delegates `.direct` to a base signal without forwarding the internal `_v` field via getter. The compiler-emitted `_bindText` fast path reads `source._v` directly (skipping the function call) for the JSX shape `{() => signal()}`, so a callable that has `.direct` but not `._v` silently binds to `undefined` — initial render and every subsequent subscriber notification write `''` to the textNode. Catches the bug class fixed in PR #546 (where `@pyreon/storage`'s 5 reactive primitives shipped this shape from inception for ~9 months). Per-function scope, no false positives on plain `signal()` implementations (their `.direct = _direct` RHS is a bare Identifier, not a delegate). The rule's tests serve as both contract documentation and bisect-verify canary against the canonical `createStorageSignal` shape. Locked into `recommended` / `strict` / `app` / `lib` automatically via `buildRecommended` reading `allRules` severities — no per-preset wiring needed.
- 4 presets: `recommended`, `strict` (warns→errors), `app` (lib rules off), `lib` (strict + architecture)
- Powered by `oxc-parser` — ESTree/TS-ESTree AST with Visitor
- **Per-rule options** via ESLint-style tuple form in config — `"pyreon/no-window-in-ssr": ["error", { "exemptPaths": ["packages/core/runtime-dom/"] }]`. Rules that support path-based exemption read `options.exemptPaths: string[]`. Each rule declares its option shape in `meta.schema` (`Record<string, 'string' | 'string[]' | 'number' | 'boolean'>`); wrong-typed values disable the rule and surface an error on `LintResult.configDiagnostics`, unknown keys emit a warning. The Pyreon monorepo's `.pyreonlintrc.json` at repo root configures server-only / DOM-runtime / hook-foundation exemptions that were previously hardcoded in rule source. JSON Schema for the config file ships at `@pyreon/lint/schema/pyreonlintrc.schema.json` (reference via `"$schema"` for IDE autocomplete). CLI flag `--rule-options id='{json}'` passes options for a single run.
- **`no-window-in-ssr` precision.** Recognised safe contexts: `onMount`, `onUnmount`, `onCleanup`, `effect`, `renderEffect`, `requestAnimationFrame` (entire call arguments are safe). `watch(source, cb)` is handled precisely: only the 2nd arg (callback) is safe — the 1st (source) is evaluated at setup to track signals, so browser globals there still fire. Recognised typeof guards: `if (typeof X !== 'undefined')` body, ternary consequent, const-captured idiom (`const isBrowser = typeof window !== 'undefined'; if (isBrowser) { … }`), negated-form early returns (`if (typeof X === 'undefined') return` + OR-chained form), logical-and guards where either side is a typeof-derived const (`IS_BROWSER && active() ? <Portal target={document.body} /> : null`), and const bindings from AND-chains / ternaries where any term is a typeof check (`const useVT = _isBrowser && meta && typeof document.startViewTransition === 'function'`, `const handler = _isBrowser ? fn : null`). Identifiers in type position (`let x: Window`, `type T = Document`, etc.), member-expression property names (`x.addEventListener`), object-property keys, import-specifier names, **function-parameter names that happen to match a browser global** (`function push(location) { location.X }` — the parameter shadows `window.location`), and **module-level imports that shadow a browser global** (`import { history } from '@codemirror/commands'` — every later `history` references the import, not `window.history`; same for default and namespace imports) are all skipped. Test files (`__tests__/`, `*.test.*`) and `no-unbatched-updates` are both test-exemptable via `exemptPaths`. **oxc visitor gotcha**: oxc's walker does NOT pass `parent` — `VisitorCallback = (node: any) => void`. Rules needing parent context must use enter/exit depth counters or pre-mark child nodes via WeakSet on the way in. Inert `parent?.type === '…'` checks were the root cause of several silent false-positive clusters.
- **`no-imperative-navigate-in-render` precision.** Fires ONLY when `navigate()` / `router.push()` runs synchronously in the component function body (the actual infinite-render-loop case). Any nested function inside the component body — event handler (`const handleClick = (e) => router.push(…)`), `setTimeout`/`setInterval` callback, `requestAnimationFrame` callback, lifecycle hook, `.then()` callback — is deferred execution and doesn't fire. Previously only recognised `onMount`/`effect`/`onUnmount` by name; now tracks a nested-function depth counter that covers every deferred-execution shape.
- **`no-dom-in-setup` safe contexts.** `document.querySelector`/`getElementById`/etc. are silent inside `onMount`, `onUnmount`, `onCleanup`, `effect`, `renderEffect`, AND `requestAnimationFrame` (the callback always runs inside a browser frame post-setup). Also recognises early-return-on-typeof guards at function head (`if (typeof document === 'undefined') return|throw …`) — same heuristic as `no-window-in-ssr` — **AND the canonical `@pyreon/reactivity` SSR primitive: `if (isServer) return|throw` / `if (!isClient) return|throw`** (recognised by name, like `dev-guard-warnings`'s `__DEV__`). This last form is load-bearing for consistency with `pyreon/prefer-isserver`: that rule pushes the `typeof document` guards TO `isServer`, so `no-dom-in-setup` must accept `isServer` as the same guard or the two rules contradict (prefer-isserver says "use isServer", then no-dom-in-setup flags the now-"unguarded" DOM access). Surfaced during the prefer-isserver framework burn-down — migrating a guard to `isServer` introduced 5 false-positive `no-dom-in-setup` findings until the rule learned the primitive.
- **Universal `fetch`.** Removed from the `no-window-in-ssr` browser-globals set — `fetch` is universal in Node 18+, Bun, Deno, browsers, and edge runtimes. Code using it isn't browser-specific. (`XMLHttpRequest` and `WebSocket` remain DOM-only.)
- **`no-window-in-ssr` typeof-guard functions.** A function whose body is `return <typeof check>` (or AND-chain of typeof checks) counts as a typeof guard at its call sites — e.g. `function isBrowser() { return typeof window !== 'undefined' }` makes `if (!isBrowser()) return` an early-return guard for the rule. Both function declarations and `const fn = () => …` form are recognised. Conventional names `isBrowser` / `isClient` / `isServer` / `isSSR` are pre-seeded so cross-module imports work without follow-the-import analysis (same name-convention basis as `dev-guard-warnings` recognising `__DEV__`).
- **Early-return guard `throw` form.** Both `no-window-in-ssr` and `no-dom-in-setup` recognise `if (typeof X === 'undefined') throw …` as an early-return guard (in addition to `return`). Common in entry-point functions like `startClient` that hard-fail in SSR rather than silently no-op.
- **`no-bare-signal-in-jsx` skip allowlist.** Skips call-expressions in JSX text whose callee name is one of `use…`/`get…`/`is…`/`has…`/`[A-Z]…` (hook / component conventions) OR a framework VNode-producing helper: `render` (`@pyreon/ui-core`), `h` (`@pyreon/core` hyperscript), `cloneVNode` (`@pyreon/core`). Their JSX call sites always produce a VNode, not a signal value. User-defined signals named identically would slip through — rename to `rendered`/`hyperscript`/etc. if you hit that.
- **`dev-guard-warnings` dev-flag recognition.** Accepts `__DEV__`, `import.meta.env.DEV` / `import.meta.env?.DEV`, `&&`/ternary test expressions, early-return guards (`if (!__DEV__) return` at function head), and locally-bound consts (`const flag = import.meta.env.DEV === true`). Recognised identifier names: `__DEV__`, `IS_DEV`, `IS_DEVELOPMENT`, `isDev` — extensible per project via the `"devFlagNames": ["__DEBUG__"]` rule option (merged with built-ins). The rule can't follow cross-module imports, so the name is the contract — pick one of the four defaults (or the user-configured list) for imported flags.
- **Inline suppression comments.** Two equivalent syntaxes: `// pyreon-lint-ignore <rule-id>` (legacy / short) and `// pyreon-lint-disable-next-line <rule-id>` (long-form, matches several rules' docstrings). Both suppress diagnostics on the IMMEDIATELY following line. Omitting the rule ID suppresses all rules on the next line. The runner accepts both prefixes — earlier docs documented `disable-next-line` while only `ignore` was implemented; that gap is closed.

### @pyreon/router — New Features

- **`resolveRoute` hot path (2026-06 overhaul) — fastest router at realistic table sizes on `bench:router`** (vs find-my-way / Hono / radix3 / react-router / TanStack / vue-router / path-to-regexp): 1.00× leader at 50 + 200 routes, ahead of radix3 at every size; only Hono's mega-regex leads the 10-route toy table (and measures ~4.5× SLOWER than Pyreon at 50+). The shape, in priority order: (1) **identity memo + single WeakMap probe** for the per-`routes`-array index; (2) **null-prototype dictionary** `staticMap`/`segmentMap` (~3× faster hit than `Map`; hostile keys like `__proto__` are plain own props); (3) **`validateFn` precomputed per flattened route** at flatten time (leaf→root, most-specific wins) so resolves never walk the matched chain; (4) **offset-walking fast lane** for plain paths (no `%` / `//` / trailing slash, certified by a one-pass `scanCleanPath`) — static segments compare in place via `startsWith`-at-offset, only param values slice, no parts array; weird shapes route to the preserved split-based general lane, so edge semantics are unchanged by construction; (5) **per-bucket segment-count dispatch** (`BucketDispatch.exact[n]`) for all-fixed buckets — count-mismatch rejects disappear structurally; buckets containing splat/optional candidates return `null` and keep the ordered flat scan because **definition-order priority (first match wins) cannot be expressed count-indexed** (a splat defined before a same-bucket param route must keep winning — bisect-verified spec in `match-fastlane.test.ts`); (6) **frozen empty singletons** for no-params/query/search results (the `meta`-freeze precedent — mutation of an EMPTY result field throws instead of polluting a shared object; non-empty values stay fresh per resolve). Reference: `packages/core/router/src/match.ts` (`scanCleanPath` / `matchFlattenedFast` / `buildBucketDispatch` / `EMPTY_PARAMS`); specs `src/tests/match-fastlane.test.ts` (lane equivalence + priority + frozen contract + validateFn).
- **URL hash/query split follows WHATWG order** (fixed in the same overhaul): fragment = everything after `#`, query sits between `?` and `#`. Pre-fix, `?` split first — `/user/42?tab=posts#bio` leaked the hash into the query (`{ tab: 'posts#bio' }`) and a `?` inside a fragment was misread as a query separator. Two legacy specs had CODIFIED the wrong order (the "test codified the bug" trap) and were corrected.
- `useIsActive(path, exact?)` — returns reactive boolean for whether a path matches the current route
- Segment-aware prefix matching: `/admin` matches `/admin/users` when `exact` is false
- `useTypedSearchParams({ page: 'number', q: 'string' })` — typed search params with auto-coercion
- `useTransition()` — returns a reactive accessor `() => boolean` (call it: `useTransition()()`), true during route transitions. `useMiddlewareData()` likewise returns `() => Record<string, unknown>` — both are accessors, not destructurable objects
- Hash scrolling — navigating to `#id` auto-scrolls to the matching element
- Route error boundaries — `errorComponent` on route records catches render errors (not just loader errors)
- View Transitions API — auto-enabled for route navigations, opt out per route via `meta.viewTransition: false`
- Middleware chain — `RouteMiddleware` with `ctx.data` for passing data between middleware, `useMiddlewareData()` to read in components
- `Router<TNames>` generic — typed named navigation, compile-time checked (`createRouter<'home' | 'user'>({ routes })` → `router.push({ name: 'typo' })` is a TS error)
- `aria-current="page"` on active `RouterLink` (WCAG 2.1 accessibility)
- Query parsing decodes `+` as space per `application/x-www-form-urlencoded` spec
- `useTypedSearchParams` guards NaN — non-numeric strings coerce to `0` not `NaN`
- `_middlewareData` properly typed on `ResolvedRoute` (no `as any` casts)
- Prefetch cache capped at 50 entries to prevent unbounded memory growth
- `prefetch="intent"` (default) — prefetches on hover AND focus (keyboard + mouse, so keyboard / screen-reader users get the same head-start as mouse users). Also: `"hover"`, `"viewport"`, `"none"`. **`"viewport"` polish**: the IntersectionObserver uses `rootMargin: '200px'` (fetch starts before the link is fully on screen) and the prefetch is scheduled via `requestIdleCallback` (falls back to `setTimeout(1)`) so it never contends with the scroll the user is performing. The observer disconnects synchronously on first intersection before the idle callback is queued, so scroll jitter can't double-schedule. `RouterLink` prefetch is on by default — the discoverability gap (docs previously omitted `'intent'` and misdocumented the default as `'hover'`) was closed in the prefetch-DX PR; see `docs/src/content/docs/router.md` "Prefetch Strategies".
- `notFound()` + `NotFoundBoundary` — throw `notFound()` in a loader or component to trigger a 404 boundary. `isNotFoundError()` type guard for custom handling
- `redirect(url, status?)` — throw inside a route loader to redirect the navigation BEFORE the layout renders. SSR-side: `@pyreon/server`'s handler catches the thrown error and returns a real HTTP `302`/`307` `Location:` Response (no layout HTML leaks server-side). CSR-side: the router's loader-runner catches and propagates as `router.replace(target)` with redirect-depth bookkeeping. Default status is `307` (method-preserving). Companion guards `isRedirectError()` / `getRedirectInfo()` are exported for custom error boundaries that should let redirects pass through. **Replaces the fragile `onMount + router.push('/login')` workaround** for auth-gates under nested-layout dev SSR + hydration — the prior pattern would briefly render the auth-gated layout before redirecting, leaking authenticated UI structure to anonymous users.
- `LoaderContext.request?: Request` — populated only when a loader runs during SSR (via `prefetchLoaderData(router, path, request)`); `undefined` on every CSR navigation. Lets server-side loaders read cookies / auth headers and decide whether to `redirect()` BEFORE the layout renders. Closes the `request: Request` audit-types finding from the original 0.14.0 baseline (the field was previously typed but not populated by any runtime path).
- **Nested-absolute-path matched-chain fix in `match.ts`**: when fs-router emits absolute paths for nested children (e.g. parent `/app` with child `/app/dashboard`, NOT child `dashboard`), `flattenOne` previously concatenated parent segments with child's already-absolute segments, producing wrong staticPaths like `/app/app/dashboard`. The staticMap lookup missed and `resolveRoute('/app/dashboard')` returned `matched: []` — no SSR rendering happened for any nested-layout route. The fix detects "child path starts with `/`" as the absolute-path signal and uses the child's own segments directly. Relative children (`dashboard`, `:id`) continue to inherit the parent's segments via concatenation. PR #415 fixed this for the dispatcher's pattern-flattening; this PR closes the matching gap inside the router itself.
- `pendingComponent` per route — shown while loader runs, with `pendingMs` (delay before showing) and `pendingMinMs` (minimum display time). Signal-based state machine: hidden → pending → ready
- `validateSearch` per route — transform raw query strings into typed values. Accepts any `(raw: Record<string, string>) => T` function — works with Zod `.parse`, Valibot, or plain functions. Result available on `route.search` and via `useValidatedSearch<T>()`
- `useValidatedSearch<T>()` — reactive accessor for validated search params with structural sharing (shallow-equal check prevents re-renders when unrelated query params change)
- **Loader cache** — key-based caching with dedup and TTL. `loaderKey: ({ params }) => \`user-${params.id}\``controls cache identity.`gcTime`(default 5min) controls expiry.`router.invalidateLoader(key?)` clears cache entries. In-flight dedup prevents duplicate requests for the same key. SWR routes bypass cache for revalidation.

### @pyreon/hooks

- 36 signal-based hooks across 7 categories. Every hook is SSR-safe (browser API access guarded), self-cleaning (registers `onUnmount` for listeners/observers/timers), and signal-native: returns `Signal<T>` / `Computed<T>` / accessor objects, never plain values
- **State**: `useToggle`, `usePrevious`, `useLatest`, `useControllableState`
- **DOM**: `useEventListener`, `useClickOutside`, `useFocus`, `useHover`, `useFocusTrap`, `useElementSize`, `useWindowResize`, `useScrollLock`, `useIntersection`, `useInfiniteScroll`
- **Responsive**: `useBreakpoint` (theme-driven), `useMediaQuery` (raw escape hatch), `useColorScheme`, `useReducedMotion`, `useThemeValue`, `useSpacing`, `useRootSize`
- **Timing**: `useDebouncedValue`, `useDebouncedCallback`, `useThrottledCallback`, `useInterval`, `useTimeout`, `useTimeAgo`
- **Interaction**: `useClipboard` (auto-resets `copied` after 2s), `useDialog` (native `<dialog>`), `useKeyboard`, `useOnline`
- **Data**: `useFetch` — thin reactive JSON fetch (`{ data, error, isPending, refetch }`); the web half of the multiplatform `useFetch` contract (PMTC compiles the same call to native `PyreonFetch` containers)
- **Composition**: `useMergedRef`, `useUpdateEffect` (skips first run), `useIsomorphicLayoutEffect` (layout effect on client, no-op on SSR)
- **`useControllableState({ value, defaultValue, onChange })`** is the canonical controlled/uncontrolled pattern — every `@pyreon/ui-primitives` component uses it. Pass `value` and `defaultValue` as FUNCTIONS so signal reads track reactively. Reimplementing the `isControlled + signal + getter` shape by hand was the #1 anti-pattern across primitives before the helper landed
- **Never reach for `addEventListener` / `removeEventListener` directly in primitives** — use `useEventListener`. Same for observers (`useIntersection` / `useElementSize`) and timers (`useInterval` / `useTimeout`). The cleanup is the hook's job
- **`useBreakpoint` reads the theme**, `useMediaQuery` is raw — use the former for layout decisions tied to the design system, the latter for one-off queries (`(prefers-contrast: more)`, `(orientation: landscape)`, etc.)
- Manifest-driven docs (T2.1 + T2.5.1): `packages/fundamentals/hooks/src/manifest.ts` is the single source for the `llms.txt` bullet + `llms-full.txt` section + MCP `api-reference.ts` region. 14 MCP entries generated from the manifest's enriched `api[]` (was 0 hand-written — hooks had no MCP entries before). Inline-snapshot test (`manifest-snapshot.test.ts`) locks the rendered output locally in addition to the CI `Docs Sync` gate.

### Devtools

Stateful packages expose `./devtools` subpath exports with WeakRef-based registries for introspection. Tree-shakeable — zero cost unless imported. Available for: store, state-tree, form, i18n.

**`@pyreon/devtools` (private, `packages/tools/devtools`)** — the Chrome DevTools extension. `@pyreon/runtime-dom`'s `installDevTools()` (auto-run on first browser `mount()`) attaches `window.__PYREON_DEVTOOLS__` with `version` / `getComponentTree()` / `getAllComponents()` / `highlight(id)` / `onComponentMount(cb)` / `onComponentUnmount(cb)` / `enableOverlay()` / `disableOverlay()`; the extension's `page-hook` reads exactly that surface and bridges it through content-script → background service worker → the **Pyreon** DevTools panel (live component tree, click-to-highlight, inspector, **element-picker toggle** wired to `enable/disableOverlay`). Built via `vl_rolldown_build` to `dist/` (5 entry bundles + manifest/html/css/icons copy; `scripts/sync-manifest.ts` stamps the manifest version from `package.json`). Pure logic (`messages.ts` / `serialize.ts` / `tree.ts`) is unit-tested at 100% under happy-dom; the four browser-sandbox context files are coverage-excluded. `scripts/generate-icons.ts` is a dependency-free PNG encoder rendering the ember-on-ink brand mark. Not published (no npm consumer can load a Chrome extension) — built and loaded unpacked. The panel UI implements the **Claude-Design handoff** (`pyreon-devtools.jsx` + `tokens.css`): the `PxDevChrome` shell (traffic-light title bar w/ inspected origin, glyph+wordmark, ember-gradient active tab underline, breadcrumb + live status) and the `PxArtDevTree` Components split (depth-indented mono tree, hot-row ember **pulse** on fresh mount — the truthful signal-model analog of the design's RE-RENDERED state since Pyreon components mount once — + `SELECTED`-eyebrow inspector). Full design token system + JetBrains Mono/Space Grotesk, dark-default w/ a light block honoring DevTools' theme. The design's other five tabs (Graph/Signals/Effects/Profiler/Console) are implemented in the panel and consume the **reactive-devtools Foundation** — the opt-in `@pyreon/reactivity` registry surfaced as `__PYREON_DEVTOOLS__.reactive` (`activate`/`deactivate`/`getGraph`/`getFires`) by `@pyreon/runtime-dom` (merged: Foundation #703 + tabs #702 + real-app e2e #707). Signals (live node table), Graph (layered SVG dep diagram), Effects (per-node fire lanes), Profiler (100ms fire-frame buckets), Console (page-world eval bridge). The `reactive` field is consumed defensively (runtime-guarded) so a page running an older `@pyreon/runtime-dom` degrades to an explicit "needs the Foundation" notice — never a fake/empty surface (brand brief: honesty over theater). Pure presentation logic (graph layout, fire bucketing) lives in `reactive-view.ts` (100% unit-tested); `panel.ts` stays coverage-excluded (browser-only); polling runs only while a reactive tab is open. Verified at every layer: the component-surface drift lock in `framework-integration.test.ts` Omits `reactive` (it legitimately differed across the two-PR transition); `reactive-e2e.test.ts` drives real `@pyreon/reactivity` primitives through the real hook into the extension's own `layoutGraph`/`bucketFires` (end-to-end, plus a bidirectional drift lock vs `@pyreon/reactivity`'s exported `ReactiveGraph`/`ReactiveNode`/`ReactiveFire`); the `ReactiveDevtools` mirror is an exact structural copy of the Foundation's `PyreonReactiveDevtools`. Web fonts deliberately not loaded (MV3 panel CSP + offline-safety) — design font stacks degrade to system.

**Reactive devtools bridge** (`@pyreon/reactivity` `reactive-devtools.ts` → `__PYREON_DEVTOOLS__.reactive`): a **leak-free** introspection layer over the live signal/computed/effect graph. `activateReactiveDevtools()` / `deactivateReactiveDevtools()` / `isReactiveDevtoolsActive()` / `getReactiveGraph()` (nodes + dependency edges, derived fresh from the real subscriber `_s` Sets) / `getReactiveFires()` (bounded ring-buffer fire timeline), exported from `@pyreon/reactivity` and surfaced on the hook as `reactive.{activate,deactivate,getGraph,getFires}`. **Always-on in `__DEV__`, zero cost in production**: `_rdRegister` / `_rdRecordFire` run for every signal/computed/effect created in dev (so a devtools panel attached AFTER mount sees the full live graph immediately — the activate-after-creation user workflow). The caller-side `process.env.NODE_ENV !== 'production'` gate (locked by `reactive-devtools-treeshake.test.ts`, a minified-bundle bisect test) folds the entire chain to dead code in production. `_active` is now a READ gate: `getReactiveGraph()` / `getReactiveFires()` / `getFireSummaries()` return empty when no client has called `activateReactiveDevtools()` — non-attached consumers see nothing even though the registry is populated. `deactivateReactiveDevtools()` does NOT clear the registry (a close+reopen panel cycle re-exposes the live graph); test-only `__resetReactiveDevtoolsForTesting()` drops everything for cross-test isolation. `_captureCallerLocation` is ALSO always-on in `__DEV__` so signals/computeds/effects created BEFORE a devtools client attaches still get loc captured — the LPIH editor inlay-hint surfaces ("🔥 signal fired N×") work uniformly across the activate-after-creation user workflow. Cost is **deferred two-phase**: at capture time, a single cheap `new Error()` allocation (~0.14µs in V8/Bun — the stack is captured but not formatted). The expensive `.stack` formatting + `parseStackLine` is lazily resolved by `getReactiveGraph()` / `getFireSummaries()` at devtools-read time and the result is memoized on the `NodeRec` (Error object becomes GC-eligible after first resolve). Most app signals never have their loc read (LPIH inlay-hints only consume loc for hot lines the user has on screen), so the typical cost per signal is just the cheap Error allocation regardless of count. Most user signals additionally pay 0µs because `@pyreon/vite-plugin`'s `injectSignalLocations` rewrites `signal(0)` → `signal(0, { __sourceLocation })` at build time and the caller short-circuits to the injected value before invoking `_captureCallerLocation`. Verified end-to-end: 477/477 pre-existing signals in `examples/perf-dashboard` get loc populated (100%); 10k-signal runtime-fallback creation runs in ~40ms (9× faster than the eager-parse design, parity with the OLD `_active`-gated zero-cost baseline). Never retains a node (WeakRef + FinalizationRegistry; fire buffer holds only ids+timestamps). Same risk profile as the adjacent `_recordSignalWrite` / perf-harness counter calls. This is the **Foundation** that unblocks the devtools Signals/Graph/Effects/Console tabs (the Profiler tab additionally needs per-frame duration instrumentation — deferred). The opt-in design that shipped before this fix was structurally invisible for ALL user-side "open devtools after the app mounted" workflows — bisect-verified with a 4-scenario controlled experiment against `examples/perf-dashboard` (958 components, 477 live nodes): every scenario returned `0/0/0` pre-fix and `477/112/16-22` post-fix; the `@pyreon/devtools` `verify-extension.ts` step-4 reactive-graph assertion was upgraded from informational to hard `fail()` in the same PR (this is the gap that allowed PR #900's first verification to pass while shipping a broken Foundation against real-world apps).

**Live Program Inlay Hints (LPIH)** (`@pyreon/reactivity` `reactive-devtools.ts` + `lpih-bridge.ts` + `@pyreon/compiler` `lpih.ts` + `@pyreon/lint` LSP integration): surface live runtime data — signal fire counts, effect re-runs, subscriber counts — as **inlay-hint ghost text at the source line** in the editor. Same surface TypeScript uses for inferred types. **No reactive framework today shows runtime data at source lines** for any reactive framework; the data lives in a separate devtools panel that requires context-switching. LPIH closes that gap. Three layers: (1) **Runtime capture** — `signal()` / `computed()` / `effect()` creation captures source location via `new Error().stack` (parsed for V8/JSC/SpiderMonkey), stored alongside the existing devtools registry. New API: `getFireSummaries()` returns fires aggregated by `file:line:col` with `{count, lastFire, kind}`. (2) **Bridge** — at subpath export `@pyreon/reactivity/lpih`: `writeLpihCache(path)` atomically writes the snapshot to a JSON file (tmp + rename); `startLpihPolling(path, intervalMs)` writes repeatedly. Bridge is at the subpath (not main entry) because it depends on `node:fs/promises` (Node-only) and is dev-mode-only integration glue — separation keeps the core reactivity bundle smaller AND clarifies that LPIH writes are an opt-in side channel, not a core primitive (same pattern as `@pyreon/i18n/core`). (3) **LSP** — `@pyreon/lint`'s `textDocument/inlayHint` handler reads `PYREON_LPIH_CACHE` env var on every request, parses, and emits `🔥 <kind> fired N×` hints via the new `firesToCreationSiteFindings()` helper. Compiler side also exports `mergeFireDataIntoFindings()` for the rare case where reactive-read sites coincide with creation lines. **Zero production cost** — entire capture path tree-shakes under `NODE_ENV=production` (locked by the existing `reactive-devtools-treeshake.test.ts`). **Dev-mode opt-in cost**: ~2.2µs per signal creation when devtools active (measured: 100k signals + 10k computeds + 10k effects = 269ms total; per-session real-app cost 0.2-2.3ms — invisible). **Measured impact**: 0.32ms median LSP roundtrip, ~150ms user-perceived save→hint with debounce, **20× faster than the 3-5s devtools-panel workflow** for "is this signal firing?"-type questions. Workflow-step reduction: 9→2 ("which signal fires most?"), 8→2 ("is this effect over-running?"), 10→4 ("did memoization help?"). 60 tests across the three layers (15 source-location + 6 bridge + 24 compiler + 15 LSP integration), bisect-verified (stashing LSP wiring fails 11/15 integration tests). Bridge uses filesystem cache because LSP servers are stdio-only — can't directly IPC with a browser; filesystem is the universal lowest-common-denominator transport. Reference: `packages/core/reactivity/src/{reactive-devtools,lpih-bridge}.ts`, `packages/core/compiler/src/lpih.ts`, `packages/tools/lint/src/lsp/index.ts` (`_readLpihCache` + `liveFires` option). Future: `@pyreon/vite-plugin` build-time location injection eliminates the 2.2µs/creation overhead by replacing stack capture with compile-time literals. Future: VS Code / Neovim extension that auto-wires `startLpihPolling` + `PYREON_LPIH_CACHE` for zero-config setup.

**The framework registers components POST-ORDER** — `mount.ts` calls `registerComponent(id, name, el, parentId)` *after* a component's subtree has mounted, so children register before their parent. `registerComponent` only pushes a child into `parent.childIds` if the parent is already in the registry at that moment — which it never is under post-order. **Consequence: a parent entry's `childIds` is empty in `getAllComponents()` output; only `parentId` (captured from the mounting stack at mount *start*) is reliable.** The extension's `tree.ts:getChildren()` rebuilds the tree by indexing on `parentId`; the panel renders from that, never from `childIds`. This was caught by `src/tests/framework-integration.test.ts` (real `mount()` → live hook → extension's own `serialize`/`buildMap`/`getChildren`/`getRoots`), which also carries a compile-time bidirectional assignability lock between the extension's local `PyreonDevtools`/`DevtoolsComponentEntry` mirror (`src/types.ts`) and `@pyreon/runtime-dom`'s exported types — a framework API add/rename/remove fails `tsc` instead of the extension silently losing a capability. Any consumer reading `getAllComponents()` (not just this extension) must reconstruct child links from `parentId`, not trust `childIds`.

## Key Architectural Patterns

### Workspace resolution (no build needed for dev)

Each package.json has `"bun": "./src/index.ts"` in exports.
Root tsconfig has `"customConditions": ["bun"]`.

**Bootstrap on fresh worktree/clone + mtime drift detection**: the `postinstall` script (`scripts/bootstrap.ts`) automatically builds all packages if any `lib/` directory is missing OR if any package's source is newer than its `lib/` (stale build, e.g. after `git pull` that touches package sources). This is needed because Vite's config bundler hardcodes `conditions: ["node"]` and resolves `import: "./lib/index.js"` — which must reflect the current source. The bootstrap runs `bun run --filter='./packages/*/*' build` (~45s) when rebuild is needed, otherwise no-ops in ~30ms (single mtime walk). You do NOT need to run `bun run build` manually after cloning, creating a worktree, or pulling changes — `bun install` handles it. **If you change package source files locally and run an example build that fails with confusing errors (`MISSING_EXPORT`, missing files, unexpected route shapes), run `bun scripts/bootstrap.ts` to rebuild stale lib/** — this catches the case where you edited source mid-session without re-running `bun install`.

**Bootstrap fails loudly on partial state (post-#398 + gap #3 closure).** After the build subprocess returns, the bootstrap re-runs the dirty-detection check against each originally-dirty package. If ANY are still missing or stale, the script exits nonzero — even on the postinstall path. PR #398 made manual / CI invocations exit nonzero on full build failure, but the postinstall path still swallowed silently because "aborting bun install over a transient lib build is worse than continuing." Gap #3 closed the remaining hole: silent partial state is **worse** than a failed install, because devs running `bun run dev` use the bun condition → `src/` and never touch `lib/` — they don't notice missing `lib/` until production-build time, far from the cause. Escape hatch: `PYREON_BOOTSTRAP_SOFT=1` swallows the postcondition failure (the install completes but `lib/` stays incomplete; consumers see confusing build errors until you re-run bootstrap manually). Companion env var `PYREON_BOOTSTRAP_FORCE_FAIL=1` jumps straight to the failure path for end-to-end testing of exit-code behavior.

**Bootstrap content + retry (post gap-#6 / gap-#3 follow-up).** Two defensive layers added on top of the postcondition check:

1. **`lib/index.js` content sanity** — the original postcondition only verified `lib/` directory existed and src/lib mtime ordering. If a prior bootstrap run crashed mid-write, wrote a 0-byte file, or hit a transient resource issue, the file's presence and recent mtime both look fine — but consumers would hit `MISSING_EXPORT` errors at example-build time. The check uses a **50-byte floor**: real Pyreon `lib/index.js` entries are hundreds to thousands of bytes, even the smallest. A package whose `lib/index.js` is <50 bytes is structurally broken — flagged as `[stale]` in BOTH the dirty-detection (catches broken state from a prior run on the next bootstrap) AND the postcondition (catches a current-run build that emitted empty output).
2. **Single sequential retry pass** — if any packages are still dirty after the first build, the bootstrap retries ONLY those packages, one at a time, via per-package `bun run --filter='@pyreon/X' build`. Sequential per-package retry avoids the parallel-load issues that may have caused the first failure (topological-order race, file-handle limits, native-binary link race after `bun install`). **Capped at one retry** — never recurse, never infinite-loop. Logs a counter line on success: `[bootstrap] Retry recovered N package(s) (first-pass-failed: M, retry-fixed: N, still-dirty: K)`. If retry also fails, the original failure-surfacing path runs (exit nonzero with per-package detail). The `PYREON_BOOTSTRAP_FORCE_FAIL=1` injection short-circuits retry so the test harness can verify exit-1 termination without burning 2 min of build time.

**Troubleshooting bootstrap failures.** If `bun install` fails on the bootstrap step:

1. **Most common**: a single workspace package failed to build (transient race, missing dep, source error). Run `bun run --filter='./packages/<category>/<pkg>' build` for the named package to see the per-package output; the bootstrap's stderr output names which packages are still dirty.
2. **If you need to unblock `bun install` immediately** (e.g. about to install a new dep that's required for the bootstrap to succeed): `PYREON_BOOTSTRAP_SOFT=1 bun install`. This restores the pre-#435 swallow-on-postinstall behaviour for one run — the install completes, `lib/` stays partial, you must re-run `bun scripts/bootstrap.ts` to complete the build before your next example build / production deploy.
3. **If `git push` isn't running validation**: your `core.hooksPath` is set to a real custom location (husky / lefthook). The bootstrap installer doesn't clobber it. Either chain Pyreon's hook into your existing pre-push, OR `git config --unset core.hooksPath` and re-run `bun scripts/install-git-hooks.ts`. **Note**: if `core.hooksPath` happened to be set to the default `<git-dir>/hooks` location (an older bootstrap may have done this explicitly), the installer now recognises that as "no real override" and replaces it with `.githooks/` — you don't need to do anything manually.

### Signal implementation

`signal<T>()` returns callable function with `.set()` and `.update()`.
Direct subscribers use a single-subscriber inline slot (`_d1`), promoting to a `Set` only on the 2nd subscriber (the dominant `<For>`-row case allocates no Set); effect subscribers use a lazy `Set`. `createSelector`'s per-key buckets use the same inline-first-subscriber shape (PR #1537). Batch uses pointer swap.

### JSX & VNode

- JSX configured via `jsxImportSource: "@pyreon/core"` in root tsconfig (`jsx: "preserve"`)
- JSX automatic runtime: `@pyreon/core/jsx-runtime` (jsx, jsxs, Fragment)
- `h<P extends Props>(type, props, ...children)` — lower-level API, children stored in `vnode.children`
- Components must merge: `props.children = vnode.children.length === 1 ? vnode.children[0] : vnode.children`
- `ComponentFn<P> = (props: P) => VNodeChild`
- `VNodeChild = VNodeChildAccessor | VNodeChildAtom | VNodeChildAtom[]` — accessor-first so `{() => cond && <X />}` typechecks
- `VNodeChildAccessor = () => VNodeChildAtom | VNodeChildAtom[]` — reactive child expression
- `VNodeChildAtom = VNode | string | number | boolean | null | undefined` — `boolean` included so `&&` patterns work without ternary
- `<For each={items} by={r => r.id}>{r => <li>...</li>}</For>` — keyed list rendering
  - Prop is `by` (not `key`) because JSX extracts `key` as a special VNode reconciliation prop
- `class` prop accepts strings, arrays, objects, or nested mix — processed by `cx()`. In the compiled template fast path, a dynamic class binding emits `typeof v === "string" ? v : _cx(v)` (string passthrough, array/object → `cx`), mirroring the runtime `applyProp` path (`runtime-dom/src/props.ts`). Before the binding-fidelity fix the fast path assigned the raw value, so reactive `class={[...]}` rendered `"a,b"` and `class={{...}}` rendered `"[object Object]"`. The injected import is aliased — `import { cx as _cx } from "@pyreon/core"` — because `cx` is a PUBLIC export a hand-written component may already import (a bare `cx` injection would collide: "Identifier `cx` has already been declared", which broke the docs build).
- JSX index signature narrowed to `[key: \`data-${string}\`]` and `[key: \`aria-${string}\`]` only (catches typos)
- `TargetedEvent<E>` types `currentTarget` per element — no manual `as HTMLInputElement` casts
- New events: `onBeforeInput`, `onInvalid`, `onResize`, `onToggle`, `onFocusIn`, `onFocusOut`
- `onFocusIn` / `onFocusOut` are the **bubbling** focus events (vs non-bubbling `onFocus`/`onBlur`), so a handler on a container fires when focus moves to/from any descendant — e.g. pausing a `<Toaster>` region's auto-dismiss when a keyboard user tabs into a toast. Already in `DELEGATED_EVENTS` (the runtime delegated them since inception via the generic `on*` → lowercased event-name path — `onFocusIn` → `focusin`); this just adds the missing JSX types in `@pyreon/core` (`jsx-runtime.ts`). The `@pyreon/toast` Toaster uses them to mirror its pause-on-hover with pause-on-focus.
- `style` prop accepts both string (`style="color: red"`) and object (`style={{ color: "red", fontSize: "14px" }}`)
  - String: inlined as HTML attribute by compiler
  - Every compiled style binding (static object, reactive object, thunk, or string) emits `_setStyle(el, value)` — the runtime `applyStyleProp` helper exported from `@pyreon/runtime-dom`. So a compiled style binding normalizes IDENTICALLY to the `applyProp`/`h()` path: string → `cssText`; object → per-property `setProperty` with kebab-casing + `normalizeStyleValue` (number→px) + **stale-key removal** (a reactive object going `{color,fontSize}` → `{color}` drops `fontSize`); dynamic bindings are wrapped in a reactive `_bind`. Before the binding-fidelity fix, object styles took a one-shot `Object.assign` (static — `style={{ color: theme() }}` never updated, no number→px, no key-removal) and the `() => ({...})` thunk emitted `cssText = <object>` → `"[object Object]"` → **no styles at all**. Both class and style now match the runtime `applyProp` value-normalization in BOTH backends (JS + Rust native), locked by `native-equivalence.test.ts` + runtime DOM regression specs in `runtime-dom/src/tests/compiler-integration.test.tsx`.

### Props Utilities

- `splitProps(props, keys)` — split props object preserving signal reactivity
- `mergeProps(...sources)` — merge default props with component props, last source wins
- `removeUndefinedProps(props)` — copy props dropping keys whose DATA value is `undefined`, preserving getter-shaped reactive props verbatim (descriptor-copy, never value-copy). The filter every prop-forwarding HOC runs before `mergeProps`. `@pyreon/attrs` + `@pyreon/rocketstyle` previously hand-rolled this identically (one shipped a value-copy reactivity bug); both now re-export it from core
- `createUniqueId()` — SSR-safe unique ID generation
- `untrack(fn)` — alias for `runUntracked`, reads signals without subscribing
- **Component-JSX spread `<Comp {...rest}>` preserves reactivity end-to-end.** The compiler emits `<Comp {..._wrapSpread(rest)}>`, which re-brands getter-shaped reactive props as `_rp` thunks. `makeReactiveProps` converts them back to getters on the consumer side. No need to think about which props are reactive when forwarding through spread — it just works. Fast path: sources with no getters return unchanged (zero cost). Reference pattern: `docs/src/content/docs/patterns/reactive-spread.md` (MCP `get_pattern('reactive-spread')`).
- **Manual prop-merging helpers must copy descriptors, not values.** If you write a helper function that copies props in plain JS (not through JSX spread), use `Object.getOwnPropertyDescriptors` + `Object.defineProperty` rather than `result[key] = source[key]` — the latter fires getters at copy time and collapses reactive props to static snapshots. Better: just use `mergeProps` / `splitProps` / `removeUndefinedProps` from `@pyreon/core` (they preserve descriptors). The framework's own HOC pipelines (`@pyreon/rocketstyle`, `@pyreon/styler`, `@pyreon/ui-core`, `@pyreon/elements`) follow this rule.

### JSX Types

- `cx(…values: ClassValue[]): string` and `ClassValue` exported from `@pyreon/core`
- `PyreonHTMLAttributes`, `CSSProperties`, `StyleValue`, `CanvasAttributes` (typed `Ref<HTMLCanvasElement>`) exported from `@pyreon/core`
- `AriaRole` exported from `@pyreon/core` — the WAI-ARIA 1.2 role union the `role` attribute is typed as: `role?: AriaRole | (() => AriaRole | undefined)`. Gives `role="…"` literal autocomplete + discoverability; the union includes `(string & {})` so it stays OPEN (any string still assigns — custom/abstract roles, `role={dynamicString}`, future tokens). Non-breaking DX refinement (so it does NOT type-error on typos — autocomplete only). Mirrors React's `AriaRole`. Type-contract locked by `packages/core/core/src/tests/aria-role.types.test.ts` (bisect-verified: closing the union → the open-ness assertions fail tsc).

### Router

Context-based: `RouterContext = createContext<RouterInstance | null>(null)`.
`RouterProvider` provides the router via `provide()` (owner-based on the client; request-scoped stack on SSR) + sets module fallback.
Hash mode uses `history.pushState` (not `window.location.hash`) to avoid double-update.
View Transitions API integration: route changes wrapped in `document.startViewTransition()` when available.

**What `await router.push()` / `.replace()` waits for** — the ViewTransition object exposes three promises, and picking the wrong one is easy to re-break:

| Promise              | Resolves when                                          | Router awaits?       |
| -------------------- | ------------------------------------------------------ | -------------------- |
| `updateCallbackDone` | Callback (DOM commit) finished; new state is live      | ✅ yes               |
| `ready`              | Snapshot captured, pseudo-elements ready for animation | no — just `.catch()` |
| `finished`           | Full animation completed (typically 200-300ms)         | no — just `.catch()` |

The router awaits `updateCallbackDone` so callers can inspect the new route immediately after `await router.push()`. It does NOT wait for `.finished` because blocking every programmatic navigation on a 200-300ms animation is unacceptable. `.ready` + `.finished` get empty `.catch()` handlers so their `AbortError: Transition was skipped` rejections (fired when a newer navigation interrupts an in-flight transition) don't leak as unhandled promise rejections.

**Hook ordering changed alongside this fix** — `afterEach` hooks and `scrollManager.restore` now fire AFTER the VT callback completes (previously they fired after `commitNavigation` returned but BEFORE the VT callback ran, which meant hooks briefly saw the OLD route state). This is the correct behavior per the hook's documented semantics ("after-navigation hook") but it is a silent behavior change for any app that relied on hooks running pre-commit.
Middleware chain: `RouteMiddleware[]` runs before guards, `ctx.data` passed through, `useMiddlewareData()` reads in components.
Hash scrolling: after navigation, `#id` fragments auto-scroll to matching DOM element.

### SSR

`renderToString(vnode)` + `renderToStream(vnode)` with Suspense streaming.
**`renderToString` is a maybe-sync renderer (2026-06)**: the internal string-render family returns `string | Promise<string>` — fully-sync subtrees concatenate with ZERO promise hops; only a real `async function Component()` promotes its own subtree, with `.then` continuations preserving strict sibling order + provider visibility + trim-after-settle (contract specs: `runtime-server/src/tests/maybe-sync-order.test.ts`). The prior always-async walk paid ~90µs of promise machinery per 500-node page vs ~10µs of HTML work. Measured: renderToString +41-78% per scenario; zero's full handler +24-45% req/s (nested-5-deep 78.6K→114.1K). Public API unchanged (`Promise<string>`); `renderToStream` untouched.
`mergeChildrenIntoProps(vnode)` called before `runWithHooks` in both paths.
`runWithRequestContext(fn)` isolates context + store per request via ALS.
**`renderToString` / `renderToStream` INHERIT an active request context** (Phase 1 render-pipeline unification): inside `runWithRequestContext`, both renderers reuse the request's ALS stack + store so request-level `provide()` frames — `provideRequestLocals(ctx.locals)`, request-scoped DI — are visible to rendered components. Pre-fix they always opened a FRESH `_contextAls.run([], …)`, so the nested scope silently discarded every request-level provide and `useRequestLocals()` could only ever resolve its default inside a component (a shipped, documented-but-broken feature). Bare calls keep the fresh isolated stack. Bisect-locked by `runtime-server/src/tests/request-context-inherit.test.ts` + the locals spec in `server/src/tests/render-page.test.ts`.
**`renderPage()` (`@pyreon/server/src/render-page.ts`) is the ONE string-mode page pipeline** — preload (lazy components + loaders, `redirect()` catch) → render with head collection → CSS-in-JS collect (empty-tag skip) → loader-data inline script → status 404 via the `notFoundComponent` chain → `routeModules` (`component._hmrId`) for SSG modulepreload. Consumed by `createHandler` (string mode), zero's SSG prerender entry, AND zero's dev SSR middleware (loaded via `ssrLoadModule("@pyreon/server")` + a nested-package alias so it shares the user code's module graph — the dual-instance hazard). Template composition stays caller-specific by design (handler: precompiled parts; SSG: `injectIntoTemplate`; dev: Vite-transformed `index.html`); streaming keeps its own pipeline. The drift class this closes: styler tag missing from SSG, dual noindex call sites, serializer divergence — per-page concerns now land in all three consumers at once. Contract: the router must be a per-request instance created AT the path (`preload` warms, it does not navigate).
**The SSR handler resolves lazy route components BEFORE rendering** via `router.preload(path, req)` (not loaders-only `prefetchLoaderData`). `renderToString` is synchronous — an unresolved `lazy()` route component falls back to `RouterView`'s empty loading state, so the page renders BLANK inside the layout (status 200, the unfilled template shell). zero's fs-router emits every route as `lazy()`; `mode: 'ssr'`/`'isr'` shipped empty pages (0.30.0 regression) until the handler switched from `prefetchLoaderData` (loaders only) to `router.preload` (lazy components + loaders + request forwarding + redirect propagation) — the same path SSG already used. `prefetchLoaderData` stays loaders-only because it is also the RouterLink-prefetch path. See `.claude/rules/anti-patterns.md` → "SSR-rendering Mistakes" (pre-resolve lazy components).
Suspense streaming has a 30s timeout — if async children do not resolve within 30 seconds, the fallback remains.
XSS escape in Suspense swap templates — all template content is properly escaped.
`For` list SSR emits per-item key markers (`<!--k:key-->`) for precise hydration matching.

**Streaming SSR + `@pyreon/styler` — inline `<style>` flushing.** `renderToStream` calls `globalThis.__PYREON_STYLER_FLUSH__()` (registered by `@pyreon/styler`'s `sheet.ts` on SSR module init) (a) ONCE after the synchronous shell render → emits `<style data-pyreon-stream="shell">…</style>` inline at the top of the app body so shell content is correctly styled before any Suspense boundary resolves; (b) inside EVERY `streamSuspenseBoundary`, BEFORE the `<template>` element → emits `<style data-pyreon-stream="<id>">…</style>` so each boundary's resolved HTML arrives with its styles already applied to the page. Closes the FOUC where the final consolidated `<style>` tag would otherwise arrive AFTER the boundary content. No hard `runtime-server → styler` dependency — the runtime reads `globalThis.__PYREON_STYLER_FLUSH__` and no-ops when absent (mirrors `__pyreon_count__` perf-counter pattern, the SSG-plugin styler-tag lookup, and the `__PYREON_SSR_TEMPLATE__` cloudflare-adapter pattern). The styler side: `sheet.flushSSRPending()` returns rules added since the previous flush + advances a watermark; emits `@layer elements, rocketstyle;` on the FIRST flush only; resets cleanly on `reset()` / `clearAll()` / `resetSSRBuffer()` (per-request boundaries). `getStyleTag()` unchanged for SSG / non-streaming SSR. Reference: `packages/ui-system/styler/src/sheet.ts:flushSSRPending` + module-init `globalThis.__PYREON_STYLER_FLUSH__` registration; `packages/core/runtime-server/src/index.ts` (`renderToStream` post-shell flush + `streamSuspenseBoundary` per-boundary flush). Bisect-verified at two layers: 13 sheet unit specs (`__tests__/streaming-flush.test.ts` — watermark + `@layer` ordering + reset semantics) + 7 runtime-server integration specs (`tests/styler-stream-flush.test.ts` — shell + per-boundary ordering, `</style` escape, multi-boundary, no-hook graceful no-op). Observable in `examples/cpa-pw-app-solid` (`mode: 'ssr', ssr: { mode: 'stream' }`). Bundle cost: ~239 gz across the two packages.

### Island Architecture

`island(loader, { name, hydrate, prefetch? })` → async ComponentFn → `<pyreon-island>` element.

**Import `island()` from `@pyreon/server/client`, NOT the `@pyreon/server` main barrel, in any code that ships to the client (the dominant case: a route/component that is bundled client-side for hydration).** `island.ts` is fully client-safe (imports only `@pyreon/core` `h` + `./island-codec`), but the `@pyreon/server` *main barrel* also re-exports `createHandler` / `prerender`, which pull `node:fs/promises` / `node:path` / `node:async_hooks` AND the package's `registerSingleton`. A barrel import (`import { island } from '@pyreon/server'`) therefore drags the entire server module into the client/route bundle → (a) `node:*` externalized-for-browser in the client bundle, and (b) a SECOND `@pyreon/*` instance vs the build's node_modules copy → the singleton sentinel throws (`[Pyreon] Duplicate @pyreon/server detected`) during the SSG render of that route, AND the dual `@pyreon/core` instance splits the context graph so the hydrated island crashes (`Cannot read properties of undefined (reading 'ref')`). This is the failure mode that made islands unusable inside `@pyreon/zero` routes (every zero route ships to the client). The fix: `island` (+ `IslandOptions`/`IslandMeta` types) is exported from the CLIENT-safe `@pyreon/server/client` subentry; `check-client-bundle-node-imports` now walks `/client` so it can never regress to pulling a `node:` import.

**Islands work natively in `@pyreon/zero` via a SELF-HYDRATING `island()`.** `@pyreon/zero` re-exports `island` (from `@pyreon/server/client`), so a zero app declares islands with `import { island } from '@pyreon/zero'` — no `@pyreon/server` dependency, `startClient({ routes })` only, no manual `hydrateIslandsAuto`. The runtime reconciliation problem this solves: `@pyreon/zero`'s route is a **reactive child of RouterView**, so on the client the SSR route DOM is **discarded + re-mounted** (`hydrateReactiveChild` → `mountChild`), NOT hydrated in place. That defeats the islands model two ways — (1) an `island()` that renders its component inline is **async**, and rendering an async component inside the host mount/`hydrateRoot` throws with no Suspense boundary (→ the route error boundary swallows it: "An error occurred while rendering this page"); (2) the one-shot external `hydrateIslandsAuto` scan races the async route mount (it runs once after `hydrateRoot`, before the lazy route + its marker mount). **Fix:** the client branch of `island()` (`typeof document !== 'undefined'`) renders ONLY the `<pyreon-island>` marker, then `onMount` dynamically imports `scheduleHydration`/`schedulePrefetch` from `@pyreon/server/client` and mounts the component INTO the marker per the `data-hydrate` strategy — the island OWNS its hydration lifecycle, robust whether the host hydrated or re-mounted. (Dynamic import = client-only → keeps the scheduler out of the SSR graph + avoids a static `client`↔`island` cycle.) **The marker render also captures `getContextOwner()` and `scheduleHydration` re-establishes it (`runWithContextOwner`) around the island's `hydrateRoot`** — under #1338's owner-based context the deferred mount has NO owner chain otherwise, so a `rocketstyle` component inside the island reads an undefined PyreonUI theme and crashes (`reading 'base'`). Capturing at the marker's render (owner active, links up through `_layout`'s PyreonUI) + restoring at hydrate time lets `useContext()` walk to the real provider; static-islands apps (no client owner) pass `null` → detached root, unchanged. 0.30.0 regression; see `.claude/rules/anti-patterns.md` → "Islands Mistakes" (deferred hydration must re-establish the captured owner). The SERVER branch is unchanged (async `loader()` → marker + content, for SEO/no-JS/first-paint). The static islands app is unaffected — there `island()` runs only at SSR and the client uses `hydrateIslandsAuto`, never this branch. Proven by `e2e/zero-islands.spec.ts` (real-Chromium: island hydrates with no wiring + signal click drives the DOM + zero console errors — Bug 3/F's `reading 'ref'` crash gone) and the 9 islands-showcase strategy specs staying green. Reference: `packages/core/server/src/island.ts` (the `typeof document` branch) + `client.ts` (`export`ed `scheduleHydration`/`schedulePrefetch`). **Follow-up (bundle):** the schedulers could be extracted to a dedicated `island-hydrate.ts` so an island chunk pulls only the scheduler, not the whole `@pyreon/server/client` (which statically imports `@pyreon/router`); deferred to keep this change low-risk.

Client: `hydrateIslands({ Name: () => import(...) })` — strategies: load, idle, visible, **interaction**, media, never. The `interaction` strategy hydrates on first user interaction (`focus` / `click` / `pointerenter` / `touchstart` / `submit` by default; customize via `'interaction(<events>)'` form). **Both click events AND form submits are replayed** post-hydration on the equivalent live element — click via a synthetic `MouseEvent('click')`, submit via a synthetic `SubmitEvent('submit')` re-dispatched on the live form (so the live submit handler reads current `FormData` with the user's input values, instead of the browser doing its full-page POST/GET before hydration). The pre-hydrate handler calls `preventDefault()` + `stopImmediatePropagation()` for both, closing the "user clicks/submits but nothing happens until they retry" UX trap AND the more severe "form silently navigates away before the island ever wakes up" data-loss trap. The replay path uses `data-testid` when present, falling back to a tag+child-index walk relative to the island root, which survives the DOM swap that hydrateRoot performs on mismatch / structural reshape. Reference: `packages/core/server/src/client.ts:scheduleInteractionHydration` (the `CapturedInteraction` discriminated union carries the event type so the post-hydrate `dispatchEvent` builds the right synthetic event).

**`hydrate: 'never'` islands MUST NOT have a registry entry.** The whole point of the strategy is shipping zero client JS — registering a loader in `hydrateIslands({ ... })` defeats it by pulling the component module into the client bundle graph. `hydrateIslands` short-circuits before the registry lookup for never-strategy islands so missing entries are silent (NOT a `data-island-error="no-loader"` flag). Reference: `packages/core/server/src/client.ts:hydrateIslands`.

**Island prop codec** (`packages/core/server/src/island-codec.ts`): roundtrip-preserving JSON encoder/decoder pair used by SSR's `serializeIslandProps` + the client's `hydrateIsland`. Replaces the naïve `JSON.stringify` path, which silently lost fidelity: `Date → ISO string`, `Map`/`Set`/`RegExp` → `{}`, `BigInt` → empty-props fallback, class instances → `{}` with no warning. The codec tags non-JSON-native types with an internal `__pyreon_t` marker (`'d'`/`'m'`/`'s'`/`'r'`/`'B'`); the inverse decoder on the client unwraps the marker to restore real `Date`/`Map`/`Set`/`RegExp`/`BigInt`. Plain objects without markers round-trip byte-identically. Objects whose OWN key is literally `__pyreon_t` get a transparent `'e'`-escape wrapping (decoder unwraps; user sees the original shape). **Fail-loud (was: silent `{}`)**: class instances, circular references, and >100-deep nesting emit `IslandPropEncodeError` with a `$.foo.bar`-style path + offender name + island name; the caller (`serializeIslandProps`) catches and falls back to empty props as before, BUT the dev-mode `console.error` now NAMES the offender instead of hiding it as a generic "BigInt or circular reference" message. Forward-compatible decoder: unknown tag values pass through verbatim so an older `client.ts` doesn't crash on a future-encoded type. Tests at `packages/core/server/src/tests/island-codec.test.ts` (21 roundtrip + escape + fail-loud + forward-compat specs) + 3 contract-update tests in `tests/server.test.ts` (BigInt now roundtrips, class instance fails loud, circular still falls back). **Contract change for consumers**: code that received `Date` props as ISO strings (e.g., `new Date(props.someDate)` to revive) still works (Date constructor accepts Date); `typeof props.someDate === 'string'` checks would need updating.

**Prefetch hint** (`prefetch: 'idle' | 'visible' | 'none'`, default `'none'`): pair a deferred-hydration strategy with a prefetch hint to pre-warm the chunk BEFORE the hydration trigger fires. With `hydrate: 'visible'` alone, scrolling into view kicks off both the chunk fetch AND the hydration — users see a blank island flash while the chunk arrives. With `hydrate: 'visible', prefetch: 'idle'`, the chunk is fetched during browser idle so the IntersectionObserver fires against an already-warm module cache. Independent scheduling: prefetch and hydration each register their own `requestIdleCallback` / `IntersectionObserver`, fire-and-forget the loader, and rely on JS's import-promise dedup to share the underlying module fetch. Suppressed (no `data-prefetch` attribute emitted) when `hydrate: 'load'` (loader runs synchronously) or `hydrate: 'never'` (defeats zero-JS). Reference: `packages/core/server/src/client.ts:schedulePrefetch`.

**Auto-registry** (`@pyreon/vite-plugin` `islands: true`, default-on as of post-#461 Tier 1). Eliminates the manual sync between `island()` declarations and the client `hydrateIslands({ ... })` call — typo / forgotten entry / registry drift was the #1 author foot-gun. Plugin pre-scans the source tree at `buildStart` for `island(() => import('PATH'), { name, hydrate })` calls (mirrors the existing `prescanSignalExports` pattern), emits a `virtual:pyreon/islands-registry` virtual module the user imports in `entry-client.ts` and passes to `hydrateIslandsAuto(registry)`. Never-strategy islands are deliberately omitted from the auto-registry so their components stay out of the client bundle. **Why the user passes the import explicitly** rather than `hydrateIslandsAuto()` resolving the virtual module itself: Rolldown's static-import analysis runs before plugin `resolveId` hooks for workspace package sources, so an `import 'virtual:...'` inside `@pyreon/server/client` would fail to resolve at build time. Putting the import in the user's `entry-client.ts` (where the plugin's `resolveId` fires natively) is the clean shape. Reference: `packages/tools/vite-plugin/src/index.ts` (`prescanIslandDeclarations` / `scanIslandDeclarations` / `renderIslandsRegistry`); manual `hydrateIslands({ ... })` stays public for non-Vite consumers.

**Project-wide audit** (`pyreon doctor --check-islands` + MCP `audit_islands` tool, PR C of the islands DX roadmap). Walks `packages/` and `examples/` and runs five cross-file detectors that auto-registry can't reach AND PR G's per-file `island-never-with-registry-entry` detector misses (it only catches the same-file shape):

1. **`duplicate-name`** — two `island()` declarations with the same `name`. Runtime hydrates only the FIRST loader; every other declaration fails silently with no error flag.
2. **`never-with-registry-entry`** — project-wide cross-file version of PR G's per-file detector. Fires when ANY file's `hydrate: 'never'` declaration matches a key in ANY other file's manual `hydrateIslands({...})` call.
3. **`registry-mismatch`** — manual `hydrateIslands({ X })` entry where `X` has no matching `island()` declaration anywhere. Catches typos / removed islands / forgotten imports. Suppressed implicitly when consumers use auto-registry (`hydrateIslandsAuto()`) — there's nothing to manually sync.
4. **`nested-island`** — an `island()` whose loader-imported file ALSO contains an `island()` call. Outer's `hydrateRoot` would replace the inner subtree before its loader runs.
5. **`dead-island`** — an `island()` declared in a file that no other source imports (statically OR dynamically). Heuristic catches the common shape of "declared but never wired up." Auto-registry's `() => import('PATH')` counts as an import, so registered islands stay alive.

Each finding ships with file path + line/column + actionable fix suggestion. Both detectors emit JSON via `--json` for CI gates. Implementation: [packages/core/compiler/src/island-audit.ts](packages/core/compiler/src/island-audit.ts) — entirely syntactic (TypeScript compiler API, no type-check pass), bisect-verified per-detector in [tests/island-audit.test.ts](packages/core/compiler/src/tests/island-audit.test.ts) (19 tests cover broken + fixed shapes for each finding type, plus discovery + formatter + integration). Bisect-verified at the real-repo level too: planting a typo + a never-strategy registry entry in `examples/islands-showcase` produces both findings with correct line numbers + cross-file pointers.

**Real-app coverage**: `examples/islands-showcase` exercises one island per strategy (Counter / IdleClock / VisibleComments / MobileMenu / StaticBadge), with `VisibleComments` pairing `hydrate: 'visible'` + `prefetch: 'idle'` as the canonical pattern, and uses `hydrateIslandsAuto()`. Real-Chromium e2e gate at `e2e/islands-showcase.spec.ts` + `e2e/islands-showcase-mobile.spec.ts` (separate config `e2e-configs/islands-showcase.config.ts`, separate webServer boot — same isolation rationale as ui-regression / app-showcase / compat-layers). Per-package browser smoke at `packages/core/server/src/tests/islands.browser.test.tsx` covers each strategy in isolation under real `IntersectionObserver` / `requestIdleCallback` / `matchMedia` (the unit-level happy-dom suite at `client.test.ts` cannot exercise these), plus 3 prefetch specs proving the chunk warms BEFORE the hydration trigger fires. Auto-registry unit tests at `packages/tools/vite-plugin/src/tests/islands-registry.test.ts` (10 specs covering scanner edge cases — prescan walk skips, never-strategy omission, duplicate-name dedup, missing-name skip, multiple files, stub mode).

### JSX Compiler

**Dual-backend architecture**: Rust native binary (napi-rs, 3.7-8.9x faster) with automatic JS fallback.

- **Rust path** (`packages/core/compiler/native/`): full reactive pass in Rust using `oxc_parser`/`oxc_ast` crates directly — zero JSON serialization, zero JS object traversal. Single-pass recursive walk with `FxHashMap`-cached `isDynamic` analysis. 2,800+ lines of Rust, compiled via `cargo build --release` to a ~1MB `.node` binary.
- **JS fallback** (`src/jsx.ts`): uses `oxc-parser` (Rust NAPI binding) for parsing + JS reactive pass. Activated automatically when the native binary isn't available (CI, WASM, wrong platform).
- **Auto-detection**: `transformJSX()` loads the native binary via `createRequire(import.meta.url)` (ESM-safe), falls back per-call with try/catch so a Rust panic doesn't crash the Vite dev server.
- **527 tests**: 347 original + 180 cross-backend equivalence tests verifying identical output between JS and Rust on Unicode, TypeScript syntax, control flow, classes, string collisions, circular refs, and real-world patterns.

**Dual-backend deep validation (2026-06) — text-vs-vnode classifier made generic (whole-class fix) + a component-prop registration fix; remaining gaps characterized.** The curated `native-equivalence.test.ts` fixtures all pass (380, byte-identical JS↔Rust), but a **644-file real-corpus byte-diff sweep** (every `.tsx`/`.jsx` in `examples/`+`docs/`+`packages/`, run through BOTH backends) surfaced Rust-only divergences the curated set missed. Two were in `contains_jsx_in_expr` (the text-vs-vnode child classifier) and one in component-prop registration. Reduction: 30 → 20 code-diffs, 14 → 4 REAL primitive-profile divergences. The fixes (`native/src/lib.rs`, locked by the `corpus-sweep regressions` describe block, bisect-verified 7/9 fail against the pre-fix binary):
  1. **Text-vs-vnode classifier rewritten to a GENERIC walk (whole-class fix).** The JS oracle (`jsx.ts:containsJSXInExpr`) is a generic recursive walk over EVERY child (`forEachChild`) — returns true if any descendant is a JSXElement/Fragment. The Rust `contains_jsx_in_expr` was a hand-rolled arm-by-arm PARTIAL mirror that drifted and mis-classified nested-JSX shapes as TEXT (`_bind .data` → renders `[object Object]`): `obj?.map(x => <jsx/>)` (JSX in a `ChainExpression`'s call args — hit `@pyreon/zero`'s `image.tsx` `img.formats?.map(f => <source/>)`) and IIFEs `(() => { … return <jsx/> })()` (JSX in the CallExpression CALLEE — hit `@pyreon/zero-content`'s `Playground.tsx`). Both were the SAME class: the partial walk missing a node shape. Rather than patch shapes one at a time, `contains_jsx_in_expr` now uses an `oxc_ast_visit::Visit` walker (a `JsxFinder` that flips a flag on `visit_jsx_element`/`visit_jsx_fragment`) — the trait's default walk IS `forEachChild`, so RS classification is **identical to the JS oracle BY CONSTRUCTION** (both visit every node). This kills the entire "RS misses a nested-JSX shape → `[object Object]`" class, including exotic shapes not present in the corpus. (`oxc_ast_visit = "0.126"` added as a direct dep — it was already a transitive dep of `oxc_parser`.) The component-detection walkers (`body_contains_jsx`/`expr_contains_jsx`/`stmt_contains_jsx`, used by `maybe_register_component_props`) are a SEPARATE concern and left untouched.
  2. **`.map`/any-CallExpression-argument callback param over-bound as reactive** (documented-contract violation + per-row perf). The Rust `in_jsx_child_callback` flag only suppressed component-prop registration for DIRECT JSX-child render callbacks, NOT nested CallExpression-arg callbacks (`items().map((item) => <div key={item.id}><span>{item.label}</span></div>)`), so RS emitted a wasteful per-row `_bind()` for `{item.label}` where JS bakes static `textContent`. Fixed by a new `in_callback_argument` Ctx flag set in the CallExpression arm when the argument is directly an arrow/function, consumed by the arrow/function arms alongside `in_jsx_child_callback` — mirrors JS `jsx.ts:1801` (`parent === CallExpression && arguments.includes(node)`). Attribute-value render fns (`renderItem={(p) => …}`) are NOT call-args → stay reactive props (parity preserved).

  **Remaining 4 code-divergences are STRUCTURAL, not renders-wrong (characterized, not fixed).** All four (`collab-board/CardPanel.tsx`, `zero/image.tsx`, two `_error.tsx`) are the **prop-derived-const inlining heuristic** differing between backends — JS and Rust make different decisions about WHEN to inline a `const` derived from `props.*` at a JSX use site, and the divergence is **bidirectional** (JS inlines `const editor = createEditor(props.x)` into a reactive `_rp(() => …)` where RS keeps the stable `instance={editor}` var — RS arguably safer there, doesn't re-run the factory; conversely RS inlines `const message = props.error…` into a reactive child where JS keeps `message` bare/static). Both backends render correct output for the common case; forcing byte-parity would require aligning the deep transitive-resolution heuristics (`resolveVarToString` ↔ `find_init_in_expression`) — high-risk, not a renders-wrong bug, deferred. The sweep also found **296 reactivity-LENS span-offset divergences** (RS lens spans drift from JS on complex files), but this is an **unconsumed sidecar**: `analyzeReactivity` (the only LSP-facing consumer) forces `transformJSX_JS`, and production codegen is byte-identical with the lens option on/off — so the lens drift has zero production/LSP impact (the `compareLens` gate covers 9 fixtures; a stronger gate is a low-priority follow-up). Reproduce the sweep: a throwaway dual-backend diff script over the corpus (no committed scratch — the `corpus-sweep regressions` fixtures are the permanent lock).

**Signal auto-call**: `const x = signal(...)` and `const x = computed(...)` declarations are tracked. Bare references in JSX are auto-called: `{x}` → `{() => x()}`. Scope-aware — shadowed variables (inner `const x = 'text'`, function parameters, destructured params) are NOT auto-called. Already-called signals (`x()`) are NOT double-called. Cross-module support via the Vite plugin's signal export registry (pre-scans all files in `buildStart`, detects `export const x = signal(`, separate `export { x }`, and `export default signal(`). The `knownSignals: string[]` option on `TransformOptions` seeds the compiler with imported signal names. **Both backends implement signal auto-call**: the Rust binary tracks `signal_vars` / `shadowed_signals` / `is_active_signal` with full scope-aware shadowing semantics, exercised by 15+ cross-backend equivalence tests in [`src/tests/native-equivalence.test.ts`](packages/core/compiler/src/tests/native-equivalence.test.ts) — JS and Rust paths produce byte-identical output for every signal-auto-call pattern.

`shouldWrap` only wraps if the fused `isDynamic` check finds a non-pure call, props/prop-derived access, or signal variable reference.
Static JSX nodes hoisted to module scope as `const _$h0 = ...`.
Template emission: JSX element trees with ≥1 DOM element emit `_tpl()` + `_bind()`.
Supports mixed element+expression children (via `childNodes[]` indexing), multiple expressions, and fragment inlining.
Reactive text writes `TextNode.data` (not `.textContent`). **Sole-dynamic-text children bake a `' '` placeholder text node into the template HTML and grab it via `.firstChild`** — no `createTextNode`/`appendChild` per instantiation (PR #1540; per ROW under `<For>`); mixed element+text content emits a `<!>` comment placeholder swapped via `createTextNode("") + replaceChild`.
Per-text-node independent binding: each text node gets its own binding (`_bindText` / `_bindDirect` / `sel.subscribe` fast paths; `_bind()` fallback) for fine-grained reactivity (instead of grouping all bindings).
**Bare-signal attribute values take the direct path**: a bare signal/computed as an attribute value (`class={active}`, `style={styleSig}`, `data-x={count}`, DOM props) emits `_bindDirect(active, …)` — **byte-identical** to the accessor form `class={() => active()}` — instead of falling through to a re-tracking `_bind(() => … active() …)`. Before this fix the two semantically-identical forms compiled differently (the bare form re-tracked). `tryDirectSignalRef`'s bare-identifier branch is guarded by `isActiveSignal` (selectors stay on the general path; `_bindDirect` also renderEffect-falls-back for any source lacking `.direct`) and **scoped to attributes via `allowBareSignal`** — bare-signal TEXT children (`<div>{name}</div>`, the dominant text shape) keep their existing `_bind` emission, since promoting that to `_bindText` is a larger, separately-measured change. Uses `inner.name` (NOT `sliceExpr` — the signal-auto-call records a `active`→`active()` source replacement). Cross-backend byte-identical (JS `jsx.ts:tryDirectSignalRef` + the attr call site's `true`; Rust `lib.rs:try_direct_signal_ref` + `allow_bare_signal`); locked by `jsx.test.ts` "bare-signal attribute → _bindDirect" + `native-equivalence.test.ts`, bisect-verified.
**Element-conditional children keep the `_tpl` fast path**: a DOM wrapper around an inline element-conditional (`<div class="card">{() => open() ? <Panel/> : <Empty/>}</div>`, `<section>{n() > 0 && <List/>}</section>`, or a `.map(x => <li/>)` child) templatizes the wrapper (`_tpl("<div class=\"card\"><!></div>", …)`) and routes the conditional child through `_mountSlot` + a `<!>` placeholder — the SAME path `.map`-returning children and element-valued-`const` children already take. Previously the whole wrapper bailed to the jsx runtime (`h()`). The conditional's own reactive boundary (`mountChild` → `mountReactive`, which creates its own marker before the placeholder so `_mountSlot`'s placeholder-removal can't strand it) is unchanged — behaviour is identical, only the wrapper gains cloneNode. The distinction in `countChildForTemplate`: a DIRECT static JSX element/fragment child (`{<span/>}`) returns `-1` (keeps the cheaper static-hoist path); an expression that CONTAINS JSX but isn't directly one (ternary / `&&` / `.map`) returns `0` (templatable via `_mountSlot`). Inner JSX inside the conditional stays raw (compiled downstream by esbuild to `h()`), consistent with how ALL expression-nested JSX is handled (component props `_rp(() => …<x>)`, `.map` callbacks). Cross-backend byte-identical (JS `jsx.ts:countChildForTemplate` + `processOneChild`; Rust `lib.rs:count_child_for_template` + `process_one_child`); runtime contract locked by `runtime-dom/src/tests/element-conditional-tpl.browser.test.tsx` (reactive swap + disposal).
Pure static call detection: 40+ functions treated as pure (Math._, JSON._, Object.keys/values/entries, Array.isArray, etc.) — not wrapped in reactive getters.
Spread props on root element: when a root element has `{...props}`, emit `_tpl()` + `_applyProps()` instead of falling back to `h()` calls.
Reactive props inlining: the compiler auto-detects `const` variables derived from `props.*` or `splitProps` results and inlines them at JSX use sites. `const x = props.y ?? 'default'; return <div>{x}</div>` compiles to `_bind(() => { t.data = (props.y ?? 'default') })` — fully reactive. Transitive resolution is fully AST-based: `collect_prop_derived_idents` walks `IdentifierReference` nodes in the expression subtree (never string-scans source text). `const a = props.x; const b = a + 1` inlines `b` as `((props.x) + 1)`. Only `const` is tracked (`let`/`var` are mutable, unsafe to inline). Non-JSX usage (e.g., `console.log(x)`) stays static (uses captured value). **Circular references are safe**: cycle detection via `resolving` set breaks infinite recursion, leaving the cyclic identifier as-is (falls back to captured const value at runtime).

**Component-child stable-reference carve-out**: when the JSX parent is a COMPONENT (uppercase tag) AND the expression is a stable reference (bare `Identifier`, simple non-computed `MemberExpression` chain `obj.x.y`, OR any of the above wrapped in TS type-only layers `as T` / `satisfies T` / `!` / parens) AND the expression does NOT reference a tracked signal var, the compiler emits the inlined value BARE — without the `() => …` accessor wrap. The default for component children is otherwise wrap (`<Comp>{count()}</Comp>` → `<Comp>{() => count()}</Comp>` for reactive consumers). The carve-out prevents the silent footgun where a pass-through pattern (`const children = childHolder.children; <Inner>{children}</Inner>` after `splitProps`) delivered `props.children` as a FUNCTION instead of the expected `VNode | VNode[]` — libraries that iterated children directly or `cloneVNode`'d them saw `{type: undefined}` and the DOM rendered literal `<undefined>` tags. Reproducer was the kinetic Stagger + bokisch.com Intro shape (PR #731 shipped the library-side `resolveChildren` workaround; this compiler fix is the upstream root cause). The carve-out is complementary to the library-side fix — they each cover different shapes (stable refs vs CallExpression-inside-JSX-child like `cloneVNode(...)` calls). Signal references explicitly KEEP the wrap (`<Comp>{count}</Comp>` → `<Comp>{() => count()}</Comp>` so the receiving component subscribes reactively). Cross-backend (JS + Rust native binary) implemented byte-identically. Reference: `packages/core/compiler/src/jsx.ts:handleJsxExpression` + `isStableReference` + `unwrapTypeLayers`; tests at `packages/core/compiler/src/tests/component-child-no-wrap.test.ts` + cross-backend specs in `native-equivalence.test.ts`.

**`<For>`/render-callback item params are NOT reactive props**: a JSX-child render callback — `<For each={rows}>{(row) => <td>{String(row.id)}</td>}</For>` (also `<Index>`, `<Show>`, `<Switch>`) — receives a runtime ITEM the framework passes per row, NOT reactive component props. So a BARE property read (`row.id`) is provably static and the compiler bakes it as a one-time `textContent = String(row.id)` instead of a per-row `_bind(() => …)` renderEffect. (Reactivity in Pyreon is via signal CALLS — `row.label()` / `() => row.x` — which keep their own `_bindText`/wrap paths; only non-call property reads bake.) **Detection (`maybeRegisterComponentProps`)**: a function is registered as a component (first param = reactive `props`) ONLY if it returns JSX AND is NOT a `.map`/`.filter` CallExpression callback AND is NOT a JSX-child render callback (parent is a `JSXExpressionContainer` whose grandparent is a `JSXElement`/`JSXFragment`). Attribute-value functions (`component={(p) => …}`) are NOT skipped — those can be real inline components receiving props. **Impact**: a 1k–10k-row `<For>` previously allocated 1k–10k unnecessary `renderEffect`s + disposer closures (retained until unmount — a real per-row CPU + retained-heap cost); the compiler now matches hand-written `_tpl()` for the dominant `{item.id}` shape. Cross-backend byte-identical (JS `maybeRegisterComponentProps` JSX-child skip + Rust `Ctx.in_jsx_child_callback` flag set in `handle_jsx_expression_child`, consumed by the arrow/function `walk_expression` branches). **The JS skip also covers ANY CallExpression-argument callback** (`items().map((item) => …)`, `.filter`, etc. — `jsx.ts:1801` `parent === CallExpression && arguments.includes(node)`); the Rust port originally missed the NESTED `.map`-arg case (the `in_jsx_child_callback` flag was cleared by the outer `{() => …}` JSX-child callback, so the inner `.map((item) => …)` callback's param was re-registered as reactive props → a per-row `_bind` for `{item.label}` instead of static `textContent`). Fixed by the dedicated `in_callback_argument` Ctx flag (see "Dual-backend deep validation" above) — set in the CallExpression arm when the argument is directly an arrow/function. Reference: `packages/core/compiler/src/jsx.ts:maybeRegisterComponentProps` + `native/src/lib.rs:maybe_register_component_props_arrow` / `in_jsx_child_callback` / `in_callback_argument`; tests at `packages/core/compiler/src/tests/static-text-baking.test.ts` (`<For> render-callback item params` describe block) + `native-equivalence.test.ts` (`corpus-sweep regressions`), self-discriminating + bisect-verified.

### Compile-time rocketstyle collapse (opt-in — `pyreon({ collapse: true })`)

P0's vertical slice — shipped. A literal-prop rocketstyle call site
(`<Button state="primary" size="medium">Save</Button>` — every dimension
prop a string literal, no spread, static-text children) collapses from a
5-layer wrapper mount (rocketstyle → attrs HOC → Element → Wrapper →
styled) into ONE `_rsCollapse` cloneNode. E2 measured **44× wall-clock**,
`mountChild` 9→1, `styler.resolve` 22→0. **OFF by default** (zero
behaviour change); enable with `pyreon({ collapse: true })` or
`pyreon({ collapse: { sources, components, provider, theme, mode } })`.

**How it works (parity by construction — no chain reimplementation):**

- **Resolve** (`@pyreon/vite-plugin:createCollapseResolver`): ONE
  programmatic Vite-SSR server bound to the consumer's OWN `vite.config`
  renders the REAL component twice (`<Provider mode="light">` /
  `"dark"`), captures the root `class` per mode + the styler rule text
  via `sheet.getStyleRules()`, strips the root class → a `_tpl` template.
  Because it's the same `renderToString` + `@pyreon/styler` code path the
  app uses (styler's FNV-1a class hash is identical SSR vs DOM — its
  hydration contract), the resolved class is byte-for-byte the
  client-mounted class. Memoised; `closeBundle` disposes the server.
  Generalises beyond app-local — works cross-package too (the RFC's
  "sidecar manifest" deferral proved unnecessary).
- **Scan + detect** (`@pyreon/compiler:scanCollapsibleSites` +
  `tryRocketstyleCollapse`): a SINGLE shared `detectCollapsibleShape`
  bail catalogue used by BOTH the plugin scan (what to resolve) and the
  compiler emit (what to collapse) — keys cannot drift.
  `rocketstyleCollapseKey(localName, props, childrenText)` is the
  canonical, order-independent key both sides compute. Bail (keep the
  normal mount) on: any non-literal/`{expr}`/signal dimension prop,
  `{...spread}`, element/expression children, non-candidate component,
  or an unresolved key. Conservative — uncertain ⇒ no collapse.
- **Emit** (`TransformOptions.collapseRocketstyle`): replaces the JSX with
  `__rsCollapse(<templateHtml>, <lightClass>, <darkClass>, () =>
  __pyrMode() === "dark")` + a once-per-module idempotent
  `__rsSheet.injectRules(<rules>, <ruleKey>)` (RFC decision 1 dual-emit;
  decision 4 hoisted `_tpl`). Only the CLIENT graph collapses — the SSR
  graph keeps the real mount (renderToString needs the VNode tree, and
  the resolver itself SSR-renders). **Both backends emit collapse.** The
  port to Rust shipped all four variants (`__rsCollapse` full /
  `__rsCollapseH` on\*-handler partial / `__rsCollapseDyn` +
  `__rsCollapseDynH` dynamic-prop / element-child reusing `__rsCollapse`),
  each locked byte-for-byte against the JS path by the cross-backend
  equivalence suite. The old `transformJSX` force-route to the JS path
  (`if (options.collapseRocketstyle) return transformJSX_JS(...)`) was
  REMOVED once all variants landed: the dispatcher now lowers the
  `collapseRocketstyle` config from its `Set`/`Map` shape to the napi
  array/Record shape (`toNativeCollapse`) and threads it as `transformJsx`'s
  6th arg, so a collapse build gets the native backend's 3.7-8.x× transform
  speed (the JS path stays the graceful fallback when the native binary is
  absent). Verified end-to-end by the `verify-modes ui-showcase × spa`
  collapse cell building the rs-collapse / dyn / elem probes through native.
- **Runtime** (`@pyreon/runtime-dom:_rsCollapse`): one html-keyed `_tpl`
  cloneNode (shared parsed template across N mounts) whose class is
  reactively bound to the live mode accessor — a mode swap re-runs ONLY
  the className assignment on the SAME node (no remount). The styler
  injection is the emitted code's job (`@pyreon/styler:injectRules` —
  raw pre-resolved rule text, idempotent by key, NO re-hash since the
  class is already baked into the template); `runtime-dom` stays
  layer-pure (never imports styler/ui-core).

**Proven:** 5 layers tested — styler `injectRules` (3 browser specs),
`_rsCollapse` (4 real-Chromium specs: light class, mode-flip-no-remount,
children dispose, shared template), resolver vs REAL ui-components Button
via Vite SSR (8 specs incl. determinism + graceful bail), compiler
detection/emission/bail-catalogue/dedupe (13 specs, bisect-verified:
disable detection → 4 emission specs fail), end-to-end pipeline (real
Button → resolver → scanner → compiler emits `__rsCollapse` with the real
classes byte-for-byte). **SSR→hydrate seam closed + proven** (the prior
"thinnest unproven seam"): `_rsCollapse` returns a `NativeItem` via
`_tpl`, so under `hydrateRoot` it hits `hydrate.ts`'s `__isNative === true`
branch — the framework-wide, correctness-first `_tpl` hydration path
SWAPS the SSR (real 5-layer) subtree for the freshly-built collapsed node
(same final DOM, byte-for-byte class via the FNV-1a SSR↔DOM contract, NO
crash, NO mismatch warning, reactivity intact). NOT collapse-specific —
`_rsCollapse` inherits it from `_tpl` for free; no runtime change needed.
Proven by a real-Chromium e2 spec (`examples/experiments/e2-static-rocketstyle/e2.browser.test.ts`
— `SSR markup → hydrateRoot(_rsCollapse)`): real Button outerHTML placed
as the container's SSR DOM → `hydrateRoot` the collapsed `_rsCollapse` →
asserts (A) the in-DOM node is the swapped clone, not the SSR node, (B)
byte-for-byte SSR class parity + `isEqualNode` + identical computed style
+ zero hydration-mismatch `console.error`, (C) reactivity SURVIVES — a
post-hydrate mode flip patches `className` IN PLACE on the SAME node (no
remount). Bisect-verified-with-restore: neutralizing the `__isNative`
branch fails ONLY this spec with `expected <button> not to be <button>`
(no swap → still the SSR node); restored → 11/11. Additive: all 1079
compiler tests unchanged with collapse off. **Build-artifact gate closed + proven** (the prior deferred coverage item): a `verify-modes` `ui-showcase × spa` cell with `pyreon({ collapse: true })` builds a dedicated `examples/ui-showcase/src/routes/rs-collapse-probe.tsx` (canonical literal-prop `<Button state="primary" size="medium">Save</Button>` — ui-showcase's other Buttons all carry `onClick` and correctly bail) through the full plugin→resolver→compiler→bundle pipeline in a real production `vite build`, and asserts the emitted `rs-collapse-probe-*.js` chunk collapsed via MINIFICATION-STABLE, collapse-exclusive fingerprints (the baked-children template literal `…Save</span></button>` + the `=== "dark"` dual-emit mode accessor) — NOT the pre-minification `__rsCollapse(` identifier, which Vite renames in prod (`__rsCollapse`→`t`, `__rsSheet`→`e`) so a literal-identifier assertion can never match a real build (that was a real bug in the gate's first cut, caught by the build itself proving the feature works while the assertion didn't). Bisect-verified-with-restore: flipping the cell to `collapse: false` builds the real 5-layer Button → the probe chunk has neither fingerprint → the cell fails with `expected probe chunk … to be COLLAPSED (bakedChildren=false, modeAccessor=false)` while the plain `ui-showcase × spa` cell stays green; restored → 2/2 green. First proof of the fully-assembled production artifact (unit + e2 layers prove the pieces). **Build-only is now an EXPLICIT, intentional, tested contract — not a deferral.** The collapse block in `vite-plugin.ts:transform` is gated `if (collapseEnabled && isBuild && !isSsr)`. Dev (`command: 'serve'`) deliberately keeps the normal rocketstyle mount and the resolver is NEVER constructed (zero per-dev-process orphan nested Vite server). **Why this is correct, not a limitation:** the resolver freezes each site's class from a SEPARATE nested Vite-SSR module graph and caches it — in `vite dev` that frozen class would NOT react to the user's theme-source HMR edits, which is strictly *worse* than the normal mount (which IS reactive). So collapse trades dev-time theme-edit reactivity for production speed; dev must keep the normal mount. Pre-fix the block was gated only `&& !isSsr`, so it silently attempted to run in dev (per-transform nested-Vite spin + a frozen-against-HMR class). The plugin now surfaces the contract ONCE per dev process via `this.info('[Pyreon] collapse is build-only — …')` so an opted-in `vite dev` consumer isn't left wondering why nothing collapsed. Bisect-verified-with-restore (`packages/tools/vite-plugin/src/tests/rocketstyle-collapse-dev.test.ts`, stub resolver so the bisect is faithful locally with no `lib/`): remove the `&& isBuild` gate → serve-mode transform emits `__rsCollapse(` → the `command:serve` spec fails on `.not.toContain('__rsCollapse(')`; the companion `command:build` spec (same source, same stub) still collapses, proving the gate is the only difference; restored → 2/2. Reference: `packages/tools/vite-plugin/src/rocketstyle-collapse.ts`,
`packages/core/compiler/src/jsx.ts` (`scanCollapsibleSites` /
`tryRocketstyleCollapse` / `rocketstyleCollapseKey`),
`packages/core/runtime-dom/src/template.ts:_rsCollapse`,
`packages/ui-system/styler/src/sheet.ts:injectRules`.

**Dynamic-prop partial-collapse — fully shipped.** Extension targeting the dynamic-prop bail bucket (15.4% of all real-corpus sites per the bail census, vs 7.8% for the `on*`-handler bucket). Two runtime helpers cover the dynamic-collapse surface, both layer-pure (no styler/ui-core imports; styler injection stays the emitted code's job via the existing `injectRules`):

- **`_rsCollapseDyn(html, classes, valueIndex, isDark, bind?)`** — generalises `_rsCollapse`'s 2-class dispatch to N-class for sites where ONE dimension prop is an enumerable dynamic expression. Canonical case: `<Button state={cond ? 'primary' : 'secondary'}>` — ternary of two literals.
- **`_rsCollapseDynH(html, classes, valueIndex, isDark, handlers, bind?)`** — combined helper for the handler-bearing variant (`<Button state={cond ? 'a' : 'b'} onClick={h}>`), the most common real-world shape. Structurally the union of `_rsCollapseDyn`'s class dispatch + `_rsCollapseH`'s canonical `_bindEvent` handler attachment. Closed the bulk of the 15.4% bucket the strict no-handler `_rsCollapseDyn` could only address at 0.2%.

Class layout for both: **stride-2, value-major** — index = `2 * valueIndex + (isDark ? 1 : 0)`. Both accessors are reactive — a value flip OR a mode flip patches className IN PLACE on the SAME node (no remount); handlers stay attached across both.

Compiler path: `detectDynamicCollapsibleShape` (ternary-of-two-literals on ≤1 dim prop; composes with `on*`-handler relaxation) → `scanCollapsibleSites` expands each dynamic site into TWO `CollapsibleSite` entries (one per literal) → the resolver pre-renders both via the existing SSR pipeline → `tryRocketstyleCollapse`'s fallthrough chain (full → on*-handler-partial → dynamic) emits `__rsCollapseDyn(...)` (no handlers) or `__rsCollapseDynH(...)` (with handlers) when both expanded sites resolve AND structural template parity holds. Conservative bail discipline at every layer: half-resolved sites bail, divergent templates bail, multi-axis bails (separable scope — 2^N value-set growth).

Out-of-range `valueIndex` coerces to empty className (graceful — never crashes); compiler is source of truth for the index domain. Bisect-verified at three layers: 7 real-Chromium specs for `_rsCollapseDyn` + 8 for `_rsCollapseDynH`; compiler detector + emit + scan specs (28 total); build-artifact `verify-modes ui-showcase × spa` cell with a dual-Button probe (`Dyn` + `DynH`) that asserts both the stride-2 class array fingerprint AND the handler-combined `===\`dark\`,{` 5-arg signature unique to `_rsCollapseDynH`. Reference: `packages/core/runtime-dom/src/template.ts:_rsCollapseDyn` + `_rsCollapseDynH`, `packages/core/compiler/src/jsx.ts:detectDynamicCollapsibleShape` + `tryDynamicCollapse`, `examples/ui-showcase/src/routes/rs-collapse-dyn-probe.tsx`.

**Element-child collapse — fully shipped.** Extension targeting the element-child bail bucket (the largest remaining frontier at ~9.2% of all real-corpus sites; the recursively-static SAFE subset measured 3.0%, lifting addressed coverage 83.0% → **86.0%** per the bail census). **NO new runtime helper** — unlike partial (`_rsCollapseH`) and dynamic (`_rsCollapseDyn`/`DynH`), element-child reuses the EXISTING `_rsCollapse(html, light, dark, isDark)` UNCHANGED. Reason: the resolver SSR-renders the REAL component WITH its child subtree and bakes the FULL output HTML, so the cloned `_tpl` template already contains `<button class=X><span>…</span>Save</button>` and `_rsCollapse` clones the whole thing. The emit is byte-identical to full-collapse; only the detector + child-serialization + scanner-expansion + resolver-reconstruction are new.

- **Correctness boundary (recursively-static)** — `detectStaticElementChild` / `detectElementChildCollapsibleShape` (`@pyreon/compiler` `jsx.ts`) accept an element child iff, recursively: it's a **DOM element** (lowercase tag — a component child has its own setup/reactivity → hard bail), **every attribute is a string literal** (no `{expr}`, no spread, no boolean-shorthand, NO `on*` handlers — a child handler can't survive baking into a static clone), and **children are static text OR recursively-static elements**. Anything else bails (the site keeps its normal mount). Conservative by construction: a false-negative (missed collapse) is fine; a false-positive (baking reactivity) is the only real danger and the rule structurally prevents it.
- **Scan + serialize** — `scanCollapsibleSites` expands each static-element-child site into ONE `CollapsibleSite` carrying `childTree` (the structured `StaticChild[]` subtree) + `childrenText = serializeStaticChildren(childTree)` (the cache/key discriminator). `serializeStaticChildren` is deterministic — distinct subtrees produce distinct strings, so the `rocketstyleCollapseKey` (reused unchanged) can't collide across subtrees. Child text is normalized via the compiler's own `cleanJsxText` (verbatim if no newline; line-trim + single-space-join if multiline) so the baked subtree is byte-faithful to a real mount.
- **Resolver reconstruction** — `ResolveInput.childTree` channel + `buildChildVNodes(tree, h)` (`@pyreon/vite-plugin` `rocketstyle-collapse.ts`) rebuild the real child VNodes via `h()` (`typeof c === 'string' ? c : h(c.tag, c.props, ...buildChildVNodes(c.children, h))`) and render them as the component's children instead of `childrenText` as a string. The resolve cache key includes the child tree.
- **Emit** — `tryRocketstyleCollapse` falls through `tryPartialCollapse → tryDynamicCollapse → tryElementChildCollapse`; the element-child arm emits the UNCHANGED `__rsCollapse(...)` on key match. `StaticChild` / `StaticChildNode` are exported from `@pyreon/compiler`.

**Proven** at every layer (the resolver reconstruction was the genuine correctness risk — it's the only new render path): resolver real-Vite-SSR specs render the REAL `@pyreon/ui-components` Button WITH a `childTree` and assert the baked subtree appears in `templateHtml` — single element, mixed text+element ("Press `<kbd>`Enter`</kbd>` now"), and distinct-subtree-distinct-template (3 specs, bisect-verified: neutering the `childTree` branch fails exactly those 3); compiler detector + scan + emit specs (PR 1+2, bisect-verified); real-Chromium `rs-collapse.browser.test` specs proving the baked static subtree renders as real nested elements + survives a mode flip with root AND child node identity preserved (no remount) + a recursive 2-level subtree; build-artifact gate (`verify-modes ui-showcase × spa` cell + `examples/ui-showcase/src/routes/rs-collapse-elem-probe.tsx` — asserts the baked `<span class="elemico">ElemSave` subtree + the `=== "dark"` mode accessor in the route chunk; bisect-verified: collapse-off → no `=== "dark"` → fails); census reclassification (the element-child static-addressable sites now count toward the 86.0% TOTAL ADDRESSED headline). Reference: `packages/core/compiler/src/jsx.ts:detectStaticElementChild` + `detectElementChildCollapsibleShape` + `serializeStaticChildren` + `tryElementChildCollapse`, `packages/tools/vite-plugin/src/rocketstyle-collapse.ts:buildChildVNodes`, `examples/ui-showcase/src/routes/rs-collapse-elem-probe.tsx`.

**Collapse coverage ceiling — 86.0% is the practical maximum (measured, not assumed).** The `collapse-bail-census` test measures the next-frontier go/no-go before any further build. As of the element-child PR, **86.0%** of 566 real `@pyreon/ui-components` call sites collapse end-to-end (full 72.8% + on\*-handler partial 7.8% + dynamic-prop 2.5% + element-child 3.0%). The ~14% un-addressed remainder is OUT OF SCOPE for **structural** reasons, not laziness — and the census distinguishes the two cases:
- **Structurally IMPOSSIBLE** (will never collapse): element-child where a child is a COMPONENT (it owns its own setup/reactivity/state — baking would freeze it); dynamic-prop that's non-enumerable (`state={fn()}` has no finite value set to pre-render); expression children (`{expr}` — dynamic, can't bake); spread (`{...x}` — unknown prop set, no resolvable class); boolean-attr (a valueless attr is ambiguous between a static HTML bool like `disabled` and a rocketstyle dimension shorthand like `primary` — the compile-time detector can't disambiguate without component introspection).
- **Bounded-but-DEFERRED** (could collapse, judged not worth it): **multi-axis-ternary** (≥2 ternary-of-two-literals dimension props → 2^k pre-renderable combinations). The census added a `multiAxisTernaryAddressable` counter for exactly this go/no-go — **measured 0 sites in the real corpus** (no component uses ≥2 ternary dims), so building dynamic-multi-axis collapse would address zero real sites. The counter is logged + locked in `collapse-bail-census.test.ts`; if the corpus ever grows such a site the number flips and the decision can be revisited. Until then, **86.0% is the ceiling and no further collapse work is warranted.**

**Element-child — SSR→hydrate + perf, CI-gated + MEASURED (the gates' final layer).** The SSR→hydrate seam and the perf win are proven against the runtime AND the real component in CI (the `examples/experiments/e2-static-rocketstyle` E2 file is a one-off, un-gated measurement; these are the gated equivalents):

- **`@pyreon/runtime-dom` `rs-collapse.browser.test.ts`** (real Chromium, CI `Test (browser)`): (1) SSR markup with a baked element subtree → `hydrateRoot(_rsCollapse(strippedTpl, …))` → the `__isNative` swap replaces the SSR node (in-DOM node ≠ the SSR node) with NO hydration-mismatch `console.error`, the baked `<span class="ico">` child is carried on the swapped-in node, and a post-hydrate mode flip patches the root class IN PLACE (same node, child subtree preserved) — the element-child SSR→hydrate seam (inherited from `_tpl`, proven for the baked-subtree shape). (2) **Perf signature** — a deliberately DEEP subtree (`<button>Press <kbd>…</kbd> <span class="w"><b>Hi</b></span></button>`) mounts with EXACTLY ONE `runtime.mountChild` + `runtime.tpl ≥ 1` regardless of depth (the whole subtree is ONE `cloneNode`). Counter read via a manual `globalThis.__pyreon_count__` sink (no perf-harness dep; counters fire under vitest's Vite DEV mode). Bisect-verified: replacing `_rsCollapse` with a per-element mount makes `mountChild` scale with depth.
- **`@pyreon/ui-components` `elem-child-collapse.browser.test.tsx`** (real Chromium, CI `Test (browser)`): real `<Button state size><span class="ico">Save</span></Button>` → capture outerHTML → strip root class → `_rsCollapse` → **byte-for-byte parity** (`isEqualNode` the real 5-layer mount, identical class + computed style + the nested `<span class="ico">` present as an element). **Perf MEASURED** (provider amortized for fairness): the real element-child Button fires **9** `runtime.mountChild`; the collapsed clone fires **1** (9× fewer) — and a wall-clock benchmark (N=150 × 5 runs) measures **~8.6 ms → ~0.8 ms (~11× faster)**. Counter delta is the deterministic CI lock; wall-clock asserts direction only (`collapsed < baseline`, robust under CI load) + logs the figure. Bisect-verified: replacing the collapsed mount with the real Button makes `collapsedMountChild` 9 → the `toBe(1)` assertion fails. (ui-components gained `@pyreon/runtime-dom` + `@pyreon/perf-harness` devDeps for these imports.)

### Reactivity Lens (experimental — surface the compiler's analysis back to the author)

The compiler ALREADY decides per-expression whether code is reactive (`isDynamic`/`shouldWrap`/`unwrapAccessor`'s `isReactive`), emits codegen, and discards the analysis. The Lens pipes that ground truth back to the editor so Pyreon's silent rule — "components run once; reactivity depends on WHERE you read a signal" — becomes **visible at the cursor**. Prevention upstream, not another downstream detector.

- **Additive, opt-in.** `TransformOptions.reactivityLens: true` populates `TransformResult.reactivityLens: ReactivitySpan[]`. Emitted `code` is **byte-identical** with the option on or off (asserted by an `additive` test across fixtures + all 1000 compiler tests passing unchanged). The lens is recorded at the SAME ~5 codegen-decision sites as the existing `replacements.push` — it never runs a second analysis pass.
- **Kinds** (each a faithful RECORD of a codegen branch, never an approximation): `reactive` (text `_bind`/`_bindText` / `() =>` wrap), `reactive-prop` (`_rp(() => …)`), `reactive-attr` (live attribute binding), `static-text` (the literal `else`-branch of the reactive text decision — baked once, the high-precision "this `{x}` is dead" signal), `hoisted-static` (`const _$h\d+ =` module hoist). **Absence of a span is "not asserted", never an implicit static claim** (asymmetric-precision commitment).
- **`analyzeReactivity(code, filename, { knownSignals? })`** (exported from `@pyreon/compiler`) → `{ findings: ReactivityFinding[], spans: ReactivitySpan[] }`. Merges the structural compiler facts with the EXISTING `detectPyreonPatterns` footgun detectors (`kind: 'footgun'`, carries the detector `code`) under one taxonomy — the Lens unifies the surface, it does not reinvent footgun analysis. `formatReactivityLens(code, result)` renders an annotated-source CLI/debug view. `analyzeReactivity` itself forces `transformJSX_JS` (the JS path is the oracle for the merged footgun+structural taxonomy), but **the raw `TransformResult.reactivityLens` sidecar is now emitted by BOTH backends** (Phase 3): `transformJSX(code, f, { reactivityLens: true })` carries the spans whether it dispatched to the Rust native binary (the ~80% path) or the JS fallback. The Rust binary mirrors the 6 `lens()` codegen-decision sites byte-for-byte (`native/src/lib.rs:Ctx::lens` + the napi `ReactivitySpan` struct; napi maps snake_case → camelCase so the JS shape is identical). Additive on the native path too — codegen is byte-identical with the option on or off. Proven by the `compareLens` cross-backend gate in `native-equivalence.test.ts` (9 fixtures, one per structural kind + mixed/multi-line/auto-call; asserts JS↔Rust span-set parity AND the additive guarantee; bisect-verified by removing any single `ctx.lens(...)` call).
- **Drift gate** (`packages/core/compiler/src/tests/reactivity-lens.test.ts`): per-fixture, the lens kind is asserted against the matching emitted token (`_bind`/`_bindText`/`_rp`/`_$h`). Bisect-verified-with-restore: reverting the `reactive` `lens()` call → `expected +0 to be 1`; restored → 12/12. Real-corpus sweep: 177 `examples/**/*.tsx`, 0 crashes, ~0.78ms/file.
- **LSP surface** (`@pyreon/lint`'s EXISTING server — no new server): `inlayHintProvider` capability + `textDocument/inlayHint` handler renders structural kinds as end-of-span ghost text (`live` / `static` / `live·prop` / `hoisted`); footguns publish as `source: 'pyreon-lens'` warning diagnostics. `computeReactivityHints(filePath, text)` is the pure testable core; `_handleMessage` is exported (underscore convention) for the JSON-RPC integration test (initialize → didOpen → inlayHint round-trip, real compiler, no mocks). `@pyreon/lint` gained a `@pyreon/compiler` workspace dep (acyclic — compiler is layer 3, standalone). Reference: `packages/core/compiler/src/reactivity-lens.ts`, `packages/tools/lint/src/lsp/index.ts`. Roadmap status: Rust parity SHIPPED (Phase 3); **manifest/llms enrichment SHIPPED** (dense `analyzeReactivity` entry in `packages/core/compiler/src/manifest.ts` + llms bullets); **body-scope `const {x}=props` SHIPPED** — `props-destructured-body` (#618, TS-compiler-API: PascalCase JSX component + bare `= props` first-param + top-of-body destructure) is surfaced by the lens via the `detectPyreonPatterns` merge and locked by `reactivity-lens.test.ts` ("body-scope … via the merge"; bisect-verified — neutering the merge fails it). The ONLY genuine remainder is a **rendered-editor (VS Code) inlay-hint e2e**, whose contract is already covered by `lsp-reactivity.test.ts`'s JSON-RPC `initialize → didOpen → inlayHint` round-trip against the real compiler — a low-value defer (real editor harness for marginal gain).

### Context providing pattern

Two context types:

- `createContext<T>(default)` — static context, `useContext()` returns `T`, safe to destructure
- `createReactiveContext<T>(default)` — reactive context, `useContext()` returns `() => T`, must call to read

`provide(ctx, value)` — provides a value for the current component's subtree; released automatically when the component unmounts (no manual cleanup).

**Resolution model — owner-based (client) / stack (SSR).** On the client each mounted component's `EffectScope` doubles as a context OWNER: `provide()` writes onto the current owner (`scope._contexts`), `useContext()` walks the owner chain (`scope._parent`, set by the renderer so it mirrors the component tree), and context is released when the scope is disposed. There is no global stack to grow and no frame to orphan — deferred boundaries (`<Show>`/`<For>`) just capture the owner reference (`getContextOwner()`) and restore it (`runWithContextOwner`) when they mount children later. This **replaced** a global mutable `Map[]` stack whose snapshot / restore / dedup / identity-removal machinery (`captureContextStack` / `restoreContextStack` / `removeContextFrame`, the 321k-frame-leak fixes) existed only to fake tree-position across deferred mounts; that entire bug class is now structurally impossible. SSR keeps the request-scoped stack (a synchronous top-down render needs no band-aids; `@pyreon/runtime-server` isolates concurrent requests via an AsyncLocalStorage-backed stack). `useContext()` resolves owner-chain-first, then falls back to the stack — so SSR and the `*-compat` layers' own stack-based provide/inject keep working. Reactivity exports `getContextOwner` / `setContextOwner` / `runWithContextOwner`; `EffectScope` carries `_parent` / `_contexts` + `provideContext` / `lookupContext`. Reference: `packages/core/reactivity/src/scope.ts` + `packages/core/core/src/context.ts` (236 lines, down from 425). The historical stack-leak entries below ("321k-frame leak", "restoreContextStack splices", "Effects that read context across re-runs") describe the SUPERSEDED stack model — kept for archaeology; the owner model deleted the band-aids they document.

For reactive values (mode, locale, etc.), always use `createReactiveContext`:

```tsx
const ModeCtx = createReactiveContext<'light' | 'dark'>('light')
// Provider: provide(ModeCtx, () => modeSignal())
// Consumer: const getMode = useContext(ModeCtx); getMode() // 'light'
```

### onMount signature

`onMount(fn: () => CleanupFn | void)` — callbacks can return nothing or a cleanup function.

### Compat-mode native marker contract

`@pyreon/{react,preact,vue,solid}-compat` ship a JSX runtime that wraps every user component in a `wrapCompatComponent` HOC — the wrapper relocates the render context so React/Preact/Vue/Solid-style component bodies (which expect a fresh render frame on every state change) work inside Pyreon's mount pipeline. **This wrapping is a problem for Pyreon framework components** that use `provide()` / `onMount()` / `onUnmount()` / `effect()` at component-body scope: their setup code lands inside the wrapper's accessor, not inside Pyreon's setup frame, so `provide()` writes land on a torn-down / wrong owner scope and `effect()` re-runs lose access to live signals.

**Solution: `nativeCompat(Component)` from `@pyreon/core`.** Marks a component so the four compat jsx() runtimes route it through `h(type, props)` directly — no wrapper, body runs in Pyreon's setup frame. The marker is a `Symbol.for('pyreon:native-compat')` property on the function (registry symbol → cross-package identity without import direction). 24 framework components ship marked today across 13 packages:

- **`@pyreon/core`**: `ErrorBoundary`
- **`@pyreon/runtime-dom`**: `Transition`, `TransitionGroup`, `KeepAlive`
- **`@pyreon/router`**: `RouterProvider`, `RouterView`, `RouterLink`
- **`@pyreon/head`**: `HeadProvider`
- **`@pyreon/query`**: `QueryClientProvider`, `QueryErrorResetBoundary`
- **`@pyreon/i18n`**: `I18nProvider`
- **`@pyreon/form`**: `FormProvider`, `Form`, `Submit`
- **`@pyreon/permissions`**: `PermissionsProvider`
- **`@pyreon/toast`**: `Toaster`
- **`@pyreon/ui-core`**: `PyreonUI`, `CoreProvider`
- **`@pyreon/unistyle`**: `UnistyleProvider`
- **`@pyreon/styler`**: `ThemeProvider`
- **`@pyreon/rocketstyle`**: `Provider`
- **`@pyreon/coolgrid`**: `Container`, `Row`
- **`@pyreon/elements`**: `Overlay`, `OverlayContextProvider`

The marker is internal infrastructure for framework components — user code in compat-mode apps doesn't normally need to call it. **Exception**: if you write your own Pyreon-flavored helper component using `provide()` / `onMount()` and use it in a compat-mode app, mark it: `nativeCompat(MyHelper)`. See `.claude/rules/anti-patterns.md` for the foot-gun catalog.

**Internal Provider components are also marked** even when they're `@internal` / `@deprecated` (CoreProvider, UnistyleProvider, RocketstyleProvider, OverlayContextProvider). Reason: PyreonUI's JSX body still routes through the active jsx() runtime in compat-mode apps — any unmarked Provider rendered inside PyreonUI gets wrapped, which swallows its `provide()` call before reaching descendants.

Test layering: per-compat marker-bypass unit tests in each `*-compat/src/tests/native-marker-bypass.test.tsx` (bisect-verified by removing the `if (isNativeCompat(type))` branch from each compat's `jsx-runtime.ts`), plus the cpa-app-compat e2e gate in `e2e/cpa-app-compat.shared.ts` (3 tests × 4 compat scaffolds — proves real-app shape including loader-driven routes work end-to-end). The unit tests catch jsx-runtime regressions; the e2e tests catch multi-render-cycle regressions (signal change re-fires the wrapper's accessor → `provide()` in re-run lands in stale stack — only visible against real router state).

Reference implementation: `packages/core/core/src/compat-marker.ts`.

### Code Splitting & Dynamic Components

- `lazy(loader)` — wraps dynamic import with Suspense `__loading` integration
- `Dynamic({ component, ...props })` — renders component by reference or string tag
- Re-exported from `@pyreon/react-compat` for compatibility

### Signal-Preserving HMR (Vite plugin)

- Top-level `signal()` calls rewritten to `__hmr_signal(moduleId, name, signal, initialValue)`
- `import.meta.hot.dispose` saves signal values to `globalThis.__pyreon_hmr_registry__`
- On hot reload, signals restore their previous values instead of reinitializing
- Virtual module `virtual:pyreon/hmr-runtime` serves the HMR helpers
- **Component-level fast-refresh (zero / router apps).** `injectHmr` emits `import.meta.hot.accept((m) => { const s = globalThis.__pyreon_hmr_swap__; if (typeof s === 'function' && m && s("<id>", m)) return; import.meta.hot.invalidate() })` — NOT a bare `accept()`. The bare form (pre-2026-05-16) re-evaluated the module but re-rendered nothing AND suppressed Vite's reload fallback → a component/JSX edit produced a **silently-stale UI until a manual refresh**. Now: `@pyreon/router` registers `globalThis.__pyreon_hmr_swap__` (dev+browser only, zero import coupling — same sink pattern as perf-harness) → `RouterInstance._hmrSwap(id, freshModule)`. It walks the active matched chain for any lazy record whose `_hmrId` matches `id` (`@pyreon/zero`'s fs-router `lazy(() => import("/abs/X"), { hmrId: "/abs/X" })`), sets `_componentCache[record] = freshModule.default`, and bumps `_loadingSignal`. `RouterView`'s `depthEntry` computed re-emits and (via its identity `equals`) re-renders ONLY that subtree **in place** — no page reload, so `__pyreon_hmr_registry__` survives and module-scope signals keep their values. Using the namespace Vite hands the accept callback (NOT a re-run of the lazy thunk) sidesteps the stale-`?t=` trap: the dynamic-import thunk lives in the non-invalidated virtual routes module, so re-importing it would return the OLD module. `_hmrSwap` returns falsy (edit outside the active route tree — nested non-route component, unrelated route, signal-only module — or no coordinator: plain `@pyreon/runtime-dom` app) → `import.meta.hot.invalidate()` → Vite propagates → an **automatic** full reload (still no manual refresh). Reference: `packages/tools/vite-plugin/src/index.ts:injectHmr`, `packages/core/router/src/router.ts:_hmrSwap` + `_hmrIdMatches`, `packages/zero/zero/src/fs-router.ts:nextLazy`. Real-Chromium gate: `e2e/zero-hmr.spec.ts` (`bun run test:e2e:zero-hmr`, own config + ssr-showcase dev boot on 5201) — edits `examples/ssr-showcase/src/routes/hmr-probe.tsx` mid-test, asserts the marker updates in place + a `window` sentinel survives (no reload) + the module-scope `count` signal keeps its value. Bisect-verified: reverting injectHmr to the bare `accept()` → the spec times out with `hmr-marker` stuck at `MARKER_V1` (the exact reported bug); restored → passes.

### Auto Signal Naming (Vite plugin, dev only)

- `const count = signal(0)` → `const count = signal(0, { name: "count" })`
- Applies to all signal() calls (module-scope and function-scope)
- Module-scope signals get names via `__hmr_signal`; function-scope via injected options
- Skips signals that already have an options argument
- Not applied in production builds (tree-shaken)

### Runtime DOM — SVG/MathML and Custom Elements

- SVG namespace: 67 SVG/MathML tags auto-detected, created via `createElementNS` with correct namespace URI
- **SVG/MathML attribute application**: SVG and MathML elements ALWAYS use `setAttribute()` for prop forwarding, never property assignment. Many SVG properties (`SVGMarkerElement.markerWidth`, `SVGMarkerElement.refX`, `SVGRectElement.x`, etc.) are read-only `SVGAnimatedLength` getters — `el[key] = value` would crash with `Cannot set property X of [object Object] which has only a getter`. Detected by `el.namespaceURI !== 'http://www.w3.org/1999/xhtml'`. Standard React/Vue/Solid behavior.
- Custom elements: props set as properties (not attributes) on elements with a hyphen in the tag name — EXCEPT `data-*`/`aria-*`, which always go through `setAttribute` (the hyphenated-tag property branch broke `getAttribute`/`dataset`/CSS attribute selectors — the server-island marker bug, PR #1523)
- **Boolean `aria-*` attributes render as the STRING `"true"`/`"false"`, NOT presence-only `""`.** ARIA state/property attrs (`aria-checked`, `aria-selected`, `aria-expanded`, `aria-disabled`, `aria-pressed`, `aria-hidden`, …) are string enums — assistive tech does NOT read `aria-checked=""` (presence) as "true"; it falls back to the default → effectively unchecked. So `applyStaticProp` (client) and `renderPropValue` (SSR) BOTH check `typeof value === 'boolean' && key.startsWith('aria-')` BEFORE the generic boolean→presence branch and emit `String(value)`. **HTML boolean attrs (`disabled`/`hidden`/`checked`/…) keep presence semantics** (`disabled=""`), and `data-*` (author-defined) keeps presence — only `aria-*` booleans coerce to `"true"`/`"false"`. Both renderers do this identically so SSR markup matches client hydration. So a component author can write `aria-checked={isChecked()}` (boolean) and it renders correctly — explicit string `aria-checked={isChecked() ? 'true' : 'false'}` is also fine but no longer required. Reference: `packages/core/runtime-dom/src/props.ts:applyStaticProp` + `packages/core/runtime-server/src/index.ts:renderPropValue`; regression tests in `runtime-dom/src/tests/props.test.ts` + `runtime-server/src/tests/ssr.test.ts` (bisect-verified — disabling either branch renders presence-only).
- Transition 5s timeout: if `transitionend`/`animationend` never fires, the transition completes automatically after 5 seconds
- Duplicate key production guard: duplicate `key` values in lists emit a one-time console warning in production (not just dev)
- **Subpath exports**: `@pyreon/runtime-dom/transition` (Transition + TransitionGroup) and `@pyreon/runtime-dom/keep-alive` (KeepAlive) for explicit opt-in. Main entry re-exports everything for backward compat.
- **URL-attribute injection guard (`setStaticProp` + the `dangerouslySetInnerHTML` sanitizer)**: URL-bearing attrs (`href`, `src`, `action`, `formaction`, `poster`, `cite`, `data`) are checked against `UNSAFE_URL_RE = /^\s*(?:javascript|data):/i` and dropped (dev-mode `[Pyreon] Blocked unsafe URL` warning; silent in prod). **`data:image/*` is allowed when the attribute is an image source (`src`/`srcset`/`poster`) on an image-context element (`<img>`/`<source>`/`<video>`)** — `isSafeImageDataUri`, single-sourced in `@pyreon/core` (see below). This is required because the framework's own `imagePlugin` ships `data:image/webp;base64,…` (blur) and `data:image/svg+xml,…` (color) placeholders that flow into `<Image>`/`<OptimizedImage>`'s `<img src={props.placeholder}>`; blocking them silently disabled the placeholder feature (functional regression fixed post-0.28.0). Raster types always pass; SVG is allowed only when it carries no `<script>`/`on*=` handlers (payload decoded for both base64 and url-encoded forms). `javascript:` stays blocked everywhere, `data:text/html` stays blocked on `<iframe>`/`<object>`/`<embed>`, and `data:` on navigable elements (`<a href>`, `<form action>`) stays blocked. **General rule when a framework feature emits a `data:` URI for an attribute: it MUST land on an image-context element/attr, or the runtime guard rejects it.** **The guard is single-sourced in `@pyreon/core` (`url-guard.ts`)** — `URL_ATTRS`, `UNSAFE_URL_RE`, and `isSafeImageDataUri(tagName, key, value)` are exported from core and imported by BOTH renderers: `@pyreon/runtime-dom`'s client `setStaticProp` + DOMParser sanitizer (passing `el.tagName`) and `@pyreon/runtime-server`'s SSR `renderProp` (passing the JSX `tag`; `renderProp` now takes the element tag so the guard can check image context). `tagName` is matched case-insensitively so both the uppercase DOM `Element.tagName` and lowercase JSX tag resolve. **This extraction exists because the two renderers previously carried independent copies that DRIFTED** — the client guard allowed `data:image/*` (0.28.1) while the SSR copy still stripped ALL `data:` URIs, so `renderToString`/`renderToStream` shipped prerendered/SSG static HTML with `<img>`/`<video>` that had no `src`/`poster` (a functional regression invisible to client-only tests; the original runtime-dom-only fix was PR #1212, the SSR mirror was #1314, and this extraction collapses both into one source so the drift class can't recur). The exhaustive allow/block matrix lives once in `packages/core/core/src/tests/url-guard.test.ts`; each renderer keeps a thin behavior test of its own wiring (`coverage-gaps.test.ts` client + `image-data-uri-guard.browser.test.tsx` real-Chromium + `runtime-server/src/tests/ssr.test.ts` SSR), and the full `<Image>` → SSR pipeline is locked by `packages/zero/zero/src/tests/image-ssr-placeholder.test.ts`. Reference: `packages/core/core/src/url-guard.ts`.

### Mount Pipeline Performance

The mount pipeline is optimized for zero unnecessary allocations in production:

- **Devtools gated on `__DEV__`**: `compId` generation (`Math.random`), `_mountingStack` tracking, `registerComponent`/`unregisterComponent` are all behind `if (__DEV__)` — Vite tree-shakes the entire devtools module from prod bundles. Zero production cost.
- **Lazy LifecycleHooks**: `mount`/`unmount`/`update`/`error` arrays start as `null`, allocated on first `onMount()`/`onUnmount()`/etc. call. Components with no hooks (80%+) skip all hook iteration.
- **Lazy EffectScope**: `_effects` and `_updateHooks` arrays start as `null`, allocated on first `add()`/`addUpdateHook()`. Components with no effects save 2 array allocations.
- **makeReactiveProps scan-first**: Scans for `REACTIVE_PROP`-branded functions before allocating the getter-backed result object. Static-only components (60%+) return the raw props object immediately — no allocation, no property copying.
- **Lazy mountCleanups**: Only allocated when an `onMount` callback actually returns a cleanup function.
- **Per 150-component page**: ~2,020 allocations + ~1,050 operations eliminated vs naive implementation.

### Memory characteristics — measured

Run `bun run measure-memory` (`scripts/measure-memory.mjs`, `node --expose-gc`, `NODE_ENV=production`) for reproducible **retained bytes per reactive primitive**. This fills the one instrumentation gap the other gates didn't cover — bundle-budgets gates *bundle* bytes (deterministic, CI-gated), perf-harness journeys gate *allocation counts*, leak-sweep gates *heap-growth slope*, but nothing measured per-primitive retained heap. It is a TOOL, not a CI gate (runtime heap is ±~10% noisy run-to-run; gating it would flake — use it for RELATIVE before/after checks on a reactivity change).

Production per-primitive cost (V8 heapUsed, N=100k, stable):

| primitive | retained bytes |
| --- | --- |
| `signal` | **~152 B** (≈ the ~170 B the in-browser perf-harness reads on `reactivitySignalCreate-100k ≈ 17 MB`; node V8 reads slightly leaner) |
| `signal + effect` | ~930 B (combined) |
| `signal + computed` | ~1.65 KB (combined — a computed is **~10× a signal**: dep tracking + recompute closure + value cache; prefer plain signals where derivation isn't needed) |
| `effectScope` | **~64 B** (the lazy-null-array design — `_effects`/`_updateHooks` start `null`; an empty scope is barely more than its object header) |

The signal struct is near its floor: 1 read closure + 5 fields, methods on a shared `SignalProto` via ONE `setPrototypeOf` (not 6 per-instance assignments; monomorphic + `instanceof Function` preserved), `_s`/`_d` lazy-null, single-subscriber inline `_d1` (no `Set` for the dominant `<For>`-row case). **Important: the measurement MUST run under `NODE_ENV=production`** — in dev the reactive-devtools machinery (per-primitive `new Error()` source-capture + always-on registry) dominates and OOMs at 100k; that overhead is real but prod-gated (tree-shaken), so it is not what consumers pay.

**`@pyreon/runtime-dom` bundle tree-shakes correctly** (`sideEffects: false`, pure modules — measured, esbuild prod+gzip): a `mount`-only import is **~7.4 KB gz**; core (`mount`+`hydrateRoot`+`mountChild`) ~8.6 KB; the full kitchen-sink (incl. `Transition`/`TransitionGroup`/`KeepAlive`) ~9.8 KB. The optional animation/keep-alive features (~1.2 KB) tree-shake OUT when unused AND have subpath exports — a minimal app ships ~7.4 KB, not the 12 KB kitchen-sink budget figure. No main-entry split is warranted (it would break back-compat for zero gain).

**App-level: virtualize large lists.** Fine-grained reactivity allocates per reactive node — a 10k-row `<For>` = ~10k signals (~1.5 MB) + cleanup closures + key map (the ~13%-over-Vanilla bulk-create cost; Solid pays it identically). The signal is already near its floor, so the lever is **`@pyreon/virtual`** (`useVirtualizer` renders only visible rows → only ~visible-count signals), not a smaller signal.

### Mount-loop closure hazards — reactive entry points read marker.parentNode live

Any framework primitive that (a) accepts `parent` as a setup arg, (b) inserts a marker into that `parent`, and (c) calls `parent.insertBefore(...)` from inside an effect re-run is structurally unsafe under `mountFor`'s frag-then-move pattern. `mountFor` builds its children into a `DocumentFragment` and commits via `liveParent.insertBefore(frag, tailMarker)` — the move carries markers and all sibling DOM along, but inner mount loops still hold the original (now-empty) fragment as `parent`. Next signal-driven re-run → `insertBefore(node, marker)` against the stale fragment → `NotFoundError` → child-loss.

Closed across two PRs in 2026-Q2:
- **#776** — `mountReactive` reads `marker.parentNode ?? parent` at each effect run. Surfaced by the For-of-`<Show>` batched-toggle journey in `scripts/leak-sweep.ts` (#772). CONTRACT regression: `packages/core/runtime-dom/src/tests/show-of-for-batched-toggle.browser.test.ts`.
- **#783** — `mountKeyedList` reads `tailMarker.parentNode ?? parent` at each effect run, threads `liveParent` through `mountNewEntries` + `keyedListReorder` (which propagates to `applyKeyedMoves` → `moveEntryBefore`). Bug reachable only when a `<For>` child returns a function directly (`(i) => () => signal().map(...)`) so the inner keyed array lands in the For's frag — a `<div>` wrapper isolates from the frag-move. CONTRACT regression: `packages/core/runtime-dom/src/tests/keyed-array-in-for-batched-toggle.browser.test.ts`.

Sibling primitives audited safe — no remaining instances of this class in runtime-dom:
- `mountFor` itself reads `startMarker.parentNode` at the top of its effect (pre-existing pattern; same shape as the post-#776/#783 fixes).
- `KeepAlive` / `TransitionGroup` / `Transition` use `containerRef.current` (live DOM-element refs that move atomically with the surrounding tree, never stale).
- `_bindText` / `_bindDirect` (template.ts) operate on text-node `.data` only — no `parent.insertBefore` from inside an effect.
- Hydrate paths inherit through `mountReactive`'s call.

General rule for framework code: any mount loop running inside an effect that accepts `parent` as a setup arg should compute the live parent from a marker at each re-run — `parent` is captured at setup and the DOM moves; markers move with the DOM, so their `parentNode` is the source of truth. Full anti-pattern entry in `.claude/rules/anti-patterns.md` under "Architecture Mistakes". Reproducer for the For-of-Show shape: `bun run perf:leak-sweep --app perf-dashboard --journeys domConditionalToggle-1000`.

### Dev-Mode Warnings (`__DEV__`)

- `mount()` validates container is not null/undefined
- Component output validation (must return VNode, string, null, or function)
- Duplicate `by` keys in `<For>` loops logged as warnings
- Passing raw signal (function) as child instead of calling it
- Devtools: component tree tracking, inspector overlay, highlight
- All guarded by `__DEV__` — tree-shaken in production builds

### Dev-mode perf counters (`@pyreon/perf-harness`)

Framework packages emit named call counters for perf-driven debugging (real-app-shape instrumentation, not synthetic benchmarks). The counter contract is:

```ts
// In a framework package's hot path — NO import from @pyreon/perf-harness.
interface ViteMeta {
  readonly env?: { readonly DEV?: boolean }
}
declare const globalThis: { __pyreon_count__?: (name: string, n?: number) => void }

if ((import.meta as ViteMeta).env?.DEV === true) globalThis.__pyreon_count__?.('styler.resolve')
```

- **Zero cross-package coupling.** Framework packages (styler, unistyle, rocketstyle, runtime-dom, reactivity, router) are published to npm; they must not depend on the private `@pyreon/perf-harness` package. The global sink lets them emit without an import.
- **Zero cost until opt-in.** `globalThis.__pyreon_count__` is `undefined` until a consumer calls `perfHarness.install()` or `perfHarness.enable()`. Optional-chaining short-circuits; prod tree-shakes the whole block via the `import.meta.env.DEV` gate.
- **Counter names live in ONE place**: `packages/internals/perf-harness/COUNTERS.md`. Adding a new counter means (a) adding the emit in source, and (b) adding a row to `COUNTERS.md`. The drift test (`catalog-drift.test.ts`) enforces both directions — emits without catalog entries fail CI, and catalog entries with no emitters also fail.
- **Instrumented layers (66 counters today)**:
  - `styler`: `resolve`, `sheet.insert`, `sheet.insert.hit`
  - `unistyle`: `styles`, `descriptor`, `descriptor.fallback-scan`
  - `rocketstyle`: `getTheme`, `dimensionsMap.hit`, `localThemeManager.hit`, `omitSet.hit`
  - `runtime`: `mount`, `unmount`, `mountChild`, `mountFor.lisOps`, `tpl`, `applyProp`, `applyEvent`, `bindText`, `bindDirect`, `mountReactive`, `cleanup`. Foundation PR added 6 new counters + 5 stress journeys + baselines for the rendering layer. **Counter scope notes**: `runtime.applyProp` fires per `applyProp(el, key, value)` call — i.e. the imperative-prop path used by `h()`-based code, prop spreads, and styled-component theme forwarding; NOT for JSX-compiled static templates (compiler bakes static props into `_tpl()` HTML) or compiler-emitted event delegation. `runtime.applyEvent` is a strict subset of `applyProp` for `on*` props — near-zero in pure-JSX-compiled code because the compiler emits `el.addEventListener(...)` directly, bypassing `applyProp`. `runtime.bindText` / `runtime.bindDirect` fire per compiler-emitted reactive binding setup — stable on a static page, spike during HMR. `runtime.mountReactive` fires per `<Show>` / accessor-child boundary mount. `runtime.cleanup` fires per registered mount cleanup that actually runs (gated on `hasCleanup` so the placeholder noop on first mountReactive run doesn't count). The 5 journey baselines (`domMount-1000`, `domShuffleLis-1000`, `domAppend-10k`, `domConditionalToggle-1000`, `domEventAttach-1000`) lock in the counter signatures: `domMount-1000` proves `mountChild × tpl × bindText × signalCreate` scale together (1000 each), `domShuffleLis-1000` pins worst-case `mountFor.lisOps` ≈ 999 (full reversal degenerates LIS to length 1), `domAppend-10k` proves the three-tier fast path's "extend" tier (`mountFor.lisOps` absent — 0), `domConditionalToggle-1000` shows `mountReactive ≈ 4000` + `cleanup ≈ 3003` for the 1000-Show × 2-cycle toggle pattern (catches the bug shape PR #505 fixed at root), `domEventAttach-1000` shows the JSX-compiled event path bypasses applyEvent (`runtime.applyEvent` absent in baseline — informational, not a regression).
  - `runtime-server` (SSR): `render`, `stream`, `component`, `escape`, `suspense.boundary`, `suspense.fallback`, `for.keyMarker`
  - `reactivity`: `signalCreate`, `signalWrite`, `effectRun`, `computedRecompute`. Five isolated stress journeys + baselines lock in per-primitive cost without folding it into the app-shaped cascade (form / query / rx / store). Each isolates ONE counter dimension so regressions can't hide:
    - **`reactivitySignalCreate-100k`** — 100k fresh signal allocations, no subscribers. Baseline: `signalCreate: 100_000`, others 0. Heap ~17MB. Diagnoses per-signal allocation overhead.
    - **`reactivitySignalWrite-100k`** — 1 signal, 100k unique-value writes (loop counter bypasses `Object.is`). Baseline: `signalCreate: 1`, `signalWrite: 100_000`. Diagnoses write-path cost in the no-subscriber fast path. Any non-zero `effectRun` / `computedRecompute` here = stray subscriber leak.
    - **`reactivityEffectFanout-1k`** — 1 signal, 1000 effect subscribers under one EffectScope, 10 writes. Baseline: `effectRun: 11_000` (1000 initial + 10 × 1000 cascades). `effectRun > 11_000` = batch dedup broken.
    - **`reactivityComputedDiamond-1k`** — 1 source → 1000 computeds all reading source → 1 effect reading all 1000 → 1 write. Baseline: `computedRecompute: 2000` (1000 cold + 1000 cascades), `effectRun: 2` (1 initial + 1 cascade — DEDUP WORKS, NOT 1000). The `effectRun: 2` count is the structural proof that the two-tier flush dedupes diamond-shaped dependencies correctly.
    - **`reactivityScopeDispose-10k`** — 10k effects in 1 scope, dispose scope, then write source once. Baseline: `effectRun: 10_000` (initial mounts only — post-dispose write fires ZERO cascades). Leak detector: `effectRun > 10_000` means `scope.stop()` left orphan effects attached to the signal.

    Reference: [packages/internals/perf-harness/COUNTERS.md](packages/internals/perf-harness/COUNTERS.md) + [examples/perf-dashboard/src/components/ReactivityStressSection.tsx](examples/perf-dashboard/src/components/ReactivityStressSection.tsx) + per-journey docstrings in [examples/perf-dashboard/src/journeys.ts](examples/perf-dashboard/src/journeys.ts).
  - `router`: `navigate`, `loaderRun`, `loaderCache.hit`, `prefetch`
  - `store`: `defineStore`, `pluginRun`, `patchKey`, `subscribeNotify`, `actionListenerNotify`, `actionCall` (foundation PR — baselines for 5 stress journeys (`storeMount-1000`, `storeAction-10k`, `storeWrite-10k`, `storeSubscribeNotify-1k`, `storePluginScale-1000`) recorded in `perf-results/`. The earlier "plugin-chain caching" follow-up was measured + REJECTED (2026-06): plugins are side-effecting per-instance initializers — each fresh store must run each plugin, so `storePluginScale-1000`'s `pluginRun: 5000` (5 plugins × 1000 stores) is the CORRECT signature, not a cache miss; a noop plugin costs ~0.2µs and the loop skips entirely in the zero-plugin common case.)
  - `rx`: `transform.signal`, `transform.raw`, `pipe`, `debounce.create`, `throttle.create` (foundation PR — baselines for 5 stress journeys (`rxFilterMap-10k`, `rxPipe-10k`, `rxSortBy-10k`, `rxAggregate-10k`, `rxDebounceRapid-1k`) recorded in `perf-results/`. The `transform.signal` / `transform.raw` pair is the structural diagnostic — non-zero `.raw` = consumer accidentally pre-resolved a signal where rx expected one. `rxPipe-10k` proves pipe collapses the chain into 1 computed: `rx.pipe: 1`, `rx.transform.signal: 0`, vs equivalent separate-call chain `rxFilterMap-10k`'s `rx.transform.signal: 2`.)
  - `query`: `useQuery`, `useMutation`, `observerNotify`, `setOptions`, `invalidate`, `isFetchingScan`. Foundation PR added the 6 counters + 4 stress journeys + baselines. **Lazy signal allocation follow-up**: each `useQuery` / `useMutation` / `useSuspenseQuery` / `useInfiniteQuery` previously allocated 8-13 fine-grained `Signal<T>` instances at setup AND wrote to all of them on every observer notify, but real consumers read 1-2 fields. The hooks now use a slot-bag with property getters that `??=` materialize each signal on first access; the subscribe callback only writes to materialized slots. Same `Signal<T>` identity is preserved across repeat property access (load-bearing for Pyreon's identity-keyed effect tracking). Baseline diffs (5-run median): `queryMount-1000` `signalCreate: 9000 → 0`, `signalWrite: 10006 → 6` (-99.9%), wall-clock -15%, heap -10.7%. `queryNotify-10k` `signalWrite: 20070 → 7` (-100%), wall-clock -22%. `mutationInvalidate-1000` `signalCreate: 908 → 0`, heap -25.8%. The journey numbers are the EXTREME case (the QueryAtScale stress component doesn't read any result fields → zero materialization); real apps reading 1-3 fields land in the 67-89% range. Bisect-verified in the PR by reverting just the `data` slot of `useQuery` to eager: `signalCreate: 0 → 10`, `signalWrite: 7 → 10005` (= 10 subs × 1000 events for that one field) — proving the optimization drives the counter drop. Lazy contract locked in by `tests/lazy-signals.test.tsx` (4 tests: same-signal-identity guarantee for both `useQuery` and `useMutation`, plus end-to-end "read field, await mutate, see updated value" round-trips). The 5th `queryReactiveKey-1000` journey (100 useQuery with shared reactive queryKey × 10 flips = 1000 setOptions runs) was originally deferred from the foundation PR; root cause turned out to be a missing `runUntracked` wrap around child mounts in `<For>` — see "Reactive-render entry point untracking" below.
  - **Reactive-render entry point untracking (post-#490 fix)**: `mountFor` / `mountKeyedList` / `KeepAlive` / `TransitionGroup` all wrap their child mount work in `runUntracked(() => ...)` — same pattern `mountReactive` already had. Without this, signals read during a child's setup (`useQuery`'s `new QueryObserver(client, options())` reading the queryKey signal at construction time, `useTheme` reads, etc.) leak their subscription up to the OUTER reactive primitive's effect. When the leaked signal flips, the OUTER effect re-runs → `runCleanup()` disposes ALL inner effects (the children's per-component effects), and the keyed-update path skips re-mount on unchanged keys → the children's reactivity is gone forever. This was the structural cause of PR #490's deferred `queryReactiveKey-1000` journey: 100 useQuery hooks under `<For>`, 10 flips of a shared reactive queryKey, expected 100 × 10 = 1000 setOptions runs, observed 0 across the 10 flips because `<For>`'s effect tore down all 100 setOptions effects on the first flip. Bisect-verified at unit level: `packages/core/runtime-dom/src/tests/fanout-repro.test.tsx` fails with `expected 100 to be 0` against the unwrapped version. Post-fix baseline: `query.setOptions: 1100` (was 100 pre-fix), `effectRun: 1104` (was 104). Reference: `packages/core/runtime-dom/src/nodes.ts` (mountFor + mountKeyedList), `keep-alive.ts`, `transition-group.ts`.
  - `island` (`@pyreon/server` browser-side): `scheduled`, `hydrated`, `skipped.never`, `skipped.nested`, `skipped.no-loader`, `error`, `prefetch`. The `island` namespace is emitted from `@pyreon/server`'s browser-running `client.ts` (per-island hydration scheduling, prefetch warming, success/error tracking). `scheduled - hydrated` at steady state = islands still waiting on a deferred trigger (`idle` / `visible` / `interaction` / `media`); `skipped.no-loader` should be zero (registry drift); `error` should be zero (pair with `data-island-error="invalid-props"|"hydration-failed"` on the failing element to diagnose). Note: the special-case mapping in `catalog-drift.test.ts` recognises that the `server` package emits the `island.*` prefix — see `LAYER_PREFIX_OVERRIDE`.
  - `i18n`: `t`, `lookupKey`, `lookupKey.fallback`, `interpolate`, `pluralResolve`, `namespaceLoad`. Foundation PR added the 6 counters + 4 stress journeys + baselines. Pair `t` with `lookupKey`/`interpolate` for per-call cost breakdown. `localeFlip-100` journey shape (`i18n.t: 1100` = 100 initial mount + 100×10 flips) confirms the locale signal's fan-out works as expected. Plural journey's lookupKey count is 1000 (not the predicted ~2000) because the plural-suffix probe hits on first try (`items_one`/`items_other` exist in `en`) — no fallback path fires.
- **Gate shape differs by package category.** Browser packages (runtime-dom, router, styler, reactivity, etc.) use `import.meta.env.DEV` so the gate folds to `false` at Vite build time and the counter strings tree-shake out. Server packages (runtime-server) use `typeof process !== 'undefined' && process.env.NODE_ENV !== 'production'` because they don't run through a Vite bundle in production — the gate evaluates at runtime under Node and correctly short-circuits when `NODE_ENV=production`. Consequence: server-side counter strings remain in the bundle (few bytes) but the runtime cost is zero. Verified by `src/tests/runtime-server-runtime-gate.test.ts` in perf-harness.
- **Dev-mode overhead is trivial.** `src/tests/ssr-overhead.test.ts` measures a 1k-row SSR render three ways: NODE_ENV=production (~1.14ms baseline), NODE_ENV=development with no sink (~1.12ms — within noise), NODE_ENV=development with a no-op sink installed (~1.20ms — ~5% overhead). The gate + optional-chain is essentially free, and even fully-firing counters add only a few percent.
- **Consumer APIs**:
  - `perfHarness.snapshot()` / `perfHarness.reset()` / `perfHarness.record(label, fn)` / `perfHarness.diff(a, b)`
  - `perfHarness.overlay()` — in-page shadow-DOM panel with live counters, Ctrl+Shift+P toggle, reset/record/export buttons
  - `install()` attaches everything to `globalThis.__pyreon_perf__` so devtools console can use it directly
- **Tree-shake regression test**: `src/tests/treeshake.test.ts` in perf-harness Vite-production-bundles each instrumented file and asserts both the `__pyreon_count__` identifier and every counter name string are absent from the prod output. Proves the dev-gate folds to dead code across all five layers.
- **Automation**:
  - `examples/perf-dashboard` — single-page real-app-shape stress rig; auto-installs the harness in dev so counters are live from boot. Journeys exported from `src/journeys.ts`.
  - `bun run perf:record --app <name> --journey <name>` — Playwright drives the example through the journey, N runs (default 5), writes `perf-results/<sha>-<app>-<journey>.json` with median counters + wall-clock + heap
  - `bun run perf:diff <baseline> <current>` — compares two records, exits nonzero on regression (upward move > threshold, absolute floor max(3, before × threshold) prevents noisy rare-counter false positives)
  - `.github/workflows/perf.yml` — advisory-only workflow; runs on manual dispatch, nightly schedule, and `perf`-labelled PRs; posts a sticky comment with the diff when a baseline is committed

### exactOptionalPropertyTypes

Enabled in root tsconfig — optional properties need explicit `| undefined` when assigned from functions that may return undefined.

### Manifest-driven docs pipeline (T2.1 + T2.5.1)

One source of truth per package — `packages/<category>/<pkg>/src/manifest.ts` — feeds every generated doc surface. `bun run gen-docs` regenerates `llms.txt` (bullets), `llms-full.txt` (per-package sections), and `packages/tools/mcp/src/api-reference.ts` (MCP regions). CI `Docs Sync` job runs `bun run gen-docs --check` against all three.

- **llms.txt / llms-full.txt** are regenerated wholesale for every migrated package.
- **api-reference.ts** uses OPT-IN region markers per package: `// <gen-docs:api-reference:start @pyreon/<name>>` / `... end @pyreon/<name>>`. **Coverage is partial, not complete: as of this writing 42 of 63 published Pyreon packages have a `src/manifest.ts` and are on the pipeline** (PR #319 onward). The remaining 21 have NO manifest and NO api-reference region — they are simply ABSENT from the generated surfaces (this is NOT the same as a hand-written block; there are no hand-written MCP api-reference blocks left — un-migrated packages just produce nothing). **Before any "enrich package X's docs/MCP" task, `ls packages/<cat>/X/src/manifest.ts` first — if it's absent the task is a migration, not an enrichment.** Real-API backlog worth migrating (packages whose public API agents actually `get_api`): `rocketstyle`, `attrs`, `coolgrid`, `kinetic`, `kinetic-presets`, `connector-document`, `dnd`, `sized-map`. Tooling / scaffolding (`cli`, `zero-cli`, `create-zero`, `meta`, `storybook`, `vite-plugin`, `typescript`, the 5 `*-compat` shims) mostly has no `get_api`-shaped surface — exempt those explicitly rather than ship thin filler manifests. Adding a new package: drop a `src/manifest.ts` matching the `@pyreon/manifest` `defineManifest()` shape, add `@pyreon/manifest` as a `workspace:*` devDependency (the `@pyreon/lint` convention — `manifest.ts` is gen-docs-only, tree-shaken from published `lib/`), add the marker pair to `api-reference.ts`, run `bun run gen-docs`. Quality bar varies by package — `flow` / `query` / `form` / `hooks` are at MCP density (dense `summary`, 6+ `mistakes` per flagship API); the more recently migrated packages (head, server, ui-core, unistyle, lint, mcp, document-primitives) start with verbatim ports of the prior hand-written entries plus a few obvious omissions filled in (e.g. all 11 MCP tools and all 18 doc primitives now have entries).
- **Field mapping** (manifest `ApiEntry` → MCP `ApiEntry`): `signature` / `example` passthrough; `summary` → `notes` (with `[DEPRECATED]` / `[EXPERIMENTAL]` prefix when `stability` is set, plus `seeAlso: X, Y`, `Added in vX.Y.Z`, `Deprecated since vX.Y.Z` trailers); `mistakes[]` → `mistakes` (joined `- item` bullets). The `McpApiReferenceEntry` type in `@pyreon/manifest` is asserted structurally equal to MCP's real `ApiEntry` via a compile-time `Equal<...>` check in `packages/tools/mcp/src/tests/api-reference.test.ts` — any drift fails `tsc --noEmit` before the generator produces stale output.
- **Migration recipe** for flipping a package to the MCP pipeline: (1) enrich the manifest's `api[]` entries to MCP density — 2-3 sentence `summary` carrying architectural rationale + per-API foot-gun `mistakes[]` catalog (see flow's manifest for the quality bar); (2) wrap the existing hand-written block in `api-reference.ts` with the marker pair; (3) `bun run gen-docs`; (4) add `renderApiReferenceEntries(manifest)` spot-checks to the package's `manifest-snapshot.test.ts`.
- **MCP `validate` tool runs two detectors (T2.5.2)**: `detectReactPatterns` (from `@pyreon/compiler`) catches "coming from React" mistakes — `useState`, `useEffect`, `className`, `htmlFor`, `onChange` on inputs, `.value` writes on signals, React-package imports. `detectPyreonPatterns` (added 2026-Q2) catches "using Pyreon wrong" mistakes — 15 detector codes today (`for-missing-by`, `for-with-key`, `props-destructured`, `props-destructured-body`, `process-dev-gate`, `empty-theme`, `raw-add-event-listener`, `raw-remove-event-listener`, `date-math-random-id`, `on-click-undefined`, `signal-write-as-call`, `static-return-null-conditional`, `as-unknown-as-vnodechild`, `island-never-with-registry-entry`, `query-options-as-function`). `query-options-as-function` makes the `@pyreon/query` best-practice surface **proactive**: the same rule ships as the opt-in `@pyreon/lint` rule `pyreon/query-options-as-function` (reactive — seen after running `pyreon doctor`/`pyreon-lint`), and now ALSO as a compiler detector so MCP `validate({ code })` flags it BEFORE the code is committed — an AI agent calling `validate` sees the fix (`useQuery(() => (...))`) without running lint. Object-literal first arg to `useQuery`/`useInfiniteQuery`/`useQueries`/`useSuspenseQuery` only; `useMutation` excluded by design; identifier/call args stay silent (unprovable). `props-destructured-body` is the body-scope companion to `props-destructured` — it flags `const { x } = props` written synchronously in a component body (precise: PascalCase JSX components only, bare `= props` first-param identifier unwrapped through `as`/`satisfies`/`!`/parens, does NOT descend into nested functions since handler/effect/accessor destructures re-read per invocation). Both detectors are AST-based (TypeScript compiler API, same style as `react-intercept.ts`). The merged result is sorted by line + column so the MCP output reads top-down. Idiomatic Pyreon code hits neither detector — the combined zero-false-positive test in `packages/tools/mcp/src/tests/validate.test.ts` is the regression guard.
- **MCP proactive docs: `get_pattern` + `get_anti_patterns` tools (T2.5.3 + T2.5.4)**: `validate` is reactive (catches bad code). These two are proactive — AI agents call them BEFORE writing to see the canonical pattern or the anti-pattern catalogue. `get_pattern({ name })` serves `docs/patterns/<name>.md` from the monorepo; 16 foundational patterns ship today (`controllable-state`, `data-fetching`, `dev-warnings`, `dynamic-fields`, `event-listeners`, `form-fields`, `imperative-toasts`, `islands`, `keyed-lists`, `reactive-context`, `reactive-spread`, `routing-setup`, `signal-writes`, `ssr-safe-hooks`, `state-management`, `styler-theming`). `get_anti_patterns({ category?, name?, full? })` parses `.claude/rules/anti-patterns.md`; each entry surfaces its `[detector: <code>]` tag inline so an agent can pair the catalog entry with the live static detector. **Token-frugal by default (mcp-token-slim PR):** no-arg returns a COMPACT INDEX — one line per entry (title + detector tag + one-sentence hook), ≈3.3K tokens vs the ≈14K full dump (a ~76% cut on the common orient call), keeping per-category `## <Heading>` markers so categories stay discoverable in one call. Drill in with `{ name }` (single entry full body — cheapest), `{ category }` (one category\'s full bodies — unchanged contract), or `{ full: true }` (entire catalog, explicit ≈14K opt-in). A `token-budget.test.ts` regression gate pins `tools/list` < 1,300 tokens and `get_anti_patterns({})` < 5,000, bisect-verified to fail if the full-dump default returns. Parser + loader + index formatter live in `packages/tools/mcp/src/anti-patterns.ts` and `.../patterns.ts`. Add a new pattern by dropping a new `docs/patterns/<slug>.md` file — the tool picks it up on next call, no code changes needed.
- **MCP `get_changelog` tool (T2.5.8)**: AI agents get recent release notes for any `@pyreon/*` package without scraping `git log` or raw markdown. Parses changesets-populated `packages/**/CHANGELOG.md` into version entries (`{ version, changes[], dependencyUpdates[], empty }`) and returns the N most recent substantive versions. Filters out ceremonial version bumps by default (pure dependency-update releases with no user-facing body) since they're noise for agent consumers. Accepts both `"query"` and `"@pyreon/query"`. Params: `limit` (default 5) controls depth; `includeDependencyUpdates` opts the dep bumps back in; `since` filters to versions strictly newer than a floor (`since: "0.12.0"` returns the delta — especially useful when an agent knows the version it was trained against). Loader walks up to the nearest monorepo root. Parser + loader in `packages/tools/mcp/src/changelog.ts`; 44 tests cover synthetic edge cases (ceremonial bumps, multi-line bullets, mixed Major/Minor/Patch sections), the real-repo discovery (40+ packages), the `compareVersions` / `filterSince` comparator (semver-ish with pre-release ordering), and the tool handler over JSON-RPC.
- **Test-environment audit (T2.5.7)**: scanner + formatter in `packages/core/compiler/src/test-audit.ts`, exported from `@pyreon/compiler` so both `@pyreon/mcp` and `@pyreon/cli` consume it without pulling in each other. Scans every `*.test.ts(x)` under `packages/` for mock-vnode patterns — tests that construct `{ type, props, children }` object literals (or a custom `vnode()` helper / call-site) instead of going through the real `h()` from `@pyreon/core`. The bug class that caused PR #197's silent metadata drop: mock-only tests pass while rocketstyle / compiler / attrs work stays unexercised. Three risk tiers (HIGH / MEDIUM / LOW) from the balance of `mockVNodeLiteralCount` + `mockHelperCount` (defs) + `mockHelperCallCount` (call-sites, the pervasiveness metric) + `realHCallCount` + `importsH`. Two consumer surfaces: MCP `audit_test_environment` tool (options `minRisk`, `limit`) and CLI `pyreon doctor --audit-tests [--audit-min-risk high|medium|low] [--json]`. 32+ tests cover synthetic fixtures per tier, real-repo sanity, the `.test.ts(x)` filename filter, node_modules skip, zod/CLI-flag rejection of invalid minRisk, and JSON output contract. Use before modifying an existing test to check if strengthening is warranted, or after a framework change to audit for regression.
- **Scanner false-positive heuristics (T1.2 sweep)**: three context-aware skips were added to keep the audit honest after the initial scanner caught 14 false positives out of 24 HIGH files. (1) **Helper-def discrimination**: `(const|let)\s+vnode\s*=\s*(\(|\{|function|async)` — RHS must be an arrow fn / inline object / function keyword. `const vnode = defaultRender(...)` and `const vnode = <span/>` are real-VNode bindings, not factory definitions, so they no longer count. (2) **Type-guard call-args skip**: literals passed as args to `is*(...)`, `has*(...)`, `assert*(...)`, `validate*(...)`, `check*(...)` (uppercase-suffix identifiers, type-guard convention) are skipped — the literal IS the test input for a duck-type check, not a mock-render input. Closes `utils-coverage.test.ts` and similar. (3) **Template-string masking**: `maskTemplateStrings(source)` blanks the inside of every backtick-delimited region (preserving positions for line/column reporting). Fixture content written to disk via a `writeFile()` call whose body is a template-literal source string no longer counts as code under audit — closes `cli/doctor.test.ts`, the audit tool's own test fixtures. All three are bisect-verified by `tests/test-audit.test.ts` — reverting any one of them fails specific tests.

## Docs Website

Pyreon-native documentation site at `docs/` (package `@pyreon/docs`) — powered by `@pyreon/zero` + `@pyreon/zero-content`. 178 doc pages covering all packages (incl. a "Why Pyreon" honest-positioning page, a consolidated **Accessibility** guide (`accessibility.md` — the out-of-the-box a11y map: form label/error auto-wiring, `announce()` / `<RouteAnnouncer>`, modal focus trap + restore, primitive keyboard navigation, the multiplatform `accessibilityLabel`/`accessibilityHidden` vocabulary, reduced-motion, typed roles, and the opt-in a11y lint rules), a Quickstart with a live no-install `<Example>`, 43 auto-generated per-package API-reference pages (495 symbols) emitted from `src/manifest.ts` by `docs/scripts/gen-reference.ts` (rendering Features + end-to-end Complete-example + **Parameters / Returns tables** when a manifest `ApiEntry` provides the optional structured `params` / `returns` fields — `@pyreon/reactivity`'s core primitives populated today, other packages incremental), a generated Troubleshooting reference (7 categories, 100 anti-patterns) emitted from `.claude/rules/anti-patterns.md` by `docs/scripts/gen-troubleshooting.ts`, an Examples gallery (42 runnable `<Example>`s grouped by topic) emitted by `docs/scripts/gen-examples-gallery.ts`, 12 in-depth task-oriented guides (`guides/*` — data-fetching, routing, forms, state, styling, animations, SSR/SSG/ISR, islands, performance, testing, deployment, reactivity-in-depth, each grounded in the real package source + embedding live `<Example>`s), a Recipes cookbook (`recipes.md` — copy-paste task solutions: auth gate, optimistic UI, infinite scroll, dark mode, debounced search, persisted state, server-side form errors, polling), "Coming from React / Solid / Vue / Svelte / Angular" migration guides, a guided 6-chapter concept Tutorial track (`tutorial/01-signals` … `06-build-something`, sequenced with prev/next, each with a live `<Example>`), and a "Build a Real App in 30 Minutes" tutorial whose every step is a live inline `<Example>`). **Dogfoods the framework end-to-end** — the docs site itself runs on Pyreon's signal-based reactivity, fs-router, SSG, and content pipeline. Ships the **`<Example>` primitive** — real `.tsx` files mounted inline with optional cross-mount signal-bridging via `share="key"` — proving cross-mount reactivity no MDX-flavor framework can replicate.

Replaces the legacy VitePress site that previously lived here. The migration from `<Playground>` → `<Example>` landed in PR #1448 across 30 pages; the production cutover (delete VitePress, move docs-zero into `docs/`) is this commit.

```bash
cd docs && bun run dev       # serve docs locally
cd docs && bun run build     # build static site to dist/
cd docs && bun run preview   # preview the built site

bun run --filter=@pyreon/docs dev    # same, from repo root
bun run --filter=@pyreon/docs build
```

### Authoring docs in `docs/`

The new site uses `@pyreon/zero-content`'s markdown pipeline. Authoring contracts:

- **Live examples** — `<Example file="./examples/<topic>/<slug>" />` in any `.md`. The component file lives at `docs/src/examples/<topic>/<slug>.tsx` and is a real default-exported Pyreon component. Two `<Example>` calls with the same `share="key"` receive the SAME `Signal<unknown>` via `getOrCreateSharedSignal` — proving cross-mount reactivity no MDX-flavor framework can replicate
- **Code blocks** — every fence is highlighted by Shiki at build time using BOTH light + dark themes inlined into ONE `<span>` tree. Page's `data-theme` swap flips the visible variant via CSS — zero JS cost. Meta string drives wrapping: `filename=`, `showLineNumbers`, `highlight=` (1-indexed line numbers)
- **Code groups** — `:::code-group` with each child fence carrying `[label]` after the language. Active tab is a signal; SSR ships tab 0 visible; client-side hydration enables tab switching with **zero per-mount cost** (tabs are CSS class swaps via `data-active`, not VNode reconciliation)
- **Callouts** — `:::tip` / `:::warning` / `:::note` / `:::danger` / `:::info` container directives → `<Callout type="…">`. Levenshtein "did you mean…?" suggestion for unknown names + unclosed-fence heuristic warning
- **`registerExamples(import.meta.glob('./examples/⁎⁎/⁎.tsx'))`** must be called from `entry-client.ts` (compile-time-resolved relative to where it's called). Side-effect import + glob in the consumer's source tree
- **`<Example>` rendering contract**: example components MUST accept `{ shared?: Signal<T> }` as the prop shape and fall back to a local signal when undefined. This lets the same component work with or without sharing
- **Migration tooling**: `scripts/migrate-playground-to-example.ts` + `scripts/batch-fix-example-types.ts` automated the 36-instance migration. Reusable for future docs ports
- **`@pyreon/zero-content/README.md`** covers every plugin behavior + diagnostic; `docs/src/content/docs/zero-content.md` is the user-facing reference. Single source of truth for `Example` API lives in `@pyreon/zero-content` `manifest.ts:api[]` and MCP `get_api('@pyreon/zero-content/Example')`

- Pyreon-native: built on @pyreon/zero (fs-router + SSG) + @pyreon/zero-content (markdown pipeline)
- Deployed via GitHub Pages
- Workspace member: `"docs"` in root `package.json` workspaces
- Has `lint` script (oxlint) + typecheck (`tsc --noEmit`)

## Monorepo Structure

64 published packages across 5 categories under `packages/`, plus 7 private support packages:

- `packages/core/` — 10 packages: reactivity, core, compiler, runtime-dom, runtime-server, router, head, server, primitives, sized-map
- `packages/fundamentals/` — 26 packages: a11y, store, state-tree, form, validation, validate, query, table, virtual, i18n, feature, charts, storage, hooks, hotkeys, permissions, machine, flow, code, rich-text, document, rx, toast, url-state, dnd, sync
- `packages/tools/` — 11 published packages: cli, lint, mcp, vite-plugin, typescript, storybook, react-compat, preact-compat, vue-compat, solid-compat, svelte-compat; + `devtools` (private, not published — Chrome DevTools extension)
- `packages/ui-system/` — 11 packages: ui-core, styler, unistyle, elements, attrs, rocketstyle, coolgrid, kinetic, kinetic-presets, connector-document, document-primitives
- `packages/zero/` — 6 packages: zero, zero-cli, create-zero, create-multiplatform, meta, zero-content
- `packages/internals/` — 3 packages: test-utils, manifest, perf-harness (private, not published)
- `packages/ui/` — 3 packages: ui-theme, ui-components, ui-primitives (private, internal demo / app component library)

Plus: `docs/` (Pyreon-native docs site, package `@pyreon/docs`), `examples/` (example apps).

## Reactive vs Static — The Core Rule

In Pyreon, components run ONCE. What's reactive and what's static depends on WHERE you read a signal:

```tsx
// Component props with signal reads are REACTIVE — compiler wraps with _rp()
<MyComponent title={name()} />  // compiler → title: _rp(() => name()) → getter on props
// Reading props.title in an effect/accessor tracks the signal

// DOM text children are REACTIVE — compiler wraps in accessor
<div>{name()}</div>  // compiler → () => name() — re-evaluates on change

// Reactive accessor (explicit) — always reactive
<div>{() => `Hello ${name()}`}</div>

// Context reads — ReactiveContext returns () => T, regular Context returns T
const getMode = useContext(ModeCtx)  // ReactiveContext → () => ThemeMode
getMode()  // reactive read
```

Rule of thumb:

- **Component props** = reactive when expression contains signal reads (compiler wraps)
- **DOM children with signals** = reactive (compiler wraps)
- **Context reads** = ReactiveContext returns accessor, read it in reactive scope
- **Destructuring props** = captures once (static) — use `props.x` for reactivity
- **`const` from props in JSX** = reactive (compiler inlines `props.x` back at use site) — `const x = props.y; return <div>{x}</div>` is now reactive
- **`let`/`var` from props** = static (mutable variables are not inlined)
- **JSX spread on a component** = reactive (compiler wraps spread sources with `_wrapSpread`) — `<Wrapper {...rest} {...overrides}>` preserves every getter-shaped prop through to the receiving component, no matter how the source was constructed (`splitProps`, `mergeProps`, raw `props` of a parent). DOM-element spread `<div {...rest}>` is also reactive (template path's `_applyProps`). Manual `Object.assign({}, source)` or `{...source}` in plain JS (not JSX) is NOT covered — use `mergeProps` / `splitProps` for those.

## Memory Leak Classes

The 8-PR sweep across #725-#741 surfaced 5 distinct memory-leak classes with real bug instances. Each class is catalogued in `.claude/rules/anti-patterns.md` "Memory Leak Classes" section with: bug shape, fix shape, every PR that landed an instance, and static-analysis coverage. Read that catalog before introducing a new module-level cache / stack / registry. Classes seen in this sweep:

- **A** (position-based stack pop on shared state) — #725 / #729 / #733 vue-compat
- **C** (unbounded caches) — #733 lint AstCache / #737 createStore / #741 vite-plugin caches
- **D** (event-listener pile-up) — #734 initTheme / #739 svelte-compat ChildInstance
- **F** (Promise queue stale resolution) — #730 charts+storage / #737 createResource
- **H** (closure-captured snapshot retention) — #725 effect snapshots
- **I** (orphaned `Promise.race + setTimeout`) — #734 ISR / #735 ssg-plugin

Two preventative lint rules ship in the `recommended` preset for the classes with high static-analysis precision: `pyreon/init-fn-needs-idempotency` (Class D) and `pyreon/promise-race-needs-cleartimeout` (Class I). The other classes are caught at audit time by the leak-hunt subagent prompt.

## Common Issues & Fixes

- `ComponentFn<{ name: string }>` not assignable → solved by generic h()
- `@pyreon/reactivity` missing from deps → add to package.json + `bun install`
- `noNonNullAssertion` → use `if (!x) return` guard
- SSR empty render → forgot `mergeChildrenIntoProps` in renderComponent
- DOM tests need happy-dom preload (bunfig.toml in each package)
- Vite resolves `dist/` not `src/` → add `resolve.conditions: ["bun"]` to vite.config.ts
- `signal(5)` doesn't write → use `signal.set(5)` (dev mode warns)
- `onClick={undefined}` crashes in production → runtime now bails on non-function handlers
- Context destructuring loses reactivity → keep the object reference, access properties lazily
- Theme mode switching broken → PyreonUI now uses getter properties for reactive mode
- `mergeProps` throwing `Cannot redefine property` → a source's getter descriptor was created without `configurable: true`. `mergeProps` now forces `configurable: true` on copied descriptors, but when authoring your own `Object.defineProperty` on an object that may later be merged, always set `configurable: true` explicitly.
- Symbol-keyed props silently dropped by `splitProps` / `mergeProps` → now preserved. Both utilities use `Reflect.ownKeys` so symbol-keyed brands (e.g. `REACTIVE_PROP`) survive the split and merge.
- `RefProp<T> = Ref<T> | RefCallback<T>` — the old narrow `(el: T) => void` mount-only arm was removed. Callback refs ALWAYS receive `el | null` at runtime; if you need a mount-only handler, accept `T | null` and early-return when null. Pairs with #233's null-on-unmount fix.
- `<For>` SSR key markers (`<!--k:KEY-->`) now URL-encode the key and replace every `-` with `%2D` so user-controlled keys can never form `-->` and break out of the HTML comment.
- `<Show when={signal}>` / `<Match when={signal}>` no longer crash with `props.when is not a function`. The compiler's signal auto-call rewrites bare `when={mySignal}` to `when={mySignal()}`, which previously produced a value (boolean) where the framework expected an accessor. `Show` and `Match` now accept value OR accessor — `typeof === 'function'` check normalizes both shapes. Reactive cases still need `when={() => signal()}` so the framework can re-evaluate when the signal changes; the value form is for static booleans and the auto-call edge case.
- `<Element tag="hr" />` (and other void HTML elements: `input`, `img`, `br`, `link`, `area`, `base`, `col`, `embed`, `source`, `track`, `wbr`) no longer trip runtime-dom's "void element cannot have children" warning. The Wrapper helper used to leak an `undefined` child into the JSX slot via `{own.children}` even when Element correctly skipped passing children. Wrapper now drops the children slot entirely for void tags via `getShouldBeEmpty(own.tag)` — the same util Element uses to make the void/non-void decision.
- `@pyreon/styler` `insertRule` failures used to be silently swallowed in production. Earlier the dev gate was bare `process.env.NODE_ENV !== 'production'` (which read as dead code in real Vite browser bundles), then briefly `import.meta.env.DEV` (Vite/Rolldown-only — broken under Webpack/Next.js, esbuild, Rollup, Parcel, Bun). The current convention across the monorepo is bare `process.env.NODE_ENV !== 'production'` again — every modern bundler auto-replaces it at consumer build time. Reference: `packages/ui-system/styler/src/sheet.ts` uses a local `__DEV__` const derived from `process.env.NODE_ENV !== 'production'`. Same convention applied to `@pyreon/coolgrid` and `@pyreon/attrs`. Tree-shake regression test in `packages/ui-system/styler/src/__tests__/dev-gate-treeshake.test.ts` bundles `sheet.ts` through Vite production and asserts the warn strings are gone, plus a dev-mode bundle to assert they're preserved. Enforced by `pyreon/no-process-dev-gate`.
- Passing `layout` to `createApp` / `startClient` while ALSO using fs-router's `_layout.tsx` no longer double-mounts. fs-router emits `_layout.tsx` as a parent `RouteRecord` (children = page routes), so the matched route chain already wraps every page in the layout. Pre-fix, also passing the same component as `options.layout` to `startClient` added a second wrapper around App's RouterView — 3× `nav.sidebar` + 3× `main.content` rendered after hydration mismatch on the fundamentals-playground layout. `createApp` now detects collision (`options.routes.some(r => r.component === options.layout)`), drops the explicit option, and warns in dev. User-side: never pass `layout` to `startClient` when using fs-router's `_layout.tsx` (the SSR pipeline doesn't pass it either — they used to disagree). The 4 examples (`ssr-showcase`, `ui-showcase`, `app-showcase`, `fundamentals-playground`) all use `startClient({ routes })` only.
- `<RouterLink to={someExpr}>` with a compiler-emitted reactive expression (e.g. `to={props.path}` from a parent component) now renders the resolved URL as `href`. Pre-fix, two bugs combined to produce `<a class="active" href="() => props.path">` on the fundamentals NavItem layout: (1) `runtime-server` (SSR) and `hydrate.ts` skipped `makeReactiveProps` on the way into a component, so `props.to` was the raw `_rp` function instead of a getter — `${props.to}` stringified the function source. (2) `RouterLink` hard-overrode the user's `class` prop with its internal `activeClass` accessor instead of merging, silently dropping `class={() => isActive() ? 'active' : ''}`. Both fixed: `makeReactiveProps` is now wired through `runtime-server`'s `mergeChildrenIntoProps` and through `hydrate.ts`'s component setup (parity with `mount.ts`). `RouterLink` destructures `class` separately and composes the user-provided value with `activeClass` via `cx`, returned from a single accessor so `applyProp` wraps it once in `renderEffect`. The systemic SSR/hydrate fix closes a class of "any `<Comp prop={signalRead}>` produces wrong attribute in SSR" bugs that was masked elsewhere because most apps re-mount client-side rather than relying on hydrated values.
- `useStorage` / `useSessionStorage` / `useCookie` / `useMemoryStorage` / `useIndexedDB` no longer render empty post-hydration. Pre-fix, every `@pyreon/storage` reactive primitive wrapped a base `signal()` with a callable that delegated `.direct` / `.subscribe` / `.peek` / `.set` — but never forwarded the internal `_v` field that the compiler-emitted `_bindText` fast path reads. SSR rendered `<strong>light</strong>` correctly (server invoked `theme()` directly via the JSX expression) but post-hydration the `_bindText(theme, textNode)` fast path read `theme._v === undefined` → wrote `''` → subscribers fired on every `.set()` and re-wrote `''`. localStorage was updated correctly the whole time; only the DOM stayed empty. The bug shipped from package inception across every storage backend; no existing tests caught it because none exercised the compiler-emitted fast path against a storage signal. Fix: each storage factory now defines `_v` as a getter that forwards to the underlying `sig._v`, so the fast path reads the live value on every access. **General contract** for any callable that wraps a base signal: if you delegate `.direct`, you MUST also forward `_v` via getter (`Object.defineProperty(wrapper, '_v', { get: () => sig._v, configurable: true })`) — `_bindText` / `_bindDirect` skip the function call and read `_v` directly. The companion rule is documented under "Reactivity Mistakes" in `.claude/rules/anti-patterns.md`. Other wrapper-signal packages (`@pyreon/url-state`, `@pyreon/machine`, `@pyreon/state-tree`'s patch.ts) audit clean — they don't delegate `.direct`, so they take the safe slow path through `renderEffect`. Reference: `packages/fundamentals/storage/src/{local,cookie,custom,indexed-db}.ts`. Regression tests at unit (`packages/fundamentals/storage/src/tests/bind-text-compat.test.ts`, 6 specs) and real-Chromium e2e (`e2e/fundamentals/storage-hydration.spec.ts`, 4 specs); both bisect-verified.
- `@pyreon/core` `ErrorBoundary` cleanup no longer pops the wrong handler when sibling boundaries unmount out-of-order. **Same bug class as #725's `popContext` (followed up while sweeping core for the same pattern).** `ErrorBoundary` pushed its handler onto a module-level `_errorBoundaryStack` at setup and registered `onUnmount(() => popErrorBoundary())` where `popErrorBoundary` did `stack.pop()` — position-based. That assumed strict LIFO between push and pop, but sibling boundaries unmount in whatever order the renderer drives (keyed `<For>` removing a non-last item, `<Show>` flipping the FIRST of N siblings, route nav unmounting an outer of nested routes). A non-last sibling's `onUnmount` therefore popped the LAST sibling's handler instead of its own — the surviving (innermost) boundary's handler was removed from the stack and the unmounted boundary's stale handler was orphaned at the top. Subsequent throws in the surviving boundary's children dispatched to the orphan handler; `error.set(err)` on a disposed signal is a no-op, so the error was silently swallowed AND the surviving boundary's fallback never rendered. Real-app shape: chat lists / dashboards / any `<For>`-rendered list where each row is wrapped in `<ErrorBoundary>` — remove a row and the remaining rows silently stop catching errors. Fix: `popErrorBoundary(handler)` accepts the handler reference and removes by IDENTITY via `lastIndexOf + splice`. `ErrorBoundary`'s `onUnmount` passes its own handler. `popErrorBoundary()` (no-arg) still does `stack.pop()` for back-compat. Reference: `packages/core/core/src/{component.ts:popErrorBoundary, error-boundary.ts:71}`. Bisect-verified: reverting the fix → `error-boundary-stack-leak-repro.test.tsx` FIRST-unmounted-sibling spec fails with `AssertionError: expected null to be truthy` (surviving fallback never renders); restored → 2/2 pass.
- **Tools leak-class sweep follow-up (post-#725/#729/#730):** three findings across the 12-package `packages/tools/*` audit. **[Findings (1)+(2) are SUPERSEDED on the CLIENT by the owner-based context model — the global `pushContext`/`popContext`/`removeContextFrame` stack they describe is now SSR-only; client `provide()` writes onto the component's EffectScope owner and dies with the scope, so there is no orphan-frame growth. Finding (3) AstCache is unaffected and stands.]** (1) **`@pyreon/vue-compat` `provide(key, value)` used position-based `popContext` — exact #725 shape.** Vue-compat's `provide` pushed a Map onto Pyreon's global context stack via `pushContext` (the low-level API) and registered `unmountCallbacks.push(() => popContext())` for cleanup. `popContext` is the documented-unsafe `stack.pop()` API that the core `provide()` fix in #725 already stopped using. Sibling components unmounting out-of-order (renderer-driven `<For>` removal, `<Show>` flipping a non-last sibling, route nav unmounting an outer of nested provider chains) caused the first sibling's `popContext` to pop the LAST sibling's frame — the surviving sibling's frame was orphaned at the top forever. (2) **`@pyreon/vue-compat` `createApp(C).provide(k, v).mount(el)` pushed app-level provisions without ever popping them.** `mount()` ran `pushContext(...)` per provision before mounting, but the returned unmount only ran `pyreonMount`'s VNode cleanup — leaving app-level frames on the global stack permanently, one per provision per mount cycle. Multi-test harnesses + SSG re-mount cycles compound this. **Fix for both**: capture the pushed frame at push time, register identity-based removal via `removeContextFrame` (newly-exported from `@pyreon/core` for compat-layer consumers). Reference: `packages/tools/vue-compat/src/index.ts:provide` (line ~881) + `createApp.mount` (line ~1064). Bisect-verified: revert `vue-compat/src/index.ts` → `provide-stack-leak-repro.test.ts` both specs fail (`expected length to be baseLen, got baseLen + N`); restored → 2/2 pass. (3) **`@pyreon/lint` `AstCache` was unbounded** — keyed by FNV-1a hash of source text with no eviction. Each entry holds a multi-MB oxc-parsed AST + `LineIndex`. A long-running LSP session or `pyreon-lint --watch` editing across many files would accumulate one entry per UNIQUE content snapshot ever seen — hundreds of MB heap after a workday of editing. **Fix**: LRU bound (default 256 entries) using `Map`'s insertion-order property (first key = oldest); `get`/`set` on an existing key refresh recency by re-inserting at the tail. Reference: `packages/tools/lint/src/cache.ts:AstCache`. Bisect-verified: 5 LRU regression specs in `tests/ast-cache-lru.test.ts`; revert → all 5 fail; restored → 5/5 pass. **New `@pyreon/core` export**: `removeContextFrame(frame: Map<symbol, unknown>): void` — purely additive, used by `provide()` internally + now available to compat layers / advanced consumers that call `pushContext` directly. Audit byproducts NOT bundled: solid-compat `createStore` per-path signal Map unbounded; solid-compat `createResource` Class-F stale-resolution race; svelte-compat ChildInstance `unmountCallbacks` discard; vite-plugin per-instance caches lack file-delete invalidation.
- **Fundamentals leak-class sweep follow-up (post-#725/#729):** three correctness bugs found in 2 packages of the 22-package fundamentals audit. (1) `@pyreon/hooks` `useDialog` crashed on every component unmount — `ref` typed its parameter as `(el: HTMLDialogElement) => void` but Pyreon's RefCallback contract fires the ref with `null` on unmount; the body called `el.addEventListener('close', ...)` unconditionally, throwing `TypeError: Cannot read properties of null`. Fix: null path cleans up the previous binding + early-returns; ref param typed `HTMLDialogElement | null`. (2) `@pyreon/storage` `useStorage` cross-tab listener was retained ONCE per unique-key signal creation, NOT per consumer — same-key cached returns skipped the retain. `.remove()` always released, driving the refcount below the actual consumer count. Real-app symptom: N components each `useStorage('theme', 'light')`, one calls `.remove()` → cross-tab listener detached AND registry entry deleted → surviving N-1 consumers orphaned from the cross-tab pipeline. Fix: same-key cached returns also retain (refcount matches consumer count); `.remove()` no longer deletes the registry entry — keeps it so the listener's dispatch table remains intact. Reference: `packages/fundamentals/storage/src/local.ts:useStorage + .remove()`. Bisect-verified: revert → `Expected: "dark", Received: "light"` on surviving-consumer cross-tab test; restored → pass. (3) `@pyreon/charts` `loadAndRegister` + `@pyreon/storage` `openDB` both cached `loader().then(...)` / `new Promise(...)` in module-level Maps. Without a `.catch` clearing the entry on rejection, a single transient failure (CDN blip / IndexedDB quota) cached the rejected promise forever — any subsequent retry of the same key got the same rejection until page reload. Bounded by ~50 module keys / unique DB-store pairs but a real correctness footgun. Fix: `.catch(err => { cache.delete(key); throw err })` (same shape both files). **General rule** mirroring #725/#729's identity-removal lesson: any framework `Map<key, Promise<T>>` cache should clear on BOTH settle paths (`.then` AND `.catch`), not just resolution. Audit byproducts NOT bundled: `@pyreon/code` `<CodeEditor>` does not call `instance.dispose()` on unmount (design question — user vs component owns lifecycle); `@pyreon/state-tree` `_hookRegistry` unbounded growth with dynamic IDs (uncommon usage shape); `@pyreon/rx` `distinct`/`scan` missing dispose vs `debounce`/`throttle` having it (API inconsistency).
- `@pyreon/core` context stack no longer leaks orphan provider frames under repeated reactive remounts. Reporter shipped a 1 GB heap snapshot showing the live context stack at 321,024 entries but only 47 distinct provider `Map` instances — the same providers re-referenced thousands of times each, with 33 in-flight effect snapshots each holding a ~10,000-frame shallow copy via `captureContextStack()` from inside `effect()`'s setup-time snapshot capture. **Root cause (two cooperating bugs)**: (1) `provide()` registered `onUnmount(() => popContext())` — but `popContext` pops `stack.pop()` (the last frame). `mountReactive`'s effect-re-fire flow runs the previous-mount subtree cleanup INSIDE the effect's snapshot-restore window, so the snapshot-pushed frames sit ABOVE the descendant's own provider frame when its `onUnmount` fires. `popContext` popped the snapshot push; the descendant's provider frame was orphaned on the live stack. (2) `restoreContextStack`'s finally used `stack.splice(insertIndex, snapshot.length)` to remove its pushes — position-based. Identity-based removal by a descendant (fix 1) shifts the snapshot pushes down, making `splice(insertIndex, …)` either no-op or pull the wrong frames. **Fix**: both layers use IDENTITY-based removal. `provide()` and `withContext()` capture the frame reference at push, register `onUnmount(() => removeContextFrame(frame))` where `removeContextFrame` does `stack.splice(stack.lastIndexOf(frame), 1)` — robust to "wrong frame on top" regardless of the live stack's mutation history. `restoreContextStack`'s finally iterates `snapshot` in reverse and removes each frame via `lastIndexOf + splice`. `popContext` stays public + position-based (used by `@pyreon/runtime-server.trimContextStack` for the strict-LIFO SSR-render path where the unbounded-mutation hazard doesn't apply). Geometric amplification — each iteration leaves 1+ orphan frames; nested boundaries × thousands of toggles produce the 321k-frame state. Reference: `packages/core/core/src/context.ts:provide` + `withContext` + `restoreContextStack` + `removeContextFrame`. Bisect-verified: reverting `context.ts` to pre-fix state → the nested-boundaries regression spec in `packages/core/runtime-dom/src/tests/ctx-stack-growth-repro.test.tsx` fails with `expected 502 to be less than 10` (502 orphan frames after 500 toggle cycles); restored → 4/4 specs pass. **General contract** for any framework helper that pushes onto the context stack: capture the pushed `Map` reference at push time, register identity-based removal at cleanup. Never use `popContext()` as the cleanup callback — reactive boundaries can push frames between your push and your pop.
- `@pyreon/reactivity` `wrapSignal(base, { set, update? })` — the canonical **writable-side-effect facade** primitive (PR #1345). Wraps a base `signal()` with a callable whose reads (`()` / `.peek` / `.subscribe` / `.direct`) AND the internal `_v` field delegate to `base` (the `_v` forwarding is load-bearing for the compiler's `_bindText` fast path — see the storage-signal contract), while `.set` runs your custom `set` handler (do the side effect, then call `base.set(value)` to commit; `update` optional, defaults to `set(fn(peek()))`). Retired the hand-rolled facades in `@pyreon/storage` (5 backends) and `@pyreon/state-tree`, and FIXED a latent state-tree bind bug they shared (the facades missed `_v` forwarding). The `pyreon/storage-signal-v-forwarding` lint rule is the proof the primitive was missing — any wrapper delegating `.direct` without `_v` is now caught. Reference: `packages/core/reactivity/src/wrap-signal.ts`.
- `@pyreon/head` `useHead({ script })` — external scripts (`src` set) **default to `defer`** for non-blocking page load (PR #1365). The default applies ONLY when ALL hold: `src` present, no `type`, no `async`, no `defer` already set. Module scripts (`type="module"`) are NOT given explicit `defer` (they defer by spec); import maps + inline scripts (`children`, no `src`) are NEVER touched (must run synchronously); `jsonLd` is unaffected (`type="application/ld+json"`). Opt out with an explicit `type: ''` or `async: ''`. The `ScriptTag` type carries the full attribute surface (`crossorigin` / `integrity` / `nomodule` / `referrerpolicy` / `fetchpriority` / …). Reference: `packages/core/head/src/context.ts:ScriptTag`; docs `docs/src/content/docs/head.md` → Script Tags & Defer Default.
- `@pyreon/runtime-server` SSR attribute mapping — an internal `SVG_ATTRIBUTE_MAP` (module-level const, NOT exported) translates camelCase JSX SVG attrs to their kebab/namespaced SSR form (`strokeWidth` → `stroke-width`, `viewBox` stays, `xlinkHref` → `xlink:href`, etc.), with expanded HTML attribute coverage so `renderToString` emits the same attribute spelling the client `runtime-dom` does — closing a class of SSR↔client attribute-name drift (PR #1362). Reference: `packages/core/runtime-server/src/index.ts:SVG_ATTRIBUTE_MAP`; docs `docs/src/content/docs/runtime-server.md` → HTML & SVG Attribute Mapping.
- `@pyreon/head` `HeadProvider` no longer silently shadows an outer `HeadContext`. Pre-fix the component resolved its ctx as `props.context ?? createHeadContext()` — it ALWAYS allocated a fresh ctx when no explicit prop was passed, even when an outer `HeadContext` was already in scope. That broke every consumer of the documented composition `renderWithHead(h(HeadProvider, null, h(App)))` and, structurally, the entire `@pyreon/zero` SSG/SSR pipeline: zero's `createApp` mounts `h(HeadProvider, null, …)` unconditionally with no `context` prop, so every `useHead()` call in the App subtree wrote tags into the inner ctx while `renderWithHead` resolved the outer ctx (still empty) — static `dist/<path>/index.html` files shipped with an EMPTY `<head>` despite Meta/JSON-LD/OG/useHead working correctly. Social scrapers and non-JS crawlers saw nothing; Googlebot saw the post-hydration values so the bug stayed invisible to standard SEO sweeps. Resolution chain is now `props.context ?? useContext(HeadContext) ?? createHeadContext()` — explicit prop wins, otherwise inherit an outer ctx if one is in scope, otherwise auto-create. Inherited semantics restore the framework-wide rule that `HeadContext` is a single per-request registry; explicit-isolation cases (iframe / micro-frontend boundaries) keep working by passing `context={createHeadContext()}` explicitly. Reference: `packages/core/head/src/provider.ts:HeadProvider`. Bisect-verified: reverting `useContext(HeadContext) ??` from the chain → 2/5 specs in `provider-inherits-context.test.tsx` fail — `expected '' to contain '<title>Page Title</title>'` (zero-shape) AND `expected '<title>Outer Title</title>' to contain 'name="inner"'` (nested-shadow). Restored → 5/5 pass.
- `@pyreon/zero` `imagePlugin` placeholder strategy is now honoured on every path. `PlaceholderStrategy` typed `'dominant-color'` from the plugin's inception but never implemented it (CDN / dev / build all fell through to blur), AND `placeholder: 'none'` was only respected in the CDN path — `processImage` (build) and `loadDevImage` (dev) hard-coded `generateBlurPlaceholder`. Classic typed-but-unimplemented bug class (`audit-types` territory). Fix: a single `generatePlaceholder(input, strategy, size)` dispatcher used by all three paths. New `placeholder: 'color'` implements the dominant-colour strategy for real — sharp `.stats()` histogram swatch (approximate by design: pure-red → ~`#f80808`) as a constant ~200-byte flat SVG data URI (zero decode, instant paint, fixed size regardless of source complexity). `'dominant-color'` kept as a deprecated alias of `'color'`. Also: `quality` now accepts `number | Partial<Record<ImageFormat, number>>` for per-codec tuning (`{ avif: 55, webp: 75 }`); the single-number form is unchanged (backward-compatible). No `<Image>` changes — `placeholder` stays an opaque string. Reference: `packages/zero/zero/src/image-plugin.ts:generatePlaceholder` / `generateColorPlaceholder` / `resolveQuality` / `normalizePlaceholder`; tests `packages/zero/zero/src/tests/image-plugin.test.ts` (sharp-backed dispatch + per-format-quality + alias-normalization specs).
- `@pyreon/zero` `imagePlugin` dev-mode `?optimize` no longer serves raw filesystem paths. **Bug class — a downstream path consumer not updated when an upstream resolver started producing absolute paths.** The `resolveId` fix made `rawPath` an absolute fs path for relative/aliased imports (`/Users/…/img.png`), but `loadDevImage` still decided the dev URL with `rawPath.startsWith('/') ? rawPath : '/@fs/'+absPath` — `startsWith('/')` is `true` for an absolute fs path, so `vite dev` served `http://localhost:5173/Users/…/img.png` → 404 / broken image. Build mode was unaffected (separate `processImage` path → hashed `dist/` assets). Fix: `loadDevImage` uses the SAME `existsSync(rawPath)` discriminator the `absPath` derivation already uses — a real on-disk file (relative/aliased import) → Vite `/@fs/` prefix; only an unresolved `/foo.png` public-dir web path is served as-is (Vite serves `public/` at the web root). Audited siblings: favicon (static `/favicon-*` web hrefs, build-emitted), icons-plugin (Vite's own asset pipeline / `?raw` inline), SVG-as-component (inlined, never a fetched URL) — none share the class. Reference: `packages/zero/zero/src/image-plugin.ts:loadDevImage`; bisect-verified test `packages/zero/zero/src/tests/image-plugin-resolve.test.ts` (both branches: absolute → `/@fs/`, public web path → unchanged).
- `@pyreon/zero` ships ambient types for its custom Vite import queries out of the box (`@pyreon/zero/image-types`). **Packaging gap, not a missing-types gap** — `src/image-types.d.ts` (the correct `declare module "*.{jpg,png,webp,avif,svg}?optimize"` → `ProcessedImage`, `"*.svg?component"` → `ComponentFn`, `"*.svg?raw"` → `string`) already existed and shipped via `files: src`, but there was no `exports["./image-types"]`, so the `/// <reference types="@pyreon/zero/image-types" />` the file itself documents could not resolve — every consumer had to hand-author + manually keep a `declare module "*?optimize"` in sync with `ProcessedImage` (silent drift). Ecosystem precedent: `vite/client`, `vite-imagetools/client`, `vite-plugin-pwa/client` all ship their query ambients. Fix: wire `./image-types` as a real build entry (`{ bun: ./src/image-types.ts, import: ./lib/image-types.js, types: ./lib/types/image-types.d.ts }`, mirroring `./client`) + switch the internal `import('./image-plugin')` → `import('@pyreon/zero/image-plugin')` (resolution-stable: the package self-ref resolves via the consumer's `./image-plugin` export → clean `lib/types` declaration, not the full src `.ts`). **Two interlocking constraints made the "just add a `.d.ts`" first cut wrong (caught by CI, not local typecheck):** (1) `vl_rolldown_build` derives a build ENTRY from every exports subpath → a hand-authored `src/image-types.d.ts` with no buildable `src/image-types.ts` failed the zero build with `[UNRESOLVED_ENTRY] src/image-types.ts`. So the source is a real `src/image-types.ts` that `vl_rolldown_build` compiles → empty `lib/image-types.js` + DTS-emits `lib/types/image-types.d.ts` (the ambients, verbatim). (2) The repo tsconfig sets `moduleDetection: force` → every `.ts` is a module, so `declare module '*.svg?raw'` in a `.ts` is read as an *augmentation* of a non-existent module (TS2664). `src/image-types.ts` is therefore **excluded from zero's own `tsc --noEmit`** (it carries zero logic; the build's DTS emit produces a real `.d.ts` which is always ambient + moduleDetection-exempt for consumers). Consumers add one line to a tsconfig-covered `.d.ts`. **General lesson: a Vite plugin introducing a custom import query MUST ship + EXPORT its query ambients — and "ship a `.d.ts`" is insufficient when the build derives entries from exports (needs a buildable `.ts` source) AND `moduleDetection:force` is in play (the `.ts` ambient source must be excluded from the local typecheck; only the emitted `.d.ts` is the ambient contract).** Reference: `packages/zero/zero/src/image-types.ts` + `package.json` `exports["./image-types"]` + zero `tsconfig.json` `exclude`; test `packages/zero/zero/src/tests/image-types-export.test.ts`.
- `@pyreon/zero` no longer emits a `node:async_hooks` "externalized for browser compatibility" warning in a consumer's CLIENT build. **Bug class — a dynamic-import SITE for a `node:`-bearing module living in a client-reachable file.** `i18nRouting()` (a server-only Vite plugin) held `await import('./i18n-routing-als')` (als statically imports `node:async_hooks`), and it lived in `i18n-routing.ts` — which is client-safe and reachable from the main entry via `useLocale`/`setLocale`. A dynamic import always produces a code-split chunk, so Vite/Rolldown emitted the als chunk in the CLIENT build and warned (runtime-safe — the browser never loads the server-only chunk, `node:async_hooks` externalized — but real noise + a dead chunk + a smell). **A lazy `await import()` does NOT escape the client-bundle leak; it only downgrades a build ERROR to a build WARNING + a dead chunk.** Fix: split `i18nRouting()` + its `parseCookies`/`_parseCookiesForTesting` helpers into a new server-only `i18n-routing-plugin.ts`, exported ONLY from `@pyreon/zero/server` (the manifest already documented `i18nRouting` as server-only — this aligns the source with the contract; no public API change). `i18n-routing.ts` stays client-safe (`useLocale`/`setLocale`/`extractLocaleFromPath`/`buildLocalePath` + the `_setLocaleStoreReader` bridge), with no als reference — so the client graph never reaches the dynamic-import site. Same shape as the `island` → `@pyreon/server/client` split. The `check-client-bundle-node-imports` gate PASSED through this bug because it deliberately skips dynamic imports (can't distinguish a server-hook-gated one from a leak); the regression guard is a SCOPED reachability test (`packages/zero/zero/src/tests/i18n-routing-client-safe.test.ts`, walks the main entry following static + relative-dynamic imports, asserts `node:async_hooks` unreachable, bisect-verified). NOTE a real `vite build` INSIDE vitest does NOT reproduce (vitest's Vite context changes warning routing + tree-shaking vs a standalone consumer build) — the static reachability walk is the faithful, deterministic model. Reference: `packages/zero/zero/src/i18n-routing-plugin.ts` (new) + `i18n-routing.ts` + `server.ts`; anti-patterns.md → "node:\* module at module scope in a client-safe published package" (fix shape 3).
- `@pyreon/zero` `mode: "ssr"` and `mode: "isr"` now auto-build the SSR handler bundle at `dist/server/entry-server.js` AND auto-invoke `adapter.build({ kind: 'ssr', … })`. Same typed-but-unimplemented bug class as `mode: "ssg"` before it — `Adapter.build({ kind: 'ssr' })` was implemented for all 6 adapters (vercel/cloudflare/netlify/node/bun/static) but no production code path called it. SSR/ISR apps shipped only a client bundle; users had to bundle their own server entry by hand and wire the platform deploy config themselves. The new `ssrPlugin()` (auto-wired into `zeroPlugin()` when `mode === 'ssr' | 'isr'`) closes that gap, mirroring the `ssgPlugin` precedent exactly: separate plugin file, `apply: 'build'`, `enforce: 'post'`, per-mode env-flag gate (`PYREON_ZERO_SSR_INNER_BUILD`, distinct from SSG's `PYREON_ZERO_SSG_INNER_BUILD` so cross-mode flag-leak failure class is structurally impossible). **Synthetic-vs-user-entry contract**: when `src/entry-server.ts` exists, the plugin uses THAT file as the bundle entry (preserves user-authored `securityHeaders()` / `cacheMiddleware()` / `ssr.mode: 'stream'` overrides / `actions: { corsOrigins }` config). When absent, the plugin materializes the canonical 6-line shape (`import { routes } from "virtual:zero/routes"; … export default createServer({ routes, routeMiddleware, apiRoutes })`) and cleans it up after the build — same fallback the SSG plugin uses. The shared infrastructure between SSG and SSR/ISR (env-flag set/clear recipe, atomic-write helpers, mkdir-cache, template-injection, synthetic-entry materialization) lives in `packages/zero/zero/src/ssr-build-shared.ts` so the SSG and SSR plugins can never independently drift their cleanup contracts. The SSR plugin invokes `adapter.build({ kind: 'ssr', serverEntry, clientOutDir, outDir, config })` after the bundle lands; adapter throws are caught + logged (not rethrown) so a buggy adapter can't hide the successful SSR bundle from CI — the bundle stays on disk at `serverEntry` for hand-deploys. The recursive SSR sub-build is gated by `apply: 'build'` (never runs during `vite dev`) AND the env flag (so the inner build's plugin chain doesn't re-enter the outer closeBundle). **`zeroPlugin()` now returns `Plugin[]`** for every mode: `[mainPlugin]` for SPA, `[mainPlugin, ssgPlugin]` for SSG, `[mainPlugin, ssrPlugin]` for SSR/ISR — Vite's plugins array natively accepts nested arrays so consumer `plugins: [pyreon(), zero()]` keeps working. Reference: `packages/zero/zero/src/ssr-plugin.ts` + `packages/zero/zero/src/ssr-build-shared.ts`; SSG companion is `packages/zero/zero/src/ssg-plugin.ts` (refactored to consume the shared helpers).

  **Inner build inherits asset-emission settings from the outer config.** The inner `buildSsrBundle` runs `configFile: false` (it must NOT re-load + re-run the user's whole `vite.config.ts` — that's what the reconstructed `[pyreon(), zeroPlugin(userConfig)]` chain is for), but that ALSO dropped the user's `build.assetsInlineLimit`, so the inner build fell back to Vite's 4 KB default. Result: a `<= 4 KB` image the client build emitted as a hashed file (`/assets/logo-HASH.png`) was inlined as a `data:` URI in the SSR/SSG-rendered HTML — the two builds disagreed on the `<img src>` of every small image (a hydration mismatch; and combined with the SSR URL guard, the reason small placeholders could vanish from static output entirely). Fixed by capturing `resolved.build.assetsInlineLimit` + `resolved.build.assetsDir` in each plugin's `configResolved` and threading them into the inner build via `buildInnerBuildOptions(options)` (`ssr-build-shared.ts`) — a PURE, exported function so the config is unit-asserted without spawning a real build. **Only asset-EMISSION settings are inherited** (`assetsInlineLimit`, `assetsDir`); the SSR-runtime-fixed settings (`target: 'esnext'`, ES output, `node:` externals) are deliberately NOT inherited from the user. Apps that never set `assetsInlineLimit` keep Vite's default (no behaviour change). Proven with a real Vite SSR build (default inlines a sub-4 KB PNG as `data:`; with `assetsInlineLimit: 0` propagated it emits a file reference). Reference: `packages/zero/zero/src/ssr-build-shared.ts:buildInnerBuildOptions` + the `configResolved` capture in `ssg-plugin.ts` / `ssr-plugin.ts`; bisect-locked by `tests/ssr-build-shared.test.ts` (removing the propagation lines fails the 3 "propagates" specs).

  **SSR/ISR deploy artifact builds, runs, and HYDRATES (the staging + production-template fix).** Three latent bugs made `mode: 'ssr'` / `'isr'` unrunnable end-to-end — invisible until someone actually ran the emitted server (nobody could, because step 1 crashed). All exposed + fixed once the artifact ran:

  1. **Copy-into-self EINVAL in every deploy adapter.** The SSR plugin (`ssr-plugin.ts`) invokes each adapter with `clientOutDir === outDir === distDir` and the server bundle already at `distDir/server`. Every adapter then did `cp(clientOutDir, outDir/<subdir>)` — a copy of a directory into its own subtree → Node `fs.cp` throws `ERR_FS_CP_EINVAL` ("cannot copy … into itself"); the node/bun server copy `cp(distDir/server, distDir/server)` is the even more direct "src and dest cannot be the same". The throw was caught by the plugin and NOT rethrown (so a buggy adapter can't hide the SSR bundle from CI), which meant the deploy artifact was silently never staged — `node dist/index.js` (the runnable server) never existed. Fix: a single `materialize(src, dest, { preserve })` helper (`packages/zero/zero/src/adapters/stage.ts`) handling **same dir** (no-op — server already in place / static+cloudflare flat client), **dest inside src** (copy top-level entries INDIVIDUALLY into dest — each entry's source/dest are disjoint subtrees once the dest's own top segment is skipped, so no copy-into-self — PRESERVING the originals so the flat `outDir` still serves under `vite preview` / other tooling; a whole-dir `cp(src, dest)` is what would EINVAL), and **disjoint** (recursive copy). Wired into all SIX adapters (node, bun, static, vercel, netlify, cloudflare); each was independently broken because the client dest is always `outDir`-or-a-subdir-of-`outDir`. **Why the bug shipped: the pre-existing `adapters.test.ts` used a mock client dir DISTINCT from `outDir`, so it only ever hit the `cp` (disjoint) branch — it never exercised the `clientOutDir === outDir` shape the real plugin produces.** A same-dir regression block now covers all six adapters; bisect-verified — reverting `materialize` to a raw `cp` fails all six with the exact EINVAL.

  2. **`/` shipped the empty `<!--pyreon-app-->` shell + no HTTP wrapper.** Once staging works, the node/bun server's static-first branch served the SSR template `index.html` at `/` (it maps `/` → `index.html`, `.html` has an extension → served as a static file), shipping the UNFILLED template shell instead of server-rendering the home route — and the cp crash had also meant the `dist/index.js` HTTP wrapper was never written (the "entry-server.js not HTTP-wrapped" symptom). Fix: the emitted node/bun server now static-serves ONLY real assets (`if (ext && ext !== '.html')`, no `/` → index.html mapping); `/` and any `.html` path fall through to the SSR handler → server-rendered. (A static SPA shell at `/` is what `mode: 'spa'` / the static adapter are for; in SSR mode `dist/client/index.html` is the unfilled template, never a prerendered page.)

  3. **Production SSR shipped the DEV client entry → no hydration.** `createHandler` defaulted `clientEntry` to `/src/entry-client.ts` (a dev path Vite serves; in production there's no Vite and it 404s) + `DEFAULT_TEMPLATE` (no hashed `<script>`, no CSS). `createServer({ routes })` passed neither, so the production page server-rendered but the browser 404'd the dev entry → never hydrated, no styles. Fix (adapter-agnostic): the SSR build copies the built client `index.html` (which carries the hashed `<script type=module src=/assets/index-*.js>` + CSS `<link>` + the `<!--pyreon-app-->`/`<!--pyreon-head-->`/`<!--pyreon-scripts-->` placeholders) → `dist/server/template.html`; `createServer` reads that sibling via `readBuiltTemplate()` (`new URL('./template.html', import.meta.url)`) and uses it as the production template, suppressing the dev client-entry injection via a new `clientEntry: false` option on `createHandler`/`createServer` (the template already references the hashed entry). Every deploy adapter copies the WHOLE server dir, so `template.html` travels to node/bun/vercel/netlify/cloudflare alike. Dev/tests have no sibling → `readBuiltTemplate` returns `undefined` → the handler's defaults apply (dev SSR renders via the separate `renderSsr` path that reads the real `index.html`).

  Also fixed the `createServer` JSDoc example (`@pyreon/zero` → `@pyreon/zero/server` — the bare import throws the server-only guard; the actual create-zero template was already correct).

  **Proven end-to-end** at three layers: (a) unit — `stage.test.ts` (materialize same-dir/move/disjoint), `adapters.test.ts` (all six adapters' same-dir staging + the node/bun spawn-and-curl runtime contract now asserting `/` SSRs not the shell), `server.test.ts` (`clientEntry: false` suppression + single-script-no-duplicate); (b) build-artifact — `verify-modes` `× ssr` cells (ssr-showcase / app-showcase / playground) assert the staged `dist/{client/index.html, server/entry-server.js, server/template.html, index.js, package.json}` layout (the layout only exists if staging ran without EINVAL); (c) real-Chromium — the new `ssr-node` e2e gate (`e2e/ssr-node.spec.ts`, `bun run test:e2e:ssr-node`, own config on port 5203) builds ssr-showcase in `mode: 'ssr'` and runs the EMITTED `node dist/index.js`, asserting `/` + `/about` are server-rendered (no `<!--pyreon-app-->`), static assets serve, and the page hydrates + client-navigates. Bisect-verified: disabling `readBuiltTemplate` (production template) fails the e2e hydration + asset specs with the dev-entry 404 while the SSR-render specs stay green; reverting `materialize` to a raw `cp` fails the adapter same-dir tests with EINVAL. Reference: `packages/zero/zero/src/adapters/stage.ts` + the six `adapters/*.ts`, `packages/zero/zero/src/ssr-plugin.ts` (template.html copy), `packages/zero/zero/src/entry-server.ts` (`readBuiltTemplate` + `clientEntry: false`), `packages/core/server/src/handler.ts` (`clientEntry: string | false`).

  **ISR shares this whole path** (`mode: 'isr'` → same node-adapter staging + production template; the handler is additionally wrapped by `createISRHandler`). It is gated the same way: a `verify-modes ssr-showcase × isr` cell (staged-artifact + production template) + the `isr-node` real-Chromium e2e (`e2e/isr-node.spec.ts`, `bun run test:e2e:isr-node`, port 5207 — runs the emitted `node dist/index.js`, asserts `/` server-renders + hydrates + is **cache-consistent** across repeat requests). The ISR caching SEMANTICS (LRU / TTL / `cacheKey` / `revalidate`) are unit-covered by `packages/zero/zero/src/tests/isr.test.ts` (47 specs). Before this, ISR's end-to-end deploy had NO committed gate (only the unit caching tests) — `isr-node` + the verify-modes cell close that.

  **Cloudflare (workerd) SSR — the node-runtime template+singleton path breaks on workerd; two fixes make it hydrate.** The above `readBuiltTemplate()` (`new URL('./template.html', import.meta.url)` + `readFileSync`) and the `registerSingleton(..., import.meta.url)` self-registration both assume a Node-style runtime. The node/bun/vercel/netlify adapters run on Node, so they work. **Cloudflare runs in workerd**, which has NO filesystem and passes `undefined` for `import.meta.url` — so verified-in-real-workerd (`wrangler pages dev`) surfaced two breakages a Node-side test could never catch: **(1) startup crash** — `normalizeLocation(undefined).indexOf('?')` threw `Cannot read properties of undefined (reading 'indexOf')` at module-eval (before any handler), taking down every `@pyreon`-based Worker; **(2) no hydration** — `readBuiltTemplate`'s `readFileSync` can't reach the staged `_server/template.html` sibling on workerd → caught → `undefined` → the page shipped the dev `entry-client.ts` (no hashed `<script>`) and never hydrated. Fixes: (1) `@pyreon/reactivity` `normalizeLocation` guards `typeof url !== 'string' || url.length === 0 → '<unknown>'` (the duplicate-detection key is the package NAME + location, so a single re-registering `<unknown>` instance is idempotent via the same-location early-return; the only degraded case — two genuinely-distinct `<unknown>`-located instances → missed duplicate — is the documented safe failure mode AND structurally unreachable in workerd's single bundle). (2) the **cloudflare adapter inlines** the built template (`dist/server/template.html`, with the hashed entry) into `globalThis.__PYREON_SSR_TEMPLATE__` in `_worker.js`, then **dynamic-imports** the handler (`const { default: handler } = await import('./_server/entry-server.js')`) so the global is set BEFORE `createServer → readBuiltTemplate` evaluates; `readBuiltTemplate` reads that global FIRST, falling back to the `readFileSync` sibling for Node runtimes. `JSON.stringify(template)` is safe — `_worker.js` is an ES module (no HTML tokenizer), so `</script>`/backticks/`${`/the `<!--pyreon-app-->` markers all survive byte-for-byte (the handler's marker `.replace` finds them). **`nodejs_compat` is load-bearing, not optional**: the SSR bundle statically imports `node:async_hooks` (`runWithRequestContext`, instantiated at module-eval) + `node:fs`, which workerd resolves ONLY with the flag — the create-zero cloudflare scaffold sets it (`wrangler.toml: compatibility_flags = ["nodejs_compat"]`). **Lessons (in `.claude/rules/anti-patterns.md` → Build Pipeline Mistakes):** any `import.meta.url` deref at module-init must guard for `undefined` (workerd); a filesystem read as the ONLY delivery path for a build artifact breaks no-fs runtimes — provide a `globalThis`-inlined fallback set before the consuming module evaluates. Verified: `wrangler pages dev --compatibility-flags nodejs_compat` → `/posts` 200, server-rendered, ships hashed `/assets/index-*.js`, hydrates. Locked by `singleton-sentinel.test.ts` (workerd undefined-url block, bisect-verified → `indexOf` TypeError) + `adapters.test.ts` (the `_worker.js` global-set-before-dynamic-import + hashed-template structural assertion, bisect-verified) — node-invoke smoke alone can't catch workerd breakage (it has fs + a real `import.meta.url`, so it's a false-positive for the CF runtime). Reference: `packages/core/reactivity/src/singleton-sentinel.ts:normalizeLocation`, `packages/zero/zero/src/adapters/cloudflare.ts` (template inline + dynamic import), `packages/zero/zero/src/entry-server.ts:readBuiltTemplate` (global-first). **Follow-up (deferred):** a real-workerd CI e2e (`wrangler pages dev`, mirroring `ssr-node`/`isr-node`) — wrangler downloads workerd (cacheable like Playwright Chromium); the committed structural `_worker.js` assertion is the regression lock until then.

- `@pyreon/zero` `mode: "ssg"` now actually generates per-route HTML at build time. Before this fix, `mode: "ssg"` and `ssg.paths` were typed in `types.ts` but had no runtime — the plugin had zero Rollup build hooks, so apps configured for SSG silently shipped a SPA shell with no per-route HTML. The new `ssgPlugin()` (auto-wired into `zeroPlugin()` when `mode === 'ssg'`) hooks `closeBundle`, runs a programmatic Vite SSR sub-build of a synthetic entry that imports `virtual:zero/routes` + zero's `createServer`, loads the resulting handler, and renders each path to `dist/<path>/index.html`. Path resolution: `ssg.paths` accepts `string[]`, `() => string[]`, or `() => Promise<string[]>`; if neither is set, the plugin auto-detects static paths from the fs-router tree (anything without `:param` or `*` segments). The recursive sub-build is gated by an env-var flag so it doesn't infinitely re-trigger itself. **Breaking change for `zeroPlugin()` consumers**: it now returns `Plugin[]` (`[mainPlugin]` for non-SSG modes, `[mainPlugin, ssgPlugin]` for SSG). Vite's plugins array natively accepts nested arrays, so `plugins: [pyreon(), zero()]` keeps working — but downstream test code that assumed `zeroPlugin()` returned a single Plugin needs `.[0]` or array-aware access.

  **`getStaticPaths` per-route export (PR A of the SSG roadmap).** Dynamic routes (`/posts/[id].tsx`) used to be silently skipped during auto-detect — users had to hand-list every value in `ssg.paths`. Now: any route file can `export const getStaticPaths: GetStaticPaths<{ id: string }> = () => [{ params: { id: 'a' } }, { params: { id: 'b' } }]` (sync or async, returns `Array<{ params: TParams }>`). The fs-router scanner picks up the export at build time; the SSG plugin's auto-detect calls each route's enumerator and expands the URL pattern (`/posts/:id` × `[a, b]` → `/posts/a`, `/posts/b`). Catch-all routes (`[...slug].tsx`) work via `{ params: { slug: 'a/b' } }` → `/blog/a/b`. Errors during enumeration (function throws, returns non-array, missing params) are captured into `PrerenderResult.errors` and the build continues for other routes — SSG never aborts on a single bad enumerator. The type helper `GetStaticPaths<TParams>` is exported from `@pyreon/zero/server`. Mirrors Astro's per-route convention. Reference: `packages/zero/zero/src/fs-router.ts:detectRouteExports` (scanner), `packages/zero/zero/src/ssg-plugin.ts:autoDetectStaticPaths` (expansion), `packages/zero/zero/src/ssg-plugin.ts:expandUrlPattern` (URL substitution). Real-app coverage: `examples/cpa-pw-blog/src/routes/blog/[slug].tsx` enumerates blog post slugs.

  **PR C — auto-emit `dist/404.html` from `_404.tsx` convention.** fs-router scans `_404.tsx` / `_not-found.tsx` and attaches the default export as `notFoundComponent` on its parent layout's `RouteRecord`. The synthetic SSG entry walks the routes tree at module-eval time, picks up the first `notFoundComponent` (depth-first, supports nested per-locale 404s in the future), and exposes an async `__renderNotFound()` that pushes it through the same `renderWithHead` pipeline as regular paths — so styler tag, `@pyreon/head` meta, and asset preload links land on the rendered 404 exactly like every other prerendered page. The outer plugin reads both, renders, runs the result through the shared `injectIntoTemplate` helper (factored out of the per-path render loop in this PR — single source of truth for placeholder/fallback injection), and writes to `dist/404.html` (NOT `dist/__pyreon_404__/index.html`). Static hosts (Netlify, Cloudflare Pages, GitHub Pages, S3+CloudFront) serve `404.html` automatically for unmatched URLs — convention every major SSG framework uses. Gated by `config.ssg.emit404 !== false` (default `true`); skipped silently when no `_404.tsx` exists. The runtime's not-found handler wrapper short-circuits BEFORE the router for unmatched URLs and renders the component directly via `h(NotFound, null)`, so the SSG path mirrors that — no probe URL, no router preload. Bisect-verified at TWO layers: unit (`packages/zero/zero/src/tests/ssg-plugin.test.ts` — `walks recursively into route children` fails when the `r.children` recursion is removed) AND verify-modes (`scripts/verify-modes.ts` — `ssr-showcase × ssg` cells fail with `expected file: dist/404.html` when `findNotFoundComponent` is stubbed to return null).

  **Limitations of PR C** (most closed by PR L5 below): (1) **Host routing is required.** SSG writes `dist/404.html` but the static host (or your own reverse proxy) is responsible for SERVING it when an incoming URL doesn't match a prerendered file. Most managed hosts (Netlify / Cloudflare Pages / GitHub Pages / Vercel static) wire this by convention; S3+CloudFront / nginx / Caddy need explicit config. See `docs/src/content/docs/zero.md:_404.tsx convention` for per-platform deploy snippets. Without host routing, the framework emits the file but unmatched URLs return 404 with empty/default body — the carefully-rendered 404 HTML never reaches the user. (2) **Layout chrome was missing pre-L5** — that limitation is closed by PR L5 below: 404s now render WITH parent layout chrome (headers, footers, navigation).

  **PR L5 — 404 layout chrome via router-driven render.** Pre-L5, both PR C (default-locale 404) and PR K (per-locale 404) bypassed the router and rendered the not-found component via `h(NotFound, null)` standalone. The matched chain was empty, so parent `_layout.tsx` components didn't wrap the rendered 404 HTML — apps with branded chrome (sticky headers, sidebars, navigation, PyreonUI provider) saw a bare 404 page instead. Users had to inline the relevant markup into `_404.tsx` itself.

  L5 closes the gap by extending `@pyreon/router`'s `resolveRoute`. When a URL doesn't match any route, the resolver walks the route tree finding the deepest layout record (one with `children`) that has a `notFoundComponent` AND whose path is a prefix of the requested URL. It builds a synthetic chain `[...ancestorLayouts, syntheticLeaf]` where the synthetic leaf is a `RouteRecord` with `component = parent.notFoundComponent` and `path = '__pyreon_not_found_leaf__'`. The returned `ResolvedRoute` carries `isNotFound: true` so SSR handlers can set HTTP status 404 downstream.

  The SSG entry's `__renderNotFound(locale)` was rewritten to leverage this. Instead of standalone `h(component, null)`, it navigates the router to a synthetic non-matching probe URL per locale (`/__pyreon_not_found_probe__` or `/{locale}/__pyreon_not_found_probe__`) and runs the SAME `renderPath` pipeline that produces regular pages. `resolveRoute` returns the synthetic chain, `<RouterView />` at depth 0 renders the layout (which uses its inner `<RouterView />` to render the synthetic leaf at depth 1, producing the not-found component INSIDE the layout's chrome). Locale-aware: the probe URL's locale prefix ensures the right per-locale layout subtree matches.

  **Filter to layout records (records with `children`).** fs-router attaches `notFoundComponent` to BOTH the parent layout AND every page record under that layout. Page records have no `<RouterView />` to render the synthetic leaf at the next depth, so picking a page as the fallback parent produces a chain `[Layout, Page, syntheticLeaf]` where `Page` swallows the leaf. The walker filters to records with `Array.isArray(r.children) && r.children.length > 0` so the synthetic leaf always lands at a depth a `<RouterView />` will actually render. Discovered during integration: without this filter, the ssr-showcase 404 rendered the HOME page (deepest page with notFoundComponent → home record with path `/`) instead of the 404 component.

  **`pathPrefixApplies` segment-boundary semantics.** `/de` applies to `/de/unknown` and `/de` itself but NOT to `/encyclopedia` — full segment boundary required at the prefix's end. Root `/` applies to every URL. Deeper matches win, ties broken by path specificity (segment count). Wildcard catch-all routes (`(.*)`) still take precedence — they fire before the notFound fallback.

  **Bisect-verified at THREE layers**: (1) **router unit tests** — 9 specs in `packages/core/router/src/tests/match.test.ts` (`resolveRoute — notFoundComponent fallback` describe block), 5 of which fail when the fallback is disabled. (2) **verify-modes** `ssr-showcase × ssg` cell asserts `dist/404.html` contains BOTH `data-testid="not-found-page"` AND `data-testid="nav-home"` / `data-testid="nav-about"` — the layout chrome (nav links) and the 404 content co-occur in the rendered file. Reverting `findNotFoundFallback` → cell fails with `expected dist/404.html to contain "data-testid=\"not-found-page\""` (the empty router-view renders only layout chrome, no leaf). (3) **e2e** at `e2e/ssg-i18n.spec.ts` (per-locale 404 spec) — real-Chromium asserts both the testid AND nav links visible on `/de/404.html` AND `/404.html`. Companion `ssr-showcase × ssg` matrix cell covers default-locale 404.

  **Closes the doc limitation from L4.** L4 documented "no layout chrome" prominently in `docs/src/content/docs/zero.md:_404.tsx convention`. L5's update inverts the note: layouts DO wrap the 404 now. Host routing (the second L4 limitation) is unchanged — static hosts still need explicit per-locale 404 routing for unmatched URLs under locale prefixes.

  **noindex auto-injection (claim-3 from the render-modes audit).** The framework injects `<meta name="robots" content="noindex, nofollow">` into every emitted 404 HTML — at runtime (`render404Page` in `not-found.ts`) AND at build time (the SSG plugin's per-locale `__renderNotFound` writeFile path wraps the result of `injectIntoTemplate`). The `<Meta>` component's default of `'index, follow'` is correct for regular pages but actively wrong on a 404 — and most `_404.tsx` examples don't override it, so a stock build silently emitted `<meta name="robots" content="index, follow">` on every 404 page. Shared helper `ensureNoindexMeta(html)` is the single contract: it INJECTS the noindex tag when no robots meta is present, and OVERRIDES an index-permitting robots meta (the `<Meta>` default `'index, follow'`, an explicit `'index, follow'`, or the `'all'` shorthand) → a 404 is never indexable, so any index-positive value is rewritten to `noindex, nofollow`. A deliberate non-indexing directive (`noindex` / `none`) passes through UNCHANGED — which preserves a sophisticated app's `<Meta robots="noindex, nofollow, noarchive">` and keeps the helper idempotent against its own inject. **The original "bail if ANY robots meta exists — user override always wins" contract was too permissive**: the `<Meta>` component emits its `'index, follow'` default whenever the user doesn't pass an explicit `robots`, so every `_404.tsx` that used `<Meta>` (the common case — for title/canonical) silently shipped `<meta name="robots" content="index, follow">` on the 404. This was the bokisch.com 0.27.1 regression (Lighthouse confirmed `index, follow` on the live `/404.html`). Index-positive override is a strictly-better contract — the only value it clobbers is one that says "index this 404," which is never intended; a genuinely-deliberate `noindex…` still wins. Override case-insensitive across both quote styles; tokens split on `,` and matched exactly so `noindex` is never confused with another directive. Safe on body-only fragments (returns input unchanged when no robots meta AND no `</head>` is found). Bisect-verified at TWO layers: (1) unit specs in `ssr.test.ts` "render404Page" describe — reverting the helper to the old bail-on-any behavior fails the two `… is OVERRIDDEN …` specs (the `<Meta>` default `index, follow` case + the single-quote `all` case) with `expected … to contain '<meta name="robots" content="noindex,…'`, while the inject + deliberate-noindex-preserve specs still pass; restored → 14/14, AND (2) verify-modes `ssr-showcase × ssg` cell asserts `dist/404.html` contains `<meta name="robots" content="noindex, nofollow">` — stubbing the helper makes that cell fail with the missing-string error while every other cell stays green. Reference: `packages/zero/zero/src/not-found.ts:ensureNoindexMeta` (+ `robotsPreventsIndexing` directive parser) + the SSG call site at `ssg-plugin.ts` (`ensureNoindexMeta(injectIntoTemplate(template, result))`).

  **Limitations / known gaps** (after L5 + M1.2 + the 2026-05-13 layout-less-app fallback + the 2026-05-13 loader-skip-on-404 fix): (a) ~~**`notFoundComponent` is attached to layouts only.**~~ **Closed by the layout-less-app fallback (PR B follow-up to M1).** `findNotFoundFallback` (`packages/core/router/src/match.ts`) runs a two-pass walk: first the original layout pass (records with non-empty `children`), then a fallback for page records with `notFoundComponent` (records WITHOUT children — the layout-less single-page-app shape). When the second pass fires, the resolver synthesizes the chain as `[DefaultChromeLayout, syntheticLeaf]` where `DefaultChromeLayout` is a built-in `components.tsx` component rendering `<main data-pyreon-default-chrome><RouterView /></main>` — semantic-HTML landmark + a CSS hook for user-side styling, no prescribed visual design. The render pipeline produces 404 HTML wrapped in `<main>` instead of bare component output, accessibility / SEO improved. Layout-with-`notFoundComponent` precedence is preserved (layout pass wins; page-record fallback only fires when no layout matches). The `DefaultChromeLayout` wires through the setter pattern `_setDefaultChromeLayout()` in `match.ts` (registered at module load from `components.tsx`) to avoid a `components.tsx → match.ts` circular import — graceful degradation: if no consumer ever imports anything from `./components.tsx`, the setter doesn't fire and the fallback returns `null` (back to the standalone-render path). (b) ~~**Loaders on parent layouts run during 404 render.**~~ **Closed by the 2026-05-13 loader-skip-on-404 fix.** `router.preload(path, request?, options?)` now accepts a third arg with `skipLoaders: true` that bypasses the loader-running step entirely while keeping lazy-component resolution intact (so the synthetic chain still renders cleanly). The SSG plugin's `renderPath` forwards `{ isNotFound: true }` from `__renderNotFound` → `router.preload(probePath, undefined, { skipLoaders: true })`. Auth-touching layout loaders (`fetchUser`, session reads, private APIs) no longer fire during static 404 generation — the build has no real request context to drive them. Runtime SSR (`@pyreon/server`'s `createHandler` + dev-mode `renderSsr`) intentionally still runs loaders for 404 paths: analytics / audit-logging hooks that fire per-request should keep firing even when the request resolves to a not-found, and consumers may have wired loaders that are robust to the missing request context. The skip is opt-in at the call site; the framework only applies it for SSG build paths. Bisect-verified: reverting the `if (options?.skipLoaders) return` gate in router.ts:preload fails 2 specs in `router.preload — PR C — skipLoaders option for 404 build paths`. Public API surface change: `Router.preload` signature gained an optional third `options` parameter — back-compat (positional arg, no breakage for existing 2-arg callers). (c) ~~Runtime SSR (mode: 'ssr') still uses the old URL-pattern wrapper.~~ **Closed by M1.2.** Both production SSR (`@pyreon/server`'s `createHandler` in `packages/core/server/src/handler.ts`) AND the dev-mode `renderSsr` in `packages/zero/zero/src/vite-plugin.ts` now route unmatched URLs through the router's `resolveRoute` → `findNotFoundFallback` synthetic chain, so runtime SSR 404s render WITH layout chrome and emit real HTTP status 404. `entry-server.ts`'s legacy URL-pattern wrapper is preserved as a fallback only for apps that pass `notFoundComponent` to `createServer` WITHOUT having `_404.tsx` in the routes tree (backward-compat path; new apps should use the routes-tree convention). Bisect-verified at three layers: (1) per-package unit (`resolveRoute — notFoundComponent fallback` describe block, 9 specs), (2) verify-modes (ssr-showcase × ssg cell asserts layout chrome co-occurs in `dist/404.html`), (3) real-Chromium e2e (`e2e/ssr-showcase.spec.ts` — `runtime SSR 404 wraps not-found in layout chrome (M1.2)` spec hits an unmatched URL through the dev SSR pipeline + asserts both `data-testid="not-found-page"` AND nav-home / nav-about testids visible + HTTP 404 status). Reverting `findNotFoundFallback` in match.ts → unit specs fail; reverting handler.ts's `isNotFound` read → SSG cell still produces chrome HTML but emits status 200; reverting vite-plugin.ts's URL-pattern bailout removal → dev e2e fails with `element(s) not found` for `not-found-page` testid (handle404's static fallback fires instead).

  Reference: `packages/core/router/src/match.ts:findNotFoundFallback` + `resolveRoute`, `packages/core/router/src/types.ts:RouteRecord.notFoundComponent` + `ResolvedRoute.isNotFound`, `packages/zero/zero/src/ssg-plugin.ts:__renderNotFound` (probe-URL render path).

  **M1 correctness fixes (single PR closing four typed-but-incomplete behaviors).** Surfaces after the SSG roadmap completed; each one independently bisect-verified.

  - **M1.1 — ISR `cacheKey` opt-in + auth-incompatibility warning.** `createISRHandler(handler, config)` (`packages/zero/zero/src/isr.ts`) used to key the cache by `url.pathname` ONLY — cookies / query strings / headers were silently stripped. For auth-gated pages a single user's cached HTML was served to every other user, including anonymous visitors. The behaviour was correct for public sites but a privacy footgun for any logged-in surface. M1.1 added `ISRConfig.cacheKey?: (req: Request) => string` (opt-in) and prominently documented the auth caveat in `ISRConfig` JSDoc + `docs/src/content/docs/zero.md` ISR section + `llms-full.txt`. Companion fix in `revalidate()`: the background re-render now uses `deriveKey(originalReq, url)` so cookie-varied entries get re-rendered against the SAME cookies that produced them — pre-fix the revalidation issued a no-cookie request and stomped the user's cached entry with anonymous content. Drive-by: the cache-miss path now routes through the LRU helper `set()` (previously called `cache.set` directly, bypassing the `maxEntries` cap). Tests: `packages/zero/zero/src/tests/isr.test.ts` (3 cacheKey specs covering default pathname-only, cookie-varied, query-varied). **Default cacheKey changed to `pathname + search` (v0.27 follow-up).** M1.1's opt-in was a band-aid — the unsafe `pathname`-only default still shipped, so apps that didn't read the JSDoc still silently served `/posts?id=42` HTML for `/posts?id=99` requests. Visibly wrong content, structurally invisible to tests that probe one URL per route. The default is now `url.pathname + url.search` (matches Next.js ISR + RSC convention). Cookies / Authorization headers are still NOT included by default — that still requires an explicit `cacheKey` and the auth caveat survives. A one-time dev-mode `console.warn` fires at handler init when no `cacheKey` is configured (gated on bare `process.env.NODE_ENV !== 'production'` for bundler-agnostic tree-shake; deduped via module-level `WeakSet<deriveKey>` so a busy CMS doesn't spam logs; per-handler-instance contract so multiple handlers each surface their own warning). The warning names BOTH trade-offs explicitly: AUTH-UNSAFE for loaders reading cookies (supply `cacheKey: (req) => `${pathname}::${session}``), and HIGH-CARDINALITY for routes with analytics params like `utm_*` / `fbclid` (use `cacheKey: (req) => new URL(req.url).pathname` to strip the query). **Breaking default change in a minor bump** (pre-1.0 framework): apps relying on `pathname`-only caching with high-cardinality URLs will see cache growth proportional to unique query strings — the warning at handler init names the fix. Bisect-verified: reverting `deriveKey` to `url.pathname` makes the new default-behaviour test fail with `Expected "call 2 (q=99)" / Received "call 1 (q=42)"` (proves the test is load-bearing). Reference: `packages/zero/zero/src/isr.ts:deriveKey` + `_warnedDefaultCacheKeyHandlers`.

  - **Pluggable ISR backing store (multi-instance production unlock).** `createISRHandler` was previously hard-wired to `new Map<string, CacheEntry>` — per-process, never shared. Multi-instance deploys (load-balanced Node, autoscaled containers, edge functions) needed sticky sessions or external cache plumbing the framework didn't provide. New: `ISRConfig.store?: ISRStore` accepts any backing matching `{ get(key), set(key, entry), delete?(key) }` with sync OR async returns. Default unchanged: `createMemoryStore({ maxEntries })` (extracted from the previous in-place `Map` logic) — drop-in pass-through, behaviour-identical for existing callers. Exports from `@pyreon/zero/server`: `ISRStore`, `ISRCacheEntry` (types), `createMemoryStore` (factory). The handler `await`s every store call — for the in-memory default this is a same-tick microtask (no real cost; `await` on a non-Promise returns the value); for Redis / Vercel KV / Cloudflare KV / Upstash adapters the awaits hit the real network promises. `config.maxEntries` is ignored when a custom store is supplied (the custom store owns its eviction/TTL policy). Tests: `tests/isr.test.ts` "pluggable store" describe (6 specs: default backwards-compat, `createMemoryStore` LRU bump on `get`, fake-Redis call sequence, async store roundtrip, cache-hit short-circuit, non-cacheable response does NOT call `set`). 19/19 ISR specs pass total.
  - **M1.2 — runtime SSR 404 layout chrome.** Production SSR (`@pyreon/server`'s `createHandler`) reads `router.currentRoute().isNotFound` after render and emits HTTP status 404 when the synthetic chain fired. `entry-server.ts` (zero) defers to the handler for any app whose routes tree contains `notFoundComponent` (the canonical `_404.tsx` shape) — the legacy URL-pattern wrapper stays only as a backward-compat fallback for `createServer({ notFoundComponent })` callers with no `_404.tsx` in the tree. Dev-mode parity: `vite-plugin.ts:renderSsr` drops its URL-pattern bailout AND returns `{ html, status }`; unmatched URLs route through `routerInst.preload(pathname)` → `resolveRoute` builds the synthetic chain → the renderer produces 404 HTML INSIDE the layout's chrome. Returns null (falls through to the static `handle404` fallback) only when the routes tree has no reachable `notFoundComponent` AND `resolveRoute` returned an empty `matched` chain. Bisect-verified at the dev-e2e layer: reverting `renderSsr`'s pattern-bailout removal causes `[ssr-showcase] navigation › runtime SSR 404 wraps not-found in layout chrome (M1.2)` to fail with `element(s) not found` for `getByTestId('not-found-page')`. The companion `404 page for unknown route` spec was tightened to assert ONLY the testid (the prior `.or('text=404')` fallback was for the static `render404Page(undefined)` shape which is no longer reached when `_404.tsx` is in the routes tree). Reference: `packages/core/server/src/handler.ts:170-172` (production status 404), `packages/zero/zero/src/entry-server.ts:158-184` (handler-delegation + legacy-fallback split), `packages/zero/zero/src/vite-plugin.ts:renderSsr` (dev-mode router-driven render).
  - **M1.3 — build-time ISR docs.** PR I shipped the runtime adapter hook + manifest format but `docs/src/content/docs/zero.md` covered runtime ISR only. M1.3 replaced the ~10-line ISR section with a ~135-line section covering BOTH runtime ISR (`mode: 'isr'`) AND build-time ISR (per-route `revalidate` export + `Adapter.revalidate(path)`), with the per-platform env-var table, cacheKey auth-gated warning + two examples, per-adapter caveats (Vercel POSTs to `/api/_pyreon-revalidate`, Cloudflare hits Cache API delete with rate-limit caveats, Netlify needs a Build Hook URL, static adapters are no-op), and composition guidance for mixing both layers. Drive-by markdown lint fix on the manifest table separator (`|---|` → `| --- |`).
  - **M1.4 — SSG path collision detection.** `ssgPlugin`'s `closeBundle` (`packages/zero/zero/src/ssg-plugin.ts`) used to silently deduplicate path collisions via the `writtenPaths` Set — a user error (two `getStaticPaths` enumerators producing the same URL, or a static route overlapping a dynamic-route enumeration) appeared as "missing pages" with no diagnostic. M1.4 adds `detectPathCollisions(paths) → string[]` + `formatPathCollisionError(duplicates) → string` and wires them into `resolvePaths` so the build aborts loudly with `[Pyreon] SSG path collision — N URL(s) resolved by multiple routes: ...` listing every collision sorted. Both helpers exposed via `_internal` for unit-testability; 6 specs in `packages/zero/zero/src/tests/ssg-plugin.test.ts` cover empty, unique, deduplication, canonical collision shape, error-message format, and single-element edge cases.

  **M2 scale + observability fixes (single PR closing five operational gaps).** Surfaces after M1 + the SSG roadmap completed; each one independently bisect-verified.

  - **M2.1 — atomic manifest writes.** `closeBundle` writes 5 underscore-prefixed manifests sequentially: `_redirects`, `_redirects.json`, `_pyreon-ssg-paths.json`, `_pyreon-revalidate.json`, `_pyreon-ssg-errors.json`. Pre-M2.1, a `SIGINT` during the flush chain (or any I/O failure mid-write) left partial state: half the manifests pointing at the new build, half the old. Adapters reading these at deploy time would see inconsistent state. M2.1 adds `writeFileAtomic(target, content)` — writes to a sibling `<target>.tmp.<pid>.<seq>` first, then `rename`s into place. `rename` is atomic on POSIX (and Windows same-volume); readers see either the OLD content or the FULL new content, never a half-written file. Best-effort cleanup unlinks the tmp file on rename failure. Per-page HTML writes (`dist/<path>/index.html`) intentionally stay plain-`writeFile` — they're individually-readable (no cross-file invariants) and the rename-per-page cost on 10k-path sites would be significant. Exposed via `_internal.writeFileAtomic`; 3 specs lock the contract.
  - **M2.2 — safe loader-data serializer.** Four call sites (`html.ts` `buildScripts` / `buildScriptsFast`, ssg-plugin SSR entry source, vite-plugin dev SSR) previously did bare `JSON.stringify(loaderData).replace(/<\//g, '<\\/')`. A loader returning `{ data, fn: () => {} }` worked by accident (JSON.stringify drops function values), but a loader returning a Mongo / Prisma model with back-references intact threw `Converting circular structure to JSON` — opaque, didn't name which loader. M2.2 adds `stringifyLoaderData(data)` to `@pyreon/router` with a custom replacer + WeakSet cycle detector. Strips functions/symbols silently, throws `[Pyreon] Loader returned circular reference at "<path>". Loaders must return JSON-serializable data...` naming the offending key, escapes `</script>` for safe inline embedding. All 4 call sites updated; 7 specs cover function-drop, symbol-drop, circular-ref error shape, `</script>` escape, plain pass-through, shared-ref treatment, empty record.
  - **M2.3 — SSG perf counters.** 5 new counters under the `ssg.*` namespace, emitted from `ssg-plugin.ts:closeBundle`: `ssg.pathRender` (per-attempt), `ssg.pathWrite` (successful HTML emit), `ssg.pathRedirect` (loader-thrown redirect), `ssg.pathError` (render error), `ssg.404Emit` (per-locale 404). Standard `globalThis.__pyreon_count__?.()` sink contract — zero cost without a consumer, zero cross-package coupling. `packages/zero/zero/src` added to `INSTRUMENTED_PACKAGE_ROOTS` in catalog-drift; `zero → ssg` mapping added to `LAYER_PREFIX_OVERRIDE`. COUNTERS.md gets 5 new rows. Drift test enforces both directions: every emit must have a catalog row, every catalog row must have ≥1 emit. Useful for CI plugins tracking SSG perf over time, future `vite build --profile`, or dev-time investigation via `bun --eval 'import {perfHarness} from "@pyreon/perf-harness"; perfHarness.install(); import("./vite.config.ts")...'`.
  - **M2.4 — adapter env-var warnings in production.** Pre-M2.4 the env-var-missing warn in all 3 platform adapters (`vercelAdapter`, `cloudflareAdapter`, `netlifyAdapter`) was gated on `process.env.NODE_ENV !== 'production'` — exactly the env condition that DEPLOYED apps run under, where users would most need the signal. Symptom: a CMS triggers `adapter.revalidate(path)`, nothing happens, no console output, no failure mode reported back. M2.4 introduces a shared `warnMissingEnv(adapterName, missingVars, hint)` helper that ALWAYS warns on the FIRST invocation per `(adapterName + missingVarSet)` (regardless of NODE_ENV), deduped via a module-level Set so a busy revalidation handler doesn't spam logs. Returns the canonical `{ regenerated: false }` shape so adapters write `return warnMissingEnv(...)` as a one-liner. 4 specs lock the contract: warns in production, dedupes across multiple calls, names every missing var, returns regenerated:false.
  - **M2.5 — build summary breakdown.** The `[zero:ssg] Prerendered N page(s)...` log line at end of closeBundle now includes: (a) revalidate-manifest entry count (`+ R revalidate path(s)`) so users see at a glance whether `_pyreon-revalidate.json` actually landed; (b) per-locale breakdown when i18n is configured (`[en: 100, de: 100, cs: 100]`) so shape mismatches like `[en: 100, de: 90, cs: 100]` (10 paths skipped for de) surface immediately instead of requiring `ls dist/<locale>/ | wc -l`. The `buildLocaleSummary` helper handles both i18n strategies — `prefix-except-default` counts unprefixed paths into the default locale; `prefix` skips them (unexpected under prefix). Exposed via `_internal.buildLocaleSummary`; 5 specs.

  **M3.A DX signals (single PR shipping three independent surfaces).** Follow-up to M1 + M2 — the analysis surfaced three DX gaps that aren't correctness or observability issues but cause silent author confusion when learning the SSG / ISR APIs. Each one independently bisect-verified.

  - **M3.1 — `vercelRevalidateHandler()` scaffold.** Pre-M3.1 `vercelAdapter.revalidate(path)` (PR I) POSTed to `/api/_pyreon-revalidate?path=…&secret=…` — a CONVENTION users had to implement themselves. M3.1 adds the drop-in handler at `@pyreon/zero/server`: `export { vercelRevalidateHandler as default } from '@pyreon/zero/server'`. Validates POST + path + secret query params, reads the build-time `_pyreon-revalidate.json` manifest, refuses revalidation for paths NOT in the manifest (closes "secret-leaks-once → attacker revalidates anything" footgun), then dispatches to a user-supplied `onRevalidate` callback OR returns success. Handler caches the manifest read on first call (process lifetime) so high-throughput webhooks don't re-stat the file. Returns Web-standard `Response` so it works in Vercel Edge functions, Node serverless via `@vercel/node` bridge, and the in-process `mode: 'ssr'` runtime. Options: `manifestPath` (default `./dist/_pyreon-revalidate.json`), `secretEnvVar` (default `VERCEL_REVALIDATE_TOKEN`), `onRevalidate(path)` (user callback). 13 specs lock the contract.

  - **M3.4 — `pyreon doctor --check-ssg` project-wide audit.** Three syntactic detectors in `@pyreon/compiler:auditSsg` (mirrors `auditIslands` shape — no type-check pass, no module resolution): (a) `404-outside-layout-dir` catches `_404.tsx` / `_not-found.tsx` NOT co-located with `_layout.tsx`, which falls outside PR L5's `findNotFoundFallback` filter and renders standalone without layout chrome; (b) `dynamic-route-missing-get-static-paths` catches `[id].tsx` / `[...slug].tsx` files without `export const getStaticPaths`, which the SSG auto-detect silently skips; (c) `non-literal-revalidate-export` catches `export const revalidate = TTL` (variable refs, arithmetic) which PR I's `extractLiteralExport` silently drops from the build-time ISR manifest. Each finding ships with file path + line/column + actionable fix. Wired through the doctor pipeline as the third opt-in audit alongside `--check-islands` and `--audit-tests`. CLI: `pyreon doctor --check-ssg [--json]`. 18 specs (per-finding broken/fixed pairs + discovery + formatter).

  - **M3.5 — three `@pyreon/lint` rules under a new `ssg` category** (65 rules / 13 categories total): `pyreon/revalidate-not-pure-literal` (error) catches the same shape as M3.4's detector at lint time; `pyreon/missing-get-static-paths` (warn) catches `[id].tsx` without `getStaticPaths` (warn-level because dynamic routes in `mode: 'ssr'` / `'isr'` legitimately don't need the enumerator); `pyreon/invalid-loader-export` (error) catches `export const loader = { data: 1 }` / `export const loader = "string"` etc. — non-callable exports that crash the SSR runtime with `TypeError: loader is not a function`. All three are route-scoped via `ROUTES_PATH_RE` on `context.getFilePath()` — the conventions only have meaning under `src/routes/`. 22 specs (per-rule broken/fixed pairs).

  Reference: `packages/zero/zero/src/vercel-revalidate-handler.ts`, `packages/core/compiler/src/ssg-audit.ts`, `packages/tools/lint/src/rules/ssg/*.ts`.

  **M3.B docs + canonical example (single PR landing the SSG docs + showcase).** Follow-up to M3.A — extracts the SSG reference into a dedicated docs page AND extends the canonical blog example to exercise M3.A's three features end-to-end. Also closes a false-positive in `pyreon/missing-get-static-paths` that the example surfaced on first run.

  - **M3.2 — dedicated `docs/src/content/docs/ssg.md` page.** SSG content previously lived inside `zero.md` as a ~220-line subsection. The new dedicated page is ~580 lines covering: when-to-use SSG vs SSR/SPA/ISR, static + dynamic routes, `_404.tsx` with host-routing reference, loader-thrown redirects, per-route `revalidate` exports + the platform adapter table, `vercelRevalidateHandler` scaffold (M3.1) with security rationale, concurrency + progress reporting, subpath / base-path deploys, i18n route duplication + per-locale 404 + hreflang sitemap, sitemap auto-emit, render-error handling, path-collision detection (M1.4), the `pyreon doctor --check-ssg` audit (M3.4), the three SSG lint rules (M3.5), full manifests reference, per-platform deployment snippets (Netlify / Cloudflare Pages / GitHub Pages / Vercel / S3+CloudFront / nginx / Caddy), Next.js / Astro migration cheatsheet. Cross-referenced from `zero.md` (which is now slimmed to a quick-start overview) and wired into the docs sidebar under "Meta-Framework" right after Zero. Total doc-page count bumped to 77 (1 new).
  - **M3.3 — `examples/cpa-pw-blog` becomes the canonical SSG reference.** The existing blog example already exercised `getStaticPaths`, `_404.tsx`, sitemap auto-emit, RSS feed via API route, theme. Two M3.A-feature additions land here: (a) `export const revalidate = 3600` on `blog/[slug].tsx` — produces `dist/_pyreon-revalidate.json` entries for the 3 enumerated post URLs at build time; (b) new API route at `src/routes/api/_pyreon-revalidate.ts` using the M3.1 `vercelRevalidateHandler` scaffold with a `onRevalidate(path)` callback stub. Companion README documents what the example showcases + maps each feature to a deployable file. The verify-modes `cpa-pw-blog × ssg` cell was extended to assert `dist/_pyreon-revalidate.json` exists AND contains `"/blog/welcome": 3600` / `"/blog/why-signals": 3600` / `"/blog/static-vs-ssr": 3600` — bisect-verified by removing the `revalidate` export → cell fails with `expected file: dist/_pyreon-revalidate.json`.
  - **M3.5 false-positive fix — `pyreon/missing-get-static-paths` skips API routes.** Surfaced when running `bunx pyreon-lint .` against the showcase: the rule fired on `examples/cpa-pw-blog/src/routes/api/echo/[...path].ts` (a streaming API route with bracket filename). API routes are runtime-only by definition; `getStaticPaths` doesn't apply. Two complementary skips added: (a) **path-based** — files under `src/routes/api/` always skipped (fs-router convention); (b) **export-shape** — files without `export default` skipped wherever they sit (page routes structurally require a default-exported component; method-handler-only files are API routes by structure). Same fix mirrored in the `auditSsg` compiler detector at `packages/core/compiler/src/ssg-audit.ts:detectDynamicRouteMissingGetStaticPaths` so `pyreon doctor --check-ssg` doesn't false-positive either. Bisect-verified: removing the export-shape skip → `does NOT fire on files without export default outside api/` test fails with `expected [...] not to contain 'pyreon/missing-get-static-paths'`. 4 new lint specs + 3 new audit specs lock the contract.

  Reference: `docs/src/content/docs/ssg.md` (new), `examples/cpa-pw-blog/{src/routes/blog/[slug].tsx, src/routes/api/_pyreon-revalidate.ts, README.md}`, `packages/tools/lint/src/rules/ssg/missing-get-static-paths.ts` (API-route skip), `packages/core/compiler/src/ssg-audit.ts:detectDynamicRouteMissingGetStaticPaths` (parallel skip).

  **Dev-server bisect gotcha (M1-discovered).** The `vite-plugin.ts` source path runs through Vite's own config bundler, which uses the `node` condition → reads from `packages/zero/zero/lib/`, NOT the `bun` condition's `src/`. So reverting `src/vite-plugin.ts` alone is INVISIBLE to the running dev server — the lib was built from source as-of the last `bun install` / `bun scripts/bootstrap.ts` / `bun run --filter='@pyreon/zero' build` invocation. To bisect-verify dev-server-driven e2e specs (anything under the `examples/{ssr-showcase,…} dev` webServer), you MUST: (1) revert the source, (2) `bun run --filter='@pyreon/zero' build` to rebuild the lib, (3) kill the dev server (`lsof -ti tcp:5175 | xargs -r kill -9`), (4) re-run playwright. The user-side runtime code (`@pyreon/zero/server`, `@pyreon/runtime-server`, etc.) IS loaded via `ssrLoadModule` against the workspace `bun` condition — so `src/` changes there hot-reload — but the plugin chain is different. Same trap applies to any package whose code path lives in a Vite plugin (`@pyreon/vite-plugin`, `@pyreon/zero`). See `.claude/rules/testing.md` "Dev-server bisect" entry for the recipe.

  **Loader-redirect handling at SSG (PR B of the SSG roadmap).** Loaders that throw `redirect('/login')` for auth-gates used to silently disappear during SSG — `router.preload(path)` rejected, the path landed in `errors[]`, no HTML emitted, no redirect manifest, the user got no signal that the loader ran a redirect. Now: the SSG entry catches the redirect via `getRedirectInfo(err)` BEFORE rendering (rendering past a redirect would produce HTML for the wrong page AND leak auth-gated layout structure to unauthenticated users — same reason `@pyreon/server`'s SSR handler short-circuits on redirect-throw). The discriminated union `{ kind: 'redirect', from, to, status }` flows back to the outer plugin, which writes `dist/_redirects` (Netlify / Cloudflare Pages format: `<from> <to> <status>` per line) AND `dist/_redirects.json` (Vercel format: `{ redirects: [{ source, destination, permanent, statusCode }] }`). Both files ship together so adapters pick whichever they read; `permanent: true` for 301/308, `false` for 302/307. Optional `ssg.redirectsAsHtml: 'meta-refresh'` ALSO emits a static `<source>/index.html` with `<meta http-equiv="refresh" content="0; url=<target>">` + `<link rel="canonical">` for hosts without redirect manifest support (plain S3, GitHub Pages). Default `ssg.emitRedirects` is `true`; setting `false` restores the pre-PR-B behaviour where redirects fall into the error path. Reference: `packages/zero/zero/src/ssg-plugin.ts:renderNetlifyRedirects` / `renderVercelRedirectsJson` / `renderMetaRefreshHtml`.

  **PR F — sitemap auto-emit from resolved SSG paths.** Pre-PR-F, `seoPlugin({ sitemap })` walked the file-system route tree directly and silently skipped dynamic routes (`[id]` / `[...slug]`) — their concrete values aren't knowable without running each route's enumerator. After PR A merged `getStaticPaths`, the SSG plugin now produces real concrete URLs at build time, so the sitemap should include them. The wiring: `ssgPlugin` writes `dist/_pyreon-ssg-paths.json` after the path loop with every path that produced an `index.html` (errored + redirected paths intentionally excluded). `seoPlugin({ sitemap: { useSsgPaths: true } })` flips its sitemap-emission hook from `generateBundle` (where the SSG manifest doesn't exist yet) to `closeBundle` with `enforce: 'post'` so it runs AFTER ssgPlugin, reads the manifest, merges paths into `additionalPaths`, writes `dist/sitemap.xml` via `fs.writeFile`, and cleans up the manifest (internal artifact — not for the published static host). Falls back gracefully when the manifest doesn't exist (mode is not `ssg`, or the SSG step was skipped) — sitemap still walks the file-system routes. Default `useSsgPaths: false` preserves prior behaviour. Reference: `packages/zero/zero/src/seo.ts:seoPlugin` + `packages/zero/zero/src/ssg-plugin.ts:writtenPaths` collector. Real-app coverage: `examples/cpa-pw-blog` + `verify-modes cpa-pw-blog × ssg` cell asserts `dist/sitemap.xml` contains `/blog/welcome` / `/blog/why-signals` / `/blog/static-vs-ssr` (the dynamic blog post URLs from `getStaticPaths`).

  **Per-route `<link rel="modulepreload">` (islands-safe) — issue #1355.** Vite already modulepreloads the single ENTRY's static graph (preserved in SSG output via `injectIntoTemplate`), but a route's own component chunk is lazy-imported, so the browser discovers it LATE in the network waterfall. The SSG plugin now injects the **per-route delta** into each prerendered page's `<head>`: the matched route chain's chunk(s) + their STATIC import closure, minus the entry graph. **Islands-safe by construction** — the closure (`ssg-modulepreload.ts:collectStaticChunkClosure`) follows ONLY the Vite manifest's `imports` (static), NEVER `dynamicImports`. A route's `dynamicImports` are exactly the deferred chunks (islands `hydrate:'never'|'visible'`, `lazy()`, heavy-module-in-handler); preloading them would pull deferred code onto the first-paint critical path and defeat the islands model. Verified end-to-end against `examples/ssr-showcase`: `/island-demo` preloads its own chunk + the `island()` runtime, but the deferred `IslandProbe` component chunk is NEVER preloaded. Mechanics: a `config()` hook enables Vite `build.manifest` on the OUTER client build (skipped for the inner SSR sub-build via `isInnerBuild` + `env.isSsrBuild`); `closeBundle` reads `dist/.vite/manifest.json`, maps each matched record's `_hmrId` (the lazy COMPONENT's source path — `record.component._hmrId`, NOT `record._hmrId`) → manifest key (`relative(root, _hmrId)` posix, with `src`-field + suffix fallbacks) → static chunk closure, subtracts the entry closure (`computeEntryHrefs` — real manifests list `index.html` inside each route's `imports`, so this subtraction is load-bearing) + the template's existing modulepreloads, injects `<link rel=modulepreload href=… crossorigin>` into `result.head` per path, then DELETES the manifest (internal artifact — unless the user enabled it themselves). Renderer emit format `href=… crossorigin` is distinct from Vite's `crossorigin href=…`, so build-artifact greps match only per-route links. Graceful at every step (missing/malformed manifest or unresolvable route → no preload, page still works — `modulepreload` is a non-load-bearing hint). Default-on for `mode:'ssg'`; opt out `zero({ ssg: { modulePreload: false } })`. Gated by `verify-modes ssr-showcase × ssg` (per-route delta present + per-route specificity + IslandProbe NEVER preloaded + manifest cleaned up; bisect-verified — making the closure follow `dynamicImports` fails the IslandProbe negative gate) + 19 resolver unit specs (`ssg-modulepreload.test.ts`). The companion "route-critical font preload" was already shipped (`font.ts` emits `<link rel=preload as=font crossorigin>`); this is modulepreload only. Reference: `packages/zero/zero/src/ssg-modulepreload.ts` + `ssg-plugin.ts` (`config()` manifest enable, `renderSsgEntrySource` routeModules collection, `closeBundle` injection + cleanup).

  **Render-error fallback hook + JSON artifact (PR G of the SSG roadmap).** Render exceptions used to land silently in `PrerenderResult.errors`, get logged to `console.error`, and the build continued — CI had no machine-readable signal beyond the exit code, and there was no way to emit a per-path fallback HTML so static hosts could serve something other than a 404 at the failed URL. Now two new `ssg` config fields close both gaps:

  - `ssg.onPathError?: (path, error) => string | null | Promise<string | null>` — invoked once per failed path. Returns a string → written as the path's HTML in place of the failed render (use this for "this content is temporarily unavailable" templates, branded fallbacks, etc.). Returns null → skip; the path produces no HTML output and the error stays in `errors[]` for the post-build summary. Async callbacks are awaited. The original error is ALWAYS recorded BEFORE the callback runs, so the artifact / summary catches the failure regardless of what the callback does. Callback throws are captured as a SEPARATE entry with the path suffix `(onPathError)` — a buggy callback can't take down the whole build, AND the bug surfaces instead of being swallowed.
  - `ssg.errorArtifact?: 'json' | 'none'` (default `'json'`) — when `'json'`, writes `dist/_pyreon-ssg-errors.json` after the render loop summarising every error encountered (path traversal, render exception, getStaticPaths throw, onPathError throw, 404 render fail). Each entry has `{ path, message, name, stack? }`. Wrapped in `{ errors: [...] }` (forward-compatible — future fields like `buildId` / `timing` can be added without breaking existing consumers). The file is ONLY written when `errors.length > 0` — successful builds don't leak an empty manifest. Set to `'none'` to opt out entirely (matches pre-PR-G behaviour where errors were console-only).

  CI consumption is now structural: `cat dist/_pyreon-ssg-errors.json | jq '.errors | length' | grep -q '^0$' || exit 1` gates on render failures without parsing console output. Reference: `packages/zero/zero/src/ssg-plugin.ts:renderErrorArtifact` (serializer) + the `catch` block of the per-path render loop (the `onPathError` invocation).

  **PR E — subpath / base-path single-source-of-truth wiring.** Pre-PR-E, zero shipped THREE disconnected `base` settings: (a) `vite.config.base` controlled asset URL rewriting in built HTML/JS; (b) `zero({ base })` was typed in `ZeroConfig` and exposed to the client as `__ZERO_BASE__` — but **nothing read it**, the global was a typed-but-unimplemented surface; (c) `createRouter({ base })` controlled RouterLink href prefixing and route matching but was never wired from zero's config. Apps deployed to `/blog/` had to manually set ALL THREE in sync, and silently broke when any drifted (assets prefixed but RouterLinks weren't, or vice-versa). Now `zero({ base: '/blog/' })` is the canonical single source of truth and propagates through three coordination points:

  - **Vite's `base`**: `vite-plugin.ts`'s `config()` hook returns `base: config.base`. Vite's config-merge semantics make plugin-returned config the BASE — user's explicit `vite.config.base` still overrides if both are set, so the user's manual override wins. Asset URLs in built HTML get the prefix automatically.
  - **`createApp({ base })` → `createRouter({ base })`**: a new `base?: string` field on `CreateAppOptions` flows through to the router. RouterLink hrefs render with the prefix (`<a href="/blog/about">` not `<a href="/about">`), and `stripBase` correctly handles incoming URLs at request time.
  - **Three call sites forward base to createApp**: (1) `entry-server.ts` `createServer()` reads `config.base` from `resolveConfig`. (2) `client.ts` `startClient()` reads the Vite-defined `__ZERO_BASE__` global. (3) `ssg-plugin.ts`'s SSR_ENTRY_SOURCE template captures `__ZERO_BASE__` at module-eval time and passes through on every per-path render call.

  **Dist filesystem layout deliberately stays unprefixed.** `dist/about/index.html`, NOT `dist/blog/about/index.html`. Static hosts (Netlify, Cloudflare Pages, GitHub Pages, S3+CloudFront) serve `dist/` mounted at `/blog/`, mapping `/blog/about` → `dist/about/index.html`. The base belongs in URL contracts (asset paths in HTML, router hrefs), not in the on-disk layout. This matches how every major SSG framework handles subpath deploys.

  **`__ZERO_BASE__` fallback safety**: `typeof __ZERO_BASE__ !== 'undefined' && __ZERO_BASE__ !== '/'` — both client.ts and the SSG entry handle the no-Vite-define edge case (test environments, etc.) by treating the constant as undefined. The fallback is never hit in real Vite builds because the plugin's `config()` hook always declares the define.

  Real-app coverage gates the contract at THREE layers: (1) **build artifact** — `verify-modes ssr-showcase × ssg-subpath` cell with `zero({ base: '/blog/' })` asserts BOTH (a) asset URLs in `dist/index.html` start with `/blog/assets/` AND (b) RouterLink hrefs in `dist/about/index.html` resolve to `/blog/about` / `/blog/posts`. Bisect-verified: removing `base: config.base` from `vite-plugin.ts:config()` fails the asset-URL assertion; removing the `base: __ssgBase` forward in `ssg-plugin.ts:SSR_ENTRY_SOURCE` fails the RouterLink-href assertion. (2) **runtime navigation** — `e2e/ssg-subpath.spec.ts` (`bun run test:e2e:ssg-subpath`) builds the same dist + serves via `vite preview` on port 5198 and runs 4 specs in real Chromium: initial load + no-console-error sanity, asset URLs return 200 under base, RouterLink click triggers client-side navigation to `/blog/about` with no full page reload (asserted via window-sentinel preservation across the click), post-hydration counter increment patches DOM under base. Bisect-verified at this layer: removing the `__ZERO_BASE__` read in `client.ts startClient` → 2/4 specs fail (RouterLink-click and post-hydration-counter). (3) **API surface** — per-API unit tests in `packages/zero/zero/src/tests/app.test.ts` (`createApp — base option (PR E)` describe block, 4 specs covering forward + nested + default-`/` + undefined). The verify-modes layer covers SSR-emitted output, the e2e layer covers post-hydration runtime, and the unit layer covers the API contract directly. **Direct-link navigation to `/blog/about` (no trailing slash) is intentionally NOT covered** — that's a static-host directory-rewrite concern (Netlify / Cloudflare Pages / GitHub Pages do it; `vite preview` doesn't), not a framework concern.

  **PR D — concurrent rendering + progress reporting (SSG roadmap).** The pre-PR-D loop was fully sequential — `for (const p of paths) { await renderPath(p) }` — meaning a 1000-path site at ~50ms/page took 50 seconds, with 9× of every CPU core idle. Now: a worker-pool drains the path list `concurrency` workers at a time (default `4`, configurable via `ssg.concurrency`). Per-path semantics are preserved (timeout, redirect handling, error capture, onPathError fallback, 404 emission); only the schedule changes.

  Two new `ssg` config fields:

  - `ssg.concurrency?: number` (default `4`) — paths in flight. Set to `1` for the pre-PR-D fully-sequential shape (useful when loaders share a non-pooled resource — single in-process database connection, third-party API with strict serial rate-limits). Higher values land in the multi-core CI build sweet spot; the practical ceiling is your data layer's concurrent-connection tolerance. Clamped to `Math.max(1, value)` then to `min(value, paths.length)` so a misconfigured `0` or oversized value never silently hangs or spawns pointless idle workers.
  - `ssg.onProgress?: (info) => void | Promise<void>` — per-path settle callback invoked with `{ completed, total, currentPath, elapsed }`. Fires AFTER each path's render settles (success / redirect / error). Useful for build-tool progress bars, CI heartbeat lines on long builds (silent stretches on 10k-path sites look hung), and build-time timing instrumentation. Async callbacks are awaited PER WORKER before the same worker pulls its next item — across workers there is NO serialization, so callbacks may run in parallel; document this clearly (downstream consumer can wrap to a single-consumer queue if they need strict serial output). Callback throws captured as `(onProgress)`-suffixed entries in `errors[]` so a buggy callback can't take down the build.

  Generic worker-pool primitive `runWithConcurrency(items, concurrency, processItem, onSettled?)` is exported via `_internal` and tested in isolation (10 specs in `packages/zero/zero/src/tests/ssg-plugin.test.ts` — `runWithConcurrency (PR D)` describe block). Cap-clamping bisect-verified: removing the `max(1, …) + min(…, items.length)` clamps fails the `concurrency=0 → clamped to 1` spec with `expected [] to have a length of 3 but got 0`. The summary log includes a `(concurrency: N)` suffix when N > 1 so build output documents the actual schedule used. Reference: `packages/zero/zero/src/ssg-plugin.ts:runWithConcurrency` + the SSG closeBundle's worker pool that calls it.

  **PR H — i18n route duplication (SSG roadmap).** Pre-PR-H, the `i18nRouting()` Vite plugin handled request-time locale detection (Accept-Language, cookies, root redirect) but routes themselves were never duplicated per locale — `expandRoutesForLocales` didn't exist, the docstring at `i18n-routing.ts:179-182` falsely claimed fs-router consumed the i18n config, and SSG outputs / SSR matchers had no `/de/about` / `/cs/about` records to render against. Now: `zero({ i18n: { locales: ['en','de','cs'], defaultLocale: 'en' } })` (or `{ strategy: 'prefix' | 'prefix-except-default' }`) propagates through both build-time route generation (`vite-plugin.ts`'s virtual-routes load) AND SSG path expansion (`ssg-plugin.ts:autoDetectStaticPaths`) via the new `expandRoutesForLocales` helper.

  Two strategies:

  - **`prefix-except-default`** (default): default locale keeps unprefixed URLs (`/about` stays `/about`), non-default locales get explicit prefixes (`/de/about`, `/cs/about`). Best for SEO-on-default-locale apps — search engines see canonical URLs at `/about` while non-default speakers get explicit prefixes.
  - **`prefix`**: every locale prefixed including default (`/en/about`, `/de/about`, `/cs/about`). Root `/` becomes `/en` / `/de` / `/cs`. Better when no locale is "primary".

  Layouts, error boundaries, loading components, and 404 pages duplicate along with their pages — same source `filePath` (same component module), new locale-prefixed `urlPath` / `dirPath` / `depth`. The route tree built from the expanded array therefore has one fully-formed subtree per locale, so layout matching, dynamic params (`[id]` → `:id`), and catch-all routes (`[...slug]` → `:slug*`) all compose naturally with the locale prefix — no special cases.

  **`getStaticPaths` composition**: each duplicate route inherits the same `exports.getStaticPaths`. The SSG plugin's `expandUrlPattern` step then expands `/blog/[slug]` × locales × enumerator output into the cross-product (`/blog/a`, `/blog/b`, `/de/blog/a`, `/de/blog/b`, … under `prefix-except-default` with 2 slugs × 3 locales = 6 paths). Cardinality compounds, by design — `ssg.concurrency` (PR D) limits in-flight renders independent of route count.

  No-op safety: empty `locales` returns input unchanged; `[en] + prefix-except-default` short-circuits to identity (no other locales to prefix). The `i18nRouting()` Vite plugin (request-time middleware) is unchanged and orthogonal — both can be used together.

  Bisect-verified at TWO layers:

  - **Unit** (`packages/zero/zero/src/tests/i18n-routing.test.ts` — `expandRoutesForLocales` describe block, 18 specs): covers prefix vs prefix-except-default, catch-all routes (`[...slug]`), dynamic params (`[id]`), root index, layouts (per-locale subtree wrapping), `getStaticPaths` exports inheritance, depth recomputation, no-op cases, default-strategy fallback, multi-route trees.
  - **Real-app build** (`scripts/verify-modes.ts` — `ssr-showcase × ssg-i18n` cell with `locales: ['en','de','cs']`): asserts `dist/about/index.html`, `dist/de/about/index.html`, `dist/cs/about/index.html` all exist (per-locale variants); asserts `dist/en/about/index.html` does NOT exist (default-locale unprefixed under prefix-except-default). Bisect-verified by reverting the `expandRoutesForLocales` calls in BOTH `ssg-plugin.ts:autoDetectStaticPaths` AND `vite-plugin.ts`'s virtual-routes load — cell fails with `expected file: dist/de/index.html` (de-locale variant never enumerated, SSG didn't render it). Restore → 6/6 ssr-showcase cells pass.

  Reference: `packages/zero/zero/src/i18n-routing.ts:expandRoutesForLocales` + the new `i18n?: I18nRoutingConfig` field on `ZeroConfig`.

  **PR H follow-up — root-layout skip + e2e gate + dynamic-route × locale cross-product.** Three additions on top of PR H's core route duplication:

  1. **Root-layout skip under `prefix-except-default`** — the original duplication produced a `/de/_layout` and `/cs/_layout` from the source root `_layout.tsx` (urlPath `/`). The route tree's hierarchical matching then wraps `/de/about` under BOTH the unprefixed `/_layout` (matches all paths as the root) AND the duplicated `/de/_layout` (matches `/de/*`). Result: layout component mounts twice, two navbars, two PyreonUI providers, two `<main>`. The fix in `expandRoutesForLocales` skips duplication ONLY for root-level layouts (`isLayout && urlPath === '/'`) under `prefix-except-default`. The unprefixed default-locale root layout already wraps locale-prefixed children via hierarchical matching — duplicating it is double-counting. **Non-root layouts** (e.g. `/dashboard/_layout` at urlPath `/dashboard`) MUST still be duplicated because the unprefixed `/dashboard/_layout` doesn't match `/de/dashboard/users` (path patterns don't intersect). **Under `prefix` strategy** the skip does NOT apply: there is no unprefixed default to inherit from, so every locale needs its own root layout. The bug was invisible at the unit + verify-modes layers (both check route enumeration + dist filesystem; neither inspects rendered HTML structure for nested layouts) — only surfaced via the real-Chromium e2e gate, which then drove the fix back into `expandRoutesForLocales` AND the unit-test suite (3 new specs covering the skip rule and its inverse under `prefix`). Reference: `packages/zero/zero/src/i18n-routing.ts:expandRoutesForLocales` (the `route.isLayout && route.urlPath === '/'` guard).

  2. **Real-Chromium e2e gate** at `e2e/ssg-i18n.spec.ts` (own [`e2e-configs/ssg-i18n.config.ts`](e2e-configs/ssg-i18n.config.ts), separate webServer boot on port 5199 — same isolation rationale as ssg-subpath / ui-regression / compat-layers / app-showcase). Builds `examples/ssr-showcase` with `vite.config.i18n.ts` (sets `zero({ i18n: { locales: ['en','de','cs'], defaultLocale: 'en' } })`), serves the prerendered `dist/` via `scripts/serve-ssg.ts` (directory-rewriting static server — see "Directory-rewriting test server" below), and runs 4 specs: (a) default-locale unprefixed `/about` renders + zero console errors, (b) non-default `/de/about` renders the duplicated route, (c) third-locale `/cs/posts` renders with hydrated loader data — asserts 3 post-items AND the `window.__PYREON_LOADER_DATA__` global carries `/cs/posts` as a key, (d) RouterLink click from `/de/about` does client-side push (not full reload — window-sentinel preservation gates this). Companion to the build-artifact `verify-modes ssr-showcase × ssg-i18n` cell. The verify-modes cell asserts the dist filesystem layout; this gate asserts the runtime contract (router matches the duplicated record, hydration succeeds under the locale prefix, in-app navigation works between locale subtrees). Bisect-verified at TWO layers: (1) reverting the root-layout-skip → spec (d) fails with "matches more than one element" because two `nav-home` testids exist in the doubled layout DOM; (2) reverting the playwright config from `serve-ssg.ts` back to `vite preview` → spec (c) fails on `await expect(items).toHaveCount(3)` with `Expected: 3, Received: 0` because vite preview serves `dist/index.html` (the home page — zero post-items) for `/cs/posts` instead of `dist/cs/posts/index.html`. CI runs as a separate step in the `E2E` job: `bun run test:e2e:ssg-i18n`.

  **Test-environment artifact, NOT a framework bug.** PR #516 originally documented spec (c)'s loader-data assertion as a "known gap" — the SSG'd HTML carried 3 post-items but `useLoaderData()` returned `undefined` post-hydration. The hypothesis was "zero's dehydrated cache key doesn't include locale prefix." Investigation in this PR proved the framework was correct all along: `record.path === '/cs/posts'` on the duplicated route record (PR H sets `urlPath` to the locale-prefixed pattern), `serializeLoaderData` correctly keys the dehydrated payload as `{"/cs/posts": [...]}`, and `hydrateLoaderData` correctly walks the matched chain on the client. The bug was that `vite preview` does SPA fallback for SSG output — every URL that doesn't map to a literal file in `dist/` gets served `dist/index.html`. So `/cs/posts` was actually served the HOME page HTML, which has no loader-data inline script (the home page didn't load `/cs/posts`'s data at SSG time). `window.__PYREON_LOADER_DATA__` was `undefined` because the test was reading the home page, not the prerendered Posts page. With directory-rewriting service (real static hosts: Netlify / Cloudflare Pages / GitHub Pages / S3+CloudFront), `/cs/posts` correctly maps to `dist/cs/posts/index.html`, the inline data is present, and hydration works as designed.

  **Directory-rewriting test server**: `scripts/serve-ssg.ts` — minimal Bun static server (~80 lines, zero deps) that mirrors how real static hosts serve SSG output. `URL` without extension → `<path>/index.html`; URL ending with `/` → `<path>/index.html`; URL with extension → literal file. 404 falls back to `dist/404.html` (then plain text). Path-traversal guarded. **Honors `dist/_redirects`** (Netlify/Cloudflare format — PR L3) — `loadRedirects` parses the file at startup, `matchRedirect` short-circuits BEFORE directory rewriting and returns 301/302/etc with `Location` header. Real static hosts apply `_redirects` before any file lookup; serve-ssg matches that ordering so e2e gates exercising loader-thrown redirects (PR B) see the production shape locally. Used by `e2e-configs/ssg-i18n.config.ts` (port 5199) + `e2e-configs/ssg-i18n-prefix.config.ts` (port 5200); ssg-subpath e2e gate still uses `vite preview` because its specs don't exercise per-route prerendered HTML (only RouterLink-href and asset-URL prefixing, both of which `vite preview` serves correctly). Future SSG e2e gates that need to read the prerendered HTML for a non-index path should use `serve-ssg.ts` — `vite preview`'s SPA fallback masks the genuine production shape. Unit tests for `parseRedirects` / `loadRedirects` / `matchRedirect` / `createServeHandler` live at `packages/internals/test-utils/src/tests/serve-ssg.test.ts` (16 specs).

  3. **Dynamic-route × locale cross-product gate** — `examples/ssr-showcase/src/routes/posts/[id].ts` now exports `getStaticPaths()` enumerating 3 IDs. Combined with `zero({ i18n: { locales: ['en','de','cs'] } })`, the SSG plugin produces 9 concrete post HTML files (3 IDs × 3 locales) in addition to the static routes. The `verify-modes ssr-showcase × ssg-i18n` cell now asserts every cross-product file exists at `dist/posts/<id>/index.html` (en, unprefixed) and `dist/{de,cs}/posts/<id>/index.html` (per-locale). Locks the cross-product at the dist filesystem level — the unit test in `i18n-routing.test.ts` covers `exports.getStaticPaths` inheritance at the function level, but only this build-artifact gate catches the regression where `getStaticPaths` resolution is silently skipped for duplicated routes (e.g. if `expandUrlPattern` lost the `exports` reference somewhere in the duplication path). Reference: `examples/ssr-showcase/src/routes/posts/[id].ts:getStaticPaths` + the cross-product block in `scripts/verify-modes.ts:ssr-showcase × ssg-i18n` cell.

  **PR I — build-time ISR (per-route `revalidate` + `Adapter.revalidate(path)`).** Closes the 10-PR SSG roadmap. Two-layer design:

  **Layer 1 — build-time metadata.** Route files export `export const revalidate = <number>` (seconds, e.g. `60` / `3600`) OR `export const revalidate = false` (never revalidate). The fs-router scanner detects the export at scan time AND captures the literal value via `extractLiteralExport` (same pure-literal extraction shape as `meta` / `renderMode`); `RouteFileExports.hasRevalidate` + `revalidateLiteral` carry it through into `FileRoute.exports`. The SSG plugin's `closeBundle` emits `dist/_pyreon-revalidate.json` mapping `{ concretePath: revalidateValue }` for every prerendered path whose source route has a `revalidate` literal — including dynamic-route children (a single `posts/[id].tsx` with `revalidate = 60` and 3 enumerated IDs produces 3 manifest entries). Static hosts and adapter-driven deploys read the manifest to wire platform ISR. Filename starts with `_` so static-host publish steps don't expose it as a public asset (matches `_redirects` / `_pyreon-ssg-paths.json` convention). NOT inlined onto the route record at runtime — `revalidate` is purely a build-time deploy-config concern.

  **Layer 2 — runtime adapter hook.** New optional `Adapter.revalidate?(path: string): Promise<{ regenerated: boolean }>` on the `Adapter` interface. Each shipped adapter implements platform-specific ISR:
  - **`vercelAdapter`**: POSTs to `<deployment-url>/api/_pyreon-revalidate?path=…&secret=…` with `VERCEL_DEPLOYMENT_URL` + `VERCEL_REVALIDATE_TOKEN` env vars. User-side webhook handler reads the manifest + calls `adapter.revalidate(path)` for each entry.
  - **`cloudflareAdapter`**: POSTs to Cloudflare's `zones/<zone-id>/purge_cache` API with the full URL — Cloudflare invalidates the edge cache, next request triggers fresh origin fetch + rebuild. Needs `CLOUDFLARE_ZONE_ID` + `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_SITE_URL` env vars.
  - **`netlifyAdapter`**: POSTs to a Build Hook URL (configured per-site) with `trigger_title=revalidate:<path>` for audit-log traceability. Netlify queues a partial rebuild. Needs `NETLIFY_BUILD_HOOK_URL` env var.
  - **`staticAdapter`** / **`nodeAdapter`** / **`bunAdapter`**: no-op fallback returning `{ regenerated: false }` + dev-mode warning. Static hosts + self-hosted Node/Bun have no platform-driven ISR; users wanting cache-with-TTL need `mode: 'isr'` (runtime LRU caching) instead.

  **Distinct from runtime ISR (`mode: 'isr'`).** Runtime ISR is on-demand SSR caching with TTL via `createISRHandler` in `@pyreon/zero/server` — pages are rendered on the FIRST request, cached for `cacheTtl` ms, served stale until expiry. Build-time ISR (this PR) is static prerender + platform-driven rebuild-on-stale. They can coexist: a `mode: 'isr'` app with per-route `revalidate` exports gets BOTH runtime caching AND deploy-time ISR config.

  **Real-app coverage** at the `verify-modes ssr-showcase × ssg` autodetect cell. `examples/ssr-showcase/src/routes/posts/[id].ts` ships `revalidate = 60`; the cell asserts `dist/_pyreon-revalidate.json` exists and contains `{ '/posts/1': 60, '/posts/2': 60, '/posts/3': 60 }` AND that `/about` (no revalidate export) is NOT in the manifest. Bisect-verified: reverting the manifest-emit block in `ssg-plugin.ts:closeBundle` → cell fails with `expected file: dist/_pyreon-revalidate.json`. Restored → 6/6 ssr-showcase cells pass.

  **Per-platform unit tests** at `packages/zero/zero/src/tests/adapters.test.ts` use a stubbed `globalThis.fetch` to assert each adapter (a) returns `regenerated: false` when env vars are missing (no-credential setup), (b) issues the right HTTP request to the platform's revalidation endpoint when env vars are set, (c) returns `regenerated: false` on platform 4xx/5xx. Bisect-verified at the unit layer too: reverting Vercel's URL-builder → the URL-shape assertion fails with `expected calls[0]?.url === <full URL>`.

  **No e2e gate** for this PR — build-time ISR has no runtime UI surface to drive Playwright against. The platform-side revalidation requires real Vercel/Cloudflare/Netlify credentials + test deployments. Closest equivalent is the verify-modes cell (real Vite build, real route file with `revalidate` export, real SSG plugin chain emits the artifact) + the per-adapter stubbed-fetch unit tests (proves each adapter calls the right platform API with the right shape).

  **Limitations / known gaps**:
  - The literal-capture path skips re-export forms (`const x = 60; export { x as revalidate }`) and non-literal expressions (`export const revalidate = TTL`). `hasRevalidate` is still `true` in those cases — the export exists — but `revalidateLiteral` is undefined and the manifest skips the entry. Documented as a limitation in the unit tests; users who want the manifest entry should export inline (`export const revalidate = 60`).
  - Netlify's Build Hook is a full-site rebuild trigger, not per-page revalidation — Netlify doesn't expose a per-page ISR API. The path arg flows into `trigger_title` for audit-log traceability but doesn't scope the rebuild.
  - Vercel's revalidation endpoint (`/api/_pyreon-revalidate`) is a convention this PR introduces — user-side serverless function code that reads the request, validates the secret, and calls Vercel's `res.revalidate()` API. Future work could ship a `@pyreon/zero/vercel-revalidate-handler` helper to scaffold it.

  Reference: `packages/zero/zero/src/fs-router.ts:detectRouteExports` (literal extraction), `packages/zero/zero/src/types.ts:RouteFileExports` (`hasRevalidate` + `revalidateLiteral`) and `Adapter.revalidate?` + `AdapterRevalidateResult`, `packages/zero/zero/src/ssg-plugin.ts:buildRevalidateManifest` + the manifest-emit block in `closeBundle`, `packages/zero/zero/src/adapters/*.ts:revalidate` per-platform implementations.

  **PR J — adapter.build() wired into SSG closeBundle + per-platform SSG branches.** Pre-PR-J `Adapter.build()` was implemented for all 6 adapters (`vercel`, `cloudflare`, `netlify`, `node`, `bun`, `static`) BUT no production code path called the methods. Apps configured for `adapter: 'vercel' | 'cloudflare' | 'netlify'` got their dist directory built but no platform routing config emitted — `.vercel/output/config.json` / `_routes.json` / `netlify.toml` were absent, forcing users to write those files by hand or accept platform-default behaviour (which on Cloudflare meant invoking the Pages function for every URL even when the site was fully prerendered, wasting compute / cost on paid plans).

  PR J wires `adapter.build()` into `ssgPlugin`'s `closeBundle` AFTER the path render loop. The build pipeline is now:

  1. Render all paths (PR D worker pool)
  2. Emit `_redirects` / `_redirects.json` (PR B redirect manifests)
  3. Emit `dist/404.html` (PR C)
  4. Emit `_pyreon-ssg-paths.json` (PR F sitemap manifest)
  5. Emit `_pyreon-ssg-errors.json` if any errors (PR G)
  6. **Call `adapter.build({ kind: 'ssg', outDir, config })`** (PR J — new step)
  7. Cleanup SSR build artifacts

  **`AdapterBuildOptions` is now a discriminated union** on the new `kind` field. Pre-PR-J it was a single SSR-shaped struct with `serverEntry` / `clientOutDir` required. SSG callers couldn't supply a server entry (every page is prerendered) so they couldn't invoke `build()`. The discriminated union splits into:

  - `{ kind: 'ssr', serverEntry, clientOutDir, outDir, config }` — for SSR mode (unchanged shape; existing adapter SSR branches keep working with no behavior change)
  - `{ kind: 'ssg', outDir, config }` — new SSG variant; `outDir` IS the rendered dist directory the SSG plugin produced

  Each platform adapter's SSG branch:

  - **`vercelAdapter`**: writes `.vercel/output/config.json` v3 STATIC variant (no functions, just routes for `/assets/*` long-cache headers). Does NOT copy files into `.vercel/output/static/` — Vercel's CLI deploy auto-detects the dist root, and copying would break user post-build steps (sourcemap upload, perf scripts).
  - **`cloudflareAdapter`**: writes `_routes.json` with `version: 1, include: [], exclude: ['/*']` — i.e. "every URL is static, never invoke a Pages Function". Without this file, Cloudflare Pages defaults to running the worker on every request, wasting compute. No `_worker.js` emitted (zero-function deploy). ALSO writes `dist/_headers` (Cloudflare Pages + Netlify format) pinning `/assets/*` to `public, max-age=31536000, immutable` — without it, hashed chunks inherit the host's short default and re-fetch needlessly (the `/assets/*` rule vercel.json / netlify.toml already emit; closes the cloudflare cache-headers gap). Shared helper `adapters/cache-headers.ts:writeAssetCacheHeaders(outDir, assetsDir)` respects a user-provided `_headers` — if one already declares a policy for this `/<assetsDir>/` prefix it's untouched, otherwise the framework block is appended so user + framework rules coexist. (The `staticAdapter` deliberately stays a no-op — it's host-agnostic; emitting a Netlify/CF-specific `_headers` would be scope-creep. node/bun runtime handlers gate immutable on the `/<assetsDir>/` PATH, not file extension — see "node/bun cached immutable by extension" anti-pattern.) **`<base><assetsDir>`-aware:** the CDN adapters scope their immutable rule to the SERVED URL prefix — `base` (Vite `build.base`, default `/`) + the RESOLVED `build.assetsDir` (default `'assets'`) — NOT a hardcoded `/assets/`. The plugins capture `assetsDir` in `configResolved` and thread it into `adapter.build(options)` via `AdapterBuildOptions.assetsDir` (alongside `options.config.base`); the shared `adapters/cache-headers.ts:assetUrlPrefix(base, assetsDir)` helper computes the prefix (`/assets`, `/static`, `/blog/assets`, `/blog/static`, …) used by vercel (`config.json` `src: '<prefix>/(.*)'`), netlify (`for = "<prefix>/*"`), and cloudflare (`_headers` + `_routes.json` exclude). So a custom `build: { assetsDir: 'static' }` AND a subpath `zero({ base: '/blog/' })` deploy both keep immutable caching on their hashed chunks (pre-fix they fell to the host's short default and re-fetched every release). **node/bun stay `assetsDir`-only — base is deliberately NOT threaded** (their self-hosted handler serves files by raw `url.pathname` with no base-stripping, so a subpath deploy isn't supported there regardless; the cache check would imply support that doesn't exist). Bisect-locked by `adapters.test.ts` (`custom build.assetsDir scoping` + `base (subpath) cache-rule scoping`: custom `static` AND `base: '/blog/'` each scope every CDN adapter; node/bun honor `assetsDir` but NOT `base`; default stays `/assets/`) + two end-to-end ssr-showcase builds (`assetsDir: 'static'` → `/static/(.*)`; `base: '/blog/'` + `assetsDir: 'static'` → `/blog/static/(.*)` matching the rendered HTML's asset URLs).
  - **`netlifyAdapter`**: writes `netlify.toml` with `publish = "."` (the dist root) + `Cache-Control` headers for `/assets/*`. No functions/redirects directives — PR B's `dist/_redirects` already covers loader-redirect manifests, and the `netlify.toml` declares the static-site shape.
  - **`staticAdapter`**, **`nodeAdapter`**, **`bunAdapter`**: SSG no-op. Static dist is already at `outDir` (nothing to copy); node/bun adapters are SSR runners (irrelevant for prerendered output). The validateBuildInputs helper early-returns for `kind: 'ssg'` so SSG-mode users don't see misleading "server entry not found" errors.

  **Adapter throws are caught in the SSG closeBundle wiring** so a buggy adapter can't take down a successful render — the error lands in `errors[]` with path `(adapter:<name>)` and surfaces in the post-build summary. The summary log gains `[adapter: <name>]` when adapter is non-default (i.e. anything other than `node`).

  Bisect-verified: removing the SSG branch from `cloudflareAdapter` → 2/2 cloudflare-SSG specs fail with `TypeError: The "src" argument must be of type string ... Received undefined` (the SSR branch tries to read undefined `clientOutDir` / `serverEntry`). Restored → 9/9 PR-J SSG specs pass + all 14 pre-existing SSR specs pass. **What's NOT in PR J**: serverless adapters in SSR mode still aren't auto-invoked because zero doesn't run a separate Vite SSR build for `mode: 'ssr'` apps — that's separate infra (a future PR can wire it). Direct deploy of SSR mode requires the user to handle their own server bundle. References: `packages/zero/zero/src/types.ts:AdapterBuildOptions`, `packages/zero/zero/src/adapters/{vercel,cloudflare,netlify,static,node,bun}.ts`, `packages/zero/zero/src/ssg-plugin.ts` (closeBundle adapter.build invocation).

  **PR K — per-locale 404 emission + hreflang sitemap cross-references (i18n follow-up).** PR H landed i18n route duplication; PR K closes the remaining SEO gaps for multi-locale SSG sites: (a) every locale subtree gets its own `dist/{locale}/404.html` rendered through that locale's `notFoundComponent` (not just the default-locale `dist/404.html`); (b) `sitemap.xml` emits `<xhtml:link rel="alternate" hreflang>` cross-references between locale variants per the Google-recommended shape, with an `x-default` entry pointing at the default-locale URL.

  **Per-locale 404 walker.** The synthetic SSG entry's `findNotFoundComponent` (single result) became `findNotFoundComponentsByLocale(routes, currentLocale)` returning a `Map<string | null, Component>` keyed by detected locale (`null` = default / no-i18n). Locale detection: a RouteRecord serves locale `X` when its `path` matches `/${X}` or starts with `/${X}/`. The walker carries an ambient `locale` parameter through `r.children` recursion so per-locale `_404.tsx` duplicates (PR H fans `isNotFound` records the same way it fans pages) land in the right Map entry. The locale list is baked into the SSG entry source at build time — the outer plugin passes `config.i18n?.locales ?? []` as a JSON literal to `renderSsrEntrySource(locales)` (replacing the prior `SSR_ENTRY_SOURCE` const), so the walker has structural knowledge of which path prefixes count as locale roots without needing to import the i18n config inside the SSR sub-build.

  **`__renderNotFound(locale)`** accepts an optional locale and renders that variant from the Map; the outer plugin's closeBundle iterates `[...handlerMod.__notFoundComponentsByLocale.keys()]`, calls the renderer per locale, and writes to `dist/404.html` (null locale) or `dist/{locale}/404.html` (string locale). Static hosts (Netlify per-directory `errors_404`, Cloudflare Pages, GitHub Pages, nginx `try_files`) serve the right per-prefix 404 for unmatched URLs under each locale prefix. Back-compat: `__notFoundComponent` is kept as a derived const reading the null-locale entry so downstream code that imports it directly keeps working; closeBundle falls back to single-shot mode if a stale SSR entry doesn't expose the Map.

  **Hreflang sitemap (`seoPlugin`).** New `sitemap.hreflang` field accepts `true` (auto-detect from SSG manifest) OR `I18nRoutingConfig` (explicit) OR `false`/omitted (no hreflang). The SSG plugin now embeds `i18n` into `_pyreon-ssg-paths.json` alongside `paths`, so `hreflang: true` becomes zero-config: declare `zero({ i18n: { ... } })` once, both route duplication (PR H) AND sitemap hreflang pick it up. `generateSitemap` accepts an optional `i18n` arg; when set, `clusterPathsByLocale` groups URLs by their un-prefixed (default-locale) form via the new `stripLocalePrefix` helper. Each cluster emits ONE `<url>` with hreflang siblings per locale variant + an `x-default` entry pointing at the default-locale URL:

  ```xml
  <url>
    <loc>https://example.com/about</loc>
    <changefreq>weekly</changefreq>
    <priority>0.7</priority>
    <xhtml:link rel="alternate" hreflang="en" href="https://example.com/about"/>
    <xhtml:link rel="alternate" hreflang="de" href="https://example.com/de/about"/>
    <xhtml:link rel="alternate" hreflang="cs" href="https://example.com/cs/about"/>
    <xhtml:link rel="alternate" hreflang="x-default" href="https://example.com/about"/>
  </url>
  ```

  Three locale URLs collapse into ONE `<url>` (matches Google's "use one URL set per page, cross-reference via xhtml:link" guidance, not three separate entries). The `xmlns:xhtml` namespace declaration on `<urlset>` is conditional — only emitted when hreflang is active so plain-sitemap output stays unchanged. Single-variant clusters (URLs that exist in only one locale, e.g. a locale-specific asset page) skip xhtml:link entirely — there's nothing to alternate to. Dynamic-route × locale cross-products (PR A's `getStaticPaths` × PR H's locales) flow through the same clustering naturally: `/posts/1`, `/de/posts/1`, `/cs/posts/1` cluster as one `<url>` with three hreflang siblings.

  **`stripLocalePrefix` semantics under both strategies.** Under `prefix-except-default`, an un-prefixed path belongs to the default locale by convention (`/about` → locale `en`, `unPrefixed: /about`). Under `prefix` strategy, an un-prefixed URL doesn't fit any locale subtree (every locale should carry an explicit prefix) and is treated as standalone with `locale: null` — those URLs end up in single-variant clusters and don't get hreflang siblings. Edge case: partial-match guard — `/encyclopedia` is NOT detected as locale `en` because matching requires exact `/${locale}` or `/${locale}/` prefix, not substring containment.

  **Real-app coverage** at THREE layers: (1) **unit** — `generateSitemap` / `clusterPathsByLocale` / `stripLocalePrefix` / `resolveHreflangI18n` (19 specs in `packages/zero/zero/src/tests/seo.test.ts`); SSR entry source assertions for the per-locale walker (6 specs updated in `ssg-plugin.test.ts`). (2) **verify-modes** `ssr-showcase × ssg-i18n` cell now also asserts `dist/{de,cs}/404.html` exist + `sitemap.xml` carries the full hreflang cross-reference matrix including the `/posts/{id}` dynamic-route × locale cross-product. (3) **e2e** at `e2e/ssg-i18n.spec.ts` — 2 new specs (`per-locale 404` + `hreflang sitemap.xml`) hit `/de/404.html` + `/sitemap.xml` through `scripts/serve-ssg.ts` and validate the static-host-served output structure.

  **Bisect-verified at TWO layers.** (a) Reverting the closeBundle's per-locale `localeEntries` walker to single-shot mode → verify-modes ssg-i18n cell fails with `expected file: dist/de/404.html`. Restored → 6/6 ssr-showcase cells pass. (b) Reverting the SSG plugin's manifest-emit to omit the `i18n` field → verify-modes ssg-i18n cell fails on the hreflang assertion (`<xhtml:link rel="alternate" hreflang="en" href="https://example.com/about"/>` absent from the rendered sitemap — seoPlugin's `hreflang: true` auto-detect path returns undefined when the manifest doesn't carry i18n). Restored → cell passes.

  **Limitations / known gaps** (after PR L5 closed the layout-chrome gap): (a) **Host routing is required for unmatched URLs.** SSG writes `dist/{locale}/404.html` per locale BUT the static host must be configured to route `/de/unknown-page` → `dist/de/404.html`. Most managed hosts only serve the ROOT `dist/404.html` by convention; per-prefix 404 routing needs explicit declarations (Netlify `[[redirects]]`, Cloudflare Pages `_redirects`, nginx `try_files`, Caddy `handle_errors` per-locale matcher). GitHub Pages does NOT support per-directory 404 customization at all. Documented in `docs/src/content/docs/zero.md` `_404.tsx convention` section with per-platform snippets. (b) **One `_404.tsx` source = identical rendered HTML across locales** (now WITH layout chrome thanks to L5, but the testid + 404 content text are still byte-identical). ssr-showcase ships a single `_404.ts`; the per-locale variants all render byte-identical 404 BODY content. Real i18n apps with `useI18n()` in `_404` would see locale-specific text via the hydration-time t-call — server-side i18n context flows through the router-driven render path L5 introduced (the synthetic chain runs through `router.preload(probeUrl)` which DOES run the parent layout's loaders), so server-side `useI18n()` works correctly under L5 if the layout's loader populates the locale context. (c) **`x-default` always uses the default locale's URL** — no `hreflangXDefault: false` opt-out yet; can be added when a real app needs it. (d) **No per-locale sitemap shards** (single sitemap with hreflang clusters chosen as the Google-canonical shape; sitemap-index + per-locale sharding can be added when a site outgrows the single-file size limit ~50k URLs).

  Reference: `packages/zero/zero/src/ssg-plugin.ts:renderSsrEntrySource` + `findNotFoundComponentsByLocale` + the per-locale closeBundle loop, `packages/zero/zero/src/seo.ts:clusterPathsByLocale` + `stripLocalePrefix` + `resolveHreflangI18n` + the updated `generateSitemap`, `examples/ssr-showcase/vite.config.i18n.ts` (the e2e build config wiring both surfaces).

## Testing

```bash
bun run test                          # all package tests (via workspace filter)
bun run test:browser                  # all real-browser smoke tests (Chromium via @vitest/browser)
cd packages/<name> && bun run test    # single package
cd packages/<name> && bun run test -- --coverage  # with coverage
```

DOM-dependent packages (runtime-dom, router, head, compat layers) use `environment: "happy-dom"` in vitest config.

Real-browser smoke tests run under `@vitest/browser` with Playwright Chromium — files named `*.browser.test.ts(x)`, opt in per-package via `vitest.browser.config.ts` + `test:browser` script. Reference: `packages/internals/test-utils/src/browser/sanity.browser.test.ts`. See `.claude/rules/test-environment-parity.md` for the setup recipe.

### Test environment parity

Tests must run in the same environment as production. happy-dom is a Node-based DOM polyfill, NOT a real browser — it does not catch `typeof process` dead code, real `IntersectionObserver` timing, real CSS rendering, Vite's `import.meta.env` browser behavior, or other environment-divergence bugs. See `.claude/rules/test-environment-parity.md` for the categorization (browser packages, server packages, universal packages) and the rules for each.

The recurring failure mode this prevents: tests pass because vitest provides something (`process`, hand-constructed vnodes, mocked APIs) that production does not. PR #197 (mock-vnode silent metadata drop) and PR #200 (typeof process dead in browser) were both this shape. Both bugs were invisible until a real-world consumer hit them.

**Key rules**:

- **Browser packages** must have at least one Playwright/browser smoke test in addition to vitest tests (enforced by lint rule `pyreon/require-browser-smoke-test`).
- **Mock-vnode tests must have a parallel real-`h()` test** for any contract assertion. The mock test is the fast path; the real-`h()` test is the safety net. Always have both.
- **Dev-mode warnings must use bare `process.env.NODE_ENV !== 'production'`** — the bundler-agnostic library convention. Pyreon publishes libraries; consumers compile with Vite, Webpack/Next.js, esbuild, Rollup, Parcel, or Bun. Every modern bundler auto-replaces `process.env.NODE_ENV` at consumer build time. Do NOT use `typeof process !== 'undefined' && process.env.NODE_ENV !== 'production'` (dead in Vite browser builds) or `import.meta.env.DEV` (Vite/Rolldown-only). Reference: `flow/src/layout.ts:warnIgnoredOptions`. Enforced by `pyreon/no-process-dev-gate`.
- **Bisect-verify regression tests** before merge: revert the fix → run the test → assert it fails with the right error message → restore the fix → assert it passes. PR #200's first regression test passed even with the broken pattern, because esbuild's minifier folds dead code regardless. The bisect verification caught it.

## CI / Lint / Typecheck

```bash
bun run lint                          # lint all packages + examples (via workspace filter)
bun run typecheck                     # typecheck all packages + examples (via workspace filter)
oxlint .                              # lint (400+ rules, Rust-powered)
oxfmt --write .                       # auto-format
oxfmt --check .                       # check formatting
```

Every package and example must have `"lint": "oxlint ."` and `"typecheck": "tsc --noEmit"` in scripts.
Examples use `noEmit: true` in tsconfig (not `rootDir`) since they include vite.config.ts.

### CI architecture (post-speedup, 2026-05)

PR pipeline wall-clock: **~8.3 min** (was ~13 min pre-speedup). Five-PR plan landed via PRs #948 / #954 / #964 / #968 / #972; the architecture is non-obvious in a few places — if you're about to edit `.github/workflows/ci.yml` read this first.

**Job-DAG shape**:

```text
Install (~25s, bun install only — postinstall bootstrap SKIPPED via PYREON_BOOTSTRAP_SKIP=1)
├── lib-free jobs (start at T+25s, save ~150s critical-path):
│     typecheck-cell (8-cell matrix) → Typecheck (aggregator)
│     lint, lint-ratchet, audit-types, coverage (floor), check-doc-claims,
│     check-manifest-depth, docs-sync, audit, test-rust-binary,
│     e2e-decide
│
└── Bootstrap (~208s, runs scripts/bootstrap.ts to build lib/)
      └── lib-needing jobs (start at T+233s):
            test-cell (8-cell matrix) → Test (aggregator)
            test-browser, coverage-full
            build, verify-modes, check-bundle-budgets,
            check-distribution, scaffold-smoke-cell, benchmark
            e2e-suite (matrix, also needs e2e-decide)
```

**Why lib-free vs lib-needing.** TypeScript's root tsconfig sets `customConditions: ["bun"]` and vitest sets `resolve.conditions: ['bun']` — both resolve `@pyreon/*` to `src/*.ts` (not `lib/*.js`). So typecheck / lint / vitest-driven tests don't need `lib/` built. **Exception**: tests that ASSERT ON `lib/` as an artifact (`@pyreon/charts/src/tests/bundle-size.test.ts` measures lib/ bytes) OR boot a nested Vite SSR build at test time (`@pyreon/vite-plugin/src/tests/rocketstyle-collapse.test.ts` — Vite's config-loader is hardcoded to the node condition) DO need `lib/`. The `test-cell` matrix gates on `bootstrap` for that reason — both classes of test live under `packages/*/tests/`, can't be cheaply detected at workflow-config time, so the whole `test` job waits.

**Matrix shards** (P3b): `typecheck-cell` and `test-cell` are 8-cell matrices over `packages/*` categories (`core` / `fundamentals` / `internals` / `native` / `tools` / `ui` / `ui-system` / `zero`). Each cell narrows the affected-filter via `scripts/affected.ts --category=<name>` — cells with no affected packages skip gracefully. Aggregator jobs `Typecheck` / `Test` keep the original required-check names for branch protection. Same pattern as the pre-existing `e2e-suite` (decide → matrix → aggregator) and `scaffold-smoke-cell` (6-cell matrix from P1d).

**Composite action** (P2b): `.github/actions/setup-pyreon/action.yml` handles the 4-step preamble (setup-bun + optional setup-node + restore node_modules + restore bootstrap). 16 jobs use it. Critical contract: the caller MUST run `actions/checkout` BEFORE the composite (local composite actions need `action.yml` on disk before the runner can invoke them). Inputs: `restore-node-modules` (default true), `restore-bootstrap` (default true; set `'false'` on lib-free jobs to skip the ~5s restore), `node-version` (default empty; set `'24'` for vitest-driven jobs).

**Coverage split** (P3a): `Coverage` runs `coverage:floor` on every PR (~5s — pure config-threshold gate against `vitest.config.ts`); `Coverage (Full)` runs the full per-package coverage suite on `push:main` + `merge_group` only (the actual safety net against `main` regression). Documented trade-off: a PR that drops a package's actual coverage from 92% to 88% (still above the 90% floor) passes `Coverage` but fails `Coverage (Full)` at merge-queue time.

**`scripts/affected.ts`** computes the per-PR `--filter=<pkg>` list by walking changed files, mapping to owning workspaces, and BFS'ing the reverse-dep graph. Pure functions (`computeAffectedFlags`, `filterByCategory`, `findOwningWorkspace`, `transitiveDependents`, `isRootFile`, `discoverWorkspaces`) exported for unit testing — see `packages/internals/test-utils/src/tests/affected.test.ts` (17 specs). Root-file changes (workflow / lockfile / scripts / root tsconfig) escalate to `--filter=*`; per-category cells then translate that to `--filter=./packages/<cat>/*`.

**`scripts/bootstrap.ts` env-var matrix**:

- `PYREON_BOOTSTRAP_SKIP=1` — early-exit no-op. CI's `Install` job uses this; never set locally (devs want `lib/` built so `bun run dev` works immediately).
- `PYREON_BOOTSTRAP_SKIP_NATIVE=1` — skip the Rust napi build. Used by bootstrap exit-code subprocess tests (the cold-cache cargo build can't fit a 60s test timeout).
- `PYREON_BOOTSTRAP_SOFT=1` — swallow postcondition failure on postinstall (escape hatch when a transient lib-build failure blocks `bun install`; consumer must re-run bootstrap manually before any production build).
- `PYREON_BOOTSTRAP_FORCE_FAIL=1` — test-only; jumps straight to the failure path for exit-code verification.

**Pre-push hook** (Phase E1): runs `lint` + affected `typecheck` + affected `test` locally on `git push`. Bypass: `PYREON_SKIP_PRE_PUSH=1 git push` or `--no-verify`. See `.claude/rules/workflow.md` → "Pre-push hook".

**Remaining critical path** = `Install` (25s) → `Bootstrap` (208s) → `Scaffold Smoke` (~210s per cell, parallel after Bootstrap) ≈ ~7-8 min minimum. Bootstrap can't be skipped (it builds the workspace `lib/` outputs that the lib-needing chain reads); Scaffold Smoke runs a fresh `bun install` per cell which is genuinely slow. Larger runners (Phase 4) would help further but trade dollars for wall-clock.

**Don't undo by accident**: when adding a new job to `ci.yml`, ask "does this read `packages/*/lib/`?" — if yes, `needs: bootstrap`; if no, `needs: install` + `restore-bootstrap: 'false'` on the composite. Getting that wrong is a one-line revert when CI fails; getting it CONFIDENTLY wrong (flipping a lib-needing job to `needs: install` and not noticing it passed for the wrong reason because the test didn't exercise the bug) is the more expensive failure mode.

## Coverage thresholds — integration-tier reframing

`@pyreon/zero` and `@pyreon/lint` ship with large `coverageExclude` lists. This is INTENTIONAL — both packages have substantial integration-tier surface (real-Vite plugins, browser-runtime entries, dep-gated opt-in lint rules) whose coverage gap can't be closed by Node-side vitest without inventing fake fixtures. The exclusions document the boundary explicitly.

When auditing one of these packages and seeing "low coverage of X.ts" — check whether X.ts is in the exclude list first. If it is, the actual coverage gate is:
- For `@pyreon/zero`: real-Chromium e2e (`e2e/ssr-showcase*`, `e2e/zero-hmr.spec.ts`) + verify-modes (`scripts/verify-modes.ts`) for SSG/SSR build artifacts
- For `@pyreon/lint` rule files: dedicated `src/tests/<rule>.test.ts` covering the canonical AST shapes; the gap in the package-level number is in defensive AST helpers for shapes that need real consumer-project ASTs

If you raise a threshold by ADDING exclusions, the gate gets weaker for those files going forward — adding new uncovered branches inside an excluded file won't fail CI. Mitigation: when an exclusion lets a threshold rise, add a CLAUDE.md note OR a per-package README section documenting the trade-off so future contributors don't unknowingly drop production logic into the excluded zone.

## pyreon CLI — `lint` (front door to @pyreon/lint)

`pyreon lint` is a thin shell over `@pyreon/lint` — it forwards EVERY `pyreon-lint` flag verbatim (`--preset`/`--fix`/`--format`/`--quiet`/`--rule`/`--config`/`--ignore`/`--watch`/`--lsp`/paths). The DRY mechanism: `@pyreon/lint`'s `cli.ts` `main()` was refactored to an exported **`runCli(argv): number | null`** (returns the exit code; `null` for the long-running `--watch`/`--lsp` modes that keep the process alive) — both the `pyreon-lint` bin AND `pyreon lint` call `runCli`, so there is ONE CLI implementation, never drift. `@pyreon/cli` already depends on `@pyreon/lint` (the doctor lint gate), so this adds no new dep; `runCli` is lazy-imported in the dispatch (no main-entry bundle growth). Tested by `@pyreon/lint`'s `src/tests/cli-runcli.test.ts` (exit-code contract: help/version/list/clean → 0, error-level violation → 1; long-running modes verified by code, not a spun server). Item 3 of the CLI-unification effort. Reference: `packages/tools/lint/src/cli.ts:runCli` + `packages/tools/cli/src/index.ts` lint dispatch.

## pyreon CLI — `upgrade` (align @pyreon/* versions)

`pyreon upgrade` ([packages/tools/cli/src/upgrade.ts](packages/tools/cli/src/upgrade.ts)) is the FIX for the skew `pyreon info` detects — it rewrites every `@pyreon/*` dependency range in the project's `package.json` to one target version (default: the HIGHEST version present, aligning laggards up; `--to <v>` overrides; `--exact` pins without the caret). **Dry-run by default** (prints the plan); `--write` applies, then the user runs their install. `workspace:`/`link:`/`file:`/git specifiers + non-`@pyreon` deps are left untouched. Pure core (`resolveTarget` / `computeUpgradePlan` / `rewriteDeps` / `compareVersions` / `cleanVersion`) exported + fixture-tested (`src/tests/upgrade.test.ts`, 16 specs); reuses `info`'s `scanInstalledPyreon`/`detectSkew`. Same slim-core dispatch as `info` (lazy-imported in `index.ts`, NOT re-exported from the main entry → zero main-entry bundle growth). Item 2 of the CLI-unification effort.

## pyreon CLI — `info` (env + version-skew)

`pyreon info` ([packages/tools/cli/src/info.ts](packages/tools/cli/src/info.ts)) reports the environment (CLI version, node/bun/platform), the project name (+ whether it's a `@pyreon/zero` app), and every `@pyreon/*` package installed in `node_modules` with its version. Because Pyreon ships its packages on ONE synced version trajectory (changesets fixed-group), `info` flags **version skew** when the installed set spans >1 version — the condition that trips the `registerSingleton` duplicate-instance guard (`[Pyreon] Duplicate @pyreon/X detected`) at runtime. **Slim-core / self-contained**: reads only the project's `package.json` + `node_modules/@pyreon/*`, no framework-package coupling (deps: `@pyreon/compiler` + `@pyreon/ansi` only) — works in any project or none. The pure core (`collectInfo` / `scanInstalledPyreon` / `detectSkew` / `formatInfo`) is exported + fixture-tested (`src/tests/info.test.ts`). `--json` for tooling. This is the first slice of the **CLI-unification** effort (one `pyreon` front door, slim core + project-local resolution of feature packages — plan in `.claude/plans/cli-unification.md`, not committed). It also fixed the hardcoded `pyreon --version` (was `0.4.0` → now reads `package.json`).

## pyreon doctor — unified project health audit

`pyreon doctor` is the single entry point for the project-**health** gates Pyreon ships — the scored, finding-producing audits (correctness / architecture / testing / documentation / performance). It is NOT a runner for the CI-*pipeline* gates that don't fit a scored-health snapshot — the lint **ratchets**, `check-changeset-required`, `check-release-readiness`, `gen-docs --check` / `check-mcp-docs` drift, `verify-modes`, `scaffold-smoke` etc. stay as their own CI jobs (they're diff-relative, baseline-relative, generated-artifact-sync, or heavy build integration, not health snapshots of the code). PR 1 (#570) extracted a `Finding[]` / `GateResult` shape from the standalone scripts; PR 2 added the aggregator, 0-100 score formula, and CLI output. Modeled after [react.doctor](https://www.react.doctor/) — banner + per-category bars + top-N findings.

```bash
pyreon doctor                # default: 12 fast gates (~2-5s), scored output
pyreon doctor --full         # adds 2 slow gates: audit-types, bundle-budgets
pyreon doctor --fix          # auto-fix lint + react-patterns where possible
pyreon doctor --json         # full DoctorReport for AI agents / dashboards
pyreon doctor --gha          # GitHub Actions annotation lines (one per finding)
pyreon doctor --ci           # exit nonzero on errors only
pyreon doctor --only lint,doc-claims      # restrict to listed gates
pyreon doctor --skip pyreon-patterns      # exclude from default set
pyreon doctor --audit-min-risk high       # tighten test-environment audit
```

**Gates (14 total, 12 fast + 2 slow)** — the authoritative lists are `FAST_GATES` / `SLOW_GATES` in `orchestrator.ts`; the CLI's `VALID_GATES` is DERIVED from them (`[...FAST_GATES, ...SLOW_GATES]`) so `--only`/`--skip` can never reject a gate that actually runs:

| Gate                 | Default category | Speed | Coverage                                                    |
| -------------------- | ---------------- | ----- | ----------------------------------------------------------- |
| `react-patterns`     | correctness      | fast  | `useState` / `useEffect` / `className` / React imports      |
| `pyreon-patterns`    | correctness      | fast  | Pyreon-specific anti-patterns (detectPyreonPatterns codes); **defers to the `lint` gate** for codes a lint rule fully owns (`process-dev-gate`, `raw-add-event-listener`, `query-options-as-function`) so they aren't double-reported at a wrong severity, and honors `.pyreonlintrc.json` exemptPaths for the kept ones |
| `lint`               | correctness      | fast  | All configured `@pyreon/lint` rules (reads `.pyreonlintrc.json`); opt-in best-practice rules route to the advisory `best-practices` category |
| `distribution`       | architecture     | fast  | `sideEffects` declared + source maps shipped (not excluded) |
| `doc-claims`         | documentation    | fast  | Hand-quoted numeric claims in docs match the source         |
| `islands-audit`      | architecture     | fast  | Cross-file island foot-guns (5 detector codes)              |
| `ssg-audit`          | architecture     | fast  | `_404.tsx` placement, `getStaticPaths` (**scoped to `mode: 'ssg'` apps** — SPA/SSR/ISR never prerender), revalidate literals |
| `content-audit`      | architecture     | fast  | `@pyreon/zero-content` broken internal links / missing titles / orphaned `.md` (**per-config** — same-named collections across apps don't cross-contaminate) |
| `native-audit`       | architecture     | fast  | Multiplatform (PMTC) build hazards in `.tsx` files importing `@pyreon/primitives`: web-only-package imports (charts/flow/code/dnd/document/query/table/virtual + the CSS-in-JS stack) + native-dropped top-level `interface`/`enum`/`class` decls. **Scoped to multiplatform files** (skips when no `@pyreon/primitives` importer). `--check-native`. warnings (web build unaffected). |
| `audit-tests`        | testing          | fast  | Mock-vnode test patterns (PR #197 bug class)                |
| `check-dedup`        | architecture     | fast  | Duplicate dependency versions in the lockfile               |
| `audit-leak-classes` | **best-practices** (advisory) | fast | The 5 memory-leak classes — **advisory** (info, false-positive-prone): VISIBLE but excluded from the grade + `--ci` |
| `audit-types`        | architecture     | slow  | Typed-but-unimplemented public-interface fields             |
| `bundle-budgets`     | performance      | slow  | Gzipped main-entry size vs locked budget                    |

A gate that THROWS is isolated by the orchestrator into a single `<gate>/gate-failed` ERROR finding (it never takes down the whole run).

**Score formula**: per-finding penalty `error=10 / warning=3 / info=1`. Per-category subscore = `max(0, 100 - sum)`. Overall = mean of _included_ category subscores. Letter grades A ≥ 90, B ≥ 80, C ≥ 70, D ≥ 60, F otherwise. Categories with no gate coverage are excluded from the mean (an unmeasured category shouldn't pull the average up).

**Advisory `best-practices` category** (`FindingCategory: 'best-practices'`, `score.ts:ADVISORY_CATEGORIES`). The `@pyreon/lint` opt-in best-practice rules (`meta.optIn`: the `frontend`/`query`/`rx`/`i18n` categories + form/router opt-in rules) route here in the lint gate **regardless of their lint category** (`gates/lint.ts` checks `rule.meta.optIn`). It is **scored + displayed** (its own bar/breakdown, labeled `advisory — excluded from grade & --ci` in the text renderer) but **always `included: false`** so it NEVER enters the overall mean/grade, AND `--ci` excludes advisory errors from the exit code (`doctor.ts` filters `!isAdvisoryCategory`). This resolves the objectivity tension from the doctor-objective work (#630): enabling opinionated best-practice rules surfaces their findings for visibility without tanking the objective health grade or breaking CI — opinionated ≠ broken. `isAdvisoryCategory(c)` is exported from `doctor/score` for consumers.

**Output formats**:

- `text` (default): big-score banner + per-category bars + top-N findings + skipped-gates footer. Uses ANSI colors when stdout is a TTY (respects `NO_COLOR` / `FORCE_COLOR`). OSC-8 hyperlinks on file paths so terminals that support them (iTerm2, WezTerm, kitty, VSCode) render clickable links. **Colors follow the Pyreon brand handoff (#651), CLI spec §6.5 / `pyr doctor` §6.6**: the palette in `render/ansi.ts` maps each brand token to its nearest **xterm-256** index and emits 8-bit SGR (`38;5;N`) — the handoff mandates "256-color must survive (no truecolor-only colors)", so there is intentionally no `38;2;r;g;b`. Mapping: error/fail `red`→ember-core (202), warning/hint `yellow`→ember-warm (220), pass/grade-A `green`→ok-green (78), info/links `cyan`→brand cyan (45), muted/separators `gray`→muted-2 (245). Severity glyphs match §6.5: `✗` error, `!` warning, `ℹ` info. Ember stays scarce by construction (only error/fail states + worst grade), per the brand's "ember = signal/critical only, never decoration" rule.
- `--json`: full `DoctorReport` — score, grade, per-category scores, findings array, gates array with meta, totals, elapsedMs, timestamp.
- `--gha`: GitHub Actions annotation lines (`::error file=X,line=Y,col=Z::message`). Severity map: error → error, warning → warning, info → notice.

**Architecture**: `packages/tools/cli/src/doctor/` ships the orchestrator (parallel `Promise.all` over selected gates), score module (pure scorer), aggregator (`buildReport(gates) → DoctorReport`), three renderers (`text` / `json` / `gha`). Each gate is a thin adapter — pure-function ports (distribution, doc-claims) or subprocess-adapters (audit-types, bundle-budgets) over the existing standalone scripts.

**Legacy flags**: `--audit-tests`, `--check-islands`, `--check-ssg` still work — they map to `--only <gate>` shortcuts so existing CI scripts continue to function unchanged.

## Audit Types — typed-but-unimplemented detector

`scripts/audit-types.ts` walks every public interface field in a package's `src/` and counts non-type references across the package. Fields with zero references are flagged as HIGH (likely typed-but-unimplemented). Catches the bug class where a feature is documented and typed but the runtime never reads the configured value — exactly what shipped in 0.14.0 with `mode: "ssg"` / `ssg.paths` / `ISRConfig.revalidate` / every `Adapter.build()`.

```bash
bun run audit-types                    # default — @pyreon/zero
bun run audit-types @pyreon/router     # one specific package
bun run audit-types --all              # high-risk-package list (zero, router, core, server, runtime-server, vite-plugin)
bun run audit-types --all --strict     # exit non-zero if any HIGH findings (CI gate, post-baseline)
bun run audit-types --json             # machine-readable output
```

CI runs `audit-types --all --strict` as a required `Audit Types` job — any HIGH finding (typed-but-unimplemented field, zero non-type refs in the owning package) FAILS the build. The original 3-finding baseline was triaged in Phase E2:

- `@pyreon/zero LoaderContext` — was a duplicate type with a `request: Request` field that the actually-constructed runtime context never populated. Dropped the duplicate; zero now re-exports `LoaderContext` from `@pyreon/router`. If SSR loaders need access to the request in the future, plumb it through the router-level `LoaderContext` in a follow-up PR — do NOT add fields to a type the runtime doesn't populate.
- `@pyreon/router RouteMeta.requiresAuth` — exempt (`EXEMPT_FIELDS` entry). User-side convention read by user-defined `NavigationGuards`, not the router runtime.
- `@pyreon/core NativeItem.__isNative` — exempt (`EXEMPT_FIELDS` entry). Brand marker consumed cross-package by `@pyreon/runtime-dom` (`mount.ts`, `nodes.ts`, `hydrate.ts`, `template.ts`); the audit's heuristic only scans within the defining package.

New HIGH findings now block merge. Triage path is the same: real bug → fix the runtime to populate the field (or remove the type); false positive (cross-package brand, user-side convention, etc.) → add to `EXEMPT_FIELDS` with a one-line rationale.

**Heuristic**: counts whole-word `\bfieldName\b` occurrences across the package's src (excluding tests, .d.ts, jsx-runtime catalogs). Subtracts 1 for the declaration itself. Permissive — matches member access, destructuring, bracket access, and object literals all at once. Trade-off: same-named locals get counted (false negatives where a real bug is hidden), but the bug class we're catching (zero references anywhere in the package) survives the noise.

**Adding a new package to the high-risk list**: edit `HIGH_RISK_PACKAGES` in `scripts/audit-types.ts`. The list grows from observed bugs, not from speculation — only add when a typed-but-unimplemented bug surfaces in that package's history.

**Exempting a known-good field**: add to `EXEMPT_FIELDS` array with a one-line rationale. Use sparingly — exemptions are technical debt the audit can't see.

## Verify Modes — real-app build matrix gate

`scripts/verify-modes.ts` is the structural gate against typed-but-unimplemented features at the build-output level (audit-types catches them at the type-surface level — these complement each other). It runs `vite build` against every cell defined in `MATRIX` (example × mode) and asserts the resulting `dist/` has actual rendered content (not just a build that didn't crash).

```bash
bun run verify-modes                          # all cells
bun run verify-modes ssr-showcase             # only ssr-showcase cells
bun run verify-modes --only ssg               # only ssg-mode cells
```

CI runs this on every PR as a required check (`Verify Modes` job in `.github/workflows/ci.yml`). The gate exists because 0.14.0 shipped with `mode: "ssg"` typed in the public API but no runtime implementation — apps configured for SSG silently shipped a SPA shell. No internal test caught it because nothing exercised the typed surface end-to-end. This matrix does, against the real example apps that already live in `examples/`.

**Adding a new cell**:

1. Add to `MATRIX` in `scripts/verify-modes.ts`
2. Implement the smoke assertion — must verify CONTENT, not just that the build succeeded. "Build green but output is empty" is the whole class of bug this gate exists to catch
3. Run locally to verify the cell passes

**Adding a new mode** (isr/edge/...):

1. Add to the `Mode` union
2. Define the per-mode dist/ shape in the smoke assertion (what files should exist, what content should they contain)
3. Add at least one cell exercising the new mode against an existing example

The matrix today: 12 cells covering ssr-showcase, app-showcase, ui-showcase, playground, fundamentals-playground, **islands-showcase** across ssr/ssg/spa modes. Total runtime ~90s. SSG cells include both explicit-paths and autodetect variants (different code paths in `ssg-plugin.ts`).

**Bundle-graph assertions** (`assertChunkGraph` helper): the `islands-showcase × ssr` cell goes beyond the file-content checks the other cells use — it asserts the _chunk graph_ (per-island dynamic-import chunks exist with named filenames + content fingerprints; never-strategy islands are absent from the bundle). This is the structural gate against silent regressions in code-splitting: every browser smoke + e2e test would still pass if all island chunks were inlined into the entry, but the cell would fail with `expected chunk for "Counter" in dist/assets/`. Bisect-verified against (a) registry leak (StaticBadge added to `hydrateIslands({ ... })` → fails with `chunk "StaticBadge-*.js" exists but should NOT`) and (b) static-import leak (a hydrated island statically imports + uses StaticBadge → fails with `string "static-badge" found in chunk Counter-*.js`).

**Cell shape**: `Cell.useExampleConfig?: boolean` runs `vite build` against the example's existing `vite.config.ts` unchanged (no auto-generated zero-driven verify config). Used for examples that don't go through `@pyreon/zero` — e.g. islands-showcase uses bare `@pyreon/vite-plugin` with `ssr: { entry }`.

## Check Bundle Budgets — per-package main-entry size gate

`scripts/check-bundle-budgets.ts` enforces a locked gzipped budget on every published package's main entry. The budgets live in `scripts/bundle-budgets.json` (current size at PR-time + 25% headroom, regenerated when growth is intentional).

```bash
bun run check-bundle-budgets         # exit non-zero if over budget
bun run check-bundle-budgets --json  # machine-readable
bun run check-bundle-budgets --update # regenerate budgets after intentional growth
```

The gate exists because the audit caught `@pyreon/flow` shipping 6.8MB unpacked (mostly source maps + a correctly-architected lazy chunk) by eyeballing `du -sh`. A locked-budget gate is the structural answer: any PR that grows a main entry past its budget fails until the budget is bumped explicitly. The budget bump itself is the PR signal — "this package legitimately got bigger."

**Measurement details:**

- Bundles each package's `lib/index.js` (the published entry), not `src/`. Bundling from `src/` triggers a Bun resolver edge case where relative imports get treated as external via the package's exports field.
- Externalizes `@pyreon/*`, `node:*`, AND every bare-module specifier auto-collected by AST-walking each package's `lib/**/*.js` for `ImportDeclaration` / `ExportNamedDeclaration` / `ExportAllDeclaration` / `ImportExpression` (dynamic `import()`) / `CallExpression` (CommonJS `require()`) nodes via `oxc-parser`'s `Visitor` (the same pattern `@pyreon/lint`'s rule infrastructure uses). The AST walker replaced a regex+`validSpec`-sanitizer pair that the audit found brittle: the regex matched `from "..."` patterns INSIDE string literals containing JS source (`@pyreon/compiler` vendors its own transform output as strings), forcing a `validSpec` allowlist regex to filter the false positives. The AST walker doesn't see code-as-string as imports — confirmed empirically: regex collected 9 specifiers from compiler's lib (3 real + 6 garbage), AST collects exactly 2 (`oxc-parser`, `typescript`). The auto-collection replaces a previous hardcoded `[@tanstack/*, echarts, elkjs, codemirror]` allowlist that drifted out of sync with the real dep tree — pre-fix `@pyreon/document` failed to bundle because the allowlist missed `jszip` (transitive via the vendored `pptxgenjs` chunk), and the script silently filtered the failure out of its output. The budget reflects bytes UNIQUE to this package's own code, not bytes from any third-party (workspace, peer-installed, or transitively vendored).
- `target: 'bun'` (not `'browser'`). `'bun'` auto-externalizes Node builtins (`module`, `child_process`, `fs`, etc.) which CLI / server packages need (`@pyreon/zero-cli`, `@pyreon/cli`, `@pyreon/mcp`, `@pyreon/lint`, `@pyreon/compiler`). For pure-browser packages with no Node imports the byte output is identical, so this is the universal choice. Pre-fix the script used `'browser'` and silently failed on every server-side package.
- `splitting: true` keeps dynamic imports as separate chunks. Flow's elkjs, document's PDF/DOCX renderers, and other lazy-loaded heavy deps are EXCLUDED from the main-entry measurement by design — they only load on-demand when the consumer invokes the feature.
- **`define: { 'process.env.NODE_ENV': '"production"' }`** — measures the PRODUCTION-stripped size, matching what real consumers ship after their bundler (Vite/Webpack/esbuild/etc.) applies its own production define. Pre-fix the script omitted this, so the measurement INCLUDED every `if (process.env.NODE_ENV !== 'production') console.warn(...)` string from `lib/` — overstating the real consumer bundle by 5-20% per package and forcing budget bumps for dev-only diagnostic growth that never reaches end users. Matches the convention Vue/React/Preact use for their published-size baselines. The fix landed alongside the `__DEV__`-alias migration (see Anti-pattern catalog: "Local `__DEV__` const alias prevents bundler tree-shake"); together they shrunk the consumer-shipped Pyreon footprint by ~7 KB gzipped across 17 packages — a typical Pyreon app shipping `runtime-dom + reactivity + core + router` is ~5.7 KB lighter.
- Gzipped size is the budgeted dimension because that's what consumers actually pay over the wire.
- **Failures are surfaced, not swallowed.** Pre-fix the script filtered failed builds out of `results` and reported "All N within budget" with N undercounting the real eligible-package set — silent gate erosion. The current shape includes a `failures: [{name, error}]` field in JSON output, prints failures to stderr, and exits non-zero if any package fails to bundle. CI now catches the case where a package starts failing to bundle for any reason (new unresolved transitive dep, build-output shape change) instead of silently dropping it from the budget gate.

CI runs this as a required `Check Bundle Budgets` job. The gate covers all 54 published `@pyreon/*` packages today (vs 49 pre-fix that silently excluded `compiler`, `document`, `lint`, `mcp`, `zero-cli`).

## Check Import Budgets — per-import (minimal-import) bundle gate

`scripts/check-import-budgets.ts` is the SIBLING of `check-bundle-budgets`. That gate measures each package's FULL barrel (`export *`); this one locks the gzipped size of the **canonical minimal imports real apps actually write** — `signal`/`computed`/`effect` from `@pyreon/reactivity`, `mount`/`render` (and +`hydrateRoot`) from `@pyreon/runtime-dom`, `RouterProvider`+`RouterView`+`RouterLink` from `@pyreon/router`, `h`/`Fragment`/`createContext`/`useContext` from `@pyreon/core`. Budgets live in `scripts/import-budgets.json` (current size + 3% jitter headroom).

**Why it exists**: the full-barrel budget catches a package getting fatter overall, but it CANNOT see the failure mode that matters most for app bundle size — **an optional feature silently stopping tree-shaking**, so a minimal `import { mount }` starts dragging in code it never used to (e.g. the animation runtime, the SSR loader helpers). A 2026 core-package size audit established the baseline this gate locks: per-import minimal sizes are **reactivity ~3.2 KB / runtime-dom mount ~8.0 KB / router basic ~2.9 KB / core jsx ~1.0 KB gzipped** — the core is already near the size frontier (most optional code — devtools, store, selector, View Transitions, scroll, loader serialization — tree-shakes out of a minimal import), so the lever is GUARDING that, not shrinking further.

**Method mirrors `check-bundle-budgets` exactly** (so the two are comparable + the method is CI-proven): bundle the package's built `lib/index.js` re-exporting only the scenario's symbols via `Bun.build` (minify, `target:'bun'`, `splitting:true`, `define NODE_ENV=production`), externalizing `@pyreon/*` + `node:*` + every bare-module specifier the lib touches (AST-collected). **Documented caveat**: because workspace `@pyreon/*` deps are externalized (this monorepo doesn't symlink them into `node_modules`, so a true by-name / inline-deps build isn't available outside a full Vite app), the ABSOLUTE numbers can slightly OVER-count a separately-imported optional module vs a real app that bundles `@pyreon/core` inline and honors ITS `sideEffects:false`. The gate's job is **regression detection on a consistent method**, not byte-exact prediction of one consumer bundler. (This is exactly why the `@pyreon/runtime-dom` `transition-entry.js` ~1 KB residual shows in this gate's `mount` number but is NOT a confirmed real-app cost — it's a known, TRACKED line item, not an action item: the only fix would be risky rolldown-chunking surgery for an unconfirmed kilobyte, deliberately deferred.)

```bash
bun run check-import-budgets          # exit non-zero on regression
bun run check-import-budgets --json   # machine-readable
bun run check-import-budgets --update # relock budgets (after intentional growth)
```

CI runs this as a `Check Import Budgets` job (`needs: bootstrap` — measures built `lib/`). Pure logic (`buildEntrySource`, `compareToBudgets`, `SCENARIOS`) is exported + unit-tested in `packages/internals/test-utils/src/tests/check-import-budgets.test.ts` (10 specs); the gate itself is bisect-verified (lower a budget → exit 1, restore → exit 0). The script guards `await main()` behind `import.meta.main` so importing the pure helpers in tests doesn't run the measurement, and declares a local `Bun` ambient + lazy `import.meta.dir` access so it typechecks under `@pyreon/test-utils`' (Bun-types-free) tsconfig.

## @pyreon/table — public surface drift gate

`packages/fundamentals/table/src/tests/public-surface.test.ts` snapshots the full set of names exported from `@pyreon/table` (50+ entries — most via `export * from '@tanstack/table-core'`, plus the 2 Pyreon adapter symbols). When TanStack adds, renames, or removes an export in a minor bump, the snapshot fails and the diff becomes the deliberate-decision moment that wildcard re-exports otherwise paper over.

The wildcard is intentional — it mirrors the convention used by every official TanStack adapter (`@tanstack/react-table`, `@tanstack/solid-table`, `@tanstack/svelte-table`, `@tanstack/vue-table`). Converting to named exports would diverge from the convention without buying a real migration window (the lockfile + this snapshot give the migration window). When the snapshot fails after a TanStack version bump, run `bunx vitest run --update public-surface` from the table package and review the diff: added/removed names should match the TanStack changelog. If TanStack added a debug or internal API we DON'T want to leak through, narrow `index.ts` from `export *` to a named list (case-by-case decision).

## Check Distribution — bundle/distribution hygiene gate

`scripts/check-distribution.ts` enforces two static invariants on every published `@pyreon/*` package:

1. **`sideEffects` field declared** — required for bundlers (Vite, Webpack, Rollup, esbuild) to tree-shake unused exports. A missing field forces the bundler to assume every imported module has side effects, defeating tree-shaking even for pure library code.
2. **Source maps shipped (NOT excluded from `files`)** — the package's `files` array must NOT include `"!lib/**/*.map"`. Maps make framework stack traces readable for users — every major JS library (React, Vue, Solid, Preact, Svelte, TanStack) ships them. Bundlers strip maps from production builds automatically; they never reach end users. Excluding them ships minified framework code with no way to debug.

```bash
bun run check-distribution         # exit non-zero if violations
bun run check-distribution --json  # machine-readable
```

CI runs this on every PR as a required `Check Distribution` job. The gate ALSO simulates `npm pack --dry-run` against `@pyreon/reactivity` and asserts the would-be-published tarball contains at least one `.map` file — catching the case where the `files` field is technically right but npm's interpretation strips them anyway. Bisect-verified: removing `sideEffects` fails with `missing-sideEffects`; adding `!lib/**/*.map` back to `files` fails with both `excludes-source-maps` (config) AND `tarball-missing-maps` (live pack proof).

Adding a new published package: declare `sideEffects` (`false` for pure libraries, an array for entry-point side effects) and do NOT add `"!lib/**/*.map"` to `files`. The gate refuses to merge if either invariant is violated.

**Historical note** — pre-2026-05, the framework excluded maps via `!lib/**/*.map` to save ~19MB across the monorepo per publish. The trade-off was wrong for a 0.x framework where consumers actively debug into internals: install footprint is invisible (~350KB-1MB extra per package, never reaches production bundles), while opaque stack traces (`lib/index.js:1:42857`) are visible every time a user hits a framework bug. The flip aligns Pyreon with every comparable library (React, Vue, Solid, Preact, Svelte, TanStack) — all ship maps.

## Check Doc Claims — numeric drift gate

`scripts/check-doc-claims.ts` asserts every numeric claim in human-written docs matches the underlying source of truth. The gate catches the recurring drift mode where a count is hand-quoted in 3-5 places ("35 signal-based hooks…") and only one gets bumped when the code changes — the audit caught the hooks README claiming 16 vs actual 34, drift that lasted long enough to ship to users.

The pure gate logic lives in `@pyreon/cli` at `packages/tools/cli/src/doctor/gates/doc-claims.ts` (so `pyreon doctor` runs it too); `scripts/check-doc-claims.ts` is a thin CLI wrapper. Add claim entries to the gate's `checks[]` array, not the script.

Today the gate covers five source-of-truth counters (19 claim sites):

- **Hook export count** in `packages/fundamentals/hooks/src/index.ts`. Claim sites: hooks README, `hooks/src/manifest.ts` (tagline + description), `CLAUDE.md` (package overview row + architecture section), `docs/src/content/docs/index.md`.
- **Doc page count** in `docs/**/*.md`. Claim site: `CLAUDE.md`.
- **Lint rule count** — the `allRules` array in `packages/tools/lint/src/rules/index.ts`. Claim sites: `CLAUDE.md` (×3), `packages/tools/lint/README.md`, `docs/src/content/docs/lint.md`, `packages/tools/lint/src/manifest.ts` (6 occurrences, `all`).
- **Lint category count** — distinct `category:` literals across `packages/tools/lint/src/rules/*/*.ts`. Claim sites: `CLAUDE.md` (×2), `packages/tools/lint/README.md`, `manifest.ts` (`all`).
- **Detector-code count** — the `PyreonDiagnosticCode` union in `packages/core/compiler/src/pyreon-intercept.ts`. Claim sites: `.claude/rules/anti-patterns.md` ("flags N of the patterns"), `CLAUDE.md` ("N detector codes today").

**Counters TEXT-PARSE in-repo source by `repoRoot`, never `import { allRules }`** — a dynamic import resolves via bun's module cache to a STALE published snapshot (observed: 0.18.0 cache → 66 rules while the working tree had 76), which would make the gate assert against the wrong branch. A `ClaimSpec` can set `all: true` to assert EVERY occurrence of the pattern agrees (not just the first) — required for files like `manifest.ts` that repeat the same count, where bumping 5 of 6 would otherwise pass silently.

```bash
bun run check-doc-claims         # exit non-zero if drift
bun run check-doc-claims --json  # machine-readable
```

CI runs this on every PR as a required `Check Doc Claims` job (~2s). The gate ALSO rejects hedged forms like "33+ hooks" — write the exact number so the source-of-truth comparison stays strict. Adding a new claim with a known canonical source means adding a new entry to the `checks[]` array; arbitrary numeric prose is not in scope.

## Lint Ratchet — oxlint warn-finding burn-down gate

`scripts/check-lint-ratchet.ts` + `lint-baseline.json` are the keystone that turns the curated oxlint config (`.oxlintrc.json`) from a one-time snapshot into a **self-sustaining quality system**. oxlint runs in a three-state model (see `.claude/rules/code-style.md`): `error` rules are gated at 0 by oxlint itself (the `Lint` CI job exits non-zero on any error); `warn` rules are a **burn-down lane**. This gate covers the warn half — each rule's count in `lint-baseline.json` may only **decrease**. A change that pushes any rule above its baseline fails. So new code can't refill the backlog while it's burned down; combined with the error tier, the lint surface can only improve over time.

```bash
bun run check-lint-ratchet            # gate: fail if any rule grew above baseline
bun run check-lint-ratchet -- --json  # machine-readable {regressions, improvements}
bun run check-lint-ratchet -- --update # regenerate baseline (only DOWN — after fixing findings)
```

Same ratchet shape as `check-bundle-budgets` / `check-manifest-depth`: the baseline is the *achieved* state, moved DOWN only via `--update` (reviewed in the diff). **Never raise a count to absorb a new finding** — fix it or scope/suppress with rationale. To gate a warn rule permanently, drive it to 0 and promote it to `error` in `.oxlintrc.json` (atomic fix-and-gate; it then leaves the baseline since errors aren't ratcheted). CI runs it as a required `Lint Ratchet` job (lib-free, `needs: install`, `restore-bootstrap: 'false'`); also in `validate-fast` + the pre-push hook. Pure logic (`countWarnFindings` / `compareToBaseline` / `buildBaseline`) unit-tested in `packages/internals/test-utils/src/tests/lint-ratchet.test.ts`; the end-to-end gate is bisect-verified (lowering a baseline count by 1 → exit 1).

## Pyreon-Lint Ratchet — framework self-dogfooding burn-down gate

`scripts/check-pyreon-lint-ratchet.ts` + `pyreon-lint-baseline.json` are the SIBLING of the oxlint ratchet, applying the **same three-state + ratchet discipline to `@pyreon/lint`'s OWN rules over the framework's `packages/*/src`** — the surface the oxlint ratchet doesn't cover. The `Pyreon Lint Gate` (`pyreon doctor --only lint --ci`, the `lint:pyreon` script) fails only on **ERROR-severity** findings, so the **advisory `warning` + `info`** findings the framework's own dogfooding produces were free to drift UP forever. This gate locks that advisory backlog: each rule's count in `pyreon-lint-baseline.json` may only **decrease**.

```bash
bun run check-pyreon-lint-ratchet            # gate: fail if any rule grew above baseline
bun scripts/check-pyreon-lint-ratchet.ts --json    # machine-readable {regressions, improvements}
bun scripts/check-pyreon-lint-ratchet.ts --update  # regenerate baseline (only DOWN — after fixing/exempting)
```

Three-state model applied to the dogfooding surface: **error** = gated at 0 by the Pyreon Lint Gate; **warn/info** = a burn-down lane that can only shrink (this ratchet); **off** = a rule scoped-off for a package via `.pyreonlintrc.json` / `exemptPaths` with a rationale — which simply stops it producing findings (lowering the baseline on the next `--update`). **Many of the seed-baseline findings are the EXPECTED `off` case, not real debt** — e.g. `no-raw-addeventlistener` over `@pyreon/hooks` (which IS the wrapper layer that's supposed to call raw `addEventListener`); those get scoped-off with rationale rather than "fixed." Baseline seeded at **344 advisory findings across 22 rules** (top: `no-error-without-prefix` 88, `prefer-isserver` 60, `no-raw-addeventlistener` 47). Runs from the repo root via the `bun` workspace condition (no `lib/` build, ~1s). CI runs it alongside `check-lint-ratchet` in the `Lint Ratchet` job; also in `validate-fast` + the pre-push hook. Pure logic (`countPyreonFindings` / `buildBaseline`, reusing `compareToBaseline` from the oxlint ratchet) unit-tested in `packages/internals/test-utils/src/tests/pyreon-lint-ratchet.test.ts`; the end-to-end gate is bisect-verified (lowering a baseline count by 1 → exit 1).

## Check Manifest Depth — MCP `get_api` density ratchet

`scripts/check-manifest-depth.ts` enforces a floor on package-manifest `api[]` density. The root cause behind "`get_api` returns a signature but no foot-gun catalog for half the fundamentals" was that nothing enforced depth: `@pyreon/rx` shipped 3 `api[]` entries for 37 functions; `@pyreon/store` referenced `StoreApi` 3× in `seeAlso` with no entry at all (a silent `get_api` 404 to AI agents). The manifest-depth PR enriched store + rx to MCP density (grounded in real source — including correcting four `rx` inaccuracies: the false `.subscribe`-detection claim, the backwards `() => items()` advice, `groupBy` documented as `Map` when it returns `Record`, and `search`'s `{ keys }` vs positional signature) and added this gate so density can't silently erode.

```bash
bun run check-manifest-depth          # exit non-zero on regression
bun run check-manifest-depth --json   # machine-readable
```

It is a **ratchet, not a target** (same shape as `check-bundle-budgets` / audit-types `EXEMPT_FIELDS`): `LOCKED` records each migrated package's *achieved* `{ minEntries, minWithMistakes }` — counted via the authoritative `findManifests` loader (the same one `get_api` uses), never a source grep. The gate fails only if a locked package drops below its floor. Packages absent from `LOCKED` (i18n, charts, code, dnd, state-tree, validation, …) are the visible migration backlog — intentionally NOT gated, so the gate never flag-days CI on 15 packages at once. Migrating a package = enrich its manifest, then add it to `LOCKED` with the numbers that PR achieves; that addition is the deliberate "at standard now" signal, reviewed in the same PR. CI runs it as a required `Check Manifest Depth` job (~2s, `needs: install`).

## Release Readiness — release-pipeline config drift gate

`scripts/check-release-readiness.ts` + `.github/workflows/release-readiness-check.yml` gate every PR against two configuration invariants that, if violated, would silently break the release pipeline:

1. **`publishConfig.access: "public"` coverage** — every non-private, non-stub `@pyreon/*` package must declare it. `scripts/publish.ts` already passes `--access=public` on the CLI, but the `publishConfig` is the npm-canonical safety net + protects against manual `npm publish` accidentally treating the package as private. PR #1160 closed 4 historical drift sites under `packages/zero/`; this gate prevents the same drift from re-appearing.
2. **Changeset-group coverage** — every non-private, non-stub `@pyreon/*` package must be in EITHER the `fixed[0]` group in `.changeset/config.json` (synced version trajectory with the rest of the framework — the default for publishable packages) OR the `ignore` array (ships to git only, never to npm). A new package missing from BOTH would silently version-drift on the next release (other packages bump together, this one stays frozen).

Per-platform binary stubs (`@pyreon/compiler-{darwin,linux,win32}-*`) are exempt — they're published by `release-native.yml` not the main script, and they're already in the fixed group.

**Bypass:** `skip-release-readiness` label, intended for cases where a package is INTENTIONALLY outside the fixed group during an early-stage rollout. Use sparingly — the default expectation is that every publishable Pyreon package ships together.

**Why it exists:** PR #1153's release cascade made the abstract risk concrete. 3 new packages (`@pyreon/validate`, `@pyreon/primitives`, `@pyreon/create-multiplatform`) needed first-publish bootstrap; the main release workflow couldn't auto-bootstrap them through OIDC (trusted-publisher chicken-and-egg constraint requires a classic-token first publish). The pre-fix `scripts/publish.ts` then exited 1 on the first 404 → blocked the umbrella `v<version>` tag-push step → blocked `release-native.yml` → 7 native compiler binaries stuck on npm at 0.24.2 while source was at 0.26.x. **This gate catches the underlying configuration drift at PR time, before any release attempt.** Closes the bug class that produced the cascade.

## Published-State Verification — silent-dead-release gate

`scripts/check-published-state.ts` compares sentinel packages' repo version (`@pyreon/reactivity` / `core` / `zero`) against npm `dist-tags.latest` and exits 1 when a versioned release never reached npm. Exists because of the v0.31.0 incident: the Version PR merged, the publish job died (GHA OOM, later fixed by #1384's batched build), and npm `latest` silently stayed at 0.30.0 — users missing HIGH-severity fixes with no alarm anywhere. Two wire-ups: the post-publish verify step in `release.yml` (catches in-run failures) and the daily `published-state.yml` schedule (catches the class where the whole workflow crashed before its own verify step). Repo==npm is healthy; pending changesets don't move package.json until versioning, so between-releases states stay green. `scripts/publish.ts` is idempotent (skips already-published versions), so the remediation for a red run is simply re-running the release workflow.

## First-publish OIDC trusted-publisher procedure

For ANY new publishable `@pyreon/*` package, the very first publish to npm CANNOT go through the workflow's OIDC trusted publishing — the trusted-publisher rule lives on the npm-side package metadata, which doesn't exist until the first publish. One-time manual bootstrap procedure:

1. **From a dev machine**, ensure logged into npm with publish rights to `@pyreon`: `npm whoami` then `npm login` if needed.
2. **Build the package** fresh: `cd packages/<cat>/<pkg> && bun run build`.
3. **Dry-run pack** to inspect the tarball contents: `npm pack --dry-run` — confirm `lib/**`, `src/**`, `LICENSE`, `README.md`. Nothing else.
4. **Publish** with `bun publish --access=public`. Use `bun` (not `npm`) — auto-resolves `workspace:*` deps to real versions. 2FA OTP if your npm account has it. NO `--provenance` flag — classic tokens can't sign OIDC provenance; provenance starts with the SECOND publish via CI.
5. **Add Trusted Publisher** on npmjs.com:
   - Visit `https://www.npmjs.com/package/@pyreon/<name>/access` → Trusted Publishers → Add publisher
   - Publisher type: GitHub Actions / Organization: `pyreon` / Repository: `pyreon` / Workflow filename: `release.yml` / Environment name: (leave blank)
6. **Verify** the next changesets release publishes the package via OIDC — no further manual intervention needed.

`scripts/publish.ts` is hardened against this scenario (since PR #1161): if a future package is added without the manual bootstrap, the release WON'T cascade-fail — the script categorizes the 404 as "needs bootstrap" (non-blocking WARNING) and continues publishing the other packages. The umbrella tag still fires, `release-native.yml` still runs, native binaries publish on schedule. The maintainer sees the bootstrap-needed list at the end of the release log + actionable instructions.

## Leak Sweep — nightly heap-growth regression gate

`.github/workflows/leak-sweep.yml` drives `scripts/leak-sweep.ts` (#772) across the perf-dashboard journey catalog: ~43 journeys, 15 cycles each, two forced GCs between samples, least-squares regression over the resulting heap series. A non-trivial positive slope flags a journey as leaking. Same surface that surfaced the `<For>`-of-`<Show>` batched-toggle bug fixed in #776, and the `<For>`-of-direct-keyed-array sibling bug fixed in #783 (full discovery chain: #770 leak-audit harness → #772 leak-sweep multi-journey driver → #774 `it.fails()` CONTRACT lock → #776 `mountReactive` root-cause fix → #779 nightly gate → #783 `mountKeyedList` sibling fix → #784 anti-pattern doc entry).

Triggers (mirrors `perf.yml` shape):

- **`workflow_dispatch`** — manual run from any branch. Inputs: `app` (default `perf-dashboard`), `journeys` (comma-separated, empty = full sweep), `cycles`, `threshold`. Useful for reproducing a PR author's local leak claim in a clean VM or for re-running a subset against a specific branch.
- **`pull_request`** — auto-runs ONLY when a PR carries the `leak-sweep` label. Skipped on unlabeled PRs since most don't touch the runtime primitives the sweep exercises. Posts a sticky comment with the markdown sweep output (updates in place on re-run, marker `<!-- pyreon-leak-sweep -->`). `concurrency.cancel-in-progress` cancels an in-flight run when a fresh push lands.
- **`schedule`** — nightly cron at 04:11 UTC. Offset from `perf.yml`'s 03:17 so they don't contend for the same runner pool. Catches leaks introduced between PR labels — exactly how the For-of-Show bug was found.

Advisory-only. Exit code 6 from leak-sweep (≥1 journey exceeded the 50,000 bytes/cycle slope threshold) currently fails the job + uploads the artifact + comments on the PR — but the job is NOT in branch protection's required-checks list. Promote to required once the baseline stabilizes across multiple nightly runs.

**Dev mode is mandatory**: counter emits and the perf-harness install hook are both gated on `import.meta.env.DEV`, so a preview/prod build tree-shakes them and the page records zero heap movement (false-clean). Same constraint as `perf.yml`. The workflow hardcodes `--mode dev`; no opt-out.

**Artifacts**: every run uploads `perf-results/leak-sweep-<sha>-<app>.json` + `.md` (two-line multi-path form in the workflow's `path:`; the previous `*.{json,md}` shorthand was undocumented brace expansion that worked through @actions/glob's Minimatch but wasn't part of `actions/upload-artifact`'s contract). 30-day retention. The JSON shape (per-journey `slope`, `r2`, `cv`, plus aggregate verdict) is stable for downstream tooling — diff scripts, dashboards, future regression-comment automation.

## E2E — real-Chromium tests against example apps

`bun run test:e2e` runs Playwright against `examples/playground` in real Chromium. Tests live in `e2e/` and exercise framework primitives via `window.__pyreon` (mount/unmount, signal-driven DOM updates, computed values, batching, conditional rendering, list reconciliation, router navigation). Companion to `verify-modes` (build artifacts) — together they cover both the static output AND the runtime behavior.

```bash
bun run test:e2e                                              # default — playground project
bunx playwright test --project=playground                     # same
bunx playwright test --project=playground --grep="signal"     # filter
```

CI runs the e2e suites as a **parallel, flake-resilient matrix** (the `E2E` required check). **104 tests total** (43 playground + 12 ssr-showcase + 10 ui-showcase regression + 15 compat layer + 4 app-showcase flow + 4 app-showcase dnd + 1 app-showcase charts + 4 ssg-subpath + 6 ssg-i18n + 4 ssg-i18n-prefix + 1 fundamentals multi-route SWEEP) plus 5 fixme'd compiler-bug tests. Playwright report uploaded as artifact (per-suite name) on failure.

**CI matrix shape (`.github/workflows/ci.yml`).** Before 2026-05 the E2E job ran 11 suites SEQUENTIALLY in one runner — wall-clock = the SUM (~6-10 min), and a flake in suite N skipped every suite after it (an unrelated `storage-hydration` flake once skipped the entire `zero-hmr` gate). Now three jobs:

- **`e2e-decide`** runs `scripts/e2e-affected.ts` → a GitHub-Actions matrix `include` of only the suites the PR's changed paths can affect. `push: main` / `merge_group` always pass `--all` (the gate protecting `main` never trusts the narrowing — false-negatives are the only real danger; `forcesFullRun` escalates to ALL on any core-package / `scripts/**` / `playwright*.config.ts` / workflow / lockfile change).
- **`e2e-suite`** is a `fail-fast: false` matrix — every suite is its own parallel job + own check. One suite's flake/failure can't skip or cancel another; wall-clock ≈ the slowest single suite. A docs-only PR yields an empty matrix (job skipped).
- **`e2e`** (this exact, stable check name) aggregates the matrix so branch-protection's `E2E` requirement keeps resolving regardless of which / how-many suites ran — empty matrix = green skip; any selected suite failing = red.

**Flake resilience.** Every `playwright*.config.ts` uses `retries: process.env.CI ? 2 : 0` — a flaky spec (overlayfs / timing / HMR-ws / resource-contention race) self-heals within its job across up to 3 attempts; a real bug fails all 3. Local stays `0` for honest, fast feedback. This + the per-suite job isolation makes the "one unrelated flake reds/skips the whole gate" failure mode structurally impossible. Run a single suite locally with `bun run test:e2e:<name>` (e.g. `test:e2e:zero-hmr`).

**Retry-mask visibility (follow-up to #603).** `retries: 2` would otherwise make a passed-only-on-retry spec INVISIBLE (green job, hidden degradation). The `e2e-suite` matrix has a `Surface flaky specs` step: it tees Playwright output and, when the run reports `N flaky`, emits a `::warning::` + a `$GITHUB_STEP_SUMMARY` block listing the flaky titles. It never fails the job (the retry already passed it) — it makes the flake rate watchable so retries don't silently hide a rotting spec. Known load-sensitive specs should still be hardened at the spec level (e.g. `e2e/fundamentals/storage-hydration.spec.ts`'s post-click assertions use a 15s CI timeout — the documented HMR-suppression recipe is inapplicable there, see its `beforeEach`) so the warning surfaces only _unknown_ flakes worth root-causing, not expected noise. **The selector `scripts/e2e-affected.ts` is unit-tested** at `packages/internals/test-utils/src/tests/e2e-affected.test.ts` (pure exported `selectSuites` — the "no subprocess-tested scripts" rule; a false-negative there ships a regression unblocked, so it's covered, not just CLI-sanity-checked).

**Project status** (see [`playwright.config.ts`](playwright.config.ts) header for full detail):

| Project         | Status   | Coverage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `playground`    | ✓ active | reactivity, mount, bench, app shape (43 tests covering signal → DOM, mount/unmount, batching, computed, conditional rendering, list reconciliation, perf benchmarks, app routing). 5 fixme'd tests block on Pyreon compiler bugs (signal-method auto-call, JSX text/expression whitespace, event-name casing).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `ssr-showcase`  | ✓ active | SSR + hydration + nav + loaders + theme (12 tests). Was disabled until #345 — fixed in tandem by removing zero's dev-SSR `_layout.tsx` auto-load (it was wrapping createApp with a layout that fs-router was already emitting as a parent route, causing double mount).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ui-showcase`   | ✓ active | Real-app regression gate for the rendering/styling layer (rocketstyle, styler, unistyle, elements, runtime-dom). 10 tests in `e2e/ui-showcase-regression.spec.ts`, each mapping to one of the bug-shapes from PRs #197/#200/#336/#349. Lives in [`e2e-configs/ui-regression.config.ts`](e2e-configs/ui-regression.config.ts) — separate config so its webServer boots in isolation (Playwright's `webServer` array starts ALL servers regardless of `--project` selection; lumping all 3 into one config produces resource-contention flakes).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `fundamentals`  | ✓ active | Boots `examples/fundamentals-playground` on port 5176. 19 tests across 3 files: `playground.spec.ts` (sidebar / nav across all 16 demo routes / no-error sweep), `dom-interactions.spec.ts` (browser-API surface), `storage.spec.ts` (localStorage / sessionStorage / cookies + cross-tab events). Realigned in Phase C2 — nav had moved to `RouterLink` (renders `<a>`) and tests still selected `nav.sidebar button`. Sidebar / main count `toHaveCount(1)` assertions lock in the post-#406 layout-mount-once contract; a regression to the loop would fail the count assertion loudly instead of being silently masked by `.first()`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `compat-layers` | ✓ active | Real-app smoke gates for the 5 compat layers (`react-compat`, `preact-compat`, `vue-compat`, `solid-compat`, `svelte-compat`). 15 tests across 5 files (one per layer) on ports 5177-5180 + 5182 (5181 is taken by the app-showcase e2e config). Each suite has 3 specs: mount + render, reactive state click → DOM, multi-demo lifecycle. Closes the "compat shim looks right at the unit level but breaks in real-app shape" gap. Lives in [`e2e-configs/compat-layers.config.ts`](e2e-configs/compat-layers.config.ts) — separate config because Playwright boots ALL listed webServers regardless of `--project` filter; 5 extra dev servers in the main config would put significant resource pressure on every CI run. **react-compat re-render gotcha**: the layer does FULL DOM replacement on every state change (no VDOM diffing). Locator handles re-resolve fine via `expect(locator).toHaveText(...)`, but pre-captured `elementHandle()` would point at detached nodes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ssg-subpath`   | ✓ active | SSG subpath / base-path runtime gate (PR E). Builds `examples/ssr-showcase` with `vite.config.subpath.ts` (sets `zero({ base: '/blog/' })`) and serves the prerendered `dist/` via `vite preview` on port 5198. 4 specs in [`e2e/ssg-subpath.spec.ts`](e2e/ssg-subpath.spec.ts): initial load + no-console-error sanity, asset URLs return 200 under base, RouterLink click triggers client-side navigation to `/blog/about`, post-hydration counter signal works under base. Companion to the build-artifact `verify-modes ssr-showcase × ssg-subpath` cell — verify-modes asserts the emitted HTML strings; this asserts the runtime contract (router's `stripBase`, `__ZERO_BASE__` consumption in `startClient`, hydration under base). Bisect-verified: removing the `__ZERO_BASE__` read in `client.ts` → 2/4 specs fail (RouterLink href + post-hydration counter). Lives in [`e2e-configs/ssg-subpath.config.ts`](e2e-configs/ssg-subpath.config.ts) — separate config (same boot-isolation reason as ui-regression / compat-layers / app-showcase / islands-showcase). The webServer chains `build:subpath && preview:subpath` because `vite preview` doesn't auto-build. Direct-link navigation to `/blog/about` (no trailing slash) is intentionally NOT tested — that's a static-host directory-rewrite concern (Netlify / Cloudflare Pages do it; vite preview doesn't), not a framework concern. |
| `ssg-i18n`      | ✓ active | SSG i18n route duplication runtime gate (PR H follow-up). Builds `examples/ssr-showcase` with `vite.config.i18n.ts` (sets `zero({ i18n: { locales: ['en','de','cs'], defaultLocale: 'en' } })`) and serves the prerendered `dist/` via `scripts/serve-ssg.ts` (directory-rewriting static server) on port 5199. 6 specs in [`e2e/ssg-i18n.spec.ts`](e2e/ssg-i18n.spec.ts): default-locale unprefixed `/about`, non-default `/de/about` renders the duplicated route, third-locale `/cs/posts` renders with hydrated loader data (3 post-items + `window.__PYREON_LOADER_DATA__` keyed `/cs/posts`), RouterLink click from `/de/about` does client-side push (window-sentinel preservation), **per-locale 404 `dist/de/404.html` exists + renders the not-found page testid** (PR K), **`sitemap.xml` carries `<xhtml:link rel="alternate" hreflang>` cross-references per locale + `x-default`** including the dynamic-route × locale cross-product (PR K). Companion to the build-artifact `verify-modes ssr-showcase × ssg-i18n` cell — verify-modes asserts dist filesystem layout (per-locale `index.html` + dynamic-route × locale cross-product); this asserts the runtime contract (router matches duplicated record, hydration succeeds under locale prefix, loader-data hydration works on duplicated routes). Bisect-verified at TWO layers: (1) reverting the root-layout-skip in `expandRoutesForLocales` → spec (d) fails with "matches more than one element" because two `nav-home` testids exist in the doubled-layout DOM; (2) reverting the playwright config from `serve-ssg.ts` back to `vite preview` → spec (c) fails with `Expected: 3, Received: 0` because vite preview serves `dist/index.html` for `/cs/posts` (SPA fallback) instead of the prerendered `dist/cs/posts/index.html`. Lives in [`e2e-configs/ssg-i18n.config.ts`](e2e-configs/ssg-i18n.config.ts) — separate config (same boot-isolation reason as ui-regression / compat-layers / app-showcase / islands-showcase / ssg-subpath). **PR #516's "loader-data hydration known gap" was a test-environment artifact (vite preview SPA fallback), NOT a framework bug** — framework hydration is correct, `record.path === '/cs/posts'` keys the dehydrated payload correctly, and reading the prerendered HTML via directory-rewriting service exposes the inline `__PYREON_LOADER_DATA__` script as designed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ssg-i18n-prefix` | ✓ active | SSG i18n `prefix`-strategy runtime gate (PR L3 follow-up to PR L1). Builds `examples/ssr-showcase` with `vite.config.i18n-prefix.ts` (`strategy: 'prefix'` — every locale prefixed including default) and serves the prerendered `dist/` via `scripts/serve-ssg.ts` on port 5200. 4 specs in [`e2e/ssg-i18n-prefix.spec.ts`](e2e/ssg-i18n-prefix.spec.ts): (a) default-locale `/en/about` IS prefixed (strategy distinguishing case — under prefix-except-default this same URL 404s), (b) unprefixed `/about` returns 404 (critical contract — no unprefixed default exists under prefix), (c) per-locale dynamic-route `/de/posts/1` renders (cross-product PR A × PR H works under prefix too), (d) `sitemap.xml` carries `<xhtml:link rel="alternate" hreflang="x-default" href=".../en/..."/>` (en-PREFIXED — strategy-distinguishing contract on the sitemap side). Companion to the PR L1 verify-modes `ssr-showcase × ssg-i18n-prefix` cell (build artifact); this gates the runtime layer PR L1 deliberately left as a follow-up. Bisect-verified: forcing `prefix-except-default` semantics in `expandRoutesForLocales` regardless of config → 3/4 specs fail (`/en/about` no longer exists, unprefixed `/about` serves the page, `x-default` href points at unprefixed URL). Lives in [`e2e-configs/ssg-i18n-prefix.config.ts`](e2e-configs/ssg-i18n-prefix.config.ts) — separate config (same boot-isolation reason as ssg-i18n / ssg-subpath). Root-`/` behavior under `prefix` is intentionally NOT covered — host-config concern (Netlify / Cloudflare can redirect-to-default-locale; serve-ssg cannot), out of scope for the framework gate. |
| `app-showcase`  | ✓ active | Real-app gate for `@pyreon/flow` AND `@pyreon/dnd`. Boots `examples/app-showcase` on port 5181 and exercises the cross-package shapes that vitest can't drive: pointer events from real Chromium → flow's drag handler → signal write → runtime patches `transform: translate(...)`, plus the HTML5 drag-and-drop event lifecycle that pragmatic-drag-and-drop (under @pyreon/dnd) listens for. 4 flow specs in [`e2e/app-showcase-flow.spec.ts`](e2e/app-showcase-flow.spec.ts): render shape (4 nodes + 3 edges), `selected` class on click, node drag via dispatched `PointerEvent`s, wheel zoom via dispatched `WheelEvent`. 4 dnd specs in [`e2e/app-showcase-dnd.spec.ts`](e2e/app-showcase-dnd.spec.ts): scenarios render with all three sections + initial state, `useSortable` reorders via dispatched HTML5 drag events, `useDraggable` + `useDroppable` card-to-zone via dispatched drag events, `useFileDrop` accepts a synthetic image File via DataTransfer. Lives in [`e2e-configs/app-showcase.config.ts`](e2e-configs/app-showcase.config.ts) — separate config (same boot-isolation reason as ui-regression / compat-layers). **Event-dispatch gotcha (both flow + dnd)**: Playwright's `mouse.*` and `dragTo` don't always synthesize the right event lifecycle for libraries that use `setPointerCapture` (flow) or HTML5 drag events with a populated `dataTransfer.files` (dnd). The specs dispatch real `PointerEvent` / `WheelEvent` / `DragEvent` instances directly via `page.evaluate(() => target.dispatchEvent(new ...Event(...)))` for deterministic sequencing. The dnd file-drop spec specifically constructs a `DataTransfer` with `dt.items.add(new File(...))` because pragmatic-drag-and-drop's external/file adapter reads files via `getFiles({ source })`. **Cross-platform `dragenter` placement is critical**: the file-drop spec dispatches `dragenter` on `window` (NOT on the zone), then `dragover` + `drop` on the zone, all sharing the same `DataTransfer` instance — this matches pdnd's own Playwright suite at `pragmatic-drag-and-drop/packages/core/__tests__/playwright/external-files.spec.ts` and is the only sequence that activates the external-file adapter reliably under headless Linux Chromium. Earlier attempts that bubbled a zone-targeted `dragenter` worked on macOS Chromium but failed on Linux because `getAvailableTypes(transfer)` evaluated before the file-types population fully landed in the transfer. |

Re-enabling a disabled project: uncomment the project block + matching webServer block in `playwright.config.ts`, run locally to verify, update the CI job's project filter.

**Why this exists** (the gap pre-Phase-2): the e2e suite has been in `e2e/` for some time, but the playwright config pointed port 5173 at `fundamentals-playground` (where most tests don't work) AND the wiring wasn't in CI. Several real bugs the suite would have caught (the `_layout.tsx` double-mount, zero's `port: 3000` plugin override) shipped to users because nothing exercised these tests on PRs.

**ui-showcase regression gate.** The 5 target packages (`runtime-dom`, `styler`, `rocketstyle`, `elements`, `unistyle`) produced 24% of all `fix:` commits — every one came from a real consumer app surfacing a bug that synthetic vitest tests structurally couldn't catch. The ui-showcase gate covers cross-package shapes: rocketstyle's `attrs()` HOC moving props through styler + unistyle + elements + runtime-dom in a real app, with real signal handlers and real hydration. See `.claude/rules/test-environment-parity.md` for the bug-shape catalogue and the recipe for adding new specs when a real-app regression surfaces.

**Layout-remount loop fix (post-#402)**: `<RouterView />`'s reactive child accessor used to read `_loadingSignal()` and the full `currentRoute` snapshot directly, so any signal-driven write inside the navigate flow (start/end counters, lazy resolution, loader writes, param changes that don't actually swap the matched record) triggered `mountReactive` to tear down and rebuild the entire layout subtree. Empirically observed: ~9 cascading remounts per cold-start `router.replace()` in `examples/ui-showcase`, instrumented via a PyreonUI instance counter that rose from 3 expected mounts to 27 actual.

The root-cause fix lives in `packages/core/router/src/components.tsx` and decouples STRUCTURE (which `RouteRecord` is at this depth + which component to render for it) from DATA (params / query / loader payloads). One `computed` returns the record + resolved-component pair atomically with `equals: (a, b) => a.rec === b.rec && a.comp === b.comp && a.errored === b.errored`. The reactive child accessor reads ONLY this one computed, so it re-emits exclusively on real navigation (matched record changed at this depth), lazy resolution (cached component flips null → fn), or chunk-error transition. Loader writes / `_loadingSignal` ticks during navigate / unrelated route signals pass through silently. Layouts mount once. Companion fixes:

1. **`restoreContextStack` now splices instead of truncating.** The previous `stack.length = savedLength` destroyed provider frames that `fn()` had pushed during the synchronous mount — so signal-driven re-runs of `_bind` / `renderEffect` later saw a half-empty context stack and `useContext()` fell back to the default value (the original symptom that defeated the `<PyreonUI mode={signal()}>` reactive-mode tests). The new `stack.splice(insertIndex, snapshot.length)` removes only the snapshot frames `restoreContextStack` itself pushed; provider frames stay alive until their owning components' `onUnmount(popContext)` handlers fire on subtree teardown.
2. **Effects (`_bind` / `renderEffect` / `effect`) capture an external snapshot at SETUP time and restore it on every subsequent re-run.** Defense-in-depth: even if some other code path mutates the global stack between mount and a signal-driven re-run, the captured snapshot keeps `useContext` lookups reproducible. `@pyreon/core`'s context module registers the capture/restore pair via `setSnapshotCapture` at module load — `@pyreon/reactivity` is below `@pyreon/core` in the dep order so the dependency direction is preserved (DI hook, not direct import).

The two previously-`fixme`'d ui-showcase regression specs (`toggling the mode signal updates ModeProbe text reactively`, `mode prop change patches ModeProbe in place`) now pass — proving the framework primitive works end-to-end with the layout fix, not just in isolation.
